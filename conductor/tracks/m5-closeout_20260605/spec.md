# Specification: M5 Closeout — Checklist, Handoff Note, and Milestone Closure

**Track ID:** m5-closeout_20260605
**Type:** Chore
**Created:** 2026-06-05
**Status:** Draft

## Summary

Write the M5 manual closeout checklist (#514) and the M5 closeout/handoff note (#515), update
`docs/designs/high-level-design.md` to mark M5 complete and document the M5.5 scope boundary,
then reassign or close the eight remaining open M5 issues so the milestone can be sealed.

## Context

M5 delivered the server-authoritative game shell and turn-loop skeleton: `UnitOrderState` schema,
turn reducer + valid-actions engine, `POST /api/v1/games/:id/actions` with Socket.io events,
`GameView` with `UnitCounterLayer` + `UnitStatsPanel` + `ActionPanel`, and `useGameStore`. The
milestone is functionally complete but has never been formally closed. Eight issues remain open
under it: two docs tasks (#514, #515) that are the actual work here, four engine stubs that are
correctly deferred to M6 (#379 #381 #382 #383), one fuzz-test nice-to-have deferred to M6 (#385),
and one CSP header task deferred to M8 (#403). M5.5 ("Turn Loop Cleanup") is the next milestone
and has one open issue (#516 or similar).

## Acceptance Criteria

- [ ] `docs/testing/m5-closeout-checklist.md` exists and covers the manual verification steps
      from #514 (server start, game create/join, GameView rendering, unit selection, ActionPanel,
      END_PHASE action, state refresh).
- [ ] `docs/milestones/m5-closeout.md` exists and documents: what M5 delivered, what is
      intentionally stubbed, what moves to M5.5, and what remains M6/M7 scope (#515).
- [ ] `docs/designs/high-level-design.md` updated: M5 row marked complete, M5.5 row added,
      implementation status prose updated.
- [ ] Issues #379 #381 #382 #383 #385 reassigned from M5 to M6 (already in debt register).
- [ ] Issue #403 reassigned from M5 to M8 (already in debt register).
- [ ] Issues #514 and #515 closed with a merge summary comment.
- [ ] M5 GitHub milestone closed.

## Dependencies

- `docs/designs/high-level-design.md` — existing HLD to update
- Open M5 issues #379 #381 #382 #383 #385 #403 #514 #515
- GitHub milestone "M5: Turn Structure + Orders + Game Map UI" (milestone #8)
- GitHub milestone "M6: Combat + Morale" (milestone #9) and M8 (milestone #11)

## Out of Scope

- Implementing any of the deferred engine stubs (M6 work)
- Any M5.5 feature work
- Updating the OOB editor or map digitization docs

## Technical Notes

No code changes. All work is documentation and GitHub issue/milestone housekeeping. The checklist
and closeout note are markdown files in new `docs/testing/` and `docs/milestones/` directories.
The HLD update is a targeted prose and table edit — mark M5 "Complete", add M5.5 row, update the
"In progress" paragraph.

---

_Generated by Conductor. Review and edit as needed._
