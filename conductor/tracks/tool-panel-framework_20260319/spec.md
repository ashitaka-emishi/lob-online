# Specification: Tool Panel Framework — Shared Architecture

**Track ID:** tool-panel-framework_20260319
**Type:** Feature
**Created:** 2026-03-19
**Status:** Draft
**GitHub Issue:** #136
**Milestone:** Create usable map editor

## Summary

Foundational refactor that removes the old accordion-based editor UI and replaces it with a
declarative, tool-based architecture. Unblocks all tool implementation tickets (#137–#142).

## Context

The current `MapEditorView` uses an accordion-panel model (HexEditPanel, WedgeEditor,
LinearFeaturePanel, EditorToolbar) that mixes concerns and doesn't scale to the full set of
LOB map features. The v2 design (docs/designs/map-editor-design.md §4) establishes a shared framework
where each tool owns its overlay, composables are single-responsibility, and `HexMapOverlay`
is a pure declarative renderer.

## Acceptance Criteria

- [ ] `EditorToolbar.vue`, `LinearFeaturePanel.vue`, `useLinearFeatureTrace.js`,
      `HexEditPanel.vue`, and `WedgeEditor.vue` are removed
- [ ] `config/feature-types.js` exists as the single source of truth for type strings, colors,
      stroke styles, and icon keys
- [ ] Formula modules extracted: `formulas/hex-geometry.js`, `formulas/compass.js`,
      `formulas/edge-model.js`
- [ ] Shared composables implemented: `usePaintStroke.js`, `useHexPaintTool.js`,
      `useEdgePaintTool.js`, `useClickHexside.js`
- [ ] `BaseToolPanel.vue` provides: clear-all + ConfirmDialog, help popup, auto-rendered
      overlay toggle checkboxes from `overlayConfig`
- [ ] `ToolChooser.vue` renders a shared button-toggle group from `items` prop
- [ ] `HexMapOverlay.vue` refactored to accept declarative `overlayConfig`; all tool-specific
      logic removed; primitive layer renderers: HexGridLayer, HexFillLayer, HexLabelLayer,
      HexIconLayer, EdgeLineLayer, HexHighlightLayer
- [ ] Interaction gate: HexMapOverlay emits events only when `openPanel` ∈
      `{elevation, terrain, road, stream, contour}`
- [ ] No-tool-active state: overlay renders hover tooltip only
- [ ] All existing tests pass; coverage ≥ 70%

## Dependencies

- #135 (Map schema foundation) — complete as of PR #148

## Out of Scope

- Tool panel implementations (#137–#142) — those are separate tracks
- Game logic, auth, multiplayer
- Auto-detect contour feature (deferred out of milestone)

## Technical Notes

See `docs/designs/map-editor-design.md` §4 (component diagram, overlayConfig contract, composable
contracts) and §15 (formula + config module specs). The `overlayConfig` object shape and
`BaseToolPanel` toggle-rendering pattern are fully specified there.

---

_Generated by Conductor. Review and edit as needed._
