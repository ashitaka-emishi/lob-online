# Implementation Plan: Tool Panel Framework — Shared Architecture

**Track ID:** tool-panel-framework_20260319
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-19
**Status:** [ ] Not Started

## Overview

Execute the v2 map editor architecture in five phases: remove the old accordion UI, extract
formula/config modules, build shared composables, build shared UI components, and finally
refactor HexMapOverlay to be a pure declarative renderer. Each phase leaves the app in a
runnable state.

## Phase 1: Remove Old UI and Extract Config/Formula Modules

Delete the accordion-model components and set up the new module foundations.

### Tasks

- [~] Task 1.1: Delete `EditorToolbar.vue`, `LinearFeaturePanel.vue`, `useLinearFeatureTrace.js`,
  `HexEditPanel.vue`, `WedgeEditor.vue`; remove all imports from `MapEditorView.vue`
- [ ] Task 1.2: Create `client/src/config/feature-types.js` — export all type strings, colors
      (TERRAIN_COLORS, ROAD_GROUPS, etc.), stroke styles, icon keys per §15
- [ ] Task 1.3: Create `client/src/formulas/hex-geometry.js` — extract hex geometry helpers
      (pointy-top axial math, hex center, neighbor) from existing code per §15
- [ ] Task 1.4: Create `client/src/formulas/compass.js` — extract `compassLabel`,
      `allFaceLabels(northOffset)` per §15
- [ ] Task 1.5: Create `client/src/formulas/edge-model.js` — extract canonical edge ownership,
      `validateCoexistence`, face-index helpers per §15
- [ ] Task 1.6: Write unit tests for all three formula modules

### Verification

- [ ] `npm run test` passes; no broken imports

## Phase 2: Shared Paint Composables

Build the four interaction composables that all tool panels will use.

### Tasks

- [ ] Task 2.1: Implement `client/src/composables/usePaintStroke.js` — stroke start/end,
      suppresses per-hex localStorage saves during drag; write tests
- [ ] Task 2.2: Implement `client/src/composables/useHexPaintTool.js` — hex click/paint/
      right-click-clear, uses `usePaintStroke`; write tests
- [ ] Task 2.3: Implement `client/src/composables/useEdgePaintTool.js` — edge paint/
      right-click-clear/`clearAll(allowedTypes)`, uses `usePaintStroke`; write tests
- [ ] Task 2.4: Implement `client/src/composables/useClickHexside.js` — single-click edge
      placement with `validateFn(hexId, faceIndex) → { valid, reason }`; write tests

### Verification

- [ ] All composable unit tests pass; coverage ≥ 70%

## Phase 3: Shared UI Components

Build `BaseToolPanel` and `ToolChooser` — the shared shell and chooser used by every tool.

### Tasks

- [ ] Task 3.1: Implement `BaseToolPanel.vue` — accepts `overlayConfig` prop; auto-renders
      toggle checkboxes for non-`alwaysOn` layers; provides clear-all button + ConfirmDialog
      slot; provides help popup trigger + modal
- [ ] Task 3.2: Implement `ToolChooser.vue` — button-toggle group; items prop:
      `[{ value, label, color?, icon?, style? }]`; emits `update:modelValue`
- [ ] Task 3.3: Write component tests for BaseToolPanel and ToolChooser

### Verification

- [ ] Components render correctly with mock overlayConfig; tests pass

## Phase 4: Declarative HexMapOverlay Refactor

Replace the monolithic overlay with primitive layer renderers driven by overlayConfig.

### Tasks

- [ ] Task 4.1: Define and implement primitive layer components inside `HexMapOverlay.vue`:
      `HexGridLayer`, `HexFillLayer`, `HexLabelLayer`, `HexIconLayer`, `EdgeLineLayer`,
      `HexHighlightLayer`
- [ ] Task 4.2: Refactor `HexMapOverlay.vue` to accept `overlayConfig` prop and render layers
      declaratively; remove all tool-specific rendering logic
- [ ] Task 4.3: Implement interaction gate — emit mouse/click events only when
      `openPanel` ∈ `{elevation, terrain, road, stream, contour}`
- [ ] Task 4.4: No-tool-active state — when no panel is open, render hover tooltip only
- [ ] Task 4.5: Update HexMapOverlay tests

### Verification

- [ ] Overlay renders grid + labels with no active tool; events fire only when gated panel open

## Phase 5: Integration and Final Verification

Wire MapEditorView to the new architecture and confirm everything passes.

### Tasks

- [ ] Task 5.1: Update `MapEditorView.vue` to remove all old accordion panel state; wire
      `openPanel` tracking for the interaction gate
- [ ] Task 5.2: Add a stub `ElevationToolPanel.vue` using `BaseToolPanel` so the editor has
      at least one working tool (unblocks #137 which can replace the stub)
- [ ] Task 5.3: Run full test suite; fix any coverage gaps to maintain ≥ 70%
- [ ] Task 5.4: Run `npm run lint` and `npm run format:check`

## Final Verification

- [ ] All acceptance criteria in spec.md met
- [ ] `npm run test:coverage` ≥ 70%
- [ ] `npm run lint` clean
- [ ] `npm run format:check` clean
- [ ] Map editor loads in browser; grid renders; no console errors

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
