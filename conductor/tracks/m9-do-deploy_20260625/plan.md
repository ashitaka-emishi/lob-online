# Implementation Plan: M9 DO Deployment

**Track ID:** m9-do-deploy_20260625
**Spec:** [spec.md](./spec.md)
**Created:** 2026-06-25
**Status:** [ ] Not Started

## Overview

Manual provisioning tasks only — no app code changes. Activate the deploy pipeline
deferred from M8 Phase 3 (m8-notifications_20260622). All scaffolding (Terraform,
deploy.yml, .env.example) is already merged.

## Interaction Mode

**Mode:** Checkpointed
**Human control points:** All tasks are manual — human executes each step

## Risk Classification

**Risk:** Medium
**Reason:** Production infrastructure; no code changes but misconfiguration could
expose the app or fail the deploy pipeline.

## Quality Gates

- [ ] `deploy.yml` runs green
- [ ] App serves at Droplet IP
- [ ] Game state survives `pm2 restart`

## Debt Budget

**Allowed new deferred debt:** 0.

## Completion Contract

- [ ] Closes #653
- [ ] All acceptance criteria in spec.md met
- [ ] Ready for `/team-review` (infra review only — no code diff)

---

## Phase 1: Provision Infrastructure

### Tasks

- [ ] Task 1.1: Run `terraform apply` in `infra/` to provision Droplet, Spaces bucket,
      and Firewall (or provision manually if Terraform state is not initialized)
- [ ] Task 1.2: SSH into Droplet; install Node.js 20 via NodeSource PPA, PM2 globally,
      and nginx
- [ ] Task 1.3: Clone repo to `/opt/lob-online`; create `.env.production` with all
      required vars: `SPACES_KEY`, `SPACES_SECRET`, `SPACES_BUCKET`, `SPACES_ENDPOINT`,
      `SPACES_REGION=us-east-1`, `SPACES_FORCE_PATH_STYLE=false`, `DB_PATH`,
      `SESSION_SECRET`, `PORT=3000`
- [ ] Task 1.4: `npm ci && npm run build` in `/opt/lob-online`; start with PM2:
      `pm2 start ecosystem.config.cjs --env production`

### Verification

- [ ] `pm2 list` shows `lob-online` online
- [ ] `curl http://localhost:3000/api/v1/games` returns JSON (not a connection error)

---

## Phase 2: Wire Deploy Pipeline

### Tasks

- [ ] Task 2.1: Add three GitHub repository secrets via Settings → Secrets:
      `DO_DROPLET_IP`, `DO_SSH_USER` (e.g. `root`), `DO_SSH_KEY` (private key content)
- [ ] Task 2.2: Push a trivial commit to `master` → watch `deploy.yml` Actions run

### Verification

- [ ] `deploy.yml` job completes green in GitHub Actions
- [ ] `https://<droplet-ip>/` serves the Vue app (or `http://` if TLS not yet configured)
- [ ] Create game → restart PM2 → game state still loads from Spaces

---

_Generated by Conductor on 2026-06-25._
