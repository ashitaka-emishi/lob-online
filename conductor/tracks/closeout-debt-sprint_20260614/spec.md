# Specification: Conductor Closeout + Debt Sprint — Score 33 → 21

**Track ID:** closeout-debt-sprint_20260614
**Type:** Chore
**Created:** 2026-06-14
**Status:** Draft

## Summary

Close the stale `lobby-auth-cleanup_20260613` conductor entry, normalize `tracks.md` formatting,
update `product.md` to reflect M5.5 complete / M6 starting, and resolve all 8 closeable debt
items (#379, #538, #544, #547, #558, #564, #566, #567) to bring the net open debt score from
33 to 21.

## Context

After merging PR #565, two housekeeping gaps remain: (1) `conductor/tracks.md` has a stale
`[ ]` row for `lobby-auth-cleanup_20260613` whose `plan.md` is fully checked off and whose
work shipped in PR #561; the pre-`/conductor:new-track` section also has inconsistent blank-line
separators that make it hard to scan; `product.md` still describes M3 as the current phase.
(2) The debt register sits at score 33 across 17 items; 8 of those are actionable now without
M6/M8 prerequisites — the remaining 9 are legitimately deferred to M6 (engine stubs blocked
on combat/morale data) and M8 (auth hardening).

## Acceptance Criteria

### Conductor cleanup

- [ ] `lobby-auth-cleanup_20260613` row in `tracks.md` changed from `[ ]` to `[x]`.
- [ ] Blank-line separators in the top section of `tracks.md` normalized to match the bottom
      section style (no blank lines between rows).
- [ ] `conductor/product.md` "Current Phase" paragraph updated to reflect M5.5 complete and
      M6 starting.

### Debt items closed

- [ ] #379 closed — `getValidActions` already enumerates concrete candidates in M5.5; verify
      and close as resolved.
- [ ] #538 closed — contrast ratio on `.derived-value` spans fixed or confirmed WCAG AA
      compliant.
- [ ] #544 closed — `useModuleStore` `modulePath`/`defaultScenarioPath` helper unit tests
      added.
- [ ] #547 closed — router test coverage for legacy redirect destinations added.
- [ ] #558 closed — component test for `moduleSlug` reactivity in editor views added.
- [ ] #564 closed — lobby join-button disabled state and accessible labels restored.
- [ ] #566 closed — smoke test error-path coverage added (wrong side, invalid type, stale
      version).
- [ ] #567 closed — smoke test structural round-trip assertion on persisted state added.
- [ ] All 8 GitHub issues closed.
- [ ] `docs/tech-debt/report.md` updated: 8 items removed from Open Debt Items, 8 resolution
      rows added to Debt Over Time, Executive Summary updated to score 21 / 9 items.

## Dependencies

- `conductor/tracks.md`, `conductor/product.md`
- `conductor/tracks/lobby-auth-cleanup_20260613/metadata.json` (already correct — status
  "complete"; no change needed)
- `client/src/stores/useModuleStore.js` — helpers to test
- `client/src/router/index.js` — legacy redirects to test
- `client/src/views/MapEditorView.vue`, `ScenarioEditorView.vue`, `OobEditorView.vue` —
  moduleSlug reactivity
- `client/src/views/LobbyView.vue` — join-button a11y
- `server/src/engine/actions/smoke.test.js` — error-path and round-trip assertions
- `client/src/assets/main.css` or scoped SFC styles — `.derived-value` contrast

## Out of Scope

- M6 engine stubs (#379 only requires close as already-done, not reimplementation).
- M8 auth items (#562, #563, #403, #350) — remain deferred.
- M6-blocked stubs (#381, #382, #383, #560) — remain deferred.
- #568 (action-contract.md rot) — fix at M6 kickoff when the dead-code branch lands.
- Any new features.

## Technical Notes

The debt items split cleanly into three groups by surface:

1. **Docs/conductor** (no tests needed): `tracks.md` checkbox + formatting, `product.md`.
2. **New tests** (TDD: write test first, confirm it passes, close issue): #544, #547, #558,
   #566, #567. Tests are isolated — no production logic changes.
3. **Small fixes with tests** (implement + test): #538 (CSS color value), #564 (LobbyView
   button state + aria labels).
4. **Close as resolved** (verify + close): #379.

---

_Generated by Conductor. Review and edit as needed._
