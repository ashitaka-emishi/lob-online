# Specification: Pre-M5 Debt Sprint

**Track ID:** pre-m5-debt-sprint_20260505
**Type:** Chore
**Created:** 2026-05-05
**Status:** Draft

## Summary

Resolve all score ≥ 3 deferred findings from the M4 PR #328 review before M5 begins. Nine
GitHub issues covering data-model correctness, session security, authorization, database-layer
reliability, and storage abstraction must be closed for M5 to ship safely.

## Context

PR #328 (M4 game state) deferred 10 findings. The score-5 and score-4 items are explicit
blockers: M5 adds simultaneous order submission (silently overwrites without #332), adds hidden
orders exposed to any caller (information disclosure without #330), and adds phase-transition code
that will need error-prone compound predicates if the phase/status overlap (#333) is not resolved
first. The score-3 items (DI, session abstraction, TOCTOU, initDb) grow worse with each M5
endpoint added and are cheapest to fix now.

## Acceptance Criteria

- [ ] `GameStateSchema` phase enum no longer includes `'setup'`; `phase` starts as `null` (status
      drives the setup gate) — issue #333 closed.
- [ ] `GameStateSchema` has a `version: z.number().int().nonnegative()` field; `saveGame` enforces
      monotonic increment — issue #332 closed.
- [ ] `GET /api/v1/games/:id` requires a valid player session (returns 401 for unauthenticated
      callers) — issue #330 closed.
- [ ] Express session uses `better-sqlite3-session-store` with `cookie.maxAge` set — issue #329
      closed.
- [ ] `server/src/auth/session.js` exports `setPlayerSession` and `getPlayerSession` helpers used
      by all routes that touch the session — issue #335 closed.
- [ ] `gameSqlite.js` is refactored to a factory function with dependency injection; prepared
      statements are hoisted — issue #331 closed.
- [ ] `joinGame` uses a single conditional `UPDATE … WHERE status = 'open'` — issue #336 closed.
- [ ] `initDb()` is called inside the server startup function (not at module load); SIGTERM handler
      closes the DB cleanly — issue #338 closed.
- [ ] `server/src/store/index.js` re-exports the active store interface; all routes import from
      `store/index.js` only — issue #334 closed.
- [ ] All CI gates pass: `npm run lint`, `npm run format:check`, `npm run test` (≥ 70% coverage).
- [ ] No new deferred findings introduced (per coding-standards rule: debt PRs must not add debt).

## Dependencies

- M4 complete (PR #328 merged) — `GameStateSchema`, `saveGame`/`loadGame`, `gameSqlite.js`,
  `routes/games.js`, and `server.js` are all in place.

## Out of Scope

- Score-2 finding #337 (getScenario cache invalidation) — low priority, does not affect M5.
- M5 feature work (turn loop, order submission, phase transitions) — this sprint only clears the
  path; M5 implementation begins after merge.
- OAuth / real authentication (M6 scope) — `requireSide` middleware uses the existing
  session-token scheme from M4.

## Technical Notes

- Phase ordering matters: data-model changes (#333, #332) must land before authorization (#330),
  because `requireSide` validates the session token against the game's `sides` map.
- `better-sqlite3-session-store` is the natural choice given the project already depends on
  `better-sqlite3`; no new dependency needed.
- The `gameSqlite` DI refactor (#331) must be completed before the TOCTOU fix (#336) so that the
  new factory pattern is the only pattern in the module.
- `store/index.js` (#334) is a thin re-export shim for now; the swap to Spaces (M8) will only
  touch that one file.

---

_Generated by Conductor. Review and edit as needed._
