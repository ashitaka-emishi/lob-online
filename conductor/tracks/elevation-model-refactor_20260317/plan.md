# Implementation Plan: Refactor Map Elevation Model

**Track ID:** elevation-model-refactor_20260317
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-17
**Status:** [ ] Not Started

## Overview

Four phases: (1) schema changes + tests, (2) migrate map.json, (3) refactor WedgeEditor UX,
(4) add auto-derive button with neighbor propagation. TDD order — tests first in phases 1 and 4.

## Phase 1: Schema Updates + Tests

Update `MapSchema` to enforce integer elevation fields and new `elevationSystem` fields.

### Tasks

- [x] Task 1.1: Add `baseElevation` and `elevationLevels` to `elevationSystem` in
      `server/src/schemas/map.schema.js`
- [x] Task 1.2: Change per-hex `elevation` validation to `z.number().int().min(0).max(21)`
- [x] Task 1.3: Change `wedgeElevations` tuple to `z.number().int()` on each element
- [x] Task 1.4: Write Vitest tests in `map.schema.test.js`: rejects elevation: 550, accepts elevation: 5, rejects -1 and 1.5, rejects non-integer wedges, accepts full elevationSystem

### Verification

- [x] `npm run test` passes with new schema tests green (379 tests)

## Phase 2: Migrate map.json

Convert existing map data from absolute feet to integer level indices and add metadata.

### Tasks

- [x] Task 2.1: No conversion needed — existing elevation values (1, 2) are already integer
      level indices, not feet values
- [x] Task 2.2: Add `elevationSystem.baseElevation: 500` and `elevationSystem.elevationLevels: 22`
      to `data/scenarios/south-mountain/map.json`
- [x] Task 2.3: Updated `_note` with encoding formula
- [x] Task 2.4: MapSchema validates migrated file without errors

### Verification

- [x] Server loads `map.json` without Zod validation errors
- [x] `npm run test` still passes (379 tests)

## Phase 3: WedgeEditor UX Refactor

Replace text input controls with click-to-increment/decrement interactions.

### Tasks

- [x] Task 3.1: Remove text input / spinner controls from `WedgeEditor.vue`
- [x] Task 3.2: Add `@click` (−1) and `@contextmenu.prevent` (+1) handlers on each wedge
      triangle
- [x] Task 3.3: Values displayed on each wedge triangle (existing SVG text labels retained)
- [x] Task 3.4: Wedge editing persists via `update:wedgeElevations` emit (unchanged event flow)

### Verification

- [ ] Manual smoke test in map editor: left-click decrements, right-click increments, no
      context menu appears, value displayed on triangle
- [x] `npm run test` passes (379 tests)

## Phase 4: Auto-Derive Edges & Slope Button

Add "Auto-derive edges & slope" button to `HexEditPanel.vue` with neighbor propagation.

### Tasks

- [x] Task 4.1: Write Vitest unit test for auto-derive logic (slope index, edge types per delta)
- [x] Task 4.2: Write Vitest neighbor propagation tests (opposite index formula + edge type matching)
- [x] Task 4.3: Implement `client/src/utils/elevationDerive.js` — pure `deriveEdgesAndSlope()`
- [x] Task 4.4: Add "Auto-derive edges & slope" button to `HexEditPanel.vue`
- [x] Task 4.5: Propagate to neighbors via `adjacentHexId`; emit `hex-update` for each

## Final Verification

- [x] All acceptance criteria in spec.md met
- [x] `npm run test` passes — 395 tests (21 new schema tests + 16 new elevationDerive tests)
- [x] `npm run lint` passes — no errors
- [x] `npm run format:check` passes — all files Prettier-clean
- [ ] Map editor smoke test: auto-derive button updates hex and neighbors correctly (manual)
- [x] Ready for review

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
