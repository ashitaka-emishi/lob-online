# Specification: M3 Closeout + M4 Replan

**Track ID:** m3-done-replan_20260427
**Type:** Chore
**Created:** 2026-04-27
**Status:** Draft

## Summary

M3 is complete. All engine modules, both test tools, and 1877 tests are shipped and green.
This track updates all project documentation to reflect that reality and re-scopes M4 to
prioritize the game state model and turn loop over auth infrastructure — enabling M5 to
follow immediately.

## Context

The tracks registry shows m3-engine, map-test-tool, table-test-tool, and two debt sprints
all complete as of 2026-04-27. `product.md` still says "Starting M3"; the HLD status banner
still says "M2 Complete." The scenario data in `scenario.json` is partially complete and
will continue to be refined throughout the development process — it has enough to proceed.

## What Was Delivered in M3

- Engine modules: `hex.js`, `scenario.js`, `movement.js`, `los.js`, `command-range.js`, `map.js`
- Table modules: `combat.js`, `morale.js`, `charge.js`, `command.js`, `leader-loss.js`,
  `weapons.js`, `formations.js`
- Map Test Tool: 5 panels (Movement Path, Movement Range, Hex Inspector, LOS, Command Range)
- Table Test Tool: 11 panels covering every LOB v2.0 game table
- 1877 Vitest tests, all green; lint and format:check pass
- Scenario data: 45-turn structure, setup zones, reinforcement schedule, initial orders

## Problem Description

Docs are stale. The milestone plan needs a realistic M4 that gets to the turn loop without
a detour through Discord OAuth (which has no bearing on game logic and should slide to M8).

## Acceptance Criteria

- [ ] `product.md` states M3 complete; describes actual M3 deliverables; describes M4 goal
- [ ] HLD implementation status banner updated to "M3 Complete"
- [ ] `CLAUDE.md` current-phase description reflects M3 done, M4 next
- [ ] HLD M4 milestone re-scoped: Discord OAuth removed; focus is game state model,
      game state initializer (setup zones + entry hexes), minimal session (side selection),
      and file persistence
- [ ] HLD M4 includes explicit note on initial setup phase:
      union units placed within zone constraints per `scenario.json setup`;
      CSA units at fixed hexes; initial orders applied; reinforcement queue initialized
- [ ] HLD M5 milestone description remains intact (turn loop + UI) but dependencies
      updated to reflect the leaner M4

## Out of Scope

- No code changes
- Scenario data completion (ongoing, spans full development lifecycle)
- Any changes to M6–M8 milestones
- Counter image linkages or OOB data entry

## Technical Notes

`scenario.json` setup uses two formats: constraint-based (union: `setupZone: "within5Of"`,
`referenceHex`) and fixed-hex (confederate: `unitId`, `hex`). The game state initializer
in M4 must handle both. Reinforcements use clock-time entry (`time: "09:00"`) mapped to
turn numbers via `ceil((minutesSince0900)/20) + 1`.

---

_Generated by Conductor. Review and edit as needed._
