# Implementation Plan: M3 Closeout + M4 Replan

**Track ID:** m3-done-replan_20260427
**Spec:** [spec.md](./spec.md)
**Created:** 2026-04-27
**Status:** [ ] Not Started

## Overview

Three docs to update, one milestone to re-scope. All changes are documentation only — no
code. Phase 1 updates the two source-of-truth status files (product.md, CLAUDE.md). Phase 2
rewrites the M4 milestone block in the HLD to drop auth and focus on game state init.

---

## Phase 1: Status Files

Update the files that declare what milestone the project is on.

### Tasks

- [x] Task 1.1: Update `conductor/product.md`
  - Change "Current Phase" section: M3 complete, describe deliverables in 3–4 sentences,
    state M4 goal (game state model + initializer + minimal session + file persistence)

- [x] Task 1.2: Update `CLAUDE.md` current-state paragraph
  - Replace "Starting M3 — Rules Engine Foundation…" with M3 complete summary
  - Update the sentence describing what comes next to point at M4 game state work

### Verification

- [ ] `product.md` Current Phase section says M3 complete and describes M4 goal
- [ ] `CLAUDE.md` no longer says "Starting M3"

---

## Phase 2: HLD Milestone Re-scope

Rewrite the HLD implementation status banner and the M4 milestone definition.

### Tasks

- [ ] Task 2.1: Update HLD implementation status banner (top of file)
  - Change "M2 Complete" → "M3 Complete"
  - Update "In progress" and "Next" lines to reflect M4 game state work

- [ ] Task 2.2: Rewrite HLD M4 milestone
  - Remove Discord OAuth, passport-discord, JWT cookie auth
  - Remove SQLite users table (games table stays)
  - Add: in-memory game state model (unit positions, morale states, orders, ammo, facing)
  - Add: game state initializer — reads `scenario.json setup` (both zone-constraint and
    fixed-hex formats), places units, applies initial orders, queues reinforcements by turn
  - Add: initial setup phase — union placement within zone constraints, CSA fixed positions,
    SM §3.x; produces initial `gameState` ready for Turn 1
  - Add: minimal session — "pick a side" local dev auth (no OAuth); two players, two sides
  - Keep: file persistence (`data/games/{id}/state.json`), games table in SQLite
  - Keep: game lobby UI (list/create/join), but simpler — no Discord login required
  - Update M4 acceptance criteria to match

- [ ] Task 2.3: Update HLD milestone overview table
  - M3 row: change 🔜 to ✅, update status to "Complete"
  - M4 row: update goal description to match rewritten milestone
  - Keep M5–M8 rows unchanged

- [ ] Task 2.4: Update HLD Rule Section Coverage table
  - Mark M3 rows as complete where implementation is confirmed

### Verification

- [ ] HLD status banner says M3 Complete
- [ ] HLD M4 milestone contains no Discord OAuth references
- [ ] HLD M4 milestone covers: game state model, initializer, setup phase, minimal session,
      persistence, lobby UI
- [ ] M4 acceptance criteria are achievable without OAuth
- [ ] Milestone overview table shows M3 ✅

---

## Final Verification

- [ ] All acceptance criteria in spec.md met
- [ ] No code files touched
- [ ] CI gates still green (docs-only change; no lint/test impact expected)
- [ ] Ready for review — no PR needed for this chore (direct to master is acceptable
      given docs-only scope, but branch + PR is fine per workflow rules)

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
