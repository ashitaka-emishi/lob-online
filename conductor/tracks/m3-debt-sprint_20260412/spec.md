# Specification: M3 Debt Sprint — Score-3 Cleanup

**Track ID:** m3-debt-sprint_20260412
**Type:** Chore
**Created:** 2026-04-12
**Status:** Draft

## Summary

Resolve all 9 open score-3 technical debt items accumulated across M2 and M3 development
to clear the path for M4 (game loop / socket layer). Net open debt score reduced by 27
(66 → 39).

## Context

lob-online is entering M4 after completing the M3 rules engine and dev tools (map editor,
scenario editor, OOB editor, Map Test Tool, Table Test Tool). Nine score-3 debt items
remain open across three domains: OOB/server (carry-forward from M2), engine correctness
and security, and Map Test Tool architecture. These items represent missing abstractions,
correctness hazards, and coupling risks that will impede M4 game-loop work if not resolved.

## Acceptance Criteria

- [ ] #237 closed — processUSACavDiv delegates to withLeader; Farnsworth variants render
- [ ] #245 closed — editorRouteFactory extracted; succession/leaders/oob routes unified
- [ ] #247 closed — useOobPersistence.test.js covers all succession paths
- [ ] #284 closed — loadMap/loadScenario have directory containment guard; no path leak in errors
- [ ] #288 closed — movementPath per-step hexsideCost populated correctly (not always 0)
- [ ] #289 closed — LOS terrain heights data-driven from scenario.json, not hardcoded
- [ ] #300 closed — MapTestView orchestration tests cover togglePanel, overlay routing, click dispatch
- [ ] #302 closed — map-test routes validate hex-ID format; malformed IDs return 400
- [ ] #303 closed — MapTestView fetches from a dedicated /api/tools/map-test/data endpoint
- [ ] All tests passing; lint and format clean
- [ ] No new debt introduced (tech-debt-report score delta = 0 net)

## Dependencies

- Existing engine modules: `engine/los.js`, `engine/movement.js`, `engine/hex.js`
- Existing server routes: `server/src/routes/mapTest.js`, `mapEditor.js`,
  `leadersEditor.js`, `successionEditor.js`
- Existing client: `client/src/views/tools/MapTestView.vue`,
  `client/src/composables/useOobPersistence.js`
- `scenario.json` schema (for LOS height data-driven change)

## Out of Scope

- Score-2 and score-1 debt items (addressed in a future sprint)
- New engine features or M4 game-loop work
- Changes to `map.json` data or `scenario.json` content

## Technical Notes

Three phases ordered by risk (lowest first): OOB/server carry-forwards → engine
correctness → map-test architecture. The LOS heights change (#289) requires a
`scenario.json` schema addition and a domain-expert ruling on which terrains carry height
bonuses. The MapTestView decoupling (#303) requires a new dedicated server route mirroring
the map-editor data endpoint.

---

_Generated by Conductor. Review and edit as needed._
