# Specification: Milestone Backfill — M1/M2 Creation, Issue Assignment, Leader Counter Upload

**Track ID:** milestone-backfill_20260601
**Type:** Chore
**Created:** 2026-06-01
**Status:** Draft

## Summary

Create the missing M1 and M2 GitHub milestones, assign all untagged historical issues to their
correct milestone (M1–M5 and M8), and open a new feature issue for uploading leader counter
images in the OOB editor.

## Context

Milestones M3–M8 exist in GitHub. M1 (Scaffold) and M2 (Dev Tools) were never created —
both are complete. Roughly 174 issues (closed + open) have no milestone assignment. The
bulk of the untagged closed issues belong to M2 (map editor, OOB editor, scenario editor
features, refactors, and bug fixes). A small number of untagged issues belong to M3–M5 and
M8 (debt sprint items that were filed and closed before milestone tagging was consistently applied).

M1 scaffold work (tech stack, server, CI, Zod schemas) predates structured issue tracking
and has few or no surviving issue numbers; the M1 milestone will be created with a description
but may have no assignable issues.

The leader counter upload issue is a direct result of the M5 closeout work (PR #475): leader
units in the game state now correctly appear on the map via a fallback rect, but their counter
images cannot be uploaded or linked through the OOB editor without a new upload UI.

## Acceptance Criteria

- [ ] GitHub milestone "M1: Scaffold" created with description and closed state.
- [ ] GitHub milestone "M2: Dev Tools" created with description and closed state.
- [ ] All M2 issues (map editor, OOB editor, scenario editor, counter detection — see plan for
      the explicit list) assigned to the M2 milestone.
- [ ] Untagged M3–M5 and M8 issues assigned to their correct existing milestones.
- [ ] New GitHub issue created: "OOB editor: upload and link counter images for leader units".
- [ ] New issue labels applied (feature, oob-editor) and assigned to M2 or M3 milestone.

## Dependencies

- Existing GitHub milestones M3–M8
- PR #475 (leader counter fallback — establishes why the upload issue is needed)

## Out of Scope

- Creating any M6–M8 milestone content changes.
- Implementing the leader counter upload feature (that is the follow-up work the new issue tracks).
- Reassigning already-milestoned issues.
- Creating GitHub Projects or labels.

## Technical Notes

All milestone creation and issue assignment is done via `gh` CLI (`gh api`, `gh milestone create`,
`gh issue edit --milestone`). The leader upload issue requires changes to the OOB editor's
`CounterImageWidget` or a new dedicated upload flow for leader nodes.

---

_Generated by Conductor. Review and edit as needed._
