# Specification: Post-M5 Debt Bundle 2 — Minor Cleanups and Test Hardening

**Track ID:** post-m5-debt-2_20260523
**Type:** Chore
**Created:** 2026-05-23
**Status:** Draft

## Summary

Close 9 tech-debt items filed against PR #437's team review: one architectural
simplification in GameView, two import-path cleanups, four test-hardening
improvements, one accessibility fix, and removal of a dead export. All items are
score 1–2 with no correctness risk.

## Context

PR #437 closed the 10 original map-config contract items but the team review
introduced 10 new deferred findings (#438–#447). This track closes the 9 that
are actionable without M6+ context (excluding #440 sequential-fetch latency,
deferred to multi-game lobby work).

## Acceptance Criteria

- [ ] #438 — `GameView.vue` calibration computed no longer spreads `DEFAULT_CALIBRATION`;
      passes `gameStore.gridSpec ?? {}` directly to `sanitizeCalibration`
- [ ] #439 — `GridSpecSchema` unexported (internal to `map.schema.js`); tests verify
      the shape via `MapSchema` or removed if redundant
- [ ] #442 — 503 test in `scenarios.test.js` uses `vi.isolateModules()` or a
      `beforeEach`/`afterEach` guard; ordering-independent
- [ ] #443 — `export { DEFAULT_CALIBRATION, sanitizeCalibration }` re-export removed
      from `useCalibration.js`; no consumer imports these from that path
- [ ] #444 — `STUB_GRID_SPEC` renamed to `STUB_GRID_SPEC_WIRE` in `fixtures.js` and
      all consumers updated; `STUB_GRID_SPEC_MINI` renamed to `STUB_GRID_SPEC_MINI_WIRE`
- [ ] #445 — Banner stack in `GameView.vue` wrapped in a named landmark element
      (`<section aria-label="...">` or equivalent)
- [ ] #446 — `makeFetchSequence`/`makeMultiFetch` helpers have a comment documenting
      specificity-first ordering requirement, or patterns are anchored
- [ ] #447 — New integration test in `scenarios.test.js` exercises the success path
      with real `loadMap` data (200 + non-empty `gridSpec` and `hexes`)
- [ ] #436 — `useOobData` composable tested with 3-level OOB nesting; `oobError`
      raw-string pattern noted with a TODO

## Dependencies

- `client/src/utils/calibration.js` — `sanitizeCalibration` handles empty-object input (verified)
- `useCalibration.js` re-exports are not consumed by any file other than itself (verified)
- All STUB_GRID_SPEC consumers are in `useGameStore.test.js` and `GameView.test.js` (verified)

## Out of Scope

- #440 sequential fetch latency (needs scenarioId in route params — M6 work)
- #441 loadGame double-call race (pre-multi-game-lobby concern)
- #431 OOB production endpoint (separate feature track)
- Any new features or behavior changes
- `useCalibration.test.js` refactor beyond what's needed to remove the re-export

## Technical Notes

- `sanitizeCalibration({})` already returns `DEFAULT_CALIBRATION`, so removing the
  explicit spread in GameView does not change behavior
- `GridSpecSchema` is used internally in `MapSchema` — only the named export is removed;
  the variable declaration stays
- `STUB_GRID_SPEC_MINI` is currently imported as `STUB_GRID_SPEC` in `GameView.test.js`
  (aliased at import); the rename will require updating that alias too

---

_Generated by Conductor. Review and edit as needed._
