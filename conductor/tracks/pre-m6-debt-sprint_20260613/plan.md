# Implementation Plan: Pre-M6 Debt Sprint — Score 3+ and Low-Hanging Fruit

**Track ID:** pre-m6-debt-sprint_20260613
**Spec:** [spec.md](./spec.md)
**Created:** 2026-06-13
**Status:** [x] Complete

## Overview

Three phases: (1) fix the two score 3–4 server/composable issues, (2) extract MINUTES_PER_CONDITION, (3) low-hanging score 1–2 fruit (constants, schema guards, missing tests). Each phase is independently verifiable.

## Interaction Mode

**Mode:** Autonomous
**Human control points:** None beyond phase approvals

## Risk Classification

**Risk:** Medium
**Reason:** Route guard and composable changes touch the module API surface but are additive/narrowing; no schema migrations or rules engine.

## Quality Gates

- [ ] `npm run validate-data`
- [ ] `npm run lint`
- [ ] `npm run format:check`
- [ ] `npm run test`
- [ ] `npm run build`
- [ ] No unexpected warnings in test output

## Debt Budget

**Allowed new deferred debt:** 0

## Completion Contract

- [ ] All plan tasks complete
- [ ] All acceptance criteria in spec.md met
- [ ] Warnings fixed or explicitly classified as accepted prototype noise
- [ ] Debt register updated if any debt was accepted
- [ ] Ready for `/team-review`

---

## Phase 1: Route Guard and Composable Correctness (Score 4 + Score 3)

Fix the two highest-risk items: ungated write routes and the silent OOB persistence fallback.

### Tasks

- [x] Task 1.1: Gate all `PUT`/`POST`/`DELETE` routes in `moduleData.js` behind `MAP_EDITOR_ENABLED` — same pattern used in `server.js:116` (closes #540)
- [x] Task 1.2: Audit `useOobPersistence` callers (`useOobStore.js`, `MapEditorView.vue`) — confirm all pass `moduleSlug`; remove or warn on the silent fallback path (closes #541)
- [x] Task 1.3: Update/add tests for gated routes returning 404 when `MAP_EDITOR_ENABLED` is false

### Verification

- [ ] `npm run test` green; PUT route test verifies 404 without editor flag

---

## Phase 2: MINUTES_PER_CONDITION Extraction (Score 3)

Move the shared constant out of the Vue view into a location both client and server engine can import.

### Tasks

- [x] Task 2.1: Create `server/src/engine/turnTime.js` — export `MINUTES_PER_CONDITION` and `MINUTES_PER_CONDITION_DEFAULT` with LOB §1.1 citation
- [x] Task 2.2: Update `client/src/views/tools/ScenarioEditorView.vue` to import from the new shared location
- [x] Task 2.3: Update `server/src/engine/init.js` hardcoded `15` to use the exported constant
- [x] Task 2.4: Verify no other inline hardcodings remain (`grep -r "/ 15\b\|15\b.*turn"`)

### Verification

- [ ] `npm run test` green; grep confirms no bare hardcoded `15` in turn-time logic

---

## Phase 3: Low-Hanging Fruit (Score 1–2)

Knock out easy score 1–2 items: constants, schema validation guards, and missing unit tests.

### Tasks

- [x] Task 3.1: Add `VISIBILITY_UNLIMITED = 999` constant; replace magic number in `VISIBILITY_DEFAULTS`, template, and test fixtures (closes #537)
- [x] Task 3.2: Add upper bound and `999` sentinel doc to `visibilityHexes` Zod schema (closes #536)
- [x] Task 3.3: Add `startTurn` uniqueness `.superRefine()` check to lighting schedule schema (closes #532)
- [x] Task 3.4: Add `totalTurns` edge-case tests: empty schedule, `lastTurn < firstTurn`, missing fields (closes #535)
- [x] Task 3.5: Add `lightingStartTimes` night-turn clock accumulation test (closes #534)
- [x] Task 3.6: Evaluate #533 (remove `totalTurns` from `scenario.json`) — deferred; schema migration risk too high (all 9 data files + required schema field)
- [x] Task 3.7: Add direct unit tests for `useMapPersistence` and `useOobPersistence` URL construction (closes #543)

### Verification

- [ ] `npm run validate-data` passes after any schema changes
- [ ] `npm run test` green with new tests passing

---

## Final Verification

- [x] All acceptance criteria in spec.md met
- [x] `npm run quality:strict` passes (136 files, 2614 tests)
- [ ] Debt register updated — resolved items removed, score recalculated
- [ ] Ready for `/team-review`

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
