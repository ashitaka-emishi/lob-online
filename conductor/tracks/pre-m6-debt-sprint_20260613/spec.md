# Specification: Pre-M6 Debt Sprint — Score 3+ and Low-Hanging Fruit

**Track ID:** pre-m6-debt-sprint_20260613
**Type:** Chore
**Created:** 2026-06-13
**Status:** Draft

## Summary

Resolve all score 3+ open debt items and selected low-hanging-fruit score 1–2 items as pre-M6 housekeeping. Target: reduce the net open debt score from 41 toward ≤15.

## Context

lob-online is entering M6 (combat, morale, orders resolution). The debt register currently holds 22 open items with a net score of 41. All items scoring 3+ must be resolved before M6 work begins.

## Acceptance Criteria

- [ ] #540 — Gate moduleData PUT routes behind MAP_EDITOR_ENABLED (score 4)
- [ ] #541 — Resolve OOB persistence dual-mode divergence in useOobPersistence (score 3)
- [ ] #531 — Extract MINUTES_PER_CONDITION into shared engine module (score 3)
- [ ] Low-hanging fruit from score 1–2 items selected during implementation
- [ ] `npm run quality:strict` passes
- [ ] Debt register updated — resolved items removed, score recalculated

## Dependencies

- Existing `MAP_EDITOR_ENABLED` guard pattern in `server/src/server.js:116`
- `useOobPersistence` composable in `client/src/composables/useOobPersistence.js`
- `MINUTES_PER_CONDITION` constant in `client/src/views/tools/ScenarioEditorView.vue`
- `server/src/engine/init.js` hardcoded `15` turn-time math

## Out of Scope

- Score 2 items that require M6 work to implement (#383, #382, #381, #379)
- M8-deferred items (#403, #350)
- Further debt reduction beyond what this sprint achieves (a follow-up track can continue)

## Technical Notes

- #540: gate all write (PUT/POST/DELETE) routes in `moduleData.js` behind `MAP_EDITOR_ENABLED` using the same pattern as other editor routes in `server.js`
- #541: audit `useOobPersistence` callers and remove/warn on the silent `moduleSlug`-omitted fallback path
- #531: extract `MINUTES_PER_CONDITION` to `server/src/engine/turnTime.js` (or augment `init.js` exports); update both Vue view and engine `init.js`

---

_Generated by Conductor. Review and edit as needed._
