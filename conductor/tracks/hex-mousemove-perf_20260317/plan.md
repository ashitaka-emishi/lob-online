# Implementation Plan: Throttle HexMapOverlay onSvgMouseMove with rAF + Spatial Index

**Track ID:** hex-mousemove-perf_20260317
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-17
**Status:** [ ] Not Started

## Overview

Single-file change in `HexMapOverlay.vue`. Three discrete improvements in one pass:

1. rAF throttle on `onSvgMouseMove`
2. `getCellAndNeighbors` spatial helper for candidate reduction
3. `findNearestEdge` shared helper to eliminate duplication with `onSvgClick`

## Phase 1: Read & Understand Current Code

Read `HexMapOverlay.vue` in full; locate `onSvgMouseMove`, `onSvgClick`, and any existing
`grid.pointToHex` usage. Confirm the exact shape of grid cells to inform `getCellAndNeighbors`.

### Tasks

- [x] Task 1.1: Read `client/src/components/HexMapOverlay.vue`
- [x] Task 1.2: Identify all grid/cell data structures used in mouse handlers

### Verification

- [x] Know the exact variable names and call sites before writing any code

## Phase 2: Write / Update Tests

Add or update Vitest tests for the new helpers before implementing them (TDD).

### Tasks

- [x] Task 2.1: Locate or create the test file for `HexMapOverlay`
- [x] Task 2.2: Write tests for `getCellAndNeighbors` (returns candidate cell + up to 6 neighbors,
      no duplicates, handles grid boundaries)
- [x] Task 2.3: Write tests for `findNearestEdge` (returns same result as the current inline
      logic for a representative set of coordinates)

### Verification

- [x] New tests exist and fail (red) before implementation

## Phase 3: Implement the Three Improvements

### Tasks

- [x] Task 3.1: Extract `findNearestEdge(localX, localY, cellsToSearch)` from the existing inline
      logic in `onSvgMouseMove` (and verify it still matches `onSvgClick` logic)
- [x] Task 3.2: Implement `getCellAndNeighbors(hex)` using `grid.pointToHex` neighborhood lookup
- [x] Task 3.3: Add `rafPending` flag and rAF throttle wrapper to `onSvgMouseMove`
- [x] Task 3.4: Update `onSvgMouseMove` to call `getCellAndNeighbors` + `findNearestEdge`
- [x] Task 3.5: Update `onSvgClick` to call `findNearestEdge` (removes duplication)

### Verification

- [x] `npm run test` green (367/367)
- [ ] Manual smoke test: hover over edges and click edges in map editor — both still highlight/select
      correctly

## Phase 4: Polish

### Tasks

- [ ] Task 4.1: Remove any dead code left over from the refactor
- [ ] Task 4.2: Run `npm run format` and `npm run lint` — fix any issues

### Verification

- [ ] `npm run lint` clean
- [ ] `npm run format:check` clean

## Final Verification

- [ ] All acceptance criteria in spec.md met
- [ ] `npm run test` passes
- [ ] No regressions in edge click or edge hover in map editor
- [ ] Ready for `/pr-create`

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
