# Implementation Plan: Auth Guard + sideToken Validation on Join Endpoint

**Track ID:** auth-join-guard_20260506
**Spec:** [spec.md](./spec.md)
**Created:** 2026-05-06
**Status:** [x] Complete

## Overview

Two targeted fixes, each TDD: (1) add UUID-format validation in `gameSqlite.joinGame` at the store layer; (2) add a session-conflict guard in the join route that rejects callers already in the same game.

## Phase 1: Store-layer sideToken Validation

Add input validation to `createStore().joinGame` in `gameSqlite.js`. The store should not write a non-UUID value to `side_b_token`.

### Tasks

- [x] Task 1.1: Write failing tests in `gameSqlite.test.js` covering: `joinGame` throws on empty string, `joinGame` throws on non-string, `joinGame` throws on malformed string, valid UUID passes through
- [x] Task 1.2: Add UUID_RE validation at the top of `joinGame` in `createStore` — throw `TypeError` if `sideBToken` is not a non-empty UUID string

### Verification

- [ ] `npm run test` passes; new store tests green; no existing tests broken

## Phase 2: Session Conflict Guard on Join Route

Add a pre-check in `POST /:id/join` that rejects callers already in the same game. Uses the existing `getPlayerSession` helper.

### Tasks

- [x] Task 2.1: Write failing tests in `games.test.js` covering: returns 409 when caller's session `gameId` matches the route `:id`; returns 200 when caller has no session; returns 200 when caller has a session for a different game
- [x] Task 2.2: Import `getPlayerSession` in `games.js` (already imported via `setPlayerSession` — add `getPlayerSession` to the import) and add the session-conflict check at the top of the join handler, before `randomUUID()` / `joinGame` are called

### Verification

- [ ] `npm run test` passes; new route tests green; no existing tests broken

## Final Verification

- [ ] All acceptance criteria in spec.md met
- [x] `npm run lint && npm run format:check && npm run test` all pass
- [ ] Issue #340 closed

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
