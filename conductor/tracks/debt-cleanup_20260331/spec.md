# Specification: Technical Debt Cleanup — All Open Items

**Track ID:** debt-cleanup_20260331
**Type:** Chore
**Created:** 2026-03-31
**Status:** Draft

## Summary

Resolve all 13 open technical debt items (net score 19) accumulated across the OOB editor,
map editor, and server routes before beginning Phase 2 game logic work.

## Context

lob-online is at the end of Phase 1 — the OOB editor, map editor, and scenario editor dev
tools are feature-complete. Phase 2 (rules engine, auth, multiplayer) is next. This track
clears the debt register to zero so Phase 2 starts on a clean foundation.

## User Story

As a developer, I want to eliminate all 13 open debt items so that Phase 2 game logic work
is not slowed by known architectural compromises, test coverage gaps, or sub-optimal patterns.

## Acceptance Criteria

- [ ] All 13 open issues (#201, #204, #205, #206, #207, #212, #213, #214, #215, #216, #220,
      #221, #222) closed with a merge summary comment
- [ ] `docs/tech-debt/report.md` shows 0 open items and net score 0
- [ ] `npm run lint`, `npm run format:check`, and `npm run test` green after each phase
- [ ] No regressions to OOB editor, map editor, or scenario editor functionality

## Dependencies

Depends on merged OOB editor work (PRs #200, #208, #219, #223 — all on master).

## Out of Scope

- New features
- Refactors not tied to a specific debt item
- Map editor data digitization
- Phase 2 game logic

## Technical Notes

Three risk areas requiring care:

- **#207**: Persistence composable extraction touches `useOobStore.js`; must keep all sync
  behavior (push/pull/confirm dialogs) intact and reuse `useMapPersistence` patterns.
- **#220**: Async I/O migration (`readFileSync` → `fs.promises`) requires updating server
  test mocks from sync `vi.mock('fs')` to async equivalents.
- **#221**: `.strict()` on OOBSchema/LeadersSchema requires adding `_savedAt` to the Zod
  schema first (the server stamps it on every write; clients receive it on pull).

---

_Generated by Conductor. Review and edit as needed._
