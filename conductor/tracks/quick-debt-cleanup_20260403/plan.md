# Implementation Plan: Quick Debt Cleanup — Score-1 Trivials + PR #236 Test Gaps

**Track ID:** quick-debt-cleanup_20260403
**Spec:** [spec.md](./spec.md)
**Created:** 2026-04-03
**Status:** [x] Complete

## Overview

Two phases. Phase 1 handles all 8 score-1 trivials — doc/comment additions, one
naming rename, two simple unit tests, one style fix, one Vue pattern fix, and one
import-style alignment. Phase 2 adds test coverage for the succession feature gaps
from PR #236: a new schema test file, store test extensions, component tests, and
two test-call arity fixes.

## Phase 1: Score-1 Trivials (#206 #212 #213 #215 #222 #244 #249 #250)

Low-risk doc/style/test fixes. Each task is independent and can be committed separately.

### Tasks

- [x] Task 1.1: #250 — Add comment to `withLeader` in `oobTreeTransform.js` explaining why `_variants` is nested on `_leader` rather than flat on the parent node
- [x] Task 1.2: #244 — Rename `_isValidSidedShape` → `_isValidSidedObjectShape` in `useOobPersistence.js`; add doc comment explaining the two shape families (object-of-arrays vs flat-arrays)
- [x] Task 1.3: #249 — Add GET error-path test to `successionEditor.test.js` (mock `readFile` throwing ENOENT, expect 500)
- [x] Task 1.4: #213 — Pre-existing fix (closed without action)
- [x] Task 1.5: #212 — Pre-existing fix (closed without action)
- [x] Task 1.6: #215 — Pre-existing fix (closed without action)
- [x] Task 1.7: #222 — Pre-existing fix (closed without action)
- [x] Task 1.8: #206 — Pre-existing fix (closed without action)

### Verification

- [x] `npm run test` passes (no regressions)
- [x] `npm run lint` clean
- [x] Issues #206 #212 #213 #215 #222 #244 #249 #250 can be closed

## Phase 2: Score-2 Test Gaps from PR #236 (#238 #239 #240 #243 #246 #248)

New test files and test-case additions for the succession feature. Each task targets a specific gap identified in the PR #236 team-review.

### Tasks

- [x] Task 2.1: #243 — Fix the two 3-arg `buildDisplayTree` calls in `oobTreeTransform.test.js:41–45` to use the correct 4-arg signature `(oob, leaders, null, 'union')`
- [x] Task 2.2: #238 — Create `server/src/schemas/succession.schema.test.js`: validate real `succession.json`, reject missing `baseLeaderId`, reject float `commandValue`, reject invalid `commandLevel` enum, accept all-optional-omitted variant, reject extra fields
- [x] Task 2.3: #246 — Add union-side variant test and cavalry-division (Pleasonton) variant test to `oobTreeTransform.test.js`
- [x] Task 2.4: #248 — Create `client/src/components/OobTreeNode.test.js`: verify `leader-variant` sibling nodes render with `'Var'` badge when `_leader._variants` is present; verify ordering (variant after base leader, before other children)
- [x] Task 2.5: #240 — Add `isVariant` and `allLeaders` merge tests to `SuccessionList.test.js`: set `store.succession`, verify variant in type-ahead, `.variant-tag` renders for succession IDs, not for base leader IDs
- [x] Task 2.6: #239 — Update `useOobStore.test.js`: fix `mockFetch` to return 3 responses; add tests for `store.succession` init as null, populated after `loadData`, `usedCounterFiles` collecting from succession data, push clearing `lob-succession-editor-v1`

### Verification

- [x] `npm run test` passes including all new test files
- [x] Issues #238 #239 #240 #243 #246 #248 can be closed

## Final Verification

- [x] All 14 acceptance criteria issues closed
- [x] `npm run lint && npm run format:check && npm run test` all pass
- [ ] Tech-debt report updated (14 items resolved, net score 49 → 28)
- [x] Ready for PR

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
