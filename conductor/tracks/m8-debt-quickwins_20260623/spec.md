# Specification: M8 Debt Quick-Wins Sprint

**Track ID:** m8-debt-quickwins_20260623
**Type:** Chore
**Created:** 2026-06-23
**Status:** Draft

## Summary

Close all score-1 and score-2 M8 tech-debt items in a single PR. Ten issues covering env var
documentation, docker-compose polish, discord-sink refactors, and code comments. Closing all
ten reduces the open debt score by 14 points (32 → 18).

## Context

M8 is complete. Before starting M9, the project debt register must be driven below 15 (net
open score). The score-1/2 items are all self-contained, low-risk fixes that can be bundled
safely without DB migrations, schema changes, or auth surface touches.

## Acceptance Criteria

- [ ] #641 — `SPACES_REGION` added to `.env.example` with value `us-east-1` and inline comment
- [ ] #642 — `docker-compose.yml` MinIO service gains `healthcheck` and `restart: unless-stopped`
- [ ] #643 — `DISCORD_WEBHOOK_TEST_URL` renamed to `DISCORD_WEBHOOK_URL` everywhere (env, code, docs, tests)
- [ ] #644 — `discord-test-server.js` separates `app` export from `app.listen()` call
- [ ] #645 — `discord-test-server.js` port read from `DISCORD_SINK_PORT` env var (default 4040)
- [ ] #646 — `docs/` or `.env.example` notes the `DO_SPACES_*` → `SPACES_*` rename for existing deploys
- [ ] #654 — `.env.example` comment on `DISCORD_WEBHOOK_TEST_URL` (now `DISCORD_WEBHOOK_URL`) warns of staging SSRF risk
- [ ] #655 — `requireSide` JSDoc extended to document `req.game` shape and contract
- [ ] #656 — `requireSide` dependency made explicit in the actions route handler (comment or assertion)
- [ ] #657 — Direct unit tests for `isAllowedDiscordWebhook` added to `discord.test.js`
- [ ] All existing tests still pass; no new warnings

## Dependencies

- None — all fixes are additive or doc-only

## Out of Scope

- Score-3+ debt items (separate tracks)
- Any auth/session/DB schema changes
- New features

## Technical Notes

- #643 (webhook var rename) touches `discord.js`, `discord.test.js`, `.env.example`, `CLAUDE.md`/PROVISIONING.md,
  and the `discord:sink` npm script. Search for all occurrences before renaming.
- #644/#645 (discord-test-server refactor) must not break `npm run discord:sink` — the listen
  call must still fire when the script is run directly (use `import.meta.url === process.argv[1]`
  or equivalent ES module main-guard).
- #657 unit tests: test the allowlist function directly with known-good Discord CDN hostnames,
  a localhost attempt, an arbitrary URL, and an empty string.

---

_Generated by Conductor. Review and edit as needed._
