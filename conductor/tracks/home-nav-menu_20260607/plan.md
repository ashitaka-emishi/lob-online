# Implementation Plan: App Home Page and Editor Navigation Menu

**Track ID:** home-nav-menu_20260607
**Spec:** [spec.md](./spec.md)
**Created:** 2026-06-07
**Status:** [x] Complete

## Overview

Add a `HomeView` component at `/`, apply `menu-bg.png` as the background for home and lobby,
build a shared `EditorNav` component for all four editor/tool pages, and wire editor-enabled
gating throughout. Three phases: (1) HomeView + router update, (2) EditorNav component +
editor integration, (3) LobbyView background + test coverage closeout.

## Interaction Mode

**Mode:** Autonomous
**Human control points:** None beyond phase approvals

## Risk Classification

**Risk:** Medium
**Reason:** New UI components and router change; no auth, persistence, or rules-engine surfaces touched.

## Quality Gates

- [x] `npm run validate-data`
- [x] `npm run lint`
- [x] `npm run format:check`
- [x] `npm run test`
- [x] `npm run build`
- [x] No unexpected warnings in test output

## Debt Budget

**Allowed new deferred debt:** 0 unless explicitly approved.

## Completion Contract

- [x] All plan tasks complete
- [x] All acceptance criteria in spec.md met
- [x] Warnings fixed or explicitly classified as accepted prototype noise
- [x] Debt register updated if any debt was accepted
- [x] Ready for `/team-review`

---

## Phase 1: HomeView + Router Update

Create the home page component and make it the default route. Confirm the
editor-enabled flag mechanism available to the client.

### Tasks

- [x] Task 1.1: Audit how `MAP_EDITOR_ENABLED` is (or is not) surfaced to the Vue client today.
      Check for a `/api/v1/config` endpoint or existing `VITE_*` env var. Decide: use
      `VITE_MAP_EDITOR_ENABLED` in `client/.env` / Vite config, or fetch from the API.
- [x] Task 1.2: Write tests for `HomeView` (jsdom):
  - Renders Lobby and Editor buttons.
  - Editor button navigates to `/tools/map-editor` when editors enabled.
  - Editor button navigates to `/lobby` when editors disabled.
- [x] Task 1.3: Create `client/src/views/HomeView.vue` with `menu-bg.png` full-bleed background,
      Lobby and Editor menu buttons, and disabled-editor guard logic.
- [x] Task 1.4: Update `client/src/router/index.js` — replace `StatusView` import at `'/'`
      with `HomeView`.

### Verification

- [x] `HomeView` tests pass; visiting `/` in the browser shows the home page with background.

---

## Phase 2: EditorNav Shared Component

Build the navigation strip that appears on all four editor/tool pages.

### Tasks

- [x] Task 2.1: Write tests for `EditorNav` (jsdom):
  - All four tool links present when editors enabled.
  - Tool links hidden/disabled when editors disabled.
  - Home/Lobby escape hatch always visible.
- [x] Task 2.2: Create `client/src/components/EditorNav.vue` — links to Map Editor, OOB Editor,
      Map Test, Table Test, and a Home escape hatch; respects editor-enabled flag.
- [x] Task 2.3: Add `<EditorNav>` to `MapEditorView.vue`.
- [x] Task 2.4: Add `<EditorNav>` to `OobEditorView.vue`.
- [x] Task 2.5: Add `<EditorNav>` to `MapTestView.vue`.
- [x] Task 2.6: Add `<EditorNav>` to `TableTestView.vue`.

### Verification

- [x] `EditorNav` tests pass; each tool page renders the nav strip in the browser.

---

## Phase 3: LobbyView Background + Test Closeout

Apply the shared background image to the lobby and ensure all existing tests pass clean.

### Tasks

- [x] Task 3.1: Update `LobbyView.vue` to apply `menu-bg.png` as background (same CSS treatment
      as `HomeView`).
- [x] Task 3.2: Update `LobbyView.test.js` if snapshot or style assertions need adjustment.
- [x] Task 3.3: Update existing editor view tests (`MapEditorView.test.js`,
      `OobEditorView.test.js`, `MapTestView.test.js`, `TableTestView.test.js`) to account for
      the new `EditorNav` mount without regressions.
- [x] Task 3.4: Run `npm run quality:strict` — fix any lint, format, or test failures.

### Verification

- [x] `npm run quality:strict` passes clean with no unexpected warnings.

---

## Final Verification

- [x] All acceptance criteria in spec.md met
- [x] Tests passing at ≥ 70% line coverage
- [x] No unexpected warnings in test output
- [x] Ready for `/team-review`

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
