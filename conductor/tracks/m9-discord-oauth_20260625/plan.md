# Implementation Plan: M9 Discord OAuth

**Track ID:** m9-discord-oauth_20260625
**Spec:** [spec.md](./spec.md)
**Created:** 2026-06-25
**Status:** [ ] Not Started

## Overview

Three phases: server auth plumbing, DB schema + route wiring, client integration.
Touches auth, session, and DB surfaces — Checkpointed mode required at each phase boundary.

## Interaction Mode

**Mode:** Checkpointed
**Human control points:** Approve Phase 1 server design; approve Phase 2 DB migration;
approve Phase 3 client before opening PR

## Risk Classification

**Risk:** High
**Reason:** Auth surface, session model, and SQLite schema migration.

## Quality Gates

- [ ] `npm run validate-data`
- [ ] `npm run lint`
- [ ] `npm run format:check`
- [ ] `npm run test`
- [ ] `npm run build`
- [ ] No unexpected warnings in test output

## Debt Budget

**Allowed new deferred debt:** 0 unless explicitly approved.

## Completion Contract

- [ ] Closes #668 and #410
- [ ] All acceptance criteria in spec.md met
- [ ] `npm run quality:strict` passes
- [ ] Ready for `/team-review`

---

## Phase 1: Server Auth Plumbing

### Tasks

- [ ] Task 1.1: Install `passport`, `passport-discord`; add to `package.json` root deps
- [ ] Task 1.2: Create `server/src/auth/discord.js` — configure `passport-discord` strategy
      with `DISCORD_CLIENT_ID`, `DISCORD_CLIENT_SECRET`, `DISCORD_CALLBACK_URL` env vars;
      `verify` callback upserts user row in SQLite and calls `done(null, user)`
- [ ] Task 1.3: Wire `passport.initialize()` + `passport.session()` in `server/src/index.js`
      after `express-session` middleware; add `passport.serializeUser` / `deserializeUser`
- [ ] Task 1.4: Create `server/src/routes/auth.js` with three routes: - `GET /auth/discord` → `passport.authenticate('discord')` - `GET /auth/discord/callback` → authenticate + redirect to `/lobby` - `POST /auth/logout` → `req.logout()` + redirect - `GET /auth/me` → return `req.user` or 401
- [ ] Task 1.5: Mount `/auth` router in `server/src/index.js`

### Verification

- [ ] `GET /auth/discord` redirects to `discord.com/oauth2/authorize`
- [ ] `GET /auth/me` returns 401 when unauthenticated
- [ ] Unit tests for auth route 401 path and `passport.serializeUser` behavior

---

## Phase 2: DB Migration + Route Wiring

### Tasks

- [ ] Task 2.1: SQLite v1→v2 migration in `server/src/store/sqlite.js`: - Add `users` table: `id TEXT PRIMARY KEY, username TEXT, avatar TEXT, created_at INTEGER` - Add `side_a_user_id TEXT` and `side_b_user_id TEXT` columns to `games` table
- [ ] Task 2.2: In create-game route, set `side_a_user_id = req.user.id`
- [ ] Task 2.3: In join-game route, set `side_b_user_id = req.user.id` after faction check
- [ ] Task 2.4: In `GET /api/v1/games`, filter to rows where
      `side_a_user_id = req.user.id OR side_b_user_id = req.user.id`
- [ ] Task 2.5: Add `requireAuth` middleware (similar to `requireSide`) that returns 401 if
      `!req.user`; apply to all `/api/v1/games` routes

### Verification

- [ ] Migration runs cleanly on existing SQLite DB (no data loss)
- [ ] Integration tests: create game sets `side_a_user_id`; join game sets `side_b_user_id`
- [ ] `GET /api/v1/games` only returns the calling user's games

---

## Phase 3: Client Integration

### Tasks

- [ ] Task 3.1: In `useModuleStore` (or a new `useAuthStore`), add `currentUser` ref;
      `GET /auth/me` on app mount; expose `isLoggedIn` computed
- [ ] Task 3.2: `HomeView` — show Discord login button when `!isLoggedIn`; show username +
      avatar when logged in; Lobby and Editor buttons disabled until logged in
- [ ] Task 3.3: Vue Router navigation guard on `/lobby` — redirect to `/` if `!isLoggedIn`
- [ ] Task 3.4: `LobbyView` header — display `currentUser.username` and avatar

### Verification

- [ ] Unauthenticated user at `/lobby` redirects to `/`
- [ ] After OAuth callback, `HomeView` shows Discord username
- [ ] `npm run quality:strict` passes

---

_Generated by Conductor on 2026-06-25._
