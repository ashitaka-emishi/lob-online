# Implementation Plan: OOB Editor Debt Sprint — Issues #207 #209 #210 #211

**Track ID:** oob-debt-sprint_20260329
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-29
**Status:** [ ] Not Started

## Overview

Four debt items resolved in dependency order: store-level refactors first (nodePath
threading #210 and usedFiles hoist #209 — both pure store/component wiring changes),
then the CounterImageWidget UX fix (#211 — depends on store shape being stable), then
the push/pull safety layer (#207 — independent but last to avoid distracting from the
structural fixes).

## Phase 1: Store Structural Fixes (#210 and #209)

Convert `selectedNodePath` from a reactive computed tree-walk to a stored ref, and
move `usedFiles` out of the widget and into the store.

### Tasks

- [x] Task 1.1: Change `selectedNodePath` in `useOobStore` from `computed` to `ref(null)`; update `selectNode(node, nodeType)` signature to `selectNode(node, nodeType, nodePath = null)` and store the path (#210)
- [x] Task 1.2: Update `OobTreeNode.vue` call sites — pass the `path` it receives from the `findNodePath` call (or its own injected path) into `selectNode` (#210)
- [x] Task 1.3: Move `collectUsed` helper and `usedFiles` computed into `useOobStore` as `usedCounterFiles`; export from store (#209)
- [x] Task 1.4: Remove `usedFiles` computed and `collectUsed` helper from `CounterImageWidget.vue`; replace with `store.usedCounterFiles` (#209)
- [x] Task 1.5: Update or add tests — assert `selectedNodePath` is set by `selectNode` without a tree-walk, and that `usedCounterFiles` is present on the store (#210, #209)

### Verification

- [ ] `store.selectedNodePath` returns the path passed to `selectNode` directly (no `findNodePath` call on selection)
- [ ] `CounterImageWidget` no longer owns a `usedFiles` computed; it reads `store.usedCounterFiles`
- [ ] `npm run test` passes

## Phase 2: Remove Auto-Commit on Empty Slot Click (#211)

Remove the silent `commit()` call from `activate()` so that clicking an empty slot
enters preview mode without writing to the store.

### Tasks

- [ ] Task 2.1: In `CounterImageWidget.vue` `activate()`, remove `if (!current && list.length > 0) commit()` — the slot should display the first available counter as a preview without persisting (#211)
- [ ] Task 2.2: Ensure `commit()` is still called on ↑/↓ navigation (existing `cycleUp`/`cycleDown` paths — verify they still commit) (#211)
- [ ] Task 2.3: Add or update tests — assert that activating an empty slot does NOT call the store mutation, and that cycling does commit (#211)

### Verification

- [ ] Clicking an empty slot shows a counter visually but does not write to the store
- [ ] ↑/↓ still commits the selection
- [ ] `npm run test` passes

## Phase 3: Push Confirmation Dialog and Zod Validation (#207)

Add a confirmation step before `pushToServer` overwrites the server, and validate
parsed data in `loadData`/`pullFromServer`.

### Tasks

- [ ] Task 3.1: Add `showPushConfirm` ref to `useOobStore`; wrap `pushToServer` — set `showPushConfirm = true` rather than calling push directly; add `confirmPush()` action that performs the actual push and resets the flag (#207)
- [ ] Task 3.2: Add a confirmation dialog UI in `OobEditorView.vue` (or its toolbar component) that is shown when `showPushConfirm` is true; cancel resets to false, confirm calls `confirmPush()` (#207)
- [ ] Task 3.3: In `loadData` and `pullFromServer`, validate the parsed JSON against the existing Zod schemas (import oobSchema and leadersSchema from `server/src/schemas/`); set `syncError` on validation failure (#207)
- [ ] Task 3.4: Add tests — assert push does not fire until confirmed, assert validation failure sets `syncError` and does not update store state (#207)

### Verification

- [ ] Clicking Push shows a confirmation dialog; server is only updated after confirmation
- [ ] Pulling malformed data sets `syncError` without corrupting store state
- [ ] `npm run test` passes

## Final Verification

- [ ] All 6 acceptance criteria from spec met (#207 #209 #210 #211)
- [ ] `npm run lint` passes
- [ ] `npm run format:check` passes
- [ ] `npm run test` passes with ≥70% coverage
- [ ] OOB editor visually correct in browser — push/pull, counter slot interaction, tree selection all behave correctly
- [ ] Ready for `/pr-create`

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
