# Specification: OOB Editor Debt Sprint — Issues #207 #209 #210 #211

**Track ID:** oob-debt-sprint_20260329
**Type:** Refactor
**Created:** 2026-03-29
**Status:** Draft

## Summary

Resolve the four highest-priority open debt items from the OOB editor: remove the
silent auto-commit on counter slot click (#211), hoist the per-widget `usedFiles`
computed into the store (#209), thread `nodePath` through `selectNode` to eliminate
the O(n) tree-walk (#210), and add a push confirmation dialog plus Zod schema
validation to `useOobStore` (#207 partial).

## Context

The OOB editor was built across two tracks (`oob-editor-foundation_20260325` and
`oob-detail-panel_20260325`) and shipped with 8 deferred debt items (net score 14 of
the current 25). The two score-3 items (#211, #207) and two score-2 items (#209, #210)
are the highest-priority targets. Addressing them now prevents compounding as the
editor grows toward Phase 2 (editing) and brings the OOB persistence pattern into
alignment with `useMapPersistence`.

## Acceptance Criteria

- [ ] Clicking an empty counter slot activates visually without writing to store; only ↑/↓ commits (#211)
- [ ] `usedFiles` computed lives in the store as shared `usedCounterFiles`; `CounterImageWidget` reads from store (#209)
- [ ] `selectNode` accepts optional `nodePath`; `selectedNodePath` is a ref set during selection, not a computed tree-walk (#210)
- [ ] `pushToServer` shows a confirmation dialog before overwriting server data (#207)
- [ ] `loadData` and `pullFromServer` validate parsed data against Zod schemas (#207)
- [ ] All CI gates pass (`lint`, `format:check`, `test`)

## Dependencies

- `useOobStore` (`client/src/stores/useOobStore.js`)
- `CounterImageWidget.vue` (`client/src/components/CounterImageWidget.vue`)
- `OobTreeNode.vue` (calls `selectNode` — must be updated to pass path)
- `useMapPersistence.js` (reference pattern for #207)
- Existing Zod schemas in `server/src/schemas/` (reference for oob/leaders schema shape)

## Out of Scope

- Full alignment of `useOobStore` with `useMapPersistence` (offline detection, data
  validation on push — those are separate items #207 remaining, #202)
- The two remaining score-3 items not listed above (#216 prototype pollution guard)
- Any new OOB editor features (Phase 2 editing capability)
- Migrating the dual-script pattern in `OobTreeNode.vue` (#215)

## Technical Notes

- `selectedNodePath` fix (#210): change `selectedNodePath` from `computed(() =>
findNodePath(...))` to a plain `ref(null)`. Update `selectNode(node, nodeType,
nodePath = null)` to accept and store the path. Update `OobTreeNode.vue` call sites
  to pass the path it already holds from `findNodePath`.
- `usedFiles` hoist (#209): move the `collectUsed` walk + `computed` into `useOobStore`
  as `usedCounterFiles`. `CounterImageWidget` removes its own `usedFiles` and reads
  `store.usedCounterFiles` instead.
- Auto-commit fix (#211): in `activate()`, remove the `if (!current && list.length > 0)
commit()` call. The slot should enter a "previewing first" state (or simply show the
  first available counter without writing). ↑/↓ then navigates; explicit save or
  navigation away commits.
- Push confirmation (#207): add a `showPushConfirm` ref + a minimal confirmation modal
  (reuse the map editor's ConfirmDialog pattern or an inline `<dialog>`). Only call
  `pushToServer` after the user confirms.

---

_Generated by Conductor. Review and edit as needed._
