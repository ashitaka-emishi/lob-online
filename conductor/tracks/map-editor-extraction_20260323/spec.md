# Specification: MapEditorView Extraction Debt Bundle

**Track ID:** map-editor-extraction_20260323
**Type:** Refactor
**Created:** 2026-03-23
**Status:** Draft

## Summary

Extract `useCalibration` and `useMapExport` out of the 1193-line `MapEditorView.vue`, and reduce the `useMapPersistence` API surface from 23 return values to a structured shape. Closes GitHub issues #126 and #125.

## Context

MapEditorView is 1193 lines after 8 composables have already been extracted. Two self-contained concerns — calibration state/persistence and map export — remain inline (~100 lines combined). `useMapPersistence` returns 23 values (persistence core mixed with pull/push dialog state), which increases caller coupling and reduces reusability. Both issues will compound once Phase 2 game logic touches the same component.

## Acceptance Criteria

- [ ] `useCalibration(mapData)` composable exists; MapEditorView uses it and holds zero calibration logic inline
- [ ] `useMapExport(mapData, calibration)` composable exists; MapEditorView holds zero export logic inline
- [ ] `useMapPersistence` return surface reduced to ≤ 3 grouped sub-objects (e.g. `state`, `dialog`, `actions`)
- [ ] MapEditorView caller updated to destructure the new grouped API; all existing behavior preserved
- [ ] All new composables have co-located Vitest unit tests
- [ ] No regressions: existing tests pass, CI green

## Dependencies

Existing composables in `client/src/composables/`. MapEditorView at `client/src/views/tools/MapEditorView.vue`. useMapPersistence at `client/src/composables/useMapPersistence.js`.

## Out of Scope

- `#151` gridData full-rebuild perf fix (different file, different fix shape — own track)
- `#161` per-tool-panel wiring composable
- `#169` cellsForEdges extraction
- Any behavior changes or new features

## Technical Notes

- Calibration storage key is `'lob-map-editor-calibration-v4'` (localStorage); the composable must keep this stable
- Export must remain a non-computed function call (not a reactive computed) — avoids deep-walk on every render
- `useMapPersistence` grouping suggestion: `{ state: {...refs}, dialog: {...refs}, actions: {...fns} }` — open to alternative shape that passes review

---

_Generated by Conductor. Review and edit as needed._
