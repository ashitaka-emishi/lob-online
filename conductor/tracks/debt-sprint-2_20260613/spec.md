# Specification: Pre-M6 Debt Sprint #2 — Debt, Doc Sync, Stubs

**Track ID:** debt-sprint-2_20260613
**Type:** Chore
**Created:** 2026-06-13
**Status:** Draft

## Summary

Close four actionable score-2 debt items (#542, #545, #546, #533), populate stub metadata
for the 8 non-South-Mountain module scenario/game files so they reflect real game names and
known battle info, sync project documentation to M5.5-complete state, and update the
domain-expert agent design doc with rule references added in M5–M5.5.

## Context

lob-online is at M5.5 complete. Net open debt is 23 pts across 13 items after PR #548.
Four score-2 items are immediately actionable: #542 (editor slug reactivity), #545
(ModuleNotFoundError middleware), #546 (moduleData route test gaps), and #533 (totalTurns
removal). Eight non-SM modules (afs, ib, lcv, nbh, ntb, thg, tts, ttw) have scaffold
scenario.json files with generic placeholder names like "AFS (placeholder)" — their
module.json already has real names and battle info that should be reflected in the scenario
stubs. CLAUDE.md and high-level-design.md are stale relative to M5.5 deliverables, and the
domain-expert agent design doc needs updated rule references from M5–M5.5 implementation.

## Acceptance Criteria

- [ ] #542: Editor views use `computed(() => route.params.moduleSlug)` (reactive); no setup-time
      capture of the slug in non-reactive local variables.
- [ ] #545: `ModuleNotFoundError` registered in the central Express error-handler middleware;
      any route that throws it gets a clean 404 JSON response without an inline catch.
- [ ] #546: `moduleData` route tests cover unknown-slug 404, scenario sub-route paths, and
      invalid scenarioSlug rejection (at minimum 3 new test cases).
- [ ] #533: `totalTurns` removed from `scenario.json` data files and from `ScenarioSchema`;
      any server-side code that uses it derives the value from turn structure instead.
- [ ] Stub scenario.json files for all 8 non-SM modules updated: `name` and `id` use the real
      game name/slug (not "AFS (placeholder)"), `_status` remains `"scaffold"`, battle field
      populated from module.json where known.
- [ ] CLAUDE.md "Current state" paragraph updated to reflect M5.5 complete + M6 next.
- [ ] `docs/designs/high-level-design.md` milestone table updated (M5.5 ✅, M6 🔜 Next).
- [ ] `docs/agents/domain-expert/design.md` updated with rule references for: lighting schedule
      conditions (fog/rain, LOB §1.1), VISIBILITY_UNLIMITED sentinel (LOB §6.1), sister-module
      pattern for turnTime and visibility constants, and any M5/M5.5 rules additions.
- [ ] `npm run quality:strict` passes with no unexpected warnings.

## Dependencies

- `server/src/routes/moduleData.js` and route tests — for #545, #546
- `server/src/engine/init.js` + `server/src/schemas/scenario.schema.js` — for #533
- `data/modules/*/scenarios/full-battle/scenario.json` — stub updates (#533 + game stubs)
- Vue editor views + Vue Router — for #542
- CLAUDE.md, docs/designs/high-level-design.md, docs/agents/domain-expert/design.md

## Out of Scope

- Score-1 items (#547, #544, #538)
- M6-blocked stubs (#379, #381, #382, #383)
- M8-blocked security (#350, #403)
- Digitizing actual game data (OOB, map, leaders) for non-SM modules — stubs only
- Any new M6 feature work

## Technical Notes

- Stub scenario.json update: preserve `_status: "scaffold"` and all existing placeholder
  data; only update `id`, `name`, and add `_battle` and `_publication` fields from module.json.
  Run `npm run validate-data` after changes — stubs must still pass schema validation.
- #533 totalTurns: `init.js` currently reads `scenario.totalTurns`; replace with a derived
  computation from `firstTurn`, `lastTurn`, and `MINUTES_PER_CONDITION`. Verify scaffold
  modules (which have a `totalTurns` field) don't break validate-data after schema change —
  use `.strip()` or make it a passthrough-ignored optional field with a deprecation note, or
  remove it from all scenario.json files in a single sweep.
- Interaction Mode: Checkpointed (touches schema validation, data files, shared middleware).

---

_Generated by Conductor. Review and edit as needed._
