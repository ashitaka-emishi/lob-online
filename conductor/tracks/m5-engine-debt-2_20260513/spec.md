# Specification: M5 Engine Debt Bundle 2 — Schema Hardening & Code Quality

**Track ID:** m5-engine-debt-2_20260513
**Type:** Chore
**Created:** 2026-05-13
**Status:** Draft

## Summary

Close 9 deferred debt items from PRs #359, #375, and #386 that are actionable at M5 depth:
three UnitStateSchema invariant/default fixes, two order-schema hardening items, one engine
invariant check, one constants extraction, and two documentation/comment fixes.

## Context

M5's rules engine (dispatch, drainAutoSteps, GameStateSchema, UnitStateSchema) has accumulated
minor-to-moderate schema gaps and code quality issues from PRs #359 and #375. Three items are
schema-surface changes that will become harder to fix once M6 adds morale/combat state (#362,
#367, #372). Two items (#388, #389) must be resolved before M6 extends drainAutoSteps and the
phase sequence with morale/combat phases. #369 ('stopped' status) is already present in the schema
and needs only issue closure.

## Acceptance Criteria

- [ ] #362: Zod refinement enforces `isDetached: true` ↔ `orders !== null` invariant in UnitStateSchema
- [ ] #367: `isDetached` field carries `.default(false)` so omitting it doesn't fail validation
- [ ] #369: Verified that `'stopped'` status already exists in UnitOrderState enum; issue closed
- [ ] #372: All UnitOrderState `.refine()` calls include a `path` option for field-level errors
- [ ] #388: Phase/step string literals extracted to a shared `server/src/engine/phases.js` module
- [ ] #389: drainAutoSteps asserts phase-envelope invariant after each iteration before M6 extends it
- [ ] #365: `isDetached` comment carries specific SM section/rule citation
- [ ] #366: OrderType JSDoc corrected to remove `null = in-pipeline` conflation
- [ ] #368: Parent-order inheritance for `orders: null` documented at schema and query site
- [ ] All existing tests pass with no regressions
- [ ] `npm run quality:strict` exits 0

## Dependencies

- `server/src/schemas/gameState.schema.js` — UnitStateSchema, UnitOrderState (existing)
- `server/src/schemas/gameState.schema.test.js` — existing test suite
- `server/src/engine/actions/index.js` — drainAutoSteps
- `server/src/engine/actions/endPhase.js` — phase/step string literals
- Domain-expert agent if SM section number for isDetached rule is unclear

## Out of Scope

- #340 sideToken auth guard (own Checkpointed track — auth/session surface)
- #361 scenario-start detached brigade data fix (own track — scenario data changes)
- #360 reinforcement orderType in initGameState (own track)
- #363 migration path for orders schema (own track — needs migration design)
- #379, #381, #382, #383 (blocked on M6 morale/combat data)
- #387 (blocked on #356 HTTP action route)

## Technical Notes

Three items touch the Zod schema persistence surface → Checkpointed mode required.
Human checkpoint after Phase 1 (schema changes) before Phase 2 (engine hardening).
`'stopped'` status in #369 is already present — verify at Task 1.1 and close immediately.

---

_Generated by Conductor. Review and edit as needed._
