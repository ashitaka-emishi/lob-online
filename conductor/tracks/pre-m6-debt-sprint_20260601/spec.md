# Specification: Pre-M6 Debt Sprint

**Track ID:** pre-m6-debt-sprint_20260601
**Type:** Chore
**Created:** 2026-06-01
**Status:** Draft

## Summary

Resolve the five score-3 open debt items before M6 combat work begins. Covers one auth gap, one error-message leak, one client resilience fix, one test coverage gap, and one accessibility gap.

## Context

M5 is complete. The five items below were deferred from earlier PRs and are the highest-priority technical debt in the register (all score 3). Leaving them unresolved risks an auth boundary gap entering M6, internal Zod details reaching clients, and total loss of OOB display on a transient leaders fetch failure.

## Acceptance Criteria

- [ ] #477 — `requireSide` validates the session `sideToken` against the DB record; stale/revoked sessions are rejected
- [ ] #478 — `INVALID_STATE` and `DRAIN_LOOP` server messages are sanitized before reaching the client; `INVALID_PAYLOAD` is handled with a 400 status
- [ ] #479 — A leaders-only failure degrades gracefully: OOB unit data still loads; only leader-specific fields (counter images, leader names) are absent
- [ ] #476 — `useOobData` tests assert the leaders fetch URL, exercise the failure branch, and verify the leaders-merge path in `oobUnitMap`
- [ ] #480 — `UnitCounterLayer` counters expose `aria-pressed` for the selected unit; focus is managed correctly after selection
- [ ] All five GitHub issues closed, `npm run quality:strict` passing, no new debt accepted

## Dependencies

- `server/src/routes/games.js` — `requireSide` middleware + actions route
- `client/src/composables/useOobData.js` — leaders fetch + `oobUnitMap` merge
- `client/src/components/UnitCounterLayer.vue` — interactive counter elements
- SQLite games table: `side_a_token`, `side_b_token` columns (already present)

## Out of Scope

- M5 GameView feature work (#472 #473 #474)
- Score-1 or score-2 debt items
- New features or M6 work

## Technical Notes

- **#477** touches the auth path on `POST /api/v1/games/:id/actions` — Checkpointed surface; requires human review after implementation
- **#480** touches `UnitCounterLayer.vue` which is a shared game map component — Checkpointed surface
- Phases are ordered: server fixes first (less coupling), then client composable, then UI accessibility
- No new deferred debt allowed

---

_Generated by Conductor. Review and edit as needed._
