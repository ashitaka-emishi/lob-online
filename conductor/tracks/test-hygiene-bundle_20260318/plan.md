# Implementation Plan: Test Hygiene Bundle

**Track ID:** test-hygiene-bundle_20260318
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-18
**Status:** [ ] Not Started

## Overview

Three independent phases grouped by scope: (1) client component test fixes (CalibrationControls
selector upgrade + ElevationSystemControls edge-case tests), (2) server schema fix + tests
(superRefine dynamic bounds), (3) MapEditorView fixture refactor. Each phase is independently
verifiable and can be reviewed as a standalone commit.

---

## Phase 1: Client Component Tests (#101, #104, #105)

Fix the brittle `CalibrationControls` elevation-section selectors and fill the two missing
behavioral/edge-case gaps in `ElevationSystemControls.test.js`.

### Tasks

- [x] Task 1.1: In `CalibrationControls.test.js` "Elevation System section" describe block,
      replace all `inputs[9]` / `inputs[10]` index selectors with
      `wrapper.find('[data-testid="base-elevation-input"]')` and
      `wrapper.find('[data-testid="elevation-levels-input"]')`. Remove the
      `expect(inputs.length).toBe(11)` count assertions that depend on total input count.
- [x] Task 1.2: In `ElevationSystemControls.test.js`, add a test for #104: confirm no
      `elevation-system-change` event is emitted when input fires while `locked=true`
      (set a value then trigger `input` — emitted should be falsy).
- [x] Task 1.3: In `ElevationSystemControls.test.js`, add two tests for #105: (a) empty
      string input emits `baseElevation: 0`; (b) non-numeric string input emits
      `baseElevation: NaN` — documenting the existing `Number()` coercion contract.

### Verification

- [x] `npm run test -- CalibrationControls` passes
- [x] `npm run test -- ElevationSystemControls` passes (now 11 tests)
- [x] No lint or format errors

---

## Phase 2: Schema superRefine + Tests (#103, #106)

Fix the static ±21 wedge offset bound and tighten the superRefine rejection test assertions.

### Tasks

- [ ] Task 2.1: In `server/src/schemas/map.schema.js`, update the `superRefine` wedge
      offset check: derive `maxOffset` from
      `data.elevationSystem?.elevationLevels - 1 ?? 21` and replace the hardcoded `21`
      bounds check with `Math.abs(offset) <= maxOffset`. Add a descriptive error message
      that includes the dynamic max value.
- [ ] Task 2.2: In `map.schema.test.js` "wedgeElevations integer validation" describe block,
      add two tests for #103: (a) a map with `elevationSystem.elevationLevels: 5` rejects
      `wedgeElevations: [5, 0, 0, 0, 0, 0]` (offset equals elevationLevels, not levels−1);
      (b) accepts `wedgeElevations: [4, 0, 0, 0, 0, 0]` for the same elevationSystem.
- [ ] Task 2.3: In `map.schema.test.js`, for each existing `superRefine` rejection test (#106),
      add an assertion on `result.error.issues[0].path` (should contain `['hexes', 0,
'wedgeElevations']` or similar) or `result.error.issues[0].code === 'custom'`.

### Verification

- [ ] `npm run test -- map.schema` passes
- [ ] Existing wedge offset boundary tests still pass (±21 with `elevationLevels: 22` default)
- [ ] No lint or format errors

---

## Phase 3: MapEditorView Fixture Refactor (#102)

Replace the mutable module-level `VALID_MAP_WITH_ELEVATION` object with a factory function.

### Tasks

- [ ] Task 3.1: In `MapEditorView.test.js`, convert `const VALID_MAP_WITH_ELEVATION = {...}`
      to `function makeElevationMap() { return { ...VALID_MAP, elevationSystem: { ... } }; }`.
      Update all call sites that reference `VALID_MAP_WITH_ELEVATION` to call `makeElevationMap()`
      instead. Remove any `JSON.parse(JSON.stringify(...))` workarounds that guarded against
      mutation.

### Verification

- [ ] `npm run test -- MapEditorView` passes
- [ ] No lint or format errors

---

## Final Verification

- [ ] All acceptance criteria in spec.md met
- [ ] `npm run lint` passes
- [ ] `npm run format:check` passes
- [ ] `npm run test` passes (coverage ≥ 70%)
- [ ] Ready for PR via `/pr-create`

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
