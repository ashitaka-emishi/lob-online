# Specification: Post-M5 Debt Bundle

**Track ID:** post-m5-debt-bundle_20260522
**Type:** Refactor
**Created:** 2026-05-22
**Status:** Draft

## Summary

Resolve the 17 actionable open debt items accumulated through PR #420, reducing the project debt score from 51 to ≤ 18 and clearing both production-blocking OOB data path items before M6 begins.

## Context

After M5's game-view viewport and map-config integration work (PRs #400, #409, #420), the debt register holds 29 open items at a cumulative score of 51. Two score-3 items (#431, #401) are flagged as production blockers that must be resolved before any M6 game-view work: #431 causes unit counters to silently disappear in production builds without `MAP_EDITOR_ENABLED`, and #401 means every future game view will duplicate OOB loading logic. An additional 15 actionable items across score-2 and score-1 represent contract/coupling issues, code quality gaps, and cosmetic fixes safe to close now. Twelve items are explicitly deferred to M6 or later (engine stubs needing combat data, M8 auth hardening, performance items with no current trigger).

## User Story

As a developer starting M6, I want the production-blocking OOB data path fixed and the accumulated contract debt from M5 cleaned up, so that adding new game views does not require duplicating OOB loading logic or silently failing in production builds.

## Acceptance Criteria

- [ ] #431 resolved: GameView fetches OOB from a production-safe `/api/v1/oob` route, not the dev-tool endpoint
- [ ] #401 resolved: OOB fetch, walk, and computed moved into `useOobData.js` composable
- [ ] #404 resolved (side effect of #401): `_collectOobUnits` extracted into the composable
- [ ] #432 resolved: aria-label uses human-readable unit name from OOB data
- [ ] #421 resolved: map-config route corrected to scenario-scoped URL
- [ ] #422 resolved: non-fatal error indicator shown on map-config fetch failure
- [ ] #423 resolved: 503 startup-error branch covered by test or restructured to be coverable
- [ ] #424 resolved: `sanitizeCalibration` extracted from Map-Editor composable to shared util
- [ ] #425 resolved: Zod schema at store boundary validates gridSpec shape
- [ ] #426 resolved: explicit merge contract between gridSpec and calibration fields
- [ ] #427 resolved: double `loadMap()` startup warning eliminated
- [ ] #411 resolved: `session.regenerate()` wrapper extracted to helper in `games.js`
- [ ] #412 resolved: `SIDES` constant defined; `'union'`/`'confederate'` magic strings eliminated
- [ ] #346 resolved: overlapping test coverage consolidated
- [ ] #413 resolved: `.error` text color meets WCAG AA; non-color cue added
- [ ] #428, #429, #430 resolved: sanitizeCalibration partial-override tests added, fixtures unified, cache header added
- [ ] Debt score reduced from 51 to ≤ 18 (12 deferred items remain)
- [ ] `npm run quality:strict` passes with zero unexpected warnings

## Dependencies

- Depends on existing `GameView.vue`, `useCalibration.js`, `useGameStore`, `games.js` server routes (all delivered in M5)
- #432 depends on #401 (composable must exist before aria-label can read enriched name)
- #404 resolves as a side effect of #401 (no separate task required)

## Out of Scope

- #379, #381, #382, #383 — engine stubs requiring M6 combat data; resolve naturally in M6 combat track
- #402 — unify GameView fetch strategies; deferred to M8
- #403 — CSP headers; deferred to M8 auth hardening
- #350 — rate limiting on games routes; deferred to M8
- #204 — OobTreeNode watcher performance; no scale trigger yet
- #205 — lazy OOB JSON bundle; no bundle analysis trigger yet
- #387 — playerSide from session; needs unimplemented #356 route
- #385 — property-based fuzz tests; no milestone assigned

## Technical Notes

- #431 fix requires a new `/api/v1/oob` server route not gated by `MAP_EDITOR_ENABLED`; add Zod validation and a test
- #401 composable extraction touches `GameView.vue` and all tests that mock OOB loading — coordinate mock updates with implementation
- #424 extraction target: `client/src/utils/calibration.js` (new shared utility file)
- #425 Zod schema for gridSpec lives in `server/src/schemas/` per project conventions and is checked at store load time
- This track is Checkpointed: it touches shared Vue composable extraction, a new production API route, and API/client contract boundaries

---

_Generated by Conductor. Review and edit as needed._
