# Specification: Architecture Debt Sprint — Issues #393 #394 #370 #322 + Process Fix

**Track ID:** arch-debt-sprint_20260520
**Type:** Chore / Refactor
**Created:** 2026-05-20
**Status:** Draft

## Summary

Close five deferred tech-debt items (total 10 debt points) that are actionable now and not
blocked by any milestone dependency: route-coupling fix (#393, score 4), engine module
organisation (#394, score 3), pickMods bounds validation (#322, score 2), rule-traceability
comment (#370, score 1), and a process fix that ensures the `tech-debt` label is applied
whenever a debt issue is filed (`.claude` workflow files, uncommitted).

## Context

The debt register currently lists 14 open items (net score 24). Four of the highest-value
items that are actionable before M5 UI work are architectural refactors with no game-rule
dependencies:

- **#393** (score 4): `scenarioEditor.js` imports `clearScenarioCache` from `games.js`,
  creating a route-to-route dependency. The cache should live in `engine/scenario.js`.
- **#394** (score 3): `isOrderHolder()` is exported from `engine/init.js` despite being a
  query predicate. It should live in a dedicated `engine/queries.js`.
- **#322** (score 2): `pickMods()` in `tableTest.js` accepts unbounded numeric values for
  `leaderMoraleValue` and `range` with no min/max enforcement.
- **#370** (score 1): The `complexDefense` sentinel comment in `init.js` cites
  `LOB_GAME_UPDATES SM section` without a specific section or page reference.

Additionally, two `.claude` workflow files have uncommitted changes that add the missing
`tech-debt` label step to `tech-debt-report.md` and `agentic-quality-rails.md`.

## Acceptance Criteria

- [ ] `.claude/commands/tech-debt-report.md` and `.claude/rules/agentic-quality-rails.md`
      committed with the `tech-debt` label step included.
- [ ] `server/src/engine/queries.js` created; `isOrderHolder()` exported from it.
- [ ] `isOrderHolder` removed from `engine/init.js`; all callers updated (test + schema
      comment).
- [ ] `_scenario`, `getScenario()`, and `clearScenarioCache()` moved from `games.js` to
      `engine/scenario.js`.
- [ ] `scenarioEditor.js` imports `clearScenarioCache` from `engine/scenario.js`.
- [ ] `games.js` imports `getScenario` from `engine/scenario.js`; no longer exports or
      owns the cache.
- [ ] `pickMods()` clamps `leaderMoraleValue` to [0, 4] and `range` to [0, 99] per LOB §6.1.
- [ ] `complexDefense` sentinel comment updated with a specific LOB_GAME_UPDATES SM section
      reference.
- [ ] `npm run quality:strict` passes (lint, format, tests, build, data validation).
- [ ] Debt register updated; issues #393, #394, #370, #322 closed.

## Dependencies

- `engine/scenario.js` — existing module; cache functions will be added there.
- `engine/init.js` — `isOrderHolder` exported today; will be removed.
- `server/src/routes/games.js` — cache owner today; will import instead.
- `server/src/routes/scenarioEditor.js` — imports from `games.js` today; will change.
- `server/src/routes/tableTest.js` — `pickMods()` lives here.

## Out of Scope

- Performance items #295, #294, #324 (deferred to a separate perf sprint).
- M6-blocked stubs #379, #381, #382, #383 (require morale state and combat data).
- Auth hardening #349, #350 (deferred to M8).
- Property-based tests #385 (no milestone, nice-to-have).
- `useOobStore` lazy-loading #205 and tree-watcher #204 (UI debt, separate concern).
- Any changes to game-rule logic, auth/session paths, or schema shape.

## Technical Notes

`engine/scenario.js` already exports `loadScenario()` with a call-count guard. The lazy
`_scenario` cache in `games.js` wraps `loadScenario()` and is the correct natural companion
— both belong in the same engine module. The cache does not violate the call-count guard
because it checks before calling.

`pickMods()` bounds: `leaderMoraleValue` → `Math.max(0, Math.min(4, n))` (LOB §6.1 leaders
have morale values 0–4); `range` → `Math.max(0, Math.min(99, n))`. Document each clamp with
a rule citation.

The `complexDefense` fix requires looking up the authoritative section in `LOB_GAME_UPDATES`
(`docs/reference/LOB_GAME_UPDATES.pdf` if available, else note as "unresolvable without
source doc").

---

_Generated by Conductor. Review and edit as needed._
