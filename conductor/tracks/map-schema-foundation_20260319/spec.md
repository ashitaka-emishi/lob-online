# Specification: Map Schema Foundation — Data Model + Compass Label Fix

**Track ID:** map-schema-foundation_20260319
**Type:** Chore
**Issues:** #135 (data model/schema overhaul), #143 (compass label bug)
**Created:** 2026-03-19
**Status:** Draft

## Summary

Foundation ticket for the "Create usable map editor" milestone. Overhauls the map data model
to use integer face indices (0–2) for edges, replaces the `features` array with a single
`hexFeature` field, drops the obsolete `hexsides` field, and adds coexistence validation to
the Zod schema. Bundles the compass label off-by-one bug fix (#143) since it depends on the
same face-index semantics.

## Context

The current schema (`server/src/schemas/map.schema.js`) stores edge data with string HexDir
keys (`'N'`, `'NE'`, etc.) and allows `features: HexFeature[]` (array). The map editor
design (`docs/designs/map-editor-design.md` §2) specifies a cleaner model: integer face indices 0–2
for canonical edge ownership (faces 3–5 live on the neighbour), a single optional `hexFeature`
field, and dropped `hexsides`. Additionally, `CalibrationControls.vue` has an off-by-one bug
in its cardinal label formula (`Math.floor` vs `Math.round`) that causes the N label to render
one step clockwise from the correct position when `northOffset ≠ 0`.

## Acceptance Criteria

- [ ] `features: HexFeature[]` replaced with `hexFeature?: HexFeature` in schema; only valid
      value is `{ type: 'building' }`
- [ ] Edge keys changed from string HexDir enum to integer face indices `0–2` (canonical owner
      only; faces 3–5 stored on neighbour as `faceIndex − 3`)
- [ ] `hexsides` field removed from schema and from `map.json`
- [ ] `validateCoexistence()` enforced in `superRefine`: - `trail/road/pike` coexist freely - `elevation/slope/extremeSlope/verticalSlope` mutually exclusive per edge - `stream/stoneWall` coexist with anything - `ford` requires `stream` on same edge - `bridge` requires `road/trail/pike` on same edge
- [ ] `client/src/utils/compass.js` created with exported `compassLabel(faceIndex, northOffset)`
      using the correct `Math.round` formula
- [ ] `CalibrationControls.vue` updated to derive all ring labels from `compassLabel()`
- [ ] `data/scenarios/south-mountain/map.json` migrated to new schema (string edge keys → int,
      `features` → `hexFeature`, `hexsides` removed)
- [ ] All existing tests pass; new tests cover `compassLabel` and `validateCoexistence`

## Dependencies

- `docs/designs/map-editor-design.md` §2 — canonical data model spec (authoritative reference)
- No other open tracks blocked on this; all other map editor tickets depend on this landing first

## Out of Scope

- UI changes to the edge/feature painting tools (follow-on tracks)
- Migrating away from the `edges` field name (it stays; only key type changes)
- Any game logic rules engine changes

## Technical Notes

- `compassLabel` formula per #143: `SIX_LABELS[Math.round(((faceIndex * 2 - northOffset + 12) % 12) / 2) % 6]`
- The `cardinalLabelPositions` computed in `CalibrationControls.vue` currently uses `Math.floor`
  and inline formula — replace with `compassLabel()` import
- Schema edge key change: `z.enum(['N','NE','SE','S','SW','NW'])` → `z.union([z.literal(0), z.literal(1), z.literal(2)])`
- `map.json` likely has no `hexsides` data (all entries empty per design doc §2) — confirm before migrating
- Migration script or inline Node script preferred for `map.json` to avoid hand-editing thousands of entries

---

_Generated by Conductor. Review and edit as needed._
