# Implementation Plan: Map Schema Foundation — Data Model + Compass Label Fix

**Track ID:** map-schema-foundation_20260319
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-19
**Status:** [x] Complete

## Overview

Four phases: (1) overhaul the Zod schema with integer edge keys, single hexFeature, dropped
hexsides, and coexistence validation; (2) fix the compass label off-by-one bug; (3) migrate
map.json to the new schema; (4) tests and CI verification.

## Phase 1: Schema Overhaul

Update `server/src/schemas/map.schema.js` with all data model changes from #135.

### Tasks

- [x] Task 1.1: Replace `features: z.array(HexFeature)` with `hexFeature: z.object({ type: z.literal('building') }).optional()`
- [x] Task 1.2: Change edge key type from `z.enum(['N','NE','SE','S','SW','NW'])` to `z.union([z.literal(0), z.literal(1), z.literal(2)])` on the `edges` field
- [x] Task 1.3: Remove `hexsides` field from `HexEntry` and from the top-level `hexsideTypes` array
- [x] Task 1.4: Add `validateCoexistence()` logic in `superRefine`: mutual exclusion for elevation-type edges; ford requires stream; bridge requires road/trail/pike

### Verification

- [ ] `npm run test` passes with updated schema (schema unit tests reflect new shape)
- [ ] No references to `hexsides` or `features` array remain in schema file

## Phase 2: Compass Label Bug Fix (#143)

Create the shared `compassLabel` utility and wire it into `CalibrationControls.vue`.

### Tasks

- [x] Task 2.1: Create `client/src/utils/compass.js` exporting `compassLabel(faceIndex, northOffset)` using `Math.round` formula from #143
- [x] Task 2.2: Write Vitest unit tests for `compassLabel` in `client/src/utils/compass.test.js` covering `northOffset` 0–11 and all 6 face indices
- [x] Task 2.3: Update `CalibrationControls.vue` — replace inline formula in `cardinalLabelPositions` computed with `compassLabel()` import

### Verification

- [ ] With `northOffset=3`, the N label appears at ring position 3 (not 4)
- [ ] `compassLabel` unit tests pass

## Phase 3: Data Migration

Migrate `data/scenarios/south-mountain/map.json` to the new schema shape.

### Tasks

- [x] Task 3.1: Inspect current `map.json` for any `hexsides`, `features`, or string-keyed `edges` entries
- [x] Task 3.2: Write a one-off migration script (`scripts/migrate-map-schema.js`) that: - removes `hexsides` keys from all hex entries - renames `features` → `hexFeature` (taking first element or dropping array wrapper) - converts string edge keys (`'N'→0, 'NE'→1, 'SE'→2`) — drops `'S'/'SW'/'NW'` keys
      (they belong on the neighbour hex; log a warning if any are found)
- [x] Task 3.3: Run migration script, verify output, commit updated `map.json`
- [x] Task 3.4: Delete migration script after use (one-off; not worth maintaining)

### Verification

- [ ] `node server/src/schemas/map.schema.js` (or equivalent validation run) parses migrated `map.json` without errors
- [ ] Server starts and loads map data cleanly (`npm run dev` or server unit test)

## Phase 4: Test Suite and Final Verification

### Tasks

- [x] Task 4.1: Update any server-side schema tests that reference `hexsides`, `features` array, or string edge keys
- [x] Task 4.2: Confirm 70% line coverage threshold still passes (`npm run test:coverage`)
- [x] Task 4.3: Run full CI gate locally (`npm run lint && npm run format:check && npm run test`)

### Verification

- [x] All acceptance criteria in spec.md checked off
- [x] CI gates green (lint, format, test)
- [x] Ready for `/pr-create`

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
