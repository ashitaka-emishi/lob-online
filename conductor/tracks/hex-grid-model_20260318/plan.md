# Implementation Plan: Hex Grid Model — Fix EVEN_Q Column Offset Convention

**Track ID:** hex-grid-model_20260318
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-18
**Status:** [ ] Not Started

## Overview

Formula substitution throughout all hex coordinate helpers from ODD_Q to EVEN_Q, plus
updating the stored `map.json` gridSpec flag and fixing tests to expect correct neighbors.

## Phase 1: Fix Coordinate Math and Data

Update every location that encodes the ODD_Q offset formula or the `evenColUp: false` default.

### Tasks

- [ ] Task 1.1: In `hexGeometry.js` `adjacentHexId` — change both offset↔cube conversion
      lines from `(hcCol - (hcCol & 1))` to `(hcCol + (hcCol & 1))`. Update file header
      comment from ODD_Q to EVEN_Q.
- [ ] Task 1.2: In `hexGeometry.js` `getCellAndNeighbors` — swap neighbor deltas so even
      columns (0-based) are offset up: - even col deltas: `[[0,-1],[1,0],[1,1],[0,1],[-1,1],[-1,0]]` - odd col deltas: `[[0,-1],[1,-1],[1,0],[0,1],[-1,0],[-1,-1]]`
      Update inline comment.
- [ ] Task 1.3: In `los.js` `colRowToCube` and `cubeToColRow` — apply the same formula
      substitution. Update JSDoc and file-level comment from ODD_Q to EVEN_Q.
- [ ] Task 1.4: In `los.js` default gridSpec at line ~166 — change `evenColUp: false` to
      `evenColUp: true`.
- [ ] Task 1.5: In `map.json` gridSpec — set `evenColUp: true`.

### Verification

- [ ] `grep -r "evenColUp: false"` returns no hits in source files.
- [ ] `grep -r "ODD_Q"` returns no hits in source files (comments updated).

## Phase 2: Fix Tests

Update test expectations that encode the old ODD_Q neighbor relationships.

### Tasks

- [ ] Task 2.1: In `hexGeometry.test.js` — update `adjacentHexId` tests so expected
      neighbors match EVEN_Q convention (e.g., NE of 01.01 → 02.01, SE → 02.00).
- [ ] Task 2.2: In `los.test.js` — update `colRowToCube`/`cubeToColRow` round-trip tests
      and any LOS path expectations that encode specific hex sequences.
- [ ] Task 2.3: Audit other test files that reference hex neighbor relationships
      (`HexMapOverlay.test.js`, `useEdgeToggle.test.js`, `LosTestPanel.test.js`) and update
      any affected expectations.
- [ ] Task 2.4: Run `npm run test` — all tests must pass.

### Verification

- [ ] `npm run test` exits 0 with no failures.

## Phase 3: Manual Verification

Confirm the visual overlay matches the physical map in the map editor.

### Tasks

- [ ] Task 3.1: Start dev server (`npm run dev:map-editor`), open map editor, enable the
      hex label overlay.
- [ ] Task 3.2: Verify hex label 01.01 appears at the bottom-left corner position.
- [ ] Task 3.3: Verify that the hex to the upper-right of 01.01 shows 02.01.
- [ ] Task 3.4: Verify that the hex to the lower-right of 01.01 shows 02.00 (partially
      off-map for even cols as expected).
- [ ] Task 3.5: Verify LOS panel produces a plausible path between two hexes on the map.

### Verification

- [ ] Labels match physical map positions with no systematic offset.
- [ ] LOS tool produces correct paths without off-by-one errors.

## Final Verification

- [ ] All acceptance criteria in spec.md met.
- [ ] `npm run lint && npm run format:check && npm run test` all pass.
- [ ] Ready for review.

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
