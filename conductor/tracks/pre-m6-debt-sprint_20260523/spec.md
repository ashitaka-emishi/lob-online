# Specification: Pre-M6 Debt Sprint

**Track ID:** pre-m6-debt-sprint_20260523
**Type:** Chore
**Created:** 2026-05-23
**Status:** Draft

## Summary

Close six open debt items before M6 lands: three that are already fixed in code (housekeeping
only) and three that require targeted code changes — SVG button AT reliability (#434),
`overflow:hidden` scoping (#435), and `loadGame` double-call guard (#441).

## Context

M5 is fully shipped. The debt register carries 17 open items (net score 32 — High risk). Six
items in the score-2/3 range are actionable now without touching M6 surfaces. Three of those
(#431, #432, #402) were silently resolved during the M5 game-map-ui work but never closed;
three (#434, #435, #441) still need code fixes. Clearing these now reduces the net score by 15
and removes compounding risk before combat UI lands in M6.

## User Story

As a developer maintaining lob-online, I want known debt items resolved or formally closed
before M6 combat complexity lands, so that the debt register accurately reflects open risk
and the codebase is clean before a new major feature area begins.

## Acceptance Criteria

- [ ] #431 closed — OOB production route verified, GitHub issue closed, debt register updated
- [ ] #432 closed — OOB-enriched name in `aria-label` verified, GitHub issue closed, debt register updated
- [ ] #402 closed — unified `onMounted` fetch pattern verified, GitHub issue closed, debt register updated
- [ ] #434 closed — SVG counter buttons use a reliable AT pattern (not `role="button"` on `<image>`); keyboard-operable with visible focus indicator
- [ ] #435 closed — `overflow:hidden` removed from `html`/`body` in `App.vue`; game route uses its own scroll-lock; lobby scrolls normally
- [ ] #441 closed — `loadGame` guards against double-call stale state via AbortController or generation counter
- [ ] All CI gates pass (`lint`, `format:check`, `test`)
- [ ] Debt register net score reduced by 15 (6 items closed: scores 3+2+2+3+2+2)

## Dependencies

- Existing: `client/src/components/UnitCounterLayer.vue` (#434)
- Existing: `client/src/App.vue` + `client/src/views/GameView.vue` (#435)
- Existing: `client/src/stores/useGameStore.js` (#441)
- Existing: `docs/tech-debt/report.md` (housekeeping)

## Out of Scope

- #440 (sequential fetch latency) — requires `scenarioId` as route param; design change belongs in M6
- #379, #381, #382, #383 (stub handlers) — resolved by M6 implementation
- #350, #403 (M8 targets) — deferred to auth hardening milestone
- #385, #387, #204, #205 (score-1 items) — low risk, not blocking M6
- Any M6 combat or morale implementation

## Technical Notes

- **#434:** Replace `<image role="button">` with `<g role="button"><image/></g>` pattern. The
  `<g>` element supports ARIA roles reliably across NVDA/JAWS/VoiceOver; `<image>` does not.
  Move `tabindex`, `aria-label`, event handlers, and `:focus-visible` outline to the `<g>`.
- **#435:** Remove `overflow: hidden` from `html` and `body` selectors in `App.vue`. Add
  `overflow: hidden` to `.game-view` in `GameView.vue` (already present there per current code).
  Verify `LobbyView` scrolls on a short viewport.
- **#441:** Add a generation counter (`let loadGeneration = 0`) to `useGameStore.loadGame`.
  Increment before each call; capture the current value; skip writing state if the captured
  value no longer matches current at each async boundary.

---

_Generated by Conductor. Review and edit as needed._
