# Implementation Plan: Elevation System UI — baseElevation + elevationLevels Inputs

**Track ID:** elevation-system-ui_20260318
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-18
**Status:** [x] Complete

## Overview

Three-phase delivery: (1) add `elevationSystem` prop + inputs to `CalibrationControls.vue` with a new emit, (2) wire it up in `MapEditorView.vue` and remove the duplicate inline input, (3) fix the schema's hardcoded `.max(21)` with a `superRefine`.

## Phase 1: CalibrationControls Component

Add `elevationSystem` prop and two numeric inputs to `CalibrationControls.vue`, emitting `elevation-system-change`.

### Tasks

- [x] Task 1.1: Add `elevationSystem` prop (Object, optional) to `CalibrationControls.vue`
- [x] Task 1.2: Add `updateElevationSystem(field, value)` helper that emits `elevation-system-change` with a merged object
- [x] Task 1.3: Add **Elevation System** section to the template with `baseElevation` (int, min 0) and `elevationLevels` (int, min 1, max 99) inputs, disabled when prop is absent
- [x] Task 1.4: Write / update `CalibrationControls.test.js` — verify inputs render with correct values and emit `elevation-system-change` on change

### Verification

- [ ] `CalibrationControls` renders both inputs with SM default values when passed `{ baseElevation: 500, elevationLevels: 22 }`
- [ ] Emits `elevation-system-change` with updated object on input

## Phase 2: MapEditorView Wiring

Connect the new component event to `mapData` and remove the now-duplicate inline `elevationLevels` input.

### Tasks

- [x] Task 2.1: Pass `:elevation-system="mapData?.elevationSystem"` prop to `<CalibrationControls>`
- [x] Task 2.2: Add `onElevationSystemChange(val)` handler in `MapEditorView.vue` that merges `val` into `mapData.value.elevationSystem`
- [x] Task 2.3: Bind `@elevation-system-change="onElevationSystemChange"` on `<CalibrationControls>`
- [x] Task 2.4: Remove the `calibration-extra` div and its `onElevationLevelsChange` handler (now superseded)
- [x] Task 2.5: Update `MapEditorView.test.js` to cover the new handler and confirm the old inline input is gone

### Verification

- [ ] Changing `baseElevation` or `elevationLevels` in the UI updates `mapData.value.elevationSystem`
- [ ] No duplicate `elevationLevels` input in the DOM
- [ ] Pull from server still pre-populates both fields

## Phase 3: Schema Runtime Validation

Replace the hardcoded `.max(21)` on `HexEntry.elevation` with a cross-field `superRefine` on `MapSchema`.

### Tasks

- [x] Task 3.1: Remove `.max(21)` from `HexEntry.elevation` (keep `.min(0).int()`)
- [x] Task 3.2: Add `MapSchema.superRefine` that derives `maxElevation = (data.elevationSystem?.elevationLevels ?? 22) - 1` and issues a Zod error for any hex with `elevation > maxElevation`
- [x] Task 3.3: Update `map.schema.test.js` — add test that a hex with `elevation: 22` fails when `elevationLevels: 22`, and passes when `elevationLevels: 23`

### Verification

- [ ] Schema rejects out-of-range hex elevation against stored `elevationLevels`
- [ ] SM `map.json` still validates cleanly (22 levels → max 21)
- [ ] All existing schema tests pass

## Final Verification

- [ ] All acceptance criteria met
- [ ] `npm run test` passes
- [ ] `npm run lint` and `npm run format:check` pass
- [ ] Ready for review

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
