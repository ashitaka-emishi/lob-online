# Specification: Trace Highlight Layer — O(traceEdges) Render

**Track ID:** trace-highlight-perf_20260318
**Type:** Refactor
**Created:** 2026-03-18
**Status:** Draft
**GitHub Issue:** #96

## Summary

Replace the O(cells × 6) nested loop in the `HexMapOverlay` trace highlight layer with a direct iteration over `traceEdges`, reducing render work from ~8,400 checks per frame to O(traceEdges) — typically 10–50 — during a linear feature drag.

## Context

The map editor dev tool includes a drag-trace mode for drawing linear features. While dragging, `HexMapOverlay` re-renders the trace highlight layer on every new edge added. The current implementation iterates all cells (~1,400) × all 6 directions = 8,400 pairs per render, checking `traceEdgeSet.has(cell.id + ':' + dir)` for each. Since a trace has 10–50 edges at most, 99%+ of checks are false. The string concatenation `cell.id + ':' + dir` also runs 8,400 times per frame.

## Problem Description

**Location:** `client/src/components/HexMapOverlay.vue` lines 519–532

**Current code:**

```html
<g v-if="isDrawing" class="layer-trace">
  <template v-for="cell in cells" :key="'trace-' + cell.id">
    <template v-for="dir in DIRS" :key="dir">
      <line
        v-if="traceEdgeSet.has(cell.id + ':' + dir)"
        v-bind="edgeLine20_80(cell.corners, dir)"
        stroke="#00eeff"
        stroke-width="3"
        stroke-linecap="round"
        pointer-events="none"
      />
    </template>
  </template>
</g>
```

This creates 8,400 VNodes per render (most with `v-if="false"`) and re-runs string concatenation 8,400 times on every trace edge addition.

## Acceptance Criteria

- [ ] Trace highlight layer iterates `traceEdges` directly instead of all cells × 6 directions
- [ ] `cellMap` computed added: `Map<hexId, cell>` for O(1) lookup of cell data by hexId
- [ ] `traceEdgeSet` still used for O(1) deduplication during mousemove (not removed)
- [ ] Trace highlight visual behavior is unchanged (cyan lines on active trace edges)
- [ ] `npm run lint && npm run format:check && npm run test` pass

## Dependencies

- `traceEdges` ref already exists in `HexMapOverlay.vue` (array of `{hexId, dir}`)
- `cells` computed already exists — `cellMap` is derived from it

## Out of Scope

- Changes to trace collection logic (mousemove, mousedown, mouseup handlers)
- Changes to `traceEdgeSet` deduplication during collection
- Any other performance work in `HexMapOverlay`

## Technical Notes

The fix from the issue:

```js
const cellMap = computed(() => new Map(cells.value.map((c) => [c.id, c])));
```

```html
<g v-if="isDrawing" class="layer-trace">
  <template v-for="edge in traceEdges" :key="edge.hexId + ':' + edge.dir">
    <line
      v-if="cellMap.get(edge.hexId)"
      v-bind="edgeLine20_80(cellMap.get(edge.hexId).corners, edge.dir)"
      stroke="#00eeff"
      stroke-width="3"
      stroke-linecap="round"
      pointer-events="none"
    />
  </template>
</g>
```

`cellMap` recomputes when `cells` changes (i.e., on calibration change), which is rare — not on every trace edge addition. The `v-if="cellMap.get(edge.hexId)"` guard handles the edge case where a hexId in the trace has no corresponding cell in `gridData`.

---

_Generated by Conductor. Review and edit as needed._
