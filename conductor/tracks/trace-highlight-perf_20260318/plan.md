# Implementation Plan: Trace Highlight Layer — O(traceEdges) Render

**Track ID:** trace-highlight-perf_20260318
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-18
**Status:** [x] Complete

## Overview

Single-phase fix: add `cellMap` computed to `HexMapOverlay.vue`, replace the nested `cells × DIRS` loop in the trace highlight layer with a direct `traceEdges` iteration. Update the existing `HexMapOverlay` test to cover the new rendering path.

## Phase 1: Implement and Test

Add the `cellMap` computed and rewrite the trace highlight template block. Update the test suite to verify the new render path.

### Tasks

- [x] Task 1.1: Add `cellMap` computed to `HexMapOverlay.vue` — `new Map(cells.value.map((c) => [c.id, c]))`
- [x] Task 1.2: Replace the nested `v-for="cell in cells" / v-for="dir in DIRS"` trace highlight block with `v-for="edge in traceEdges"` using `cellMap.get(edge.hexId)` for cell lookup
- [x] Task 1.3: Update `HexMapOverlay.test.js` — add/update test asserting that trace highlight renders exactly `traceEdges.length` lines (not cells × 6)

### Verification

- [x] `npm run test` passes
- [x] Grep confirms no `v-for="cell in cells"` inside the `layer-trace` `<g>` block

## Final Verification

- [x] All acceptance criteria in spec.md met
- [x] `npm run lint` — pre-existing errors only (no new issues)
- [x] `npm run format:check` passes
- [x] `npm run test` passes (≥70% coverage)
- [x] Ready for PR review

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
