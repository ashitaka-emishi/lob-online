# Implementation Plan: Move Slope Arrow Geometry into gridData Computed

**Track ID:** slope-arrow-geometry_20260317
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-17
**Status:** [x] Complete

## Overview

Single-file refactor of `HexMapOverlay.vue`: inline slope arrow geometry into the `gridData`
forEach loop and remove the two standalone helper functions. The template is updated to read
fields directly from each cell object. No test changes needed — existing tests already assert
on rendered output, not on the helper functions.

## Phase 1: Move Geometry into gridData and Update Template

Inline the slope arrow computation into the `gridData` computed loop and remove the helpers.

### Tasks

- [x] Task 1.1: In `gridData`'s forEach loop, compute `slopeArrowLine` and `slopeArrowLabel`
      and push them into the cell object
- [x] Task 1.2: Update the slope arrows template block to use cell fields directly
- [x] Task 1.3: Delete the `slopeArrowLine(cell)` and `slopeArrowLabel(cell)` standalone
      functions

### Verification

- [x] File saves without syntax errors (ESLint/Prettier clean)
- [x] Visual diff of template shows no functional change in slope arrow rendering logic

## Phase 2: Test and Final Check

### Tasks

- [x] Task 2.1: Run `npm run test` — all existing slope arrow tests pass unmodified
- [x] Task 2.2: Run `npm run lint` and `npm run format:check` — no errors

### Verification

- [x] `npm run test` exits 0 (354/354 passing)
- [x] `npm run lint` exits 0
- [x] `npm run format:check` exits 0

## Final Verification

- [x] All acceptance criteria from spec.md met
- [x] No standalone `slopeArrowLine` or `slopeArrowLabel` functions remain in the file
- [x] Template contains no function calls for slope arrow data
- [x] Ready for review

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
