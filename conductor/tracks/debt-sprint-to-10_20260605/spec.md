# Specification: Debt Sprint — Score 32 → 10

**Track ID:** debt-sprint-to-10_20260605
**Type:** Chore
**Created:** 2026-06-05
**Status:** Draft

## Summary

Close 13 open tech-debt items (−22 pts) from M5 and earlier surfaces, reducing the net open debt
score from 32 to 10. The remaining 10 points are legitimately blocked on M6 (engine stubs #379
#381 #382 #383) or M8 (auth/rate-limit #403 #350) and are out of scope here.

## Context

The team-review of PR #507 surfaced 8 new debt points on top of the 24 already open after the
pre-M6 debt sprint, bringing total open debt to 32 across 19 items. M5 work is complete and M5.5
is starting; carrying 32 points into a new milestone creates review noise. This sprint targets all
fixable M5-and-earlier items only — no M6 engine work.

## Items to Close

| Issue | Score | Title                                                                                          |
| ----- | ----- | ---------------------------------------------------------------------------------------------- |
| #508  | 3     | Fix ineffective #506-3 guard test; add `_nodePath` forwarding test; guard `JSON.parse` scalars |
| #509  | 2     | Add resolves-to-real-node test for `_nodePath 'union.hq'` literal                              |
| #502  | 2     | Move `refreshValidActions` into `useGameStore` with generation guard                           |
| #503  | 2     | Verify `session.gameId === req.params.id` in `GET /:id/actions`                                |
| #504  | 2     | Add cross-implementation parity test for `stripNonPlayableBoundaryEdges`                       |
| #505  | 2     | Restore focus after `pending` clears; use `aria-disabled` over `disabled`                      |
| #510  | 1     | Minor test/comment polish (#506-3 catch comment, null display, HQ branch test)                 |
| #511  | 1     | Check off spec acceptance criteria for oob-editor-bugs track                                   |
| #512  | 1     | OobDetailPanel textarea labels + `specialRules` rows attribute                                 |
| #469  | 1     | Remove `migrateUnknownTerrain` (blocked on map completion — close with note)                   |
| #385  | 1     | Property-based fuzz tests for dispatch round-trips                                             |
| #205  | 1     | `useOobStore` lazy-loading bundled JSON fallbacks                                              |
| #204  | 1     | OobTreeNode 200–300 per-instance watchers on shared signals                                    |

**Total to close:** 13 items, −22 pts. Remaining open: 6 items, 10 pts.

## Acceptance Criteria

- [ ] All 13 issues above closed with merge-summary comments.
- [ ] Net open debt register score = 10 (6 items: #379 #381 #382 #383 #403 #350).
- [ ] `npm run quality:strict` passes.
- [ ] No new debt introduced.

## Items Left Open (out of scope)

| Issue | Score | Reason                                                                       |
| ----- | ----- | ---------------------------------------------------------------------------- |
| #379  | 2     | M6: `getValidActions` full enumeration requires M6 unit/leader position data |
| #381  | 2     | M6: Attack Recovery handler requires M6 combat result data                   |
| #382  | 2     | M6: Fluke Stoppage handler requires M6 attack order data                     |
| #383  | 2     | M6: Rally Phase handler requires M6 morale state tracking                    |
| #403  | 2     | M8: CSP headers — deferred to M8 auth hardening                              |
| #350  | 2     | M8: Rate limiting — deferred to M8 auth hardening                            |

## Dependencies

M5 surfaces only: `client/src/utils/oobTreeTransform.js`, `client/src/utils/oobTreeTransform.test.js`,
`client/src/components/OobDetailPanel.vue`, `client/src/components/OobDetailPanel.test.js`,
`client/src/components/OobTreeNode.vue`, `client/src/components/OobTreeNode.test.js`,
`client/src/components/ActionPanel.vue`, `client/src/views/GameView.vue`,
`client/src/stores/useGameStore.js`, `server/src/routes/games.js`,
`server/src/engine/edge-strip.js`, `server/src/engine/edge-strip.test.js`,
`client/src/formulas/edge-model.js`.

## Out of Scope

- Any M6 engine work (combat, morale, orders)
- M8 auth/rate-limit work
- Map digitization or OOB data entry
- M5.5 feature work

## Technical Notes

Items vary in complexity: #508 is the most involved (three test fixes in two files). #502 is a
moderate store refactor. #503 is a one-line route guard. #504 is a new test file. #505 is a
focused a11y fix in `ActionPanel.vue`. The rest are trivial comment, test, or documentation
touches. Group commits by issue for clean history. Use `fix:` or `test:` or `chore:` prefixes
per item.

---

_Generated by Conductor. Review and edit as needed._
