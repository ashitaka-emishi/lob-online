# Implementation Plan: M3 Debt Closeout Sprint

**Track ID:** m3-debt-closeout_20260415
**Spec:** [spec.md](./spec.md)
**Created:** 2026-04-15
**Status:** [~] In Progress

## Overview

Three phases. Phase 1 verifies the score-3 OOB register discrepancy and either
closes or implements #237/#245/#247. Phase 2 fixes the four engine correctness
items with TDD. Phase 3 cleans up the test/route items and updates the debt
register for all 9 closures.

---

## Phase 1: OOB Register Verification

Verify whether #237, #245, #247 were fully closed by the M2 debt sprint or still
require implementation work.

### Tasks

- [x] Task 1.1: Run `gh issue view 237 245 247` — all three CLOSED on 2026-04-09
- [x] Task 1.2: Fixes verified in codebase — processUSACavDiv delegates to
      withLeader (#237), editorRouteFactory.js exists (#245),
      useOobPersistence.test.js exists (#247)
- [x] Task 1.3: N/A — no implementation needed

### Verification

- [x] All three issues confirmed closed on GitHub
- [x] `npm run test` — green (existing suite confirms fixes)

---

## Phase 2: Engine Correctness Fixes

Four isolated engine fixes. TDD: write failing tests first, then implement.

### Tasks

- [x] Task 2.1: **#285** — `parseHexId` input validation
  - Test: `parseHexId(null)`, `parseHexId('')`, `parseHexId('abc')` all throw
    `TypeError` with a clear message
  - Fix: add guard at top of `parseHexId` before `split().map(Number)`
  - Rule citation required

- [x] Task 2.2: **#287** — `combatResult` out-of-range dice validation
  - Test: `combatResult(1, ...)` and `combatResult(13, ...)` throw `RangeError`
  - Fix: add range guard (valid range: 2–12) before array lookup
  - Rule citation required

- [x] Task 2.3: **#296** — hoist `noEffectTerrain` Set out of hot loop
  - Read `hexEntryCost` to confirm the Set is never mutated inside the function
  - If not mutated: hoist `new Set(noEffectTerrain ?? [])` to call site or
    accept pre-built Set as parameter
  - Test: existing movement tests still pass; add one test confirming cost is
    correct when `noEffectTerrain` is provided

- [x] Task 2.4: **#290** — consistent parameter ordering
  - Fix: swap `(mapData, scenario)` → `(scenario, mapData)` in `computeLOS`
    signature and all call sites
  - Update all affected tests and the mapTest.js route
  - Rule citation: add comment noting the canonical order is `(scenario, mapData)`

### Verification

- [x] `npm run test` — all engine tests green (1865 tests passing)
- [x] `npm run lint && npm run format:check` — clean

---

## Phase 3: Test and Route Cleanup + Register Update

Two small code fixes, then update the debt register for all 9 closures.

### Tasks

- [ ] Task 3.1: **#308** — standardize modifier param name
  - In `server/src/routes/tableTest.js` `/closing-roll` handler: rename `mods`
    parameter to `modifiers` to match the `/morale` handler convention
  - Update any affected tests

- [ ] Task 3.2: **#260** — `localStorage.removeItem` assertion
  - In `useOobStore.test.js` `confirmPush` succession test: add assertion that
    `localStorage.removeItem('lob-succession-editor-v1')` was called

- [ ] Task 3.3: Update `docs/tech-debt/report.md`
  - Remove all 9 closed issues from the Open Debt Items table
  - Append resolution rows to the Debt Over Time table with net delta
  - Recalculate Executive Summary (open items ~20, net score ~28)
  - Update Risk Assessment prose
  - Update per-PR report for this PR (create
    `docs/tech-debt/reports/pr-{N}_{date}.md`)

- [ ] Task 3.4: Close all 9 GitHub issues (`gh issue close 237 245 247 285 287
290 296 308 260 --comment "Fixed in PR #N"`)

### Verification

- [ ] `npm run test` — full suite green
- [ ] `npm run lint && npm run format:check` — clean
- [ ] `docs/tech-debt/report.md` shows ~20 open items
- [ ] All 9 GitHub issues show as closed

---

## Final Verification

- [ ] All acceptance criteria in spec.md met
- [ ] `npm run test` — full suite green
- [ ] `npm run lint && npm run format:check` — clean
- [ ] Net score visible in debt register (~28)
- [ ] Ready for `/pr-create`

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
