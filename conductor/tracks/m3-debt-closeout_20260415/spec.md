# Specification: M3 Debt Closeout Sprint

**Track ID:** m3-debt-closeout_20260415
**Type:** Refactor
**Created:** 2026-04-15
**Status:** Draft

## Summary

Close 9 open debt items — 3 register-verification closures (score-3 OOB items
already fixed per devlog) plus 6 targeted engine/test fixes — reducing the open
item count from 29 to ~20 and net score from 47 to ~28.

## Context

M3 is complete. The debt register carries 29 open items (net score 47), most
introduced during the M3 engine and tool-test PRs. The score-3 OOB items (#237,
#245, #247) were reportedly fixed in the M2 debt sprint but never removed from
the register. The remaining targets are low-risk, isolated engine correctness
gaps and test utility improvements that can be batched safely before M4 begins.

## Acceptance Criteria

- [ ] #237, #245, #247 verified on GitHub — removed from register if closed,
      implemented and closed if still open
- [ ] #290 fixed: movementPath and computeLOS share consistent parameter order
- [ ] #285 fixed: parseHexId throws or returns error sentinel on null/malformed input
- [ ] #287 fixed: combatResult throws/returns sentinel for dice roll < 2 or > 12
- [ ] #296 fixed: noEffectTerrain Set hoisted out of hexEntryCost hot loop
- [ ] #308 fixed: modifier param name standardized across morale and closing-roll routes
- [ ] #260 fixed: localStorage.removeItem assertion added to useOobStore succession test
- [ ] All 9 GitHub issues closed
- [ ] `docs/tech-debt/report.md` shows ~20 open items, net score ~28
- [ ] CI gates green

## Dependencies

Depends on existing code in `server/src/engine/`, `server/src/routes/`,
`client/src/stores/`. No incomplete tracks.

## Out of Scope

- #291 (tautological ammoTypeShift) — needs domain-expert consult before touching
- Any score-2 performance items requiring profiling (#294, #295)
- Any new features or engine behavior changes

## Technical Notes

- #296 (Set hoist): verify the Set is never mutated inside hexEntryCost before
  hoisting — if it is, use Object.freeze() or a const at module level
- #237/#245/#247: if GitHub shows issues still open, implementation work is needed;
  build time for this phase is variable
- Rule traceability comments required on any engine code touched (per coding standards)

---

_Generated by Conductor. Review and edit as needed._
