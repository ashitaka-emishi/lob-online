# Specification: Minor Debt Bundle — Issues #119 #123 #127 #128 #129 #130

**Track ID:** minor-debt-bundle_20260318
**Type:** Chore
**Created:** 2026-03-18
**Status:** Draft

## Summary

Six small, independent debt items from PR #117 and #122 team-reviews. Each is a self-contained
change (comment, rename, guard, small refactor, or focused test) with debt scores of 1–2. Bundled
into one track to reduce PR overhead.

## Context

All six issues were deferred from team-reviews with explicit debt scores. None affects
game-logic code — all are map-editor dev-tool concerns. They touch different files and have
no dependencies on each other.

## Acceptance Criteria

- [ ] **#127** — `isValidDraft` in `useMapPersistence.js` checks known top-level keys in
      addition to `hexes` array shape
- [ ] **#128** — A comment in `onEdgeClick` (or `useHexInteraction.js`) explains why
      `onHexUpdate` is called twice
- [ ] **#129** — `resolveHex` is renamed to `resolveHexOrStub` (or given an optional
      `fallback` parameter) and all call sites updated
- [ ] **#130** — `TOOL_PANEL_MODES` is no longer exported from `useEditorAccordion.js`;
      instead a helper method/computed is returned so callers don't import the constant directly
- [ ] **#123** — `onHexMouseenter` in `useHexInteraction.js` guards the `unsaved.value = true`
      write to avoid redundant reactive updates in paint mode
- [ ] **#119** — New test in `HexMapOverlay.test.js` asserts that a click on a known hex
      position emits a `hex-click` event whose ID matches the displayed label (covers at least
      one odd and one even game column)
- [ ] All existing tests continue to pass

## Dependencies

None — all six changes are independent.

## Out of Scope

- `useCalibration` / `useMapExport` extraction (#126) — score 3, separate track
- `useMapPersistence` API surface reduction (#125) — score 3, separate track
- `onHexUpdateBatch` introduction (#124) — score 3, separate track
- CSS consolidation (#112) — separate track
- `ElevationSystemControls` hoisting (#111) — separate track

## Technical Notes

- `resolveHex` rename (#129): grep all usages before renaming; update import sites and
  any JSDoc references
- `TOOL_PANEL_MODES` encapsulation (#130): the view's keyboard handler calls
  `togglePanel` conditionally — expose `closeActiveToolPanel()` or `isToolPanel(name)`
  from the composable instead
- `isValidDraft` deepening (#127): check for `gridSpec` as an object; reject drafts with
  unexpected top-level keys using an allowlist

---

_Generated by Conductor. Review and edit as needed._
