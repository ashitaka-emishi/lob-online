# Implementation Plan: Minor Debt Bundle — Issues #119 #123 #127 #128 #129 #130

**Track ID:** minor-debt-bundle_20260318
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-18
**Status:** [x] Complete

## Overview

Six independent low-risk changes across map-editor composables, a shared helper, and a test
file. Implement each in order; run the test suite after each to keep the feedback loop tight.

---

## Phase 1: Comment and Rename (Score-1 Items)

Trivial one-liner changes that carry no behaviour risk.

### Tasks

- [x] Task 1.1: **#128** — Locate `onEdgeClick` (likely `useHexInteraction.js`) and add an
      inline comment explaining why `onHexUpdate` is called twice (once per adjacent hex) and
      that the debounce in `saveMapDraft` keeps it correct
- [x] Task 1.2: **#129** — Rename `resolveHex` → `resolveHexOrStub` in its definition file;
      update all import sites and call sites; update any JSDoc or comments that reference the old name

### Verification

- [ ] `npm run lint` passes
- [ ] `npm run test` passes with no regressions

---

## Phase 2: Validation Deepening (#127)

Add an allowlist guard to `isValidDraft` so unknown top-level keys are rejected.

### Tasks

- [x] Task 2.1: **#127** — In `useMapPersistence.js`, update `isValidDraft` to:
  - Verify `obj` has only known top-level keys (e.g. `hexes`, `gridSpec`, `version`, `meta`)
  - Verify `gridSpec`, if present, is an object
  - Add or update the existing unit test in `useMapPersistence.test.js` (or the composable's
    test file) with cases for: extra unknown key → rejected, missing `gridSpec` → still valid
    (optional field), non-object `gridSpec` → rejected

### Verification

- [ ] `npm run test` passes, including the new `isValidDraft` cases

---

## Phase 3: Reactive Guard (#123)

Add a cheap conditional to suppress redundant reactive writes in the paint hot-path.

### Tasks

- [x] Task 3.1: **#123** — In `useHexInteraction.js`, find the `onHexMouseenter` handler;
      change the unconditional `unsaved.value = true` assignment to
      `if (!unsaved.value) unsaved.value = true` (or equivalent early-return guard)
- [x] Task 3.2: Add or update a test that verifies `unsaved` is set to `true` after the first
      mouse-enter during a drag, and that subsequent enters do not fire additional reactive updates
      (can be a simple call-count assertion on a reactive ref spy)

### Verification

- [ ] `npm run test` passes

---

## Phase 4: Encapsulate TOOL_PANEL_MODES (#130)

Remove the module-level export from `useEditorAccordion` so view code doesn't import the constant.

### Tasks

- [x] Task 4.1: **#130** — Add a helper to `useEditorAccordion`'s return object, e.g.
      `isToolPanel(name): boolean` that checks `TOOL_PANEL_MODES.has(name)`
      (or `closeActiveToolPanel()` if the keyboard handler only needs that action)
- [x] Task 4.2: Remove the `export` keyword from `TOOL_PANEL_MODES` (or keep it but un-export it)
- [x] Task 4.3: Update `MapEditorView.vue` keyboard handler to call the new helper instead of
      importing `TOOL_PANEL_MODES` directly; remove the import statement
- [x] Task 4.4: Verify no other file imports `TOOL_PANEL_MODES` directly (grep)

### Verification

- [ ] `npm run lint` passes (no unused import warnings)
- [ ] `npm run test` passes

---

## Phase 5: HexMapOverlay Click Test (#119)

Add the missing click/context-menu test to prevent coordinate-formula regression.

### Tasks

- [x] Task 5.1: **#119** — In `HexMapOverlay.test.js`, add a test that:
  - Mounts the component with a known `cells` array (at least one odd-column and one
    even-column hex)
  - Simulates a click at a position that maps to a known hex (use the existing `onSvgClick`
    handler or trigger a `click` event on the SVG with `clientX`/`clientY`)
  - Asserts the emitted `hex-click` payload matches the displayed label for that hex
  - Repeats for `contextmenu` → `hex-right-click` emit
- [x] Task 5.2: Confirm the test fails if the coordinate formula is manually broken (sanity check)

### Verification

- [x] `npm run test` passes with the new test included
- [x] Coverage on `HexMapOverlay.vue` click/contextmenu handlers improves

---

## Final Verification

- [x] All six acceptance criteria from `spec.md` are met
- [x] `npm run lint` clean
- [x] `npm run format:check` clean
- [x] `npm run test` passes (≥ 70% line coverage threshold)
- [x] GitHub issues #119, #123, #127, #128, #129, #130 are ready to close

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
