# Specification: M5 Engine Debt Bundle — Issues #377 #378 #380 #384

**Track ID:** m5-engine-debt_20260513
**Type:** Chore
**Created:** 2026-05-13
**Status:** Draft

## Summary

Close four actionable engine debt items from the PR #375 team-review that harden the
action engine dispatch and schema layers before the game map UI is built on top of them.

## Context

PR #375 delivered the M5 turn phase reducer and valid actions engine. The team-review
deferred nine findings; four are actionable now (not blocked on M6 combat/morale data):
error message DX (#384), dispatch playerSide guard (#377), drainAutoSteps activityPhase
invariant (#378), and cross-field schema refinements (#380). The remaining five require
morale state, combat results, or getValidActions full enumeration — all M6 work.

## Acceptance Criteria

- [ ] `dispatch()` error message for `UNKNOWN_ACTION` includes the unknown `action.type` value (#384)
- [ ] `dispatch()` throws `ActionError('INVALID_ACTION', ...)` when `action.playerSide !== state.activePlayer` and `state.activePlayer` is non-null (#377)
- [ ] `drainAutoSteps()` (or `GameStateSchema`) asserts `activityPhase === null` outside `activity` phase (#378)
- [ ] `GameStateSchema` has cross-field `.refine()` checks: `ordersPhase` non-null ↔ `phase === 'command'`; `activityPhase` non-null ↔ `phase === 'activity'` (#380)
- [ ] Each criterion covered by at least one test (new or updated)
- [ ] All four GitHub issues closed on merge

## Dependencies

- `server/src/engine/actions/index.js` — `dispatch`, `drainAutoSteps`
- `server/src/schemas/gameState.schema.js` — `GameStateSchema` cross-field refinements
- Existing test files: `index.test.js`, `gameState.schema.test.js`

## Out of Scope

- #379 — `getValidActions` full enumeration (deferred to M6)
- #381 — Attack Recovery step handler (deferred to M6 combat track)
- #382 — Fluke Stoppage step handler (deferred to M6 combat track)
- #383 — Rally Phase handler (deferred to M6 morale/rally track)
- #385 — Property-based / fuzz tests (deferred indefinitely)

## Technical Notes

- #380 modifies `GameStateSchema`, a persistence/wire-format surface → Checkpointed mode required; pause before implementing schema refinements.
- Implementation order: #384 (trivial) → #377 (dispatch guard) → #378 (drainAutoSteps guard) → #380 (schema refinements, most risky).
- For #378: prefer a cross-field Zod refinement on `GameStateSchema` over an in-engine guard — the schema is already the authoritative invariant layer.
- Tests for #377 and #378 belong in `index.test.js`; tests for #380 belong in `gameState.schema.test.js`.

---

_Generated by Conductor. Review and edit as needed._
