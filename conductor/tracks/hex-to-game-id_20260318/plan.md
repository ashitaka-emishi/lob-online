# Implementation Plan: Extract hexToGameId Helper (Tech Debt #118 + #119)

**Track ID:** hex-to-game-id_20260318
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-18
**Status:** [x] Complete

## Overview

Three-step TDD refactor: write the unit tests and helper first, then replace the three inline
formula sites in `HexMapOverlay.vue`, then add the click/context-menu event tests.

## Phase 1: Add `hexToGameId` to hexGeometry.js

Add the pure helper function to `hexGeometry.js` alongside the existing geometry utilities.

### Tasks

- [x] Task 1.1: Add `hexToGameId(hex, gridRows)` export to `client/src/utils/hexGeometry.js`
      — pure function, no side effects, positioned after `getCellAndNeighbors`
- [x] Task 1.2: Write unit tests in `hexGeometry.test.js` for `hexToGameId`:
  - `col=0, row=0, gridRows=3` → `"01.03"` (top-left corner)
  - `col=1, row=0, gridRows=3` → `"02.03"` (top of second column, must match adjacentHexId)
  - `col=3, row=2, gridRows=3` → `"04.01"` (bottom-right corner)
  - `col=0, row=1, gridRows=35` → `"01.34"` (mid-grid, full game dimensions)

### Verification

- [x] `npm run test` passes — new `hexToGameId` tests green, all existing tests unchanged

## Phase 2: Replace inline formulas in HexMapOverlay.vue

Swap all three copy-pasted blocks for calls to `hexToGameId`.

### Tasks

- [x] Task 2.1: Import `hexToGameId` in `HexMapOverlay.vue`
- [x] Task 2.2: Replace `gridData` computed formula (lines 170–172) with `hexToGameId(hex, gridRows)`
      — `gridRows` is already computed at line 145, use that variable directly
- [x] Task 2.3: Replace `onSvgClick` formula (lines 288–291) with
      `const gridRows = props.calibration.rows > 0 ? props.calibration.rows : 35;`
      `const id = hexToGameId(hex, gridRows);`
- [x] Task 2.4: Replace `onSvgContextMenu` formula (lines 357–360) identically to Task 2.3

### Verification

- [x] `npm run test` passes — existing `gridData` label regression test still green
- [x] Zero occurrences of the inline `gameCol`/`gameRow`/padStart formula remain in `HexMapOverlay.vue`
      (verify with `grep -n "gameCol" client/src/components/HexMapOverlay.vue`)

## Phase 3: Add click/context-menu hex ID tests

Cover the previously untested `onSvgClick` and `onSvgContextMenu` emit paths.

### Tasks

- [x] Task 3.1: Add `HexMapOverlay.test.js` test — `onSvgClick` emits correct hex ID
  - Trigger a synthetic click event on the SVG; assert `hex-click` emitted with a valid `"CC.RR"` ID
  - Use `wrapper.trigger('click', ...)` with a point that maps to a known cell (e.g. center of `01.03`)
- [x] Task 3.2: Add `HexMapOverlay.test.js` test — `onSvgContextMenu` emits correct hex ID
  - Same approach as 3.1; assert `hex-right-click` emitted with a valid `"CC.RR"` ID
- [x] Task 3.3: Run `npm run test:coverage` and confirm click/right-click handler lines are covered

### Verification

- [x] All tests green
- [x] `hex-click` and `hex-right-click` handler branches covered

## Final Verification

- [x] All acceptance criteria in spec.md checked off
- [x] `npm run lint` passes
- [x] `npm run format:check` passes
- [x] `npm run test` passes
- [x] No inline `gameCol`/`gameRow` formula remains in `HexMapOverlay.vue`
- [x] Ready for review

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
