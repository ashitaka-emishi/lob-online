# Specification: Debt Final Closeout

**Track ID:** debt-final-closeout_20260415
**Type:** Chore
**Created:** 2026-04-15
**Status:** Draft

## Summary

Close all actionable open debt items in the register — 15 items, net score 21 — dropping the
cumulative open score from 28 to 7 (the five pure-performance/scale awareness items that are
explicitly deferred until scale warrants them).

## Context

The lob-online project maintains a live debt register at `docs/tech-debt/report.md`. After the
m3-debt-closeout sprint (PR #313), all score-3 items were resolved. The register now holds
eight score-2 architectural/correctness items and twelve score-1 style/hardening items. Five
of those twenty items are flagged as "awareness only" scale concerns (#294 #295 #205 #204 #201)
and are permanently deferred. The remaining 15 are concrete, bounded fixes.

## Acceptance Criteria

- [ ] All 4 map-test architecture items closed: #298 #299 #301 #304
- [ ] All 4 engine quality items closed: #286 #291 #292 #293
  - #291 requires domain-expert consultation before any code change; if the consult confirms
    the code is correct-by-construction, the item is closed with a comment — no code change needed
- [ ] All 4 table-test Vue idiom items closed: #306 #309 #310 #311
- [ ] All 3 test & schema hardening items closed: #255 #256 #259
- [ ] `npm run lint`, `npm run format:check`, and `npm run test` all pass
- [ ] Debt register updated: net open score drops from 28 to 7

## Out of Scope

- Performance/scale awareness items: #294 #295 #205 #204 #201 — permanently deferred, not
  closed in this track
- New features or rule-engine additions
- Any changes outside the files directly cited in the 15 debt items

## Technical Notes

**#291 domain-expert consult required.** `combat.js` `ammoTypeShift` threshold check may be
tautological. Use the `domain-expert` agent to verify the LOB rule intent before modifying;
if the check is confirmed unreachable, close by adding a comment citing the rule — do not
silently delete it.

**Phase ordering:** map-test architecture first (highest score, self-contained), then engine
quality (requires rule consult), then table-test idioms (pure Vue), then test/schema hardening
(lowest risk).

---

_Generated by Conductor. Review and edit as needed._
