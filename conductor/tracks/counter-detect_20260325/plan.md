# Implementation Plan: Counter Auto-Detection Script

**Track ID:** counter-detect_20260325
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-25
**Status:** [~] In Progress

## Overview

Add `counterRef` fields to `oob.json` and `leaders.json` schemas and data, create the
`scripts/detect-counters.js` vision script, ensure `client/public/counters/` is tracked, run the
script to copy all images, write high-confidence `counterRef` entries, delete the source directory,
and commit the results.

## Phase 1: Data Model — Add counterRef to schemas and data files

Add `counterRef` as a nullable field to the Zod schemas for all unit node types and leader
records, then add the field (initially null) to all existing entries in `oob.json` and
`leaders.json`.

### Tasks

- [x] Task 1.1: Extend Zod schema for unit nodes (regiment, battery, brigade, division, corps) with nullable `counterRef: { front, back }`
- [x] Task 1.2: Extend Zod schema for leader records with nullable `counterRef: { front, back, promotedFront, promotedBack }`
- [x] Task 1.3: Add `"counterRef": null` to every unit node in `oob.json`
- [x] Task 1.4: Add `"counterRef": null` to every leader record in `leaders.json`
- [x] Task 1.5: Add `client/public/counters/.gitkeep` so the directory is tracked in git

### Verification

- [ ] `npm run test` passes with updated schemas validating the modified data files

## Phase 2: Detection Script

Write `scripts/detect-counters.js` as a plain Node.js ES module that orchestrates the vision API
calls, matching logic, file copying, and data file writes.

### Tasks

- [x] Task 2.1: Scaffold `scripts/detect-counters.js` — read image list from `client/public/counters/`, define confidence threshold constant (0.0)
- [x] Task 2.2: Implement `identifyCounter(imagePath, side, armyHint)` — base64-encode image, call Claude vision API, return `{ unitId, confidence }`; side determined from filename
- [x] Task 2.3: Implement matching logic — load `oob.json` and `leaders.json`, match returned `unitId` to a record, return the matching record or null
- [x] Task 2.4: Implement write path — write `counterRef` + `confidence` into the matched record for all matches (threshold = 0); no copy needed (images already in place)
- [x] Task 2.5: ~~Source-dir deletion~~ — not needed; images were pre-moved by user
- [x] Task 2.6: Filename-based side detection (`sideFromFilename`) and army hint (`armyFromFilename` for C##/U## files)
- [x] Task 2.7: Implement summary report — print matched count, unmatched count, and per-image rows with confidence score

### Verification

- [ ] `node scripts/detect-counters.js --dry-run` (or equivalent) prints expected output against a small sample without writing files
- [ ] `npm run test` still passes (script not imported by tests)

## Phase 3: Seed Run and Commit

Run the finalized script against the full counter image set, review the report, and commit the
populated data files and copied counter images.

### Tasks

- [ ] Task 3.1: Run `node scripts/detect-counters.js` with `ANTHROPIC_API_KEY` set; capture and review summary report
- [ ] Task 3.2: Manually verify a sample of matched entries in `oob.json` and `leaders.json`
- [ ] Task 3.3: Commit seeded `oob.json`, `leaders.json`, and all files in `client/public/counters/`

### Verification

- [ ] `client/public/counters/` contains all copied counter images
- [ ] `docs/reference/src-counters-sm/` has been deleted
- [ ] `oob.json` and `leaders.json` have non-null `counterRef` entries for matched records
- [ ] Summary report shows expected matched/unmatched ratio
- [ ] `npm run test` passes on the committed state

## Final Verification

- [ ] All acceptance criteria from spec.md are met
- [ ] Tests passing (`npm run test`)
- [ ] Script is idempotent (verified by running twice)
- [ ] Seeded data committed to repo
- [ ] Ready for review

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
