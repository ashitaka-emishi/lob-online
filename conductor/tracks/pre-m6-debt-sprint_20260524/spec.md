# Specification: Pre-M6 Debt Sprint — Issues #461–#464

**Track ID:** pre-m6-debt-sprint_20260524
**Type:** Refactor
**Created:** 2026-05-24
**Status:** Draft

## Summary

Close the 4 tech-debt items deferred from the PR #460 team review before M6 begins.
These items introduce coupling and missing abstractions in map-editor surfaces that M6
will extend; resolving them now reduces risk and brings the net open debt score from 28 → 18.

## Context

lob-online is an online multiplayer implementation of the Line of Battle v2.0 wargame. M5 is
complete; M6 is the next milestone (game map UI, turn structure integration). The map-editor
tool layer — specifically `MapEditorView.vue`, `useMapPersistence.js`, and
`useEdgePanelWiring.js` — will be extended in M6. Four debt items from the PR #460 team review
target these surfaces directly and were flagged as pre-M6 critical in the debt register.

## Acceptance Criteria

- [ ] `mutateEdgeFeatures` helper extracted; `handleEdgePaint` and `handleContourPaint` delegate
      to it with no behavior change (#461)
- [ ] `EDGE_DISPATCH` entries all have explicit `paintFn` and normalized clear strategy;
      fallback logic removed (#462)
- [ ] `migrateUnknownTerrain` policy decided and implemented — either a `_migrationVersion`
      stamp or a documented/asserted no-other-producer guarantee (#463)
- [ ] `activePanelOverlayConfig` coupling addressed — structural improvement or explicit
      invariant documentation with a guard (#464)
- [ ] All 4 GitHub issues closed (#461–#464)
- [ ] `npm run quality:strict` passes with zero unexpected warnings
- [ ] Net open debt score drops from 28 to 18

## Dependencies

- `client/src/views/tools/MapEditorView.vue` — `handleEdgePaint`, `handleContourPaint`,
  `EDGE_DISPATCH`, `onEdgeClick`, `onEdgeRightClick`
- `client/src/composables/useMapPersistence.js` — `migrateUnknownTerrain`
- `client/src/composables/useEdgePanelWiring.js` — `activePanelOverlayConfig` pattern

## Out of Scope

- M6 feature work (game map UI, turn structure)
- Any debt items not in #461–#464
- Changing game rules or engine logic
- New edge feature types

## Technical Notes

#463 is a design decision: the simplest safe resolution is to add a data-schema assertion
(or JSDoc contract) that `'unknown'` is a reserved migration-only value with no other
producers, plus a test that verifies no other code assigns it. This avoids adding a
`_migrationVersion` field to the map schema (which would require a schema bump).
#464 is likely a documentation + guard fix: add a runtime invariant comment or assertion
that only one panel can write `activePanelOverlayConfig` at a time, rather than restructuring
the composable API.

---

_Generated by Conductor. Review and edit as needed._
