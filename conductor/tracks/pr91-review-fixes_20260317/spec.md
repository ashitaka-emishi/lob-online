# Specification: Address PR #91 Team-Review Findings

**Track ID:** pr91-review-fixes_20260317
**Type:** Refactor
**Created:** 2026-03-17
**Status:** Draft

## Summary

Address the High and Medium findings from the team-review of PR #91: move neighbor
propagation out of HexEditPanel into MapEditorView, deduplicate DIRS, replace
JSON.parse/stringify with a shallow clone, bound wedgeElevations and baseElevation in
the schema, clamp WedgeEditor, debounce saveMapDraft, and add two documentation
comments.

## Context

PR #91 refactored the map elevation model. The team-review identified one High finding
(neighbor propagation in HexEditPanel violates single responsibility), two quick Medium
refactors (DIRS duplication, JSON.parse/stringify), and several schema/bounds gaps.
This track addresses all actionable findings before PR #91 is merged.

## Acceptance Criteria

- [ ] HexEditPanel emits `derive-wedges` event; `hexes` and `gridSpec` props removed
- [ ] MapEditorView handles `@derive-wedges` and performs all neighbor propagation
- [ ] DIRS imported from hexGeometry.js in elevationDerive.js and HexEditPanel.vue
- [ ] elevationDerive.js uses shallow Object.entries clone instead of JSON.parse/stringify
- [ ] wedgeElevations schema elements bounded with `.min(-21).max(21)`
- [ ] WedgeEditor.adjustWedge clamps result to [-21, 21]
- [ ] elevationSystem.baseElevation bounded: `z.number().int().min(0).max(9999)`
- [ ] elevationSystem.elevationLevels bounded: `z.number().int().min(1).max(99)`
- [ ] saveMapDraft debounced with 500ms trailing delay in MapEditorView
- [ ] `.max(21)` on elevation documented as SM-specific (pending #92)
- [ ] wedgeElevations tuple annotated with `[0]=N…[5]=NW` mapping
- [ ] 395+ tests pass; new tests for wedge clamping and schema bounds

## Dependencies

- PR #91 / issue-81 branch — work lands on top of that branch
- Issue #92 filed for future CalibrationControls UI (deferred, not in scope here)

## Out of Scope

- Moving elevationDerive.js to shared/ (no server rules engine yet)
- CalibrationControls UI for elevationSystem fields (tracked in #92)
- Schema max(21) driven by runtime elevationLevels value (tracked in #92)

## Technical Notes

- `derive-wedges` event payload: `{ hexId, wedgeElevations, slope, edges }`
- MapEditorView already uses `adjacentHexId` for edge mirroring — reuse same pattern
- Wedge clamp range: -21 to +21 (SM: elevationLevels − 1 = 21)
- saveMapDraft debounce: trailing 500ms, cancel on component unmount via onUnmounted
- baseElevation max 9999 covers realistic elevation values in feet

---

_Generated by Conductor. Review and edit as needed._
