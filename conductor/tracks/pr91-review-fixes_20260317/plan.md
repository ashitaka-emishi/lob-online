# Implementation Plan: Address PR #91 Team-Review Findings

**Track ID:** pr91-review-fixes_20260317
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-17
**Status:** [~] In Progress

## Overview

Three phases: (1) schema tightening + bounds tests, (2) refactor neighbor propagation
architecture + DIRS dedup + clone fix, (3) WedgeEditor clamping + saveMapDraft debounce

- documentation comments. TDD: schema and clamp tests written before implementation.

## Phase 1: Schema Bounds + Tests

Add numeric bounds to wedgeElevations, baseElevation, and elevationLevels in MapSchema.

### Tasks

- [x] Task 1.1: Write tests — wedgeElevations rejects values outside [-21, 21]; baseElevation
      rejects negative and >9999; elevationLevels rejects 0 and >99
- [x] Task 1.2: Add `.min(-21).max(21)` to each wedgeElevations tuple element in
      `server/src/schemas/map.schema.js`
- [x] Task 1.3: Add `.int().min(0).max(9999)` to `elevationSystem.baseElevation`
- [x] Task 1.4: Add `.int().min(1).max(99)` to `elevationSystem.elevationLevels`
- [x] Task 1.5: Add comment on `elevation: z.number().int().min(0).max(21)` documenting
      that 21 is SM-specific (22 levels − 1), pending #92 for runtime derivation
- [x] Task 1.6: Add JSDoc comment on `wedgeElevations` tuple documenting index-to-direction
      mapping: `[0]=N, [1]=NE, [2]=SE, [3]=S, [4]=SW, [5]=NW`

### Verification

- [ ] `npm run test` passes with new schema bound tests green

## Phase 2: Architecture Refactor — Neighbor Propagation + DIRS + Clone

Move neighbor propagation to MapEditorView; deduplicate DIRS; fix deep-clone.

### Tasks

- [x] Task 2.1: Import `DIRS` from `hexGeometry.js` in `elevationDerive.js`; remove local
      declaration
- [x] Task 2.2: Replace `JSON.parse(JSON.stringify(hex.edges))` in `elevationDerive.js`
      with a manual shallow clone:
      `for (const [k, v] of Object.entries(hex.edges)) edges[k] = [...v]`
- [x] Task 2.3: Refactor `HexEditPanel.autoDerive()` to emit a `derive-wedges` event with
      payload `{ hexId, wedgeElevations, slope, edges }` instead of performing neighbor
      lookup; remove the neighbor propagation loop, the `DIRS` local constant, and the
      `adjacentHexId` import from `HexEditPanel.vue`
- [x] Task 2.4: Remove `hexes` and `gridSpec` props from `HexEditPanel.vue`
- [x] Task 2.5: Add `@derive-wedges="onDeriveWedges"` to the `<HexEditPanel>` binding in
      `MapEditorView.vue`; remove `:hexes` and `:grid-spec` bindings
- [x] Task 2.6: Implement `onDeriveWedges({ hexId, wedgeElevations, slope, edges })` in
      `MapEditorView.vue`: apply derived data to current hex via `onHexUpdate`, then loop
      6 directions using `adjacentHexId` + `(i+3)%6` to propagate wedge values and
      derived edge types to neighbors — mirroring the existing edge-mirroring pattern
- [x] Task 2.7: Import `DIRS` from `hexGeometry.js` in `HexEditPanel.vue` if still needed;
      remove any remaining local declaration

### Verification

- [ ] `npm run test` passes — existing elevationDerive and HexEditPanel tests still green
- [ ] `npm run lint` passes

## Phase 3: WedgeEditor Clamping + saveMapDraft Debounce

Clamp wedge values in WedgeEditor; debounce saveMapDraft in MapEditorView.

### Tasks

- [x] Task 3.1: Write test — `adjustWedge` emits clamped value when click would exceed ±21
- [x] Task 3.2: Update `adjustWedge` in `WedgeEditor.vue` to clamp result:
      `Math.max(-21, Math.min(21, current + delta))`
- [x] Task 3.3: Debounce `saveMapDraft` in `MapEditorView.vue` with a 500ms trailing delay
      using a local `setTimeout`/`clearTimeout` ref; cancel on component unmount
      (`onUnmounted`)

### Verification

- [ ] `npm run test` passes (clamp test green)
- [ ] `npm run lint` and `npm run format:check` pass

## Final Verification

- [ ] All acceptance criteria in spec.md met
- [ ] `npm run test` passes — 395+ tests
- [ ] `npm run lint` and `npm run format:check` pass
- [ ] Ready for review

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
