# Specification: Tech Debt Closeout — All Open Items

**Track ID:** tech-debt-closeout_20260323
**Type:** Refactor
**Created:** 2026-03-23
**Status:** Draft

## Summary

Address all 5 open tech debt items (#151, #161, #169, #175, #176) before proceeding with
map editor tweaks and finalization. Total debt score being retired: 9.

## Context

The map editor scaffold is complete. Before moving into final map editor polish and game logic
phases, the codebase has 5 deferred debt items accumulated across recent PRs. These range from
a non-trivial performance refactor (gridData split) to structural composable extractions and
two small defensive additions.

## User Story

As a developer, I want all open tech debt resolved so that the map editor codebase is clean
and maintainable before entering the final polish and game logic phases.

## Acceptance Criteria

- [ ] #151 resolved: `gridData` split into geometry + cell layers; calibration nudges no longer
      trigger full cell array rebuild
- [ ] #161 resolved: per-tool-panel wiring in MapEditorView extracted into a shared composable
- [ ] #169 resolved: `cellsForEdges` extracted into `useEdgeLineLayer(cells, overlayConfig)`
- [ ] #175 resolved: `stripPrivateFields` has a recursion depth guard
- [ ] #176 resolved: `onCalibrationChange` applies same validation guards as the read path
- [ ] All map editor features work without regression
- [ ] All tests pass; lint and format clean
- [ ] Tech debt register updated — net open score = 0

## Dependencies

- Existing map editor codebase (MapEditorView, useCalibration, useEdgeLineLayer area, gridData computed)

## Out of Scope

- New map editor features or UI changes
- Game logic / rules engine work
- Any debt items not in the current open register

## Technical Notes

- #151 is the riskiest item: requires splitting the monolithic `gridData` computed into a
  geometry layer (recalculates on calibration changes) and a cell layer (stable unless grid
  dimensions change). Must not regress rendering correctness or test coverage.
- #161 and #169 are composable extractions — watch for Vue reactivity edge cases.
- #175 and #176 are small, low-risk additions; tackle last as cleanup.

---

_Generated by Conductor. Review and edit as needed._
