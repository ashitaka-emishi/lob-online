# Implementation Plan: Tech Debt Closeout — All Open Items

**Track ID:** tech-debt-closeout_20260323
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-23
**Status:** [ ] Not Started

## Overview

Three phases ordered by risk: tackle the non-trivial gridData split first while the slate is
clean, then extract the two structural composables, then close out the two small defensive
additions. Each phase is independently verifiable.

## Phase 1: gridData Split (#151)

Split the monolithic `gridData` computed into a geometry layer (recalculates on calibration
changes: strokeWidth, northOffset, etc.) and a cell layer (stable unless grid dimensions
change). Calibration nudges currently rebuild ~2,240 cell objects — after the split, nudges
only rebuild the geometry values they actually touch.

### Tasks

- [x] Task 1.1: Read `gridData` computed and map all consumers to understand the split boundary
- [x] Task 1.2: Write failing tests that assert cell objects are not recreated on calibration-only changes
- [x] Task 1.3: Extract geometry values (hex dimensions, stroke, offsets) into `gridGeometry` computed
- [x] Task 1.4: Refactor `gridData` to depend on `gridGeometry` rather than raw calibration refs
- [x] Task 1.5: Verify all consumers still receive correct data; fix any breakage
- [x] Task 1.6: Close GitHub issue #151

### Verification

- [ ] New tests pass: cell array is stable on strokeWidth/northOffset nudge
- [ ] All existing tests pass
- [ ] Map renders correctly; no visual regressions

## Phase 2: Composable Extractions (#161, #169)

Extract two closely related structural items: the repeated per-tool-panel wiring blocks in
MapEditorView (#161), and the `cellsForEdges` logic that belongs in its own composable (#169).

### Tasks

- [ ] Task 2.1: Read MapEditorView and identify the three repeated panel-wiring blocks (#161)
- [ ] Task 2.2: Write tests for the new `useToolPanelWiring` composable contract
- [ ] Task 2.3: Extract shared composable `useToolPanelWiring`; replace all three call sites
- [ ] Task 2.4: Close GitHub issue #161
- [ ] Task 2.5: Read `cellsForEdges` usage and define `useEdgeLineLayer(cells, overlayConfig)` interface (#169)
- [ ] Task 2.6: Write tests for `useEdgeLineLayer`
- [ ] Task 2.7: Extract `useEdgeLineLayer` composable; update MapEditorView / overlay component
- [ ] Task 2.8: Close GitHub issue #169

### Verification

- [ ] New composable tests pass
- [ ] All existing tests pass
- [ ] Map editor tool panels and edge rendering work without regression

## Phase 3: Defensive Additions (#175, #176)

Two small, low-risk items: add a recursion depth guard to `stripPrivateFields`, and add
write-path validation to `onCalibrationChange`.

### Tasks

- [ ] Task 3.1: Add depth-limit parameter (default 10) to `stripPrivateFields`; add test for deep input (#175)
- [ ] Task 3.2: Close GitHub issue #175
- [ ] Task 3.3: Add `safeNumeric`/`safeBoolean`/`safeOrientation` guards to `onCalibrationChange` write path (#176)
- [ ] Task 3.4: Add test verifying invalid write values are clamped/rejected (#176)
- [ ] Task 3.5: Close GitHub issue #176

### Verification

- [ ] New defensive tests pass
- [ ] All existing tests pass

## Final Verification

- [ ] All 5 acceptance criteria met (#151 #161 #169 #175 #176)
- [ ] `npm run test:coverage` passes at 70% threshold
- [ ] `npm run lint` and `npm run format:check` clean
- [ ] Tech debt register updated — net open score = 0
- [ ] Ready for review

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
