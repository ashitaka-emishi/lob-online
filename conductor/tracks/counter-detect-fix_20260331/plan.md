# Implementation Plan: Counter Auto-Detection Script Revision

**Track ID:** counter-detect-fix_20260331
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-31
**Status:** [x] Complete

## Overview

Revise `scripts/detect-counters.js` in three focused passes: (1) filename + color parsing to
pre-classify every image by side and face before any API call, (2) improved vision prompt that
injects the full corrected command chain from `oob.json`, (3) run the script and commit seeded
data.

## Phase 1: Image Classification Helpers

Add deterministic pre-classification so the API is only asked to distinguish units within the
correct side, not across all 220 units.

### Tasks

- [x] Task 1.1: Write `classifyImage(filename)` helper — returns `{ army: 'confederate'|'union'|null,
face: 'front'|'back' }` using prefix rules (`C##` → CSA front, `U##` → USA front,
      `CS1-Front_*` / `CS1-Back_*` → face from name, army via color probe)
- [x] Task 1.2: Write `detectArmyByColor(client, imagePath)` — lightweight Claude vision call;
      blue-dominant → union, grey/tan → confederate
- [x] Task 1.3: Unit-test `classifyImage` for all four filename patterns
- [x] Task 1.4: Unit-test `detectArmyByColor` with mocked Anthropic client

### Verification

- [x] `npm run test` passes with new unit tests green (20 new tests, 1159 total)

## Phase 2: Improved Vision Prompt + Match Logic

Replace the flat unit-list prompt with one that includes the full command chain and uses the
pre-classified army to restrict the candidate roster.

### Tasks

- [x] Task 2.1: Build `buildRoster(oobMap, leaderMap, army)` — flat array filtered to army,
      each unit annotated with full command path (corps › division › brigade)
- [x] Task 2.2: Revise `identifyCounter(client, imagePath, face, army, roster)` — use
      army-filtered roster and command-chain-enriched prompt
- [x] Task 2.3: Update match-write logic — skip overwriting any `counterRef.front` /
      `counterRef.back` that is already non-null unless `--force` flag passed (idempotency)
- [x] Task 2.4: Write correct face field (`counterRef.front` or `counterRef.back`) and
      corresponding confidence field based on `classification.face`

### Verification

- [x] Dry-run confirmed correct army-filtered rosters and command-chain context in prompts

## Phase 3: Run, Seed, and Commit

Execute the revised script against all 468 images and commit results.

### Tasks

- [x] Task 3.1: Run `node scripts/detect-counters.js` — 76 matched; parse failures identified
- [x] Task 3.2: Fixed JSON parser (regex extract first `{…}`); re-ran with `--force` — 193 matched
- [x] Task 3.3: Committed updated `oob.json` and `leaders.json` with seeded counterRef values
      (CSA 25/75 front, 27/75 back; USA 31/95 front, 14/95 back; CSA 1/17 leaders; USA 7/31)
- [x] Task 3.4: Remaining nulls left for OOB Editor manual linkage

### Verification

- [x] CSA and USA units have non-null counterRef in committed data
- [x] `npm run test` passes (1159 tests)

## Final Verification

- [x] All acceptance criteria in spec.md met
- [x] Tests passing
- [x] `oob.json` and `leaders.json` seeded data committed
- [x] Ready for review

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
