# Implementation Plan: M2 Closeout Debt Sprint

**Track ID:** m2-closeout_20260409
**Spec:** [spec.md](./spec.md)
**Created:** 2026-04-09
**Status:** [x] Complete

## Overview

Twelve debt items closed across four phases. Five issues (#221 #220 #241 #214 #216) were
already resolved by prior work (editorRouteFactory refactor in PR #261; store tests in
PR #252) and closed without code changes. Seven items required new implementation.

## Phase 1: Schema Deduplication & Shared Fixtures

### Tasks

- [x] Task 1.1: Extract `CommandLevel` enum and `CounterRef` shape into a shared schema
      module; update `succession.schema.js` and `leaders.schema.js` to import from it (#242)
- [x] Task 1.2: Constrain `specialRules` values in both schemas from `z.unknown()`
      to `z.union([z.string(), z.boolean()])` — matches all values in data files (#258)
- [x] Task 1.3: Extract shared `MINIMAL_SUCCESSION` + `MINIMAL_VARIANT` fixture into
      `succession.fixtures.js`; update `succession.schema.test.js` to import from it (#253)

### Verification

- [x] `npm run test` passes; no schema validation regressions; real data files validate

## Phase 2: Route Hardening

Tasks 2.1 (#220) and 2.2 (#241) were already done by editorRouteFactory (async I/O,
try/catch). Task 2.4 (#216) was already done by PR #252.

### Tasks

- [x] Task 2.1: Async I/O in oobEditor and leadersEditor (#220) — already done
- [x] Task 2.2: successionEditor writeFile try/catch (#241) — already done
- [x] Task 2.3: Implement atomic write (write to `.tmp` file, then `fs.rename`) in
      `editorRouteFactory.js`; `.tmp` cleanup on failure; route test mocks updated (#251)
- [x] Task 2.4: Prototype pollution guard for toString/valueOf (#216) — already done

### Verification

- [x] All 5 editor route tests pass; atomic write tests added

## Phase 3: Schema Strictness & Contract Enforcement

Task 3.1 (#221) was already done by prior schema work (.strict() + \_savedAt already present).

### Tasks

- [x] Task 3.1: Apply .strict() to OOBSchema and LeadersSchema (#221) — already done
- [x] Task 3.2: Extract `oobValidators.js` (pure JS); write `oob-client-contract.test.js`
      that imports both client validators and server Zod schemas and verifies alignment (#257)

### Verification

- [x] Contract test passes; fails if union/confederate shape changes on one side only

## Phase 4: Composable Refactor & Store Test Quality

Task 4.3 (#214) was already done by PR #252.

### Tasks

- [x] Task 4.1: Add `isOffline` ref to `useOobPersistence`; set on localStorage fallback,
      clear on server success; block push when offline (#207)
- [x] Task 4.2: Four offline-detection tests added to `useOobPersistence.test.js` (#207)
- [x] Task 4.3: pullFromServer failure assertions (#214) — already done in PR #252
- [x] Task 4.4: Refactor `useOobStore.test.js` to URL-keyed `mockFetch` dispatch (#254)

### Verification

- [x] `useOobPersistence.test.js` covers offline detection, push-blocking; all 1306 tests pass

## Final Verification

- [x] All 12 issues (#207 #258 #257 #254 #253 #251 #242 #241 #221 #220 #216 #214) closed
- [x] `npm run lint` passes (0 errors)
- [x] `npm run format:check` passes
- [x] `npm run test:coverage` passes — 67/67 test files, 1306/1306 tests, ≥ 70% threshold
- [x] No new debt items introduced
- [x] Ready for review

---

_Generated by Conductor. Tasks marked [x] complete._
