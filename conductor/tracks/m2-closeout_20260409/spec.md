# Specification: M2 Closeout Debt Sprint

**Track ID:** m2-closeout_20260409
**Type:** Chore
**Created:** 2026-04-09
**Status:** Draft

## Summary

Resolve all remaining open debt from the M2 OOB editor phase (#207 + eleven score-2 items)
before M3 game engine work begins, so the new milestone starts with a clean register.

## Context

PR #261 closed the three score-3 items (#237 #245 #247). One score-3 (#207) and eleven
score-2 items remain. Project policy: start a new milestone clean of debt.

## Problem Description

Open items after PR #261:

| Score | Issue | Title                                                                           |
| ----- | ----- | ------------------------------------------------------------------------------- |
| 3     | #207  | Extract shared persistence composable (offline detection + composable refactor) |
| 2     | #258  | succession.schema: constrain specialRules from z.unknown()                      |
| 2     | #257  | Enforce client/server schema contract (client validators vs. server Zod)        |
| 2     | #254  | useOobStore: match mockFetch on URL not call ordinal                            |
| 2     | #253  | Extract shared MINIMAL_SUCCESSION fixture across test files                     |
| 2     | #251  | Atomic writes for all 5 editor routes                                           |
| 2     | #242  | Extract shared CommandLevel enum and CounterRef shape                           |
| 2     | #241  | successionEditor writeFile: wrap in try/catch                                   |
| 2     | #221  | Apply .strict() to OOBSchema and LeadersSchema                                  |
| 2     | #220  | Async I/O in oobEditor and leadersEditor routes                                 |
| 2     | #216  | Extend prototype pollution guard (toString/valueOf)                             |
| 2     | #214  | Assert syncError and isSyncing reset on pullFromServer failure                  |

## Acceptance Criteria

- [ ] #207 closed: `useOobPersistence` refactored to share a composable base with
      `useMapPersistence`; offline detection added
- [ ] #258 closed: `specialRules` values constrained in succession schema
- [ ] #257 closed: automated contract enforcement between client validators and server schemas
- [ ] #254 closed: mockFetch matched on URL in useOobStore tests
- [ ] #253 closed: shared MINIMAL_SUCCESSION fixture extracted
- [ ] #251 closed: atomic writes (write-tmp + rename) across all 5 editor routes
- [ ] #242 closed: CommandLevel enum and CounterRef shape deduplicated
- [ ] #241 closed: successionEditor writeFile wrapped in try/catch
- [ ] #221 closed: .strict() applied after \_savedAt field resolved
- [ ] #220 closed: sync fs → fs.promises in oobEditor and leadersEditor
- [ ] #216 closed: toString/valueOf added to prototype pollution blocklist
- [ ] #214 closed: failure test asserts syncError set and isSyncing false
- [ ] All CI gates pass (lint, format, tests ≥ 70% coverage)
- [ ] No new debt items introduced

## Dependencies

Depends on existing OOB editor code (useOobPersistence, useMapPersistence, editor routes,
oobEditor.js, leadersEditor.js, succession.schema.js, leaders.schema.js).

## Out of Scope

- Score-1 items (#260 #259 #256 #255 #205 #204 #201) — deferred to M3 ongoing
- Any new OOB editor features
- M3 game engine work

## Technical Notes

- **Atomic writes (#251, risk):** Use write-to-tmp-then-rename pattern (fs.rename is atomic on
  same filesystem). Affects all 5 routes: oobEditor, leadersEditor, mapEditor, scenarioEditor,
  successionEditor. Coordinate with async I/O refactor (#220) since both touch the same files.
- **#221 (.strict()):** Must add `_savedAt` to schema first, then apply `.strict()`.
- **#257 (schema contract):** Likely a shared validation utility or test fixture that imports
  both client validators and server schemas and asserts shape equivalence.

---

_Generated by Conductor. Review and edit as needed._
