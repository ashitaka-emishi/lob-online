# Implementation Plan: OOB Editor Foundation — Launch Script, Store & Hierarchy Tree

**Track ID:** oob-editor-foundation_20260325
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-25
**Status:** [x] Complete
**Issues:** #196, #192

## Overview

Three phases: (1) wire the launch path and home-page nav (#196), (2) implement the Pinia store and write tests (#192 data layer), (3) implement the hierarchy tree UI components and write tests (#192 UI).

---

## Phase 1: Launch Path & Home-Page Nav (#196)

Wire up `scripts/oob-editor.sh`, the `dev:oob-editor` npm script, the lazy-loaded route, and the Dev Tools nav section in `StatusView.vue`.

### Tasks

- [x] Task 1.1: Create `scripts/oob-editor.sh` — copy of `scripts/map-editor.sh` with path updated to `/tools/oob-editor`.
- [x] Task 1.2: Add `"dev:oob-editor": "bash scripts/oob-editor.sh"` to root `package.json` scripts.
- [x] Task 1.3: Add lazy-loaded route `{ path: '/tools/oob-editor', component: () => import('./views/tools/OobEditorView.vue') }` with `MAP_EDITOR_ENABLED` guard to `client/src/router/index.js`.
- [x] Task 1.4: Add a **Dev Tools** section to `client/src/views/StatusView.vue` with `<RouterLink>` buttons to `/tools/map-editor` and `/tools/oob-editor`, rendered only when `VITE_MAP_EDITOR_ENABLED=true`.

### Verification

- [x] `npm run dev:oob-editor` starts without error (route resolves to placeholder view).
- [x] Home page shows Dev Tools buttons when `VITE_MAP_EDITOR_ENABLED=true`; buttons are absent when false.

---

## Phase 2: Pinia Store (#192 — Data Layer)

Implement `useOobStore.js` with server → localStorage → bundled JSON fallback, push/pull sync, and all required actions.

### Tasks

- [x] Task 2.1: Write failing tests for `useOobStore.js` — load fallback chain, `selectedNode`, `dirty` flag, `updateField`, `updateCounterRef`, push/pull sync actions.
- [x] Task 2.2: Create `client/src/stores/useOobStore.js`:
  - State: `oob` (object), `leaders` (object), `selectedNode` (null | node ref), `dirty` (bool).
  - `loadData()` — fetch `/api/oob/data` + `/api/leaders/data` → localStorage fallback → static import fallback; debounced auto-save.
  - `selectNode(node)` — set `selectedNode`.
  - `updateField(path, value)` — mutate `oob` or `leaders` by dot-path, set `dirty = true`.
  - `updateCounterRef(unitId, counterRef)` — update `counterRef` on the target unit, set `dirty = true`.
  - `pushToServer()` / `pullFromServer()` — mirror map editor sync pattern.
  - Also registered Pinia in `client/src/main.js`.
- [x] Task 2.3: Make all store tests pass.

### Verification

- [x] All store unit tests pass.
- [x] `npm run test:coverage` shows ≥ 70% line coverage for the new store file.

---

## Phase 3: Hierarchy Tree UI (#192 — Components)

Implement `OobEditorView.vue`, `OobHierarchyTree.vue`, and `OobTreeNode.vue`.

### Tasks

- [x] Task 3.1: Write failing tests for `OobHierarchyTree.vue` and `OobTreeNode.vue` — renders correct node count, expand/collapse toggles, type badges, active-side filtering.
- [x] Task 3.2: Create `client/src/views/tools/OobEditorView.vue`:
  - Toolbar: side selector (Union/Confederate), Push/Pull buttons (wired to store actions), dirty indicator.
  - Two-panel layout: left sidebar (`OobHierarchyTree`), right detail panel (placeholder `<p>Select a unit</p>`).
  - Imports and wires `useOobStore`; calls `loadData()` on mount.
- [x] Task 3.3: Create `client/src/components/OobHierarchyTree.vue`:
  - Props: `side` (string — `'union'` | `'confederate'`).
  - Renders top-level Corps nodes for the selected side from store.
  - Each top-level node rendered via `<OobTreeNode>`.
- [x] Task 3.4: Create `client/src/components/OobTreeNode.vue`:
  - Props: `node` (object), `nodeType` (string).
  - Self-referencing recursion for Corps → Division → Brigade → Regiment/Battery nesting.
  - Type badge (Corps / Div / Brig / Inf / Arty / Cav / Leader).
  - Expand/collapse toggle for nodes with children.
  - Clicking a node calls `store.selectNode(node)`.
- [x] Task 3.5: Make all component tests pass.

### Verification

- [x] All component tests pass.
- [x] `npm run test:coverage` remains ≥ 70%.
- [x] `npm run lint` and `npm run format:check` pass clean.

---

## Final Verification

- [x] All acceptance criteria in spec.md met.
- [x] `npm run dev:oob-editor` launches; hierarchy tree renders; expand/collapse works; active-side toggle works.
- [x] Push/Pull buttons visible in toolbar (wired to store).
- [x] Home-page Dev Tools nav buttons present and correct.
- [x] All tests passing (935); coverage ≥ 70%.
- [x] Ready for `/pr-create`.

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
