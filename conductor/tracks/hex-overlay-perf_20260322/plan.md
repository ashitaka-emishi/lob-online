# Implementation Plan: HexMapOverlay Performance Quick Wins Bundle

**Track ID:** hex-overlay-perf_20260322
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-22
**Status:** [x] Complete

## Overview

Four targeted, low-risk fixes in `HexMapOverlay` and its rendering path. All follow existing
patterns in the codebase. Phase order: rAF gate first (most impactful, establishes the
throttle context), then `findNearestEdge` dedup (depends on rAF gate being in place),
then Vue-side computed optimizations (independent, lowest risk).

## Phase 1: rAF Throttle on Edge-Hover Path (#160)

Apply the `requestAnimationFrame` pattern from the hex-hover handler to the edge-hover
state writes in `HexMapOverlay`, preventing Vue re-renders on every raw mousemove event.

### Tasks

- [x] Task 1.1: Read `HexMapOverlay` and locate the edge-hover write path and the existing
      hex-hover rAF pattern (PR #88) to confirm exact implementation to mirror.
- [x] Task 1.2: Write a Vitest test that asserts `setEdgeHover` is not called synchronously
      on rapid mousemove events (mock `requestAnimationFrame`).
- [x] Task 1.3: Add a `rafPending` flag to the edge-hover mousemove handler; gate the
      `setEdgeHover` write behind `requestAnimationFrame`, matching the hex-hover pattern.
- [x] Task 1.4: Verify the new test passes and no existing tests regress.

### Verification

- [x] Edge-hover rAF gate is in place and mirrors the hex-hover pattern exactly.
- [x] All Vitest tests pass.

## Phase 2: Deduplicate `findNearestEdge` Calls (#159)

Remove the second `findNearestEdge` call in the tooltip lookup path by reusing the
`nearDir` already stored in `hoverInfo`.

### Tasks

- [x] Task 2.1: Locate both `findNearestEdge` call sites in the mousemove handler and
      confirm `hoverInfo.nearDir` is available at the tooltip lookup point.
- [x] Task 2.2: Write a test (or extend existing spy) asserting `findNearestEdge` is called
      at most once per mousemove invocation.
- [x] Task 2.3: Replace the tooltip-lookup call with a read from `hoverInfo.nearDir`.
- [x] Task 2.4: Verify the dedup test passes; confirm tooltip behavior is unchanged in the
      dev server.

### Verification

- [x] `findNearestEdge` called exactly once per mousemove frame.
- [x] All Vitest tests pass.

## Phase 3: Vue Rendering Optimizations (#163, #164)

Two independent Vue-layer fixes: move the inline `v-for` filter to a computed property,
and memoize `wedgePolygonPoints` so polygon strings are not recomputed on every render.

### Tasks

- [x] Task 3.1: Locate the inline `.filter()` in the wedge/contour `v-for` and the
      `wedgePolygonPoints` call site.
- [x] Task 3.2: Extract the `v-for` filter as a `visibleWedges` (or equivalent) computed
      property; update the template reference.
- [x] Task 3.3: Memoize `wedgePolygonPoints` by wrapping the call in a `computed` keyed
      on `gridData` (already invalidates on calibration changes).
- [x] Task 3.4: Run `npm run test:coverage` and confirm coverage ≥ 70%; run lint + format check.

### Verification

- [x] No inline `.filter()` calls remain in `v-for` directives in `HexMapOverlay`.
- [x] `wedgePolygonPoints` is computed, not called inline in the template.
- [x] CI gates pass (lint, format, test).

## Final Verification

- [x] All four debt items (#159, #160, #163, #164) addressed.
- [x] All Vitest tests pass with coverage ≥ 70%.
- [x] `npm run lint` and `npm run format:check` pass.
- [ ] No regressions visible in the map editor dev server. ← manual smoke test
- [x] Ready for `/pr-create` → `/team-review`.

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
