# Implementation Plan: M8 Local Dev Infrastructure

**Track ID:** m8-local-infra_20260622
**Spec:** [spec.md](./spec.md)
**Created:** 2026-06-22
**Status:** [x] Complete

## Overview

Add MinIO Docker Compose service and a Discord webhook sink script so all subsequent M8
work can be developed and tested locally without real cloud credentials.

## Interaction Mode

**Mode:** Autonomous
**Human control points:** None beyond phase approvals

## Risk Classification

**Risk:** Low
**Reason:** Dev-only tooling; no production code changes, no game engine changes.

## Quality Gates

- [x] `npm run validate-data`
- [x] `npm run lint`
- [x] `npm run format:check`
- [x] `npm run test`
- [x] `npm run build`
- [x] No unexpected warnings in test output

## Debt Budget

**Allowed new deferred debt:** 0 unless explicitly approved.

## Completion Contract

- [x] All plan tasks complete
- [x] All acceptance criteria in spec.md met
- [x] Warnings fixed or explicitly classified as accepted prototype noise
- [x] Debt register updated if any debt was accepted
- [x] Ready for `/team-review`

---

## Phase 1: MinIO Docker Compose

### Tasks

- [x] Task 1.1: Add `docker-compose.yml` at repo root with MinIO service:
  - Image `minio/minio:latest`
  - Ports `9000:9000` (S3 API) and `9001:9001` (console)
  - Command `server /data --console-address ":9001"`
  - Env vars `MINIO_ROOT_USER` and `MINIO_ROOT_PASSWORD` read from `.env`
  - Volume `./data/minio:/data`
- [x] Task 1.2: Add `./data/minio` to `.gitignore`
- [x] Task 1.3: Update `.env.example` with all M8 env vars and local dev defaults:
  - `SPACES_KEY=minioadmin`
  - `SPACES_SECRET=minioadmin`
  - `SPACES_BUCKET=lob-online-dev`
  - `SPACES_ENDPOINT=http://localhost:9000`
  - `SPACES_FORCE_PATH_STYLE=true`
  - `DISCORD_WEBHOOK_TEST_URL=http://localhost:4040`

### Verification

- [x] `docker compose up -d` starts without errors
- [x] MinIO console reachable at `http://localhost:9001`
- [x] Bucket can be created via console UI

---

## Phase 2: Discord Webhook Sink Script

### Tasks

- [x] Task 2.1: Write `scripts/discord-test-server.js` — Express server on port 4040:
  - `POST /` — logs request body as pretty JSON, returns 204
  - `GET /` — returns 200 with `{"status":"ok"}` for health check
  - Startup log: `[discord-sink] Listening on http://localhost:4040`
- [x] Task 2.2: Add `"discord:sink": "node scripts/discord-test-server.js"` to root `package.json` scripts
- [x] Task 2.3: Ensure `express` is available to the script (root devDependency or reuse server workspace)

### Verification

- [x] `npm run discord:sink` starts the server
- [x] `curl -X POST http://localhost:4040 -H 'Content-Type: application/json' -d '{"test":1}'` returns 204 and logs body
- [x] `curl http://localhost:4040` returns `{"status":"ok"}`

---

## Final Verification

- [x] All acceptance criteria in spec.md met
- [x] `npm run quality:strict` passes
- [x] No unexpected warnings in test output

---

_Generated by Conductor on 2026-06-22._
