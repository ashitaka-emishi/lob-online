# Implementation Plan: LOS Display Off-by-One — Hex Column Offset

**Track ID:** los-offset-fix_20260318
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-18
**Status:** [x] Complete

## Overview

Three sequential phases: commit pre-existing work, write a failing test that pins the bug, fix the formula in all three locations, then verify all tests pass.

## Phase 1: Commit Pre-existing Uncommitted Work

Commits the two unrelated but ready changes so they don't tangle with the bug-fix diff.

### Tasks

- [x] Task 1.1: Create feature branch `fix/los-offset-#114`
- [x] Task 1.2: Stage and commit `.claude/settings.json` — add `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` env var
- [x] Task 1.3: Stage and commit `data/scenarios/south-mountain/map.json` — NW sector terrain digitization (~100 hexes, cols 01–21 rows 01–10, with elevation)

### Verification

- [ ] `git log --oneline -3` shows the two new commits on the feature branch
- [ ] `npm run test` passes with no regressions

## Phase 2: Reproduce — Write a Failing Test

Pins the exact parity bug before touching the formula, so the fix is verifiable.

### Tasks

- [x] Task 2.1: In `client/src/utils/los.test.js`, add a round-trip test for `colRowToCube` → `cubeToColRow` covering at least one odd game column (e.g. col 7) and one even game column (e.g. col 8) — these should already pass and serve as a baseline
- [x] Task 2.2: In `client/src/components/HexMapOverlay.test.js`, add a test that instantiates the `gameRow` formula (extracted or imported as a helper, or tested via cell ID output) and asserts that odd game columns produce the correct row — this test must **fail** on the current code to confirm it targets the right defect

### Verification

- [ ] `npm run test` shows exactly the new test(s) in Phase 2 failing; all pre-existing tests still pass

## Phase 3: Fix the Formula

Corrects the parity in all three occurrences in `HexMapOverlay.vue`.

### Tasks

- [x] Task 3.1: Determine the correct parity by inspecting honeycomb-grid behaviour with `offset: -1` for flat-top hexes — confirm whether odd or even `hex.col` is the shifted column
- [x] Task 3.2: Update the `gameRow` formula in `gridData` computed (~line 171) to apply the offset correction to the correct column parity
- [x] Task 3.3: Apply the identical fix to `onSvgClick` (~line 290)
- [x] Task 3.4: Apply the identical fix to `onSvgContextMenu` (~line 359)
- [x] Task 3.5: Commit the fix with a clear message referencing issue #114

### Verification

- [x] The failing test from Phase 2 now passes
- [x] `npm run test` passes in full (448 tests)
- [x] `npm run lint && npm run format:check` passes

## Final Verification

- [ ] All acceptance criteria in `spec.md` are met
- [ ] Tests passing with `npm run test:coverage` at or above 70% line coverage
- [ ] Ready for PR review

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
