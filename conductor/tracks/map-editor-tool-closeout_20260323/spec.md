# Specification: Map Editor Tool Closeout — Issues #137 #138 #139 #140 #141 #142

**Track ID:** map-editor-tool-closeout_20260323
**Type:** Feature
**Created:** 2026-03-23
**Status:** Draft

## Summary

Close all six open map editor tool issues. Three are already complete and need
only issue closure; three have genuine gaps requiring implementation.

## Context

The "Create usable map editor" milestone has six open issues (#137–#142) created
when the tool-panel-framework track (#136) landed. Most tools were subsequently
implemented across several tracks (edge-tools-bundle, elevation-system-ui, etc.)
but the issues were never explicitly closed. A quick audit confirms #140, #141,
and #142 are fully implemented; #137, #138, and #139 have specific missing
features described below.

## User Story

As a map digitizer, I want the elevation, terrain, and road tools to match their
v2 design spec so that the map editor is fully usable for accurate South Mountain
digitization.

## Acceptance Criteria

- [ ] #137: ElevationToolPanel has a fixed-value slider; click/paint sets all
      touched hexes to the slider value (not increment/decrement)
- [ ] #138: TerrainToolPanel buttons show color swatches from TERRAIN_COLORS;
      a building button is present and sets hexFeature to `{ type: 'building' }`
- [ ] #139: RoadToolPanel overlay uses `through-hex` style — edge lines connect
      entry/exit edge midpoints via the hex center; HexMapOverlay renders it
- [ ] #140, #141, #142: Issues closed as already complete
- [ ] All existing tests pass; coverage ≥ 70%

## Dependencies

- Existing: `ElevationToolPanel.vue`, `TerrainToolPanel.vue`, `RoadToolPanel.vue`,
  `HexMapOverlay.vue`, `feature-types.js`, `hexGeometry.js`

## Out of Scope

- New tool types beyond building
- Any game logic or scenario editor changes

## Technical Notes

- `through-hex` rendering: `hexGeometry.js` already has `edgeMidpoint()`; the
  through-hex line is two segments: entry-midpoint → hex-center → exit-midpoint.
  HexMapOverlay needs a new render branch for `style: 'through-hex'` in its
  edge-rendering computed and SVG template.
- Elevation slider: `<input type="range">` bound to a new `targetElevation` ref;
  interaction handlers set `hex.elevation = targetElevation` instead of ±1.
- Building: add `'building'` to TerrainToolPanel's type list with icon '⊞';
  no TERRAIN_COLORS entry (buildings render as icon only); `hexFeature-set` emit
  is distinct from `terrain-change`.

---

_Generated by Conductor. Review and edit as needed._
