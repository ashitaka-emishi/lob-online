# Implementation Plan: Map Editor Tool Closeout — Issues #137 #138 #139 #140 #141 #142

**Track ID:** map-editor-tool-closeout_20260323
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-23
**Status:** [x] Complete

## Overview

Three phases of real implementation (elevation slider, terrain swatches/building,
road through-hex rendering), followed by a cleanup phase to close the three stale
issues. Each phase is independently testable.

## Phase 1: Elevation Fixed-Value Slider (#137)

Replace click-to-increment/decrement with a fixed-value slider. Interaction
handlers set touched hexes to the slider's current target value.

### Tasks

- [x] Task 1.1: Write failing tests for ElevationToolPanel covering: slider
      renders with min=0/max=elevationLevels−1, click/paint emits fixed value
      not ±1 delta, right-click still clears (sets 0), raise-all/lower-all
      retained as bulk ops
- [x] Task 1.2: Add `targetElevation` ref (default 1) and `<input type="range">`
      to ElevationToolPanel; update HELP_TEXT; emit `elevation-set` with fixed
      value on click/paint; retain `raise-all` / `lower-all` / `clear-all` bulk
      ops unchanged
- [x] Task 1.3: Update MapEditorView to handle `elevation-set` in place of the
      current increment/decrement handlers; update existing MapEditorView tests
      that reference elevation interaction

### Verification

- [ ] ElevationToolPanel tests pass; slider present in component; MapEditorView
      wires the new event correctly; full test suite green

## Phase 2: Terrain Color Swatches + Building (#138)

Add color swatches to terrain type buttons and a building button type.

### Tasks

- [x] Task 2.1: Write failing tests for TerrainToolPanel covering: each terrain
      type button renders a color swatch using TERRAIN_COLORS; building button
      present and emits `terrain-change` with value `'building'`; building
      placement handled separately via `hexFeature-set`
- [x] Task 2.2: Update TerrainToolPanel template — add a `<span>` swatch element
      inside each ToolChooser item using TERRAIN_COLORS[type] as background; add
      `'building'` to the type list with icon '⊞' and no swatch (buildings have
      no fill color); emit `hexFeature-set` for building placement
- [x] Task 2.3: Update MapEditorView to handle `hexFeature-set` from
      TerrainToolPanel (set `hex.hexFeature = { type: 'building' }` on paint);
      right-click clears hexFeature

### Verification

- [ ] TerrainToolPanel tests pass; swatches visible in component; building
      placement wired in MapEditorView; full test suite green

## Phase 3: Road Through-Hex Rendering (#139)

Add `through-hex` style branch to HexMapOverlay and switch RoadToolPanel to use
it. Roads draw entry-midpoint → hex-center → exit-midpoint instead of along-edge.

### Tasks

- [x] Task 3.1: Write failing tests for HexMapOverlay `through-hex` style:
      given a hex with road on two edges, rendered SVG contains a polyline
      through hex center; single road edge renders midpoint→center stub; cell
      with no road edges produces no through-hex line
- [x] Task 3.2: In HexMapOverlay, add `throughHexSegments` computed (alongside
      `cellsForEdges`): for each cell, collect edges with `style: 'through-hex'`
      features, pair them (or stub to center for odd count), compute polyline
      points using `edgeMidpoint()` + hex center from polygon corners; render as
      `<polyline>` elements in the SVG
- [x] Task 3.3: Update RoadToolPanel `ownOverlayConfig` to use
      `style: 'through-hex'` instead of `'along-edge'`; update RoadToolPanel
      tests that assert overlay style

### Verification

- [ ] HexMapOverlay through-hex tests pass; road segments visually connect
      through hex centers; full test suite green

## Phase 4: Close Stale Issues + Final Verification

### Tasks

- [x] Task 4.1: Close GitHub issues #140, #141, #142 with a comment confirming
      implementation is complete and citing the delivering commit
- [x] Task 4.2: Run `npm run test:coverage` — confirm ≥ 70% threshold
- [x] Task 4.3: Manual smoke test checklist: elevation slider sets fixed values,
      terrain swatches render, building button works, roads render through-hex

### Verification

- [x] Issues #140–#142 closed; all acceptance criteria met; ready for PR

## Final Verification

- [x] All acceptance criteria met
- [x] `npm run test:coverage` ≥ 70% threshold maintained (85.68% lines)
- [ ] `npm run lint` and `npm run format:check` pass
- [ ] Ready for `/team-review`

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
