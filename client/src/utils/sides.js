// Canonical side identifiers used across components, composables, and tests. (#412)
// Server-side mirror: server/src/util/sides.js — update both if sides change.
export const SIDES = Object.freeze({ UNION: 'union', CONFEDERATE: 'confederate' });
