// Canonical side identifiers used across routes, engine, and tests. (#412)
// Client-side mirror: client/src/utils/sides.js — update both if sides change.
export const SIDES = Object.freeze({ UNION: 'union', CONFEDERATE: 'confederate' });
