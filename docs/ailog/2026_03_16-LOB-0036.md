# LOB-0036 — fix(issue-implement): add explicit issue-close step with HCP

**Branch:** feat/36-fix-issue-implement-add-explicit-close
**Started:** 2026-03-16 09:07

---

## AI Plan

Three files need modification — no new files required. `.claude/commands/issue-implement.md`
gains a Step 9 after the merge step: display the merged commit SHA and issue URL, present
HCP 4 (wait for explicit "close" signal), then call `gh issue close <number> --comment "..."`;
if the user does not say "close", stop and report the URL for manual closing.
`docs/workflows/issue-implement/issue-implement.workflow.json` gains two new steps appended
after `gate-merged`: a `gate-close` gate step (HCP 4, choices: close / skip) and a
`close-issue` agent step. `docs/workflows/issue-implement/issue-implement.states.md` is
updated with `gate_close` and `close_issue` nodes in the Mermaid diagram and a new row in
the gate checkpoint table. No unit tests are required; verification is `/dev-build` passing
(format clean, lint clean, build succeeded).

---

## HCP 1 — Plan Accepted

**Accepted:** 2026-03-16 09:08
**Notes:** none

---

## Branch Created

`feat/36-fix-issue-implement-add-explicit-close` — 2026-03-16 09:08

---

## Implementation Notes

All three changes are confined to documentation/config files. The `close-issue` step in the
workflow JSON references `agentId: "issue-close"` — this agent does not yet exist in the
registry, but `onError: "continue_with_warning"` ensures the workflow degrades gracefully
rather than halting. The skill (`issue-implement.md`) is the authoritative execution path;
the workflow JSON mirrors the structure for the orchestration runtime. HCP 4 uses "close" /
"skip" (not "yes" / "no") to match the imperative verb pattern used by HCP 3 ("merge" /
"hold").

---

## Build & Test Results

format: 2 files reformatted (ailog + states.md whitespace). lint: clean. build: 197.50 kB. tests: 307/307 passed.

---

## HCP 2 — Implementation Accepted

**Accepted:** 2026-03-16 09:11
**Notes:** none

---

## HCP 3 — Merge Approved

**Approved:** 2026-03-16 09:18

---

## Merge Complete

**Commit:** 3036722
**Merged:** 2026-03-16 09:18
