# LOB-0054 — Add map-autodetect-config.json with seed hex management UI

**Branch:** feat/54-map-autodetect-config-json-seed-hex-management-ui
**Started:** 2026-03-16 16:07

---

## AI Plan

**Server:** `map-autodetect-config.schema.js` defines three Zod schemas (`ElevationBandSchema`, `SeedHexSchema`, `AutoDetectConfigSchema`) matching design doc §9. Initial data file `map-autodetect-config.json` contains `{ "seedHexes": [] }`. A new Express router `autoDetectConfig.js` provides GET/PUT `/data` endpoints with versioned backup logic identical to `mapEditor.js`; registered in `server.js` under the `MAP_EDITOR_ENABLED` guard at `/api/tools/map-autodetect-config`. Route tests follow the `mapEditor.test.js` mock-fs pattern.

**Client:** `HexEditPanel.vue` gains a `isSeedHex` boolean prop and a "Mark as Seed Hex" toggle button (enabled when terrain ≠ 'unknown' and elevation is set); clicking emits `seed-toggle` with the hex's `confirmedData`. `ElevationSeedEditor.vue` accepts a `config` prop (the full `AutoDetectConfig` object) and renders: a coverage grid showing which elevation bands have ≥1 seed hex, a list of current elevation seed hexes each with a delete button emitting `seed-remove`. `TerrainSeedEditor.vue` mirrors this for terrain types (open, woods, town, orchard, rough). Both editors update reactively via prop. Component tests use `@vue/test-utils` mount pattern. `cropBase64` is stored as an empty string placeholder — crop extraction is deferred to Issue C.

---

## Branch Created

`feat/54-map-autodetect-config-json-seed-hex-management-ui` — 2026-03-16 16:10

---

## HCP 1 — Plan Accepted

**Accepted:** 2026-03-16 16:10
**Notes:** none

---

## Implementation Notes

- Three Zod schemas (`ElevationBandSchema`, `SeedHexSchema`, `AutoDetectConfigSchema`) live in a dedicated schema file, following the existing pattern. All fields optional at the config level so an empty `{}` is valid.
- `autoDetectConfig.js` route mirrors the `mapEditor.js` backup pattern exactly (20-backup rolling window, `backups/autodetect/` subdir, `_savedAt` timestamp injected on write).
- Registered in `server.js` under the `MAP_EDITOR_ENABLED` guard at `/api/tools/map-autodetect-config` — consistent with the other dev-tool routes.
- `HexEditPanel.vue` `canMarkAsSeed` guard requires terrain ≠ `'unknown'` and elevation not empty/null. `cropBase64` stored as `""` placeholder; crop extraction deferred to Issue C.
- Hardcoded 5 elevation bands in `ElevationSeedEditor` (0–300, 301–450, 451–600, 601–750, 751+) match design doc §3 palette ranges. No `elevationPalette` needed in initial config.
- CLAUDE.md Data Models table updated with `SM_AUTODETECT_CFG` row pointing to the new file.

---

## Build & Test Results

- `npm run format` — reformatted 4 files (ElevationSeedEditor.vue, TerrainSeedEditor.vue, autoDetectConfig.js, map-autodetect-config.schema.test.js)
- `npm run lint` — clean
- `npm run build` — succeeded
- `npm test` — 358/358 passed, 0 failed, 0 skipped

---
