# LOB-0077 — Integrate wshobson/agents plugins and best-practice Claude patterns

**Branch:** feat/77-integrate-wshobson-plugins-best-practice
**Started:** 2026-03-17 07:01

---

## AI Plan

Most of the settings infrastructure already exists: `settings.json` has shared permissions and
a PostToolUse format hook, and two agents already carry `model: sonnet`. The remaining work is:
simplify the hook to remove the hardcoded absolute path; clean up `settings.local.json` to
remove portable entries (`npm test:*`, `WebFetch(domain:github.com)`) that belong in
`settings.json`; add `model: sonnet` to `project-manager.md`, `code-review.md`, and
`issue-intake.md`; split the Coding Standards and four-phase SDLC sections of `CLAUDE.md`
into `.claude/rules/coding-standards.md` and `.claude/rules/sdlc.md` with `@` references;
add a plugin coexistence note to `CLAUDE.md`; document and register the `wshobson/agents`
marketplace (verified manually at HCP 2a); update memory files; and add
`CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` to `.env.example`. No source code is touched;
build and test verify all existing skills remain functional.

---

## Branch Created

`feat/77-integrate-wshobson-agents-plugins-and-b` — 2026-03-17 07:02

---

## HCP 1 — Plan Accepted

**Accepted:** 2026-03-17 07:01
**Notes:** none

---

## Implementation Notes

All five agents already had `model: sonnet` and `settings.json` already existed with shared
permissions — only the PostToolUse hook absolute path and a few non-portable entries in
`settings.local.json` needed cleaning. Work completed:

1. **settings.json**: Removed hardcoded `cd /Users/andrewhammer/...` prefix from PostToolUse
   hook (now just `npm run format -- --log-level silent 2>/dev/null || true`); added
   `WebFetch(domain:github.com)` to shared permissions.
2. **settings.local.json**: Removed `Bash(npm test:*)` and `WebFetch(domain:github.com)`
   (now in settings.json); now contains only absolute-path git config, brew, and gs:\*.
3. **`.claude/rules/coding-standards.md`**: Extracted Coding Standards section from CLAUDE.md.
4. **`.claude/rules/sdlc.md`**: Extracted four-phase SDLC section from CLAUDE.md.
5. **CLAUDE.md**: Replaced extracted sections with `@.claude/rules/` references; added Plugin
   Ecosystem section documenting wshobson/agents marketplace and conductor/agent-teams install
   commands and coexistence table. 152 lines.
6. **`.env.example`**: Added `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` entry.
7. **doc-sync**: Updated `settings.local.json` → `settings.json` in README.md, SKILL_TEMPLATE.md,
   and issue-intake/design.md.
8. **ecosystem-docs-generate**: Updated `tutorial-new-agent.md` (5 occurrences of
   settings.local.json → settings.json).

HCP 2a skipped — no visual UI changes, nothing to review in browser.

---

## Build & Test Results

`/dev-build`: format — no changes; lint — clean; build — 198.71 kB JS (68.69 kB gzip)
`/dev-test`: 373 passed, 0 failed, 0 skipped

---

## HCP 2 — Implementation Accepted

**Accepted:** 2026-03-17 07:19
**Notes:** map.json digitization edits added to branch before push

---
