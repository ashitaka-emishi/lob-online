# LOB-0044 — Add /ecosystem-docs-generate skill to regenerate claude-ecosystem docs

**Branch:** feat/44-ecosystem-docs-generate
**Started:** 2026-03-16 13:18

---

## AI Plan

The implementation adds two artifacts. First, `.claude/commands/ecosystem-docs-generate.md` — a skill that reads the four source-of-truth inputs (`docs/agents/*/design.md`, `.claude/agents/registry.json`, `.claude/commands/*.md`, `docs/workflows/*/`) and regenerates the five auto-generated files in `docs/claude-ecosystem/` (`README.md`, `agents.md`, `skills.md`, `network-diagram.md`, `guardrails-and-logging.md`). The skill prose will instruct Claude to read all source files, derive the current ecosystem state, build each target document from scratch, diff the proposed content against the existing file using a side-by-side comparison, report the delta, and only write if the engineer confirms. `blog.md` and all three tutorial files are explicitly listed as excluded. Every generated file header will carry the `> Auto-generated` warning and a three-layer summary. Second, `.claude/agents/registry.json` gains an `ecosystem-docs-generate` entry with `type: "skill"`. No JS, Vue, or data files are touched. Verification: `/dev-build` passes.

---

## HCP 1 — Plan Accepted

**Accepted:** 2026-03-16 13:18
**Notes:** none

---

## Branch Created

`feat/44-ecosystem-docs-generate` — 2026-03-16 13:18

---
