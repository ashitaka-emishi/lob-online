# LOB-0040 — Write design document for hex map editor auto-detect feature

**Branch:** feat/40-write-design-document-for-hex-map-editor-aut
**Started:** 2026-03-16 14:13

---

## AI Plan

This issue produces a single documentation artifact: `docs/map-editor-auto-detect-design.md`. The document will be written fresh from first principles, drawing on the existing `docs/map-editor-design.md` (for `gridSpec` and `HexEntry` data model context), `data/scenarios/south-mountain/scenario.json` (for the `autoDetectConfig` storage proposal), and the `client/src/views/tools/MapEditorView.vue` component (for UX context). Architecture: one-time Claude Vision call derives elevation color palette from the map image and stores it in scenario.json; subsequent hex classification uses local canvas analysis against that palette; Claude Vision fallback (with seed hex few-shot data) handles low-confidence hexes. Seed hexes (~25) span all terrain types and elevation bands. Hex data model gains a `playable` flag on hexes, hexsides, and vertices for off-map edge positions. All unresolvable questions are parked in the decision log. No code written. No tests required.

---

## Branch Created

`feat/40-write-design-document-for-hex-map-editor-aut` — 2026-03-16 14:22

---

## HCP 1 — Plan Accepted

**Accepted:** 2026-03-16 14:22
**Notes:** Realistic v1 scope confirmed. Palette derivation is one-time Vision call (not per-hex). Seed hexes ~25, spanning all terrain/elevation types. Hex data model needs playable flag on hex, hexside, vertex. Seed data persisted in scenario.json as autoDetectConfig.seedHexes.

---
