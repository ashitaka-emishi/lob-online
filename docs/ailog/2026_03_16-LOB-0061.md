# LOB-0061 — Add live editor review gate to issue-implement workflow

**Branch:** feat/0061-add-live-editor-review-gate-to-issue-impl
**Started:** 2026-03-16 17:30

---

## AI Plan

Three files need updating: `issue-implement.workflow.json` gets three new steps inserted between `test` and `doc-sync` — a `dev-start` step (agentId: `dev-start`), a `gate-editor` gate step with "Approve" (→ next step) and "Fix" (→ `implement`) choices, and a `dev-stop` step (agentId: `dev-stop`) that runs on the Approve path before continuing; a second `dev-stop` step also needs to be wired in on the Fix loop-back path before returning to `implement`. The `issue-implement.states.md` state diagram and gate table get corresponding updates. The `.claude/commands/issue-implement.md` skill prompt gets a new Step 5a between the Test step and HCP 2, describing the dev-start launch, the `gate-editor` HCP mechanic, and how the Fix path captures and injects engineer feedback. No source code or data files change — this is pure SDLC tooling. There are no automated tests; the JSON must be valid (CI parse check) and the AC items serve as the manual verification checklist.

---

## Branch Created

`feat/61-add-live-editor-review-gate-to-issue-im` — 2026-03-16 17:37

---

## Implementation Notes

Three files changed. In `issue-implement.workflow.json`, the `implement` step gained an `editorFeedback` input that reads from `$.gate-editor.note` — this is only populated on the Fix loop-back path, so first-run implementations receive an empty/undefined value harmlessly. The `gate-editor` "Fix" choice routes directly to `implement` (not through a separate dev-stop step) because the workflow JSON's loop-back mechanism only supports `gateConfig.onChoice.nextStep` routing; the skill prompt explicitly instructs the AI to call `/dev-stop` before looping so the server is never left running on that path. The `dev-stop` step on the Approve path is positioned between `gate-editor` and `gate-impl` in array order, so `stepIndex += 1` naturally advances to it after the gate's `nextStep: "dev-stop"` jump.

---

## Build & Test Results

Format: no changes. Lint: clean. Build: 198 kB bundle, 436 ms. Tests: 358/358 passed, 0 failed, 0 flaky.

---

## HCP 2 — Implementation Accepted

**Accepted:** 2026-03-16 17:52
**Notes:** none

---

## HCP 1 — Plan Accepted

**Accepted:** 2026-03-16 17:37
**Notes:** none

---
