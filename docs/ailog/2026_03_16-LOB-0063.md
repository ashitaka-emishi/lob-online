# LOB-0063 — Fix map editor tool selection, hex display, and UI bugs

**Branch:** feat/63-fix-map-editor-tool-selection-hex-display
**Started:** 2026-03-16 20:58

---

## AI Plan

Fix seven UI bugs in the map editor dev tool, all in the client layer with no server or data
changes. The work touches four files: `MapEditorView.vue` (add active-tool name to the top bar,
fix the Hex Edit accordion header to always read "Hex Edit" rather than showing the selected hex
ID), `HexEditPanel.vue` (diagnose and fix the hex data form not rendering inside the panel when a
hex is selected), `HexMapOverlay.vue` (change hex ID label color to dark blue `#00008b`, match
font size to accordion headers, and decrement the displayed row by 1 for even-numbered columns),
and `EditorToolbar.vue` (remove the duplicate Export button). Tests are unit-level in Vitest/jsdom:
new assertions in `MapEditorView.test.js`, `HexEditPanel.test.js`, and `HexMapOverlay.test.js`
covering each AC item. No rules-lawyer consultation needed — these are dev-tool-only UI bugs.

---

## Branch Created

`feat/63-fix-map-editor-tool-selection-hex-di` — 2026-03-16 21:00

---

## HCP 1 — Plan Accepted

**Accepted:** 2026-03-16 21:00
**Notes:** none

---
