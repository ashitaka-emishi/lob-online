# LOB-0053 — Add playable and auto-detect provenance fields to HexEntry schema

**Branch:** feat/53-playable-flag-and-auto-detect-provenance-fields
**Started:** 2026-03-16 15:39

---

## AI Plan

Three additive fields are appended to `HexEntry` in `map.schema.js`: `playable: z.boolean().optional()` (omitted = true), `autoDetected: z.boolean().optional()`, and `detectionConfidence: z.number().min(0).max(1).optional()`. The schema change is strictly additive — all 31 existing `map.json` entries will continue to validate. Boundary hex pre-seeding adds entries for row 00 even-column hexes and row 36 even-column hexes in `map.json`. The current map data uses cols 1–56; even columns 2, 4, ..., 56 yield 28 hexes per boundary row (56 total). These are minimal entries (`hex`, `terrain: "unknown"`, `playable: false`) since the boundary positions are not playable game hexes. Four new test cases cover the three new fields.

---

## Branch Created

`feat/53-playable-flag-and-auto-detect-provenance-fields` — 2026-03-16 15:40

---

## HCP 1 — Plan Accepted

**Accepted:** 2026-03-16 15:40
**Notes:** none

---

## Implementation Notes

Schema change is purely additive — three optional fields appended to `HexEntry`. Boundary hex pre-seeding uses even columns 02–56 (28 hexes × 2 rows = 56 entries) with minimal `{ hex, terrain: "unknown", playable: false }` shape. The current map.json had no gridSpec defined yet, but the hex ID format (`col.row`) confirmed by existing data; row 00 and row 36 follow the same pattern. Prettier reformatted `map.json` from multi-line per-field style to compact single-line objects, reducing line count significantly. Six test cases added covering `playable: false`, all three fields present, all three omitted, `detectionConfidence` out-of-range (above and below), and boundary values.

doc-sync: no stale references — changed files referenced at subsystem level only in CLAUDE.md and HLD.
ecosystem-docs-generate: no changes — no registry/agent/skill/workflow changes.

---

## Build & Test Results

format: 1 file changed (map.json), lint: clean, build: 197.50 kB, tests: 313/313 passed (+6 new)

---

## HCP 2 — Implementation Accepted

**Accepted:** 2026-03-16 15:47
**Notes:** none

---

## HCP 3 — Merge Approved

**Approved:** 2026-03-16 16:01

---

## Merge Complete

**Commit:** 88d7ae8f3c0e
**Merged:** 2026-03-16 16:01

---
