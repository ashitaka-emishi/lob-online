# LOB-0046 — Add code-doc consistency check step to issue-implement workflow

**Branch:** feat/46-add-code-doc-consistency-check-step-to-issue
**Started:** 2026-03-16 13:55

---

## AI Plan

This issue adds a `/doc-sync` skill that actively brings project documentation into sync with changed source files. The skill reads `git diff --name-only origin/master...HEAD`, identifies which of `CLAUDE.md`, `docs/high-level-design.md`, and `docs/agents/*/design.md` reference each changed file or subsystem, then reads both the changed source and the referencing docs and edits the docs directly to correct stale facts, remove duplication, and ensure accuracy. The skill is wired into `issue-implement.md` between Step 3 (implement) and Step 4 (build), alongside `/ecosystem-docs-generate` which handles `docs/claude-ecosystem/`. The workflow JSON gains a `doc-sync` step with `onError: "continue_with_warning"`. No JS code extraction; the skill is pure Markdown+bash.

---

## Branch Created

`feat/46-add-code-doc-consistency-check-step-to-issu` — 2026-03-16 13:58

---

## HCP 1 — Plan Accepted

**Accepted:** 2026-03-16 13:58
**Notes:** Scope clarified: /doc-sync targets CLAUDE.md, HLD, and agent design docs (diff-driven, makes edits directly); /ecosystem-docs-generate handles claude-ecosystem/ (full rebuild). Both wired into issue-implement between Step 3 and Step 4.

---
