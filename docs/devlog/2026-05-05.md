# 2026-05-05

## 01:31 — PR: Issue closeout — close 34 resolved GitHub issues

With M3 complete and PR #325 merged, a large backlog of GitHub issues had been addressed by merged code but never formally closed. This PR audits all 40 open issues, confirms resolution via commit history and code inspection, and closes the 34 that are done — leaving only the 4 intentionally-deferred tech debt items (#294, #295, #322, #324).

The audit was structured as two passes before closing. The first pass mapped each open issue to the PR that resolved it: issues #264–#274 (engine modules) were resolved by PR #283; #268 and #275–#277 (map test tool and LosTestPanel removal) by PR #297; #278–#282 (table test tool) by PR #312 (squashed into the M3 debt sprint commit, so they weren't listed in the PR title); and #255, #256, #259, #286, #291–#293, #298–#299, #301, #304, #306, #309–#311 by PR #315. The second pass verified each implementation is present in the codebase — engine files, test tool routes and panels, and all 13 specific debt fixes (panel-base.css, mockFetch.js, dynamic component dispatch, defineAsyncComponent, pickMods allowlist, defaultModifiers factory, formations.js extraction, dirIndex RangeError).

One non-obvious finding: the Table Test Tool issues (#278–#282) were not explicitly listed in PR #312's title ("M3 debt sprint — score-3 cleanup"), but the table test tool implementation commits were squashed into that PR. A git log of when tableTest.js and TableTestView.vue were first added confirmed both landed in the `86fbecc` squash commit that became PR #312.

The four issues left open are all intentional: #294 (memoize formatHexId/parseHexId hot-path allocations), #295 (Dijkstra early termination), #322 (domain bounds on numeric pickMods fields), and #324 (hoist noEffectTerrain Set out of hexEntryCost). These are performance/refinement flags with no correctness risk and no M4 blockers — they stay open until a future debt sprint targets them.
