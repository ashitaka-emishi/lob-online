# Devlog — 2026-06-09

## 09:32 — feat(#529): Multi-Scenario Platform complete

Implemented track `multi-scenario-platform_20260608` across 5 phases.

**What was built:**

- `server/src/utils/scenarioFolders.js` — `resolveScenarioPath(slug, file)` mapping 9 scenario slugs to data folders; `ScenarioNotFoundError` (404). SM → `south-mountain`; 8 scaffold folders created for THG, TTS, AFS, LCV, NBH, TTW, NTB, IB.
- `server/src/routes/scenarioData.js` — slug-parameterized GET/PUT for map, oob, scenario, leaders, succession. Mounted at `/api/v1/scenarios`.
- All existing editor route handlers (mapEditor, oobEditor, leadersEditor, scenarioEditor, successionEditor) updated to delegate to `resolveScenarioPath` instead of hardcoded `south-mountain` paths.
- `client/src/stores/useScenarioStore.js` — Pinia store with localStorage persistence, default `'THG'`, `scenarioPath()` helper.
- Vue Router updated to `/scenarios/:scenarioSlug/...` parameterized routes + legacy redirects from old flat paths.
- `EditorNav` reads `route.params.scenarioSlug` for nav link construction.
- `useMapPersistence` and `useOobPersistence` composables accept optional `scenarioSlug` to build scenario-scoped API URLs.
- `OobEditorView`, `MapEditorView`, `ScenarioEditorView` read `useRoute()` for slug and pass to their persistence layers.
- `HomeView` scenario dropdown wired to `useScenarioStore`; lobby/editor links use `scenarioStore.scenarioPath()`.

**Quality gate:** `npm run quality:strict` fully green — 2594 tests pass, lint/format/build clean, no unexpected warnings.
