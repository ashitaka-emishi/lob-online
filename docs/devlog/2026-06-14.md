# 2026-06-14

## 12:28 — PR: Conductor closeout + debt sprint: score 33 → 21, team-review fixes

This PR closes out the `closeout-debt-sprint_20260614` Conductor track, which had three goals: Conductor housekeeping, coverage-gap test additions for six debt issues, and small production fixes for CSS contrast (#538) and lobby a11y (#564). The track then went through a full `/team-review` cycle, and all ten findings were fixed in place before the PR was opened — keeping the debt register flat at 0 new deferred items.

**Phase 1 — Conductor housekeeping:** the stale `[ ]` checkbox on `lobby-auth-cleanup_20260613` was closed, blank lines between rows in the pre-`new-track` section of `tracks.md` were removed to match the bottom section's style, and `conductor/product.md` was updated from an M3 description to the current M5.5 complete / M6 starting state that mirrors `CLAUDE.md`.

**Phase 2 — New tests for six debt issues:** `#379` (getValidActions concrete candidates) and `#544` (useModuleStore helper tests) were already resolved by M5.5 implementation and closed without code changes. New tests were added for `#547` (router legacy redirect destination assertions), `#558` (moduleSlug reactivity for MapEditorView, ScenarioEditorView, and OobEditorView), `#566` (smoke test error-path coverage: wrong-side dispatch, invalid-action-type dispatch, stale-version saveGame conflict), and `#567` (structural round-trip assertion after persist). The smoke tests are order-sensitive — error-path tests use `loadGame`-only reads and were appended after the shared-dataDir sequence to avoid perturbing version counters.

**Phase 3 — Production fixes:** `ScenarioEditorView.vue` had `.derived-label` and `.derived-value` CSS colors at `#707060` (~3.3:1 contrast against panel backgrounds), upgraded to `#a09880` (5.53:1 on `#222222`, 4.99:1 on `#2a2a2a`, both above the WCAG AA 4.5:1 threshold). `LobbyView.vue` received `aria-disabled` attributes on USA/CSA join buttons, but the original implementation left `@click` unchecked — a Medium finding caught in review.

**Team-review fixes:** The review produced 2 Medium and 8 Low findings. M-1 (click-through despite aria-disabled) was fixed by moving join eligibility logic into `useLobbyStore.canJoin(game)` (L-1) and gating the `@click` handler; visual `button[aria-disabled='true'] { opacity: 0.5; cursor: not-allowed; }` CSS was added. M-4 strengthened moduleSlug reactivity URL assertions from loose `toContain` to exact `toBe` against the full API path. L-2 added status-text assertions to the aria-label test; L-3 replaced the em-dash with `, status:` for better screen-reader phrasing; L-4 verified disk state is unchanged after a stale-version rejection; L-5 restructured the round-trip test to save to an isolated temp dir before loading; L-6 moved the OobEditorView spy before `mount()` to observe the initial SM load; L-7 imported `DEFAULT_SLUG` from `useModuleStore.js` instead of hard-coding `'THG'` in router redirect tests. The two `#549` "side-entry always available" tests were updated to assert `joinGame` is NOT called when `canJoin` returns false — the old behavior was intentional scaffolding that the M-1 fix supersedes.

## 15:00 — PR: Pre-M6 Final Debt Sprint + Debt Report Improvements

This PR delivers the `pre-m6-debt-final_20260614` track — a four-phase sprint that fully clears M5.5 technical debt before M6 begins and upgrades the debt tracking infrastructure to enforce milestone discipline going forward. The net result: 18 issues closed, M5.5 current-milestone debt reduced to 0 items / score 0, overall open score down from 21 to 19 (the remaining 19 points are all intentional M6/M8 deferred items).

**Phase 1 — Milestone assignment:** All 26 open `tech-debt` GitHub issues were assigned to milestones (M5.5 #15, M6 #9, or M8 #11) via the GitHub API. Previously 24 of 26 had no milestone, making it impossible to filter "debt due this milestone" from "debt deferred to future milestones." Issue #560 (ROLL_INITIATIVE friendly-unit filter) was the only one with no obvious target — assigned to M6 since it becomes a correctness issue when opposing units enter the shared game state.

**Phase 2 — Report upgrades:** The debt register gained a `Milestone` column in the Open Debt Items table and a "Current-milestone open debt" row in the Executive Summary. PR #520 (home nav / editor navigation) was discovered to be entirely missing from the Debt Over Time table — it introduced issues #521–#525 (score 8 total) that were never registered. These were backfilled with correct cumulative totals, which required cascading all subsequent Cumulative Added values from 447→455 through to 494→502. The tech-debt README and `.claude/commands/tech-debt-report.md` skill prompt were both updated to make milestone assignment a required step when filing a deferred issue.

**Phase 3 — Code fixes:** Most of the 13 "code fix" issues (#531–#537, #540, #541, #543, #545, #546) were already implemented in PRs #548 and #557 but the GitHub issues had never been closed. These were confirmed in code and closed. The remaining 5 issues required new code: `App.vue` gained a `<main id="main-content">` landmark and visually-hidden skip-nav link (#522); `MenuLayout.vue` was extracted as a shared background wrapper used by both `HomeView.vue` and `LobbyView.vue`, eliminating duplicated background-image styles (#523); `StatusView.vue` and its test were deleted — the component had no router entry after `HomeView` was introduced (#524); disabled lobby join buttons gained `aria-describedby` pointing to a visually-hidden reason span (#525); and `docs/designs/action-contract.md` replaced its §6 inline socket snippet with a file+line reference and gained `TODO(M6)` anchor comments at the two stale-prone paragraphs (#568).

**Phase 4 — Register closeout:** The six issues that were in the Open Debt Items table (#521–#525, #568) were removed and resolution rows appended to the Debt Over Time. A key design decision throughout was distinguishing between issues "already fixed in code but not closed" vs "genuinely unimplemented" — the Debt Over Time already had resolution rows for #531–#546 from PRs #548/#557, so those only needed GitHub closure; the Open Debt Items table correctly never listed them. Only the PR #520 cohort (#521–#525) and #568 were genuinely untracked in the register.
