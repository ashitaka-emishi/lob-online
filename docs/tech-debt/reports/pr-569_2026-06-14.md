# Technical Debt Report — PR #569

**PR:** #569
**Date:** 2026-06-14
**Reviewer(s):** Claude (team-review — security, architecture, testing, maintainability, accessibility)

## Fixed Findings

10 findings fixed in place during this review cycle.

1. [FIXED] M-1: `@click` fires regardless of `aria-disabled` — gated handler with `store.canJoin(game) &&`; added visual `button[aria-disabled='true']` CSS
2. [FIXED] M-2: Wrong-side dispatch test used bare `.toThrow()` — pinned to `ActionError` type and `/turn/` message (committed earlier as M-2/M-3 fix)
3. [FIXED] M-3: Invalid-action-type test used bare `.toThrow()` — pinned to `ActionError` and `/not valid in the current state/` (committed earlier as M-2/M-3 fix)
4. [FIXED] M-4: moduleSlug reactivity URL assertions used loose `toContain('/THG/')` — strengthened to exact `toBe('/api/v1/modules/THG/map')` and `toBe('/api/v1/modules/THG/scenarios/full-battle/scenario')`
5. [FIXED] L-1: `joinDisabled()` eligibility logic duplicated store — moved to `useLobbyStore.canJoin(game)` getter; removed local function
6. [FIXED] L-2: aria-label test didn't assert status text — added `toContain('Waiting for player')` assertions
7. [FIXED] L-3: em-dash in aria-label may read oddly — replaced `— ${statusLabel}` with `, status: ${statusLabel}`
8. [FIXED] L-4: Stale-version rejection test didn't verify disk state unchanged — added `loadGame` post-rejection and `expect(afterReject.version).toBe(versionOnDisk)`
9. [FIXED] L-5: Round-trip test never saved before loading — restructured to save `ACTIVE_STATE` to isolated `roundTripDir` before `loadGame`
10. [FIXED] L-6: OobEditorView spy installed after mount — moved spy before `mount()` to observe initial SM load call too
11. [FIXED] L-7: Router redirect tests hard-coded `'THG'` — imported `DEFAULT_SLUG` from `useModuleStore.js`

## Second-Pass Review

COMPLETED — `useLobbyStore` addition triggered the shared-store surface check.

The `canJoin(game)` getter added to `useLobbyStore` is a pure read-only function with no side effects, no persistence, and no auth logic. It consolidates eligibility logic that already existed in the component; behavior is unchanged. All existing and new LobbyView tests pass. No additional findings were deferred.

## Deferred Findings

None — all findings fixed in place.

**Debt added this PR:** 0
**Debt closed this PR:** 0
**Net delta:** 0 (neutral)

---

_Generated by `/tech-debt-report`_
