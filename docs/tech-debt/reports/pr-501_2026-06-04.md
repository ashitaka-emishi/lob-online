# Technical Debt Report — PR #501

**PR:** #501
**Date:** 2026-06-04
**Reviewer(s):** agent-teams multi-reviewer (security, architecture, testing, maintainability, accessibility)

## Fixed Findings

6 findings fixed in place during this review cycle.

1. Missing `:focus-visible` rule on `.action-btn` — keyboard users had no visible focus indicator (WCAG 2.4.7, High)
2. No `prefers-reduced-motion` guard on `.spinner` animation — infinite rotation with no reduced-motion override (WCAG 2.2.2, Medium)
3. `.waiting` text color `#6a7a8a` failed WCAG 1.4.3 AA (4.31:1 vs 4.5:1 required) — lightened to `#7a8a9a` (Medium)
4. Live region only announced turn-handoff, not submission/pending state — screen readers received no confirmation after action submit (WCAG 4.1.3, Medium)
5. `edge-strip.js` magic `<= 2` literal and misleading "mirror" comment — replaced with `CANONICAL_FACE_COUNT = 3` and clarified the deliberate API differences vs client (Medium)
6. Dead `if (!row)` guard in `GET /:id/actions` route handler — `requireSide` already returns 404 before handler runs; removed dead code and added clarifying comment to test (Medium)

## Second-Pass Review

NOT REQUIRED — review fixes were limited to CSS additions, a computed extension, a named constant, a comment, and removing unreachable dead code. No auth, session, persistence, shared stores, or rules-engine surfaces were touched.

## Deferred Findings

| Issue | Title                                                                                      | Score | Assessment                                                                                                                                                                                                                                                      |
| ----- | ------------------------------------------------------------------------------------------ | ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #502  | refactor(gameView): move refreshValidActions into useGameStore with generation guard       | 2     | `refreshValidActions` is a component-local fetch outside `useGameStore`'s `_loadGeneration` guard. On a burst of `game:state-updated` events, actions and state could briefly misalign. The `activePlayer` gate masks most staleness; acceptable to defer.      |
| #503  | fix(games): verify session.gameId matches path :id in GET /:id/actions and sibling routes  | 2     | `requireSide` validates the token but not that `session.gameId === req.params.id`. A player for game A can call GET /games/B/actions and receive B's action-type list. No hidden state leaks; limited disclosure risk. Consistent with existing sibling routes. |
| #504  | test: add cross-implementation parity test for server/client stripNonPlayableBoundaryEdges | 2     | Two independent implementations share the same playability predicate with no parity test. A change to one (e.g. adding a semi-playable boundary) would silently break the other. Closes naturally when #492 shared-module extraction completes.                 |
| #505  | fix(actionPanel): restore focus after pending clears; consider aria-disabled over disabled | 2     | `disabled` removes buttons from the tab order mid-interaction, leaving keyboard/SR users with no focus anchor after action submission. Focus is not restored when pending clears. Low functional risk; a11y gap on keyboard-only path.                          |

**Debt added this PR:** 8
**Debt closed this PR:** 0
**Net delta:** +8 (regression — new review findings deferred)

---

_Generated by `/tech-debt-report`_
