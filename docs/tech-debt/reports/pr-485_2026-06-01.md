# Technical Debt Report — PR #485

**PR:** #485
**Date:** 2026-06-01
**Reviewer(s):** Claude team-reviewer agents (security, architecture, testing, maintainability, accessibility)

## Fixed Findings

14 findings fixed in place during this review cycle.

| Severity | Finding                                                                                                                                                            |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| High     | Promoted slots regressed from focusable buttons to non-focusable divs — fixed with `role="button" tabindex="0" aria-pressed` + Enter/Space handlers on all 4 slots |
| High     | Slot activation state not exposed to AT — fixed with `aria-pressed` on all slots                                                                                   |
| Medium   | Keyboard cycling no perceivable announcement — fixed with `aria-live="polite"` region                                                                              |
| Medium   | `getList` implicit `promotedBack` fallthrough — fixed with `LIST_BY_FACE` lookup object returning `[]` for unknown face                                            |
| Medium   | Window keydown listener leaks across tests — fixed with `afterEach(() => wrapper?.unmount())` in keyboard-cycling and leader-mode describe blocks                  |
| Medium   | `LEADERS_ROOT` / `resolveRoot()` routing convention duplicated — extracted shared constant and helper                                                              |
| Medium   | `findNodePath` name/JSDoc oob-specific but reused for leaders — renamed to `findNodePathInTree` with updated JSDoc                                                 |
| Low      | Promoted-front test asserted `any(String)` — fixed with concrete filename + `front: null` assertion                                                                |
| Low      | No test for `getDefaultCounterRef` leader branch with null counterRef — test added                                                                                 |
| Low      | Tautological Browse-button absence test — replaced with `input[type="file"]` absence assertion                                                                     |
| Low      | Missing oob-vs-leaders precedence test — test added (oob wins when same id in both trees)                                                                          |
| Low      | Face-name literals repeated at multiple sites — added `FACE_NAMES` constant; derived `imgError` shape from it                                                      |
| Low      | Clear button had only `title` attribute — added face-specific `aria-label`; glyph is `aria-hidden`                                                                 |
| Low      | Side-detection comment misleading — added cross-reference to `useOobStore.selectNode`; confederate leader path test added                                          |

## Second-Pass Review

COMPLETED — review fixes touched `useOobStore.js` (shared Pinia store), `CounterImageWidget.vue` (shared component), and `findNodePath.js` (shared utility), triggering the shared-store/composable/API-boundary criterion. All changes were refactors of existing logic (constant extraction, function rename, template attribute additions) with no behaviour changes; full 2360-test suite passed.

## Deferred Findings

| Issue | Title                                                                               | Score | Assessment                                                                                                                                                                                                                                                                                                                                                                           |
| ----- | ----------------------------------------------------------------------------------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| #486  | CounterImageWidget: pass side as explicit prop from OobDetailPanel                  | 2     | Side detection logic is duplicated between `OobDetailPanel` and `CounterImageWidget`, coupling the widget to the `leaders.` path prefix convention produced by `useOobStore.selectNode`. A path format change requires updating both files; union/confederate cut-out filtering could silently break. Risk is bounded by the new confederate-path test and the closed face-name set. |
| #487  | CounterImageWidget: release activeFace on blur to bound global keydown interception | 2     | The global `window` keydown listener suppresses Arrow key scrolling while a slot is active; if the user navigates away without pressing Escape, scrolling remains suppressed. This is a dev tool with no public user impact, the design is documented in a comment, and the fix is a one-function `@focusout` handler.                                                               |

**Debt added this PR:** 4
**Debt closed this PR:** 0
**Net delta:** +4 (regression — 2 minor deferred items)

---

_Generated by `/tech-debt-report`_
