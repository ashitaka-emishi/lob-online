# Technical Debt Report — PR #493

**PR:** #493
**Date:** 2026-06-04
**Reviewer(s):** Claude agent team (security, architecture, testing, maintainability, accessibility)

## Fixed Findings

14 findings addressed or designated for in-place fix during review cycle:

- submitAction error fallback branch untested (missing `.catch(() => ({}))` + `?? \`Action failed\`` coverage)
- gameState not asserted unchanged after submitAction failure
- pendingAction in-flight visibility on error path unverified
- submitAction success branch lacks defensive JSON parse (`.catch` guard on `res.json()`)
- mockSocket redundant nested `beforeEach` clearing (housekeeping)
- ActionPanel "disables all buttons" test uses single-button fixture; spinner conditional entirely untested
- No test for waiting-branch with non-empty `validActions`
- submit-action wiring test uses `null` payload (doesn't verify payload forwarding)
- `pending` prop uses shorthand `Boolean` instead of uniform `{ type: Boolean, default: false }`
- Magic socket event strings unconstrained (inline literals in GameView, not extracted to constants)
- Summary line renders "Turn null — null (null)" before game loads (needs `v-if` guard)
- Disabled buttons don't communicate reason; focus lost on disable (paired with #497 ARIA work)
- Color contrast of summary/waiting text not verified against WCAG AA
- mockSocket `vi.clearAllMocks()` coverage redundancy (duplicate nested clear in socket suite)

## Second-Pass Review

REQUIRED — not yet completed.

Review fixes will touch `useGameStore.js` (defensive JSON parse on success branch), `GameView.vue` (module-level socket/gameId vars, identity fetch error handling), and `ActionPanel.vue` (ARIA attributes, prop descriptor fixes). These touch shared Vue store and API/client contract boundaries — second-pass triggers apply. A targeted second-pass review covering **architecture** and **testing** dimensions is required after fixes are applied.

## Deferred Findings

| Issue | Title                                                                                                     | Score | Assessment                                                                                                                                                                                                                                                                             |
| ----- | --------------------------------------------------------------------------------------------------------- | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #494  | test(gameView): add localPlayerSide fetch and validActions derivation test coverage                       | 3     | The `/api/v1/games/me` fetch and `validActionsForState` (GameView.vue:31–72) are entirely untested. A regression in identity-fetch or derivation goes undetected; `.catch(() => {})` swallow also untested. Primary new behavior in Phase 3.                                           |
| #495  | feat(game): replace client-side validActionsForState stub with server endpoint before M6 action expansion | 3     | Client always returns only `END_PHASE` while server `getValidActions` returns a richer phase-aware set. Two sources of truth with no contract; will diverge further as M6 adds `ROLL_INITIATIVE`, `ACTIVATE_STACK`, etc. No rule citation on client copy.                              |
| #497  | fix(actionPanel): add aria-live region for turn-handoff announcement and aria-busy for pending state      | 3     | Turn-handoff state change ("Waiting for…") is invisible to screen readers — no `aria-live` region. Pending/spinner state not communicated via `aria-busy` or live announcement. Core game interaction loop inaccessible. Sibling `UnitStatsPanel` already establishes correct pattern. |
| #496  | fix(gameView): surface /games/me fetch failure in error state instead of swallowing                       | 2     | Bare `.catch(() => {})` on identity fetch leaves `localPlayerSide` null forever with no error banner, no log. User cannot distinguish "failed identity" from "not your turn." Only error path in the component producing zero output.                                                  |
| #498  | fix(actionPanel): add semantic landmark, role=region, and button group labeling                           | 2     | Panel is a bare `<div>` with no `role="region"`, no `aria-label`, and no association between the turn-summary line and the action buttons. Screen readers cannot navigate to or identify the panel.                                                                                    |
| #499  | refactor(gameView): move \_socket and \_mountedGameId from module scope to script setup scope             | 2     | Module-level mutable state shared across all GameView instances. `_` prefix misleadingly signals "unused" per project convention. Latent multi-instance hazard (safe today as singleton).                                                                                              |
| #500  | fix(actionPanel): pass pendingAction.type as prop to correctly target spinner on clicked button           | 2     | Spinner targets `validActions[0]` by position. With M6's multiple action types (ROLL_INITIATIVE, ACTIVATE_STACK), spinner will appear on the wrong button. Store already holds `pendingAction.type`.                                                                                   |

**Debt added this PR:** 17
**Debt closed this PR:** 0
**Net delta:** +17 (net regression — all findings are new; no open debt items were resolved by this PR)

---

_Generated by `/tech-debt-report`_
