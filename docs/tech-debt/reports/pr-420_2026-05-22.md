# Technical Debt Report — PR #420

**PR:** #420
**Date:** 2026-05-22
**Reviewer(s):** Claude (automated team-review via /team-review)

## Fixed Findings

11 finding(s) fixed in place during this review cycle:

- Comment clarifying 400 on non-UUID comes from `router.param('id')` — `games.test.js`
- Added `loading` assertion to reject-path test — `useGameStore.test.js`
- Fixed stale `hexSideLength` default 28→40 — `UnitCounterLayer.vue`
- Added regular-hex assumption comment — `HexMapOverlay.vue`
- Added module-init comment for `loadMap()` — `games.js`
- Added non-fatal failure comment — `useGameStore.js`
- Updated `loadGame` tests to use `makeMultiFetch` so map-config endpoint gets correct fixture data
- Added test: game-state-fails leaves gridSpec/hexes null even if map-config succeeds
- Added test: `hexes=null` falls back to empty array in HexMapOverlay prop
- Removed global `overflow:hidden` from `html`/`body` in `App.vue` — GameView manages its own overflow; global clip was breaking non-game views at high zoom (WCAG 1.4.10)
- Added `:focus-visible` outline to SVG counter buttons — `UnitCounterLayer.vue`

## Second-Pass Review

NOT REQUIRED

In-place fixes were limited to comments, test assertions, a default value correction, test mock correctness improvements, two new test cases, a global CSS fix, and a focus-indicator CSS addition. No auth, store API, or rules-engine surfaces were touched.

## Deferred Findings

| Issue | Title                                                                    | Score | Assessment                                                                                                                                                                                                                                                       |
| ----- | ------------------------------------------------------------------------ | ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #431  | OOB fetched via dev-tool endpoint (pre-existing)                         | 3     | GameView fetches OOB from `/api/tools/oob-editor/data`, gated by `MAP_EDITOR_ENABLED`. In production without the tool enabled, unit counter display silently breaks. Functional dependency on a dev tool that must be resolved before any production deployment. |
| #421  | `/:id/map-config` URL is game-scoped but data is scenario-static         | 2     | Route implies per-game data but config is scenario-static. Low impact with one scenario but misleads future contributors and will require a breaking API change for multi-scenario support.                                                                      |
| #422  | Silent map-config failure leaves blank map with no user-visible error    | 2     | When map-config fetch fails, game view silently renders with default calibration and no hexes. A minimal non-fatal error indicator would make failures diagnosable without changing the non-fatal design.                                                        |
| #423  | 503 startup-error branch untested and uncoverable with current mock      | 2     | The error branch when `loadMap()` fails at module init cannot be covered because the module is already initialized when tests run. Untested correctness gap in the error path.                                                                                   |
| #424  | `useCalibration.js` mixes Map-Editor composable with app-wide pure utils | 2     | `sanitizeCalibration` used by GameView lives in a Map-Editor composable, creating an implicit app→tool-layer dependency. Extracting the pure utility prevents accidental coupling.                                                                               |
| #425  | Store exposes raw `gridSpec` without sanitization contract               | 2     | Raw API data flows from store to GameView without shape validation. Unexpected structure from `/map-config` fails silently inside the merge. A Zod schema at store boundary would catch mismatches at load time.                                                 |
| #426  | `gridSpec`→calibration merge relies on incidental field-name overlap     | 2     | Merge works only because field names happen to match calibration fields. No contract enforces this — a renaming on either side silently falls back to defaults.                                                                                                  |
| #427  | Double `loadMap()` call causes startup warning (runtime, dev mode only)  | 2     | Both `games.js` and `mapTest.js` call `loadMap()` at module scope, emitting a runtime warning in dev mode. Not a test/prod issue but adds noise during development.                                                                                              |
| #432  | `aria-label` uses raw unit ID not human-readable name (pre-existing)     | 2     | Screen readers announce "Select unit unit-a" instead of the brigade name. Can be fixed once OOB enrichment is wired through UnitCounterLayer.                                                                                                                    |
| #428  | `sanitizeCalibration` merge semantics unverified in `GameView.test.js`   | 1     | Partial-override cases are not asserted in `GameView.test.js`. Merge behavior is tested implicitly but not documented as explicit test cases.                                                                                                                    |
| #429  | Inconsistent `STUB_GRID_SPEC` fixtures across test files                 | 1     | Two test files define the fixture independently with different values. Cosmetic inconsistency that makes cross-file schema changes harder to track.                                                                                                              |
| #430  | No caching headers on unauthenticated `/map-config` endpoint             | 1     | Scenario-static map config is refetched on every game load with no cache headers. Adding `Cache-Control` would reduce redundant requests at negligible implementation cost.                                                                                      |

**Debt added this PR:** 22
**Debt closed this PR:** 0 (issues: none)
**Net delta:** +22 (positive = more debt)

---

_Generated by `/tech-debt-report`_
