# Technical Debt Report — PR #219

**PR:** #219
**Date:** 2026-03-30
**Reviewer(s):** agent-teams parallel review

## Fixed Findings

11 finding(s) fixed in place during this review cycle.

1. CRIT-S1: `path.basename()` prevents path traversal in counter upload handler
2. HIGH-S1: MIME type check added to `fileFilter` (extension + mimetype both required)
3. HIGH-S2: Rate limiter added to counters router (60 req/min, matches other routes)
4. HIGH-A1: `express.json({ limit: '5mb' })` to align with OOB data file size
5. MED-A1: Routes renamed to `/api/tools/*` convention; `useOobStore` URLs updated
6. MED-A2 (oobEditor): try/catch added to `GET /data` handler
7. MED-A2 (leadersEditor): try/catch added to `GET /data` handler
8. MED-S2 (oobEditor): Generic `'Backup failed'` message instead of `err.message` on 500
9. MED-S2 (leadersEditor): Generic `'Backup failed'` message instead of `err.message` on 500
10. LOW-S1: `basename(req.file.originalname)` used in upload response (not raw originalname)
11. LOW-P1: `mkdirSync` hoisted to module-level init in counters (no per-request call)

## Deferred Findings

| Issue | Title                                               | Score | Assessment                                                                                                                                                                                   |
| ----- | --------------------------------------------------- | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #220  | Async I/O in oobEditor and leadersEditor routes     | 2     | Synchronous `readFileSync`/`writeFileSync` blocks the event loop. Low urgency for a dev-only tool with no production traffic; test mock migration from sync to `fs.promises` is non-trivial. |
| #221  | Apply `.strict()` to OOBSchema and LeadersSchema    | 2     | Open `z.object()` silently accepts unknown fields on PUT. `_savedAt` stamp added by the server is sent back by clients, so `.strict()` requires adding that field first. Low risk currently. |
| #222  | Counters upload — use route-level multer middleware | 1     | Inline callback handles `MulterError` variants cleanly without a separate error middleware. Pure style finding; no functional impact.                                                        |

**Total debt added this PR:** 5

---

_Generated by `/tech-debt-report`_
