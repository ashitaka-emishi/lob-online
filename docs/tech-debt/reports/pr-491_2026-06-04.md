# Technical Debt Report — PR #491

**PR:** #491
**Date:** 2026-06-04
**Reviewer(s):** Claude Sonnet 4.6 (team review: security · architecture · testing · maintainability · accessibility · performance)

## Fixed Findings

11 finding(s) fixed in place during this review cycle.

1. **H1** — Extract `isEdgeAtNonPlayableBoundary` predicate in `mapEditor.js` to mirror client structure (`server/src/routes/mapEditor.js`)
2. **M2** — Tighten `editorRouteFactory` transform JSDoc to clarify mutate-in-place vs. pure-return contract
3. **M3** — `edgeNotice` span: add `role="status" aria-live="polite"` so boundary-click and save-strip messages reach screen readers (`MapEditorView.vue`)
4. **M4** — Rename mislabeled "faces 3/4/5 coverage" tests in `edge-model.test.js` to accurately describe the S/SW/NW boundary direction being tested
5. **L1** — Clear `_edgeNoticeTimer` in `onUnmounted` (`MapEditorView.vue`)
6. **L2** — Add parity-pin assertions for `adjacentHexId('06.05', 'NE'/'SE', GRID_SPEC)` so even-column parity tests fail visibly on a geometry bug
7. **L3** — Add `socket.emit` absence assertion to `game:leave` test to lock the silent-leave contract
8. **L4** — Add `expectedVersion: 0` boundary tests (falsy-but-numeric guard edge case)
9. **L5** — Wrap `externalEl` DOM cleanup in `try/finally` in focusout tests to prevent leak on assertion failure
10. **L6** — Strengthen "side prop drives filter" test to assert slot position (3/4 vs 1/4), proving side — not path — drives the cut-out filter
11. **L7** — Extract `countEdgeFeatures` helper in `MapEditorView.vue`, eliminating the duplicated before/after counting loop in `beforeSave`

## Second-Pass Review

NOT REQUIRED

Review fixes were limited to a predicate extraction with identical behavior, a JSDoc clarification, a template attribute addition, and test improvements (assertions, try/finally, helper extraction). No auth/session, persistence-logic, shared store, or API contract changes.

## Deferred Findings

| Issue | Title                                                                          | Score | Assessment                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ----- | ------------------------------------------------------------------------------ | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #492  | refactor(mapEditor): move serverStripNonPlayableBoundaryEdges to engine/ layer | 3     | `serverStripNonPlayableBoundaryEdges` is domain/geometry logic (imports from `engine/hex.js`) living in the HTTP route module. All other rules-engine logic lives in `engine/`. The function is only testable through the route; the playability predicate is now a named mirror of the client-side `isEdgeAtNonPlayableBoundary` with a cross-reference comment, but a shared `engine/edge-strip.js` is the correct long-term home. Dev-only tool; no production impact today, but the split slows future changes to the playability rule. |

**Debt added this PR:** 3
**Debt closed this PR:** 16 (issues: #482 #481 #487 #486 #467 #468 #470 #440)
**Net delta:** −13 (net improvement)

---

_Generated by `/tech-debt-report`_
