# Technical Debt Report — PR #451

**PR:** #451
**Date:** 2026-05-23
**Reviewer(s):** Claude team-review (security, architecture, testing, maintainability, accessibility, performance)

## Fixed Findings

14 findings fixed in place during this review cycle:

1. Stale HELP_TEXT referencing "Elevation info" toggle (ContourToolPanel.vue)
2. `hexLabelFn` was a fresh closure per computed invalidation — hoisted to module scope
3. Decorative `.type-swatch` span not hidden from AT — added `aria-hidden="true"`
4. `validateCoexistence` JSDoc listed only slope types; updated to contour types, added rule note
5. `TERRAIN_COLORS.unknown` still present after all producers removed — deleted
6. `verticalSlope` stroke width (5) < `extremeSlope` (6) with no explanation — added comment
7. movement.test.js: two tests referencing 19.23→20.23 had outdated comments (20.23 is now woods)
8. `edgeLineFull` exported but not tested — added full `describe` block in hexGeometry.test.js
9. `ALL_CONTOUR_TYPES` export untested — added `describe` block in feature-types.test.js
10. Contour mutual-exclusion tests only covered one direction — expanded to all pairs both ways
11. `feature-types.test.js` still asserted `'unknown'` in TERRAIN_COLORS — updated
12. ContourToolPanel: no uncheck test for elevation checkbox — added
13. ContourToolPanel: no `hexLabelFn` stability test — added
14. `hexFill.fillFn` assertion used loose `toMatch(/^hsl\(/)` — tightened to full hsl format regex

## Second-Pass Review

REQUIRED — fix diff touches shared composable territory and formula modules.

The review fixes touch `edge-model.js` (shared formula module), `feature-types.js` (shared config module), and `ContourToolPanel.vue` (component with overlay-config contract). These are Checkpointed surfaces per the project's agentic quality rails. A targeted second-pass review of the fix diff is required before merge.

## Deferred Findings

| Issue | Title                                                                            | Score | Assessment                                                                                                                                                                                                                                                  |
| ----- | -------------------------------------------------------------------------------- | ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #452  | refactor: unify contour-type list to single source of truth                      | 2     | The same 4-value contour type set exists in three places: `ALL_CONTOUR_TYPES` in feature-types.js, private `CONTOUR_TYPES` in edge-model.js, and `CONTOUR_TYPES` array in ContourToolPanel.vue. Adding a fifth type requires editing all three in lockstep. |
| #453  | refactor: move contour coexistence/replace logic into edge-model.js              | 3     | `handleEdgePaint` performs contour replace semantics inline, bypassing `validateCoexistence`. Two policies exist for the same rule — a future caller of `validateCoexistence` will be rejected on input the map editor accepts. Layering inversion.         |
| #454  | refactor: fix useEdgePanelWiring composable to not require .value in templates   | 2     | Composable returns a plain object containing a Ref; Vue 3 does not auto-unwrap nested refs in templates. Every consumer must remember explicit `.value`, which is invisible to the type system and easy to miss.                                            |
| #455  | refactor: move 'unknown'→'clear' migration to load boundary in useMapPersistence | 2     | Migration watcher lives in the view layer, re-scans all 2261 hexes on every map replacement, and does not mark data as migrated. Belongs at the data ingress boundary in useMapPersistence.                                                                 |
| #456  | test: add geometry-coordinate assertions to useEdgeLineLayer tests               | 1     | Composable switched from edgeLine20_80 to edgeLineFull but no test verifies lineAttrs coordinates. A revert or broken geometry passes all tests.                                                                                                            |
| #457  | test: add coverage for handleEdgePaint contour replace semantics                 | 2     | Strip-then-add contour replace (the core #417 fix) has zero test coverage.                                                                                                                                                                                  |
| #458  | test: add coverage for migration watcher and active-highlight prop fix           | 2     | Two behavioral fixes from this PR (migration watcher, .value unwrap) have no regression tests.                                                                                                                                                              |
| #459  | a11y: add aria-pressed to contour/terrain type buttons and humanize labels       | 2     | Type-selection buttons have no aria-pressed; camelCase labels (extremeSlope, verticalSlope) read poorly to screen readers.                                                                                                                                  |

**Debt added this PR:** 16
**Debt closed this PR:** 0
**Net delta:** +16 (regression — new debt from review findings)

---

_Generated by `/tech-debt-report`_
