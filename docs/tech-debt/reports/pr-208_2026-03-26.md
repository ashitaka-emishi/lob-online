# Technical Debt Report — PR #208

**PR:** #208
**Date:** 2026-03-26
**Reviewer(s):** Claude Sonnet 4.6 (parallel agent-teams: security, performance, architecture, testing, correctness)

## Fixed Findings

9 finding(s) fixed in place during this review cycle.

- M2: stale `activeIndex` on node switch — writes `undefined` as counterRef filename
- M5: dual path convention in `updateField` — standardized on bare side-key paths
- M6: `updateField` store tests used `'oob.'`-prefixed paths instead of bare paths
- M7: `updateCounterRef` store test used wrong path format
- H1: `findNodePath` had zero test coverage — created `findNodePath.test.js` (10 tests)
- H2: `usedFiles` exclusion logic had zero test coverage — 2 filtering tests added
- M8: `imgError` watcher clearing not tested — test added
- M9: number field type coercion — test added (noted NaN guard not reachable via `<input type="number">` in jsdom)
- L5: redundant `beforeEach(setup)` in keyboard/clear test groups removed

## Deferred Findings

| Issue | Title                                                                        | Score | Assessment                                                                                                                                                                                                                        |
| ----- | ---------------------------------------------------------------------------- | ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #211  | auto-commit on first counter slot click (no preview-only mode)               | 3     | Clicking an empty slot immediately persists the first available counter to the store with no undo mechanism. Intent was visual preview, but `commit()` writes to the store. User must manually clear to undo an inspection click. |
| #209  | hoist `usedFiles` computed from `CounterImageWidget` into `useOobStore`      | 2     | `usedFiles` is per-widget-instance, walking the full OOB+leaders tree on every reactive change. Store-level knowledge placed in a leaf component. No active perf issue at current scale, but misplaced responsibility.            |
| #210  | pass `nodePath` through `selectNode` to avoid full tree-walk on each click   | 2     | `selectedNodePath` re-walks the full OOB tree on every selection and every field mutation. O(n) lookup where O(1) is achievable by threading path through `selectNode`. Negligible at current data size.                          |
| #214  | assert `syncError` is set and `isSyncing` resets on `pullFromServer` failure | 2     | Failure test only asserts no throw; does not verify `syncError` is set or `isSyncing` returns to `false`. A regression would leave the UI in a silent bad state with no user-facing feedback.                                     |
| #216  | extend prototype pollution guard to block `toString`/`valueOf`               | 2     | Prototype pollution blocklist omits `toString` and `valueOf`. No current exploit vector since paths come from trusted `findNodePath` output. Defense-in-depth gap to address before `updateField` is exposed to external input.   |
| #212  | add keyboard focus guard test (INPUT/SELECT/TEXTAREA blocks arrow cycling)   | 1     | The focus guard in `onKeydown` is untested. Guard exists and is correct; a regression would cause arrow keys to cycle counters while the user types in a form field.                                                              |
| #213  | add `commit()` no-op test when `CounterImageWidget` `nodePath` is null       | 1     | The null `nodePath` early-return in `commit()` is untested. Guard exists and works; failure would cause a no-op store call with undefined path.                                                                                   |
| #215  | `OobTreeNode` dual-script pattern — consider `defineOptions()`               | 1     | Uses `<script>` + `<script setup>` for recursive self-reference, violating the project's Options API ban. Standard Vue 3 workaround; resolvable with `defineOptions()` if Vue 3.3+ is confirmed available.                        |

**Total debt added this PR:** 14

---

_Generated by `/tech-debt-report`_
