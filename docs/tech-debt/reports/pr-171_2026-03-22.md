# Technical Debt Report — PR #171

**PR:** #171
**Date:** 2026-03-22
**Reviewer(s):** Claude agent-teams (security, performance, architecture dimensions)

## Fixed Findings

9 finding(s) fixed in place during this review cycle.

| #   | Dimension                  | Severity | Finding                                                                                                                                                                                                                                                                                                   |
| --- | -------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Performance                | Medium   | Duplicate `fillFn` calls — `hexFillColor` and `hexFillOpacity` each called `fillFn?.(cell)` independently, doubling invocations (~2,200 extra calls per render on a full grid). Fixed by merging into `hexFillAttrs()` returning `{fill, fill-opacity}`.                                                  |
| 2   | Performance                | Medium   | `vpHexSet`/`losPathSet`/`seedHexSet` rebuilt Sets on every `overlayConfig` object-reference change, cascading into a full `gridData` recompute even when the underlying arrays were unchanged. Fixed by replacing `computed` Sets with `watch`-based `shallowRef`s keyed on the specific array reference. |
| 3   | Performance                | Medium   | `edgeLine20_80(cell.corners, face.dir)` called inline in the template `v-for` on every render. Pre-computed as `face.lineAttrs` inside the `cellsForEdges` computed.                                                                                                                                      |
| 4   | Performance + Architecture | Medium   | `_globalOverlayState()` was a plain function called inside `overlayConfig` computed, obscuring its reactive contract and creating new nested objects on every evaluation. Converted to a `computed` ref (`globalOverlayState`).                                                                           |
| 5   | Architecture               | Medium   | Five separate panel overlay config refs plus a five-arm `if/else` in `overlayConfig` computed — adding a sixth panel required touching three locations. Collapsed to a single `activePanelOverlayConfig = ref(null)` gated by a `CONFIG_PANELS` set; new panels now touch two locations.                  |
| 6   | Architecture               | Low      | Naming collision: `props.calibration` (grid geometry object) vs `props.overlayConfig.calibration` (rendering toggle `{active: boolean}`). Renamed overlay key to `diagnosticMode` across HexMapOverlay.vue, MapEditorView.vue, overlayConfig.js, and all tests.                                           |
| 7   | Architecture               | Low      | `EMPTY_OVERLAY_CONFIG` exported from `overlayConfig.js` but never imported anywhere. Removed the dead export.                                                                                                                                                                                             |
| 8   | Architecture               | Low      | Tests in `HexMapOverlay.test.js` passed legacy flat props (`vpHexIds`, `calibrationMode`, `selectedHexId`, `losHexA/B`, etc.) that no longer exist in `defineProps`. Vue silently ignored them, making the tests document a stale API. All replaced with `overlayConfig` equivalents.                     |
| 9   | Performance                | Low      | `watch(ownOverlayConfig, emit, { immediate: true })` in `TerrainToolPanel` fires synchronously during setup. Benign given Vue's parent-first mount order; added a comment to document the intent.                                                                                                         |

## Deferred Findings

None — all findings fixed in place.

**Total debt added this PR:** 0

---

_Generated by `/tech-debt-report`_
