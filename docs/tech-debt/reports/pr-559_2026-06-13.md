# Technical Debt Report — PR #559

**PR:** #559
**Date:** 2026-06-13
**Reviewer(s):** Claude team-review (6 dimensions: security, architecture, testing, maintainability, domain, accessibility)

## Fixed Findings

11 findings fixed in place during this review cycle.

- H1: Duplicate `v-for` key — `:key="action.type"` collided for same-type candidates; fixed with `candidateKey()` using type:payload composite
- H2: ACTIVATE_STACK and ROLL_INITIATIVE multi-candidate buttons had identical labels for screen readers; `actionLabel()` extended with hex and leaderId suffixes
- M1: Dispatch gate type-only contract undocumented; comment added at `dispatch()` gate
- M4: `unitIds` recomputed inside `flatMap` per-leader; hoisted to `onBoardUnitIds` above the expansion
- L1: `pending !== null` schema-biconditional confirmed correct; inline comment added citing the invariant
- L2: Title-case reinvention in `actionLabel` — fixed to use existing `toTitleCase()` helper
- L3: Missing ROLL_INITIATIVE cardinality test — new 2×2=4 test added asserting all 4 leader×unit pairs
- L4: Missing ACTIVATE_STACK same-hex dedup test — new test asserting 2 units at same hex = 1 candidate
- Second-pass: `candidateKey()` ROLL_INITIATIVE collision on `unitId` alone — fixed with `leaderId:unitId` compound key
- Second-pass: H3 comment cited `#559` (the PR itself) instead of the real tracking issue; replaced with `#560`
- Second-pass: Two new component tests added for ACTIVATE_STACK and ROLL_INITIATIVE distinct labels

## Second-Pass Review

COMPLETED — fixes touched the shared Vue `ActionPanel` component and `server/src/engine/actions/index.js` (rules-engine), both Checkpointed surfaces per agentic-quality-rails. Second pass confirmed a `candidateKey()` ROLL_INITIATIVE key collision (multiple leaders targeting the same unit produced identical keys) and a stale `#559` issue reference in the deferral comment.

## Deferred Findings

| Issue | Title                                                                       | Score | Assessment                                                                                                                                                                                                                                                                                                                  |
| ----- | --------------------------------------------------------------------------- | ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #560  | LOB §10.3 — ROLL_INITIATIVE should filter candidates to friendly units only | 2     | No functional bug exists in M5 because the scenario seeds only one side's units. The defect will become a correctness issue in M6 when opposing units enter shared game state. Fix requires adding a `side` field to `UnitStateSchema` or OOB-data integration as a prerequisite. Risk is low until M6 adds opposing units. |

**Debt added this PR:** 2
**Debt closed this PR:** 0
**Net delta:** +2 (more debt)

---

_Generated by `/tech-debt-report`_
