# Technical Debt Report — PR #705

**PR:** #705
**Date:** 2026-08-13
**Reviewer(s):** Claude Code — 5-dimension `/team-review` (security, architecture, testing, maintainability, domain) plus a mandatory targeted second-pass review (domain-expert + correctness/maintainability) triggered by the review-fix diff touching rules-engine paths

## Fixed Findings

10 findings fixed in place across two review passes:

**Primary review (Phase 6):**

1. `resolveMovementFormationKey` misclassified every real South Mountain artillery battery as movable infantry (unset `formation` at setup; real batteries carry `gunType`, not `type: 'artillery'`) — combined with the #678 path-walk change, would have let an illegally-moving artillery unit claim VP hexes along the way
2. `activateStack.js:66` — a second #681-class duplicate OOB-lookup pattern the initial pass missed
3. `resolveFormationKey` renamed to `resolveMovementFormationKey` — collided with an unrelated function in `movement.js` and a differently-scoped table in `tables/formations.js`
4. `getValidActions` ignored `ctx.oob` entirely, always re-reading from disk even when the caller already held a validated copy — threaded `ctx` through, closing the last holdout in the `ctx.oob ?? loadOob()` seam
5. Three vacuous-under-mutation tests strengthened (error-code-only assertion, an unreached-loop call-count spy, artillery fixtures shaped nothing like real data)
6. Stale/misleading docstrings corrected on `loadOob`, `safeFindOobUnit`, `updateHexControl`
7. Conductor track-doc drift (spec.md, plan.md, metadata.json, index.md) reconciled with actual delivered state

**Second-pass review (Phase 8):**

8. `handleLimber` never restored `remainingMPs` after formation change — correctly zeroing an unset-formation battery's MPs at Unlimbered (fix #1 above) left every real battery permanently unable to limber-then-move in one activation. Fixed per a domain-expert ruling (LOB §3.6a: Limbered allowance − 3 MP formation-change cost); verified end-to-end against real OOB/scenario data
9. Vacuous `e.code`-only assertion in `move.test.js` mirroring the exact pattern fix #5 already addressed elsewhere in the same file
10. Orphaned comment (describe-block split) in `index.test.js`; dropped formation-vs-conflicting-oobUnit precedence test case in `formation.test.js` — both restored

## Second-Pass Review

COMPLETED — triggered because the Phase 6 review-fix diff touched shared rules-engine logic (formation resolution, `getValidActions`'s dispatch hot path). Found and fixed one real MEDIUM correctness bug (finding #8 above) plus three LOW test-quality findings (finding #9-10). One incidentally-discovered, pre-existing gap (`UNLIMBER` not deducting its own formation-change MP cost) was determined not to be a regression from this branch and filed as #706 rather than fixed in place.

## Deferred Findings

| Issue | Title                                                                              | Score | Assessment                                                                                                                                                                                                                                                                                              |
| ----- | ---------------------------------------------------------------------------------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #703  | SM §5.1 VP control for hexes moved through during retreat/charge-advance/Skedaddle | 2     | Domain-expert-confirmed textual gap in the #678 fix's scope — the same "moved through" rule applies to non-MOVE traversal (retreat, charge advance, Skedaddle), explicitly descoped from this PR's MOVE-only implementation with user approval.                                                         |
| #706  | UNLIMBER does not deduct the 3 MP formation-change cost (LOB §3.6b)                | 3     | `handleUnlimber` flips formation without deducting the MP cost LOB §3.6b requires — a battery can currently unlimber for free. Not a regression from this PR (already reachable pre-#705), but a genuine correctness/balance hazard discovered while investigating the related LIMBER bug (finding #8). |

**Debt added this PR:** 5
**Debt closed this PR:** 10 (issues: #676, #677, #678, #679, #681)
**Net delta:** -5 (net improvement)

---

_Generated by `/tech-debt-report`_
