# Technical Debt Report — PR #312

**PR:** #312
**Date:** 2026-04-14
**Reviewer(s):** Claude agent-teams (security, architecture, testing dimensions)

## Fixed Findings

10 finding(s) fixed in place during this review cycle.

1. Duplicated `assertContainedPath` in map.js and scenario.js — extracted to `server/src/utils/pathGuard.js`
2. Weak positive assertion in map.test.js "in-project path allowed" test — now asserts `/failed to read map file/`
3. `GET /data` test had minimal assertions — now asserts gridSpec equality and hexes length
4. `getDirectionBetween` returned 0 as silent fallback for non-adjacent hexes — now throws
5. `/los` route did not pass pre-built `hexIndex` to `computeLOS` — unnecessary rebuild per request
6. `incomingResult` unvalidated in `/morale-transition` route (Issue #307) — VALID_INCOMING_RESULTS allowlist added
7. `scenario.test.js` "file not found" test exercised containment guard, not file I/O — fixed to use tmpdir path
8. No tests for invalid `formation` or `commanderLevel` enum values — 3 new tests added
9. MapTestView orchestration tests selected panels by hardcoded button index — now select by label text
10. Defense-in-depth: `modifiers` object reflection noted as low-risk inconsistency (no code change required given engine destructuring neutralises unknowns)

## Deferred Findings

None — all findings fixed in place.

**Total debt added this PR:** 0

---

_Generated by `/tech-debt-report`_
