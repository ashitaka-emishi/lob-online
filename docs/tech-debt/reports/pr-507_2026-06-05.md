# Technical Debt Report — PR #507

**PR:** #507
**Date:** 2026-06-05
**Reviewer(s):** team-review (security · architecture · testing · maintainability · accessibility)

## Fixed Findings

5 findings fixed in place during this review cycle:

1. `specialRules` textarea displays `[object Object]` — fixed (the bug itself)
2. AotP HQ "path not resolvable" — fixed via `_nodePath` forwarding + `union.hq` in data/schema
3. Walker phantom node — fixed via `variantIds` guard in `buildLeadersMap`
4. `_nodePath` convention undocumented — comment block added in `oobTreeTransform.js`
5. `onSpecialRulesChange` catch block silent — `/* invalid JSON — store raw text as fallback */` comment added

## Second-Pass Review

NOT REQUIRED — review fixes were limited to adding a comment block in `oobTreeTransform.js` and a one-line comment in `OobDetailPanel.vue`, touching no auth/session/persistence/rules-engine/shared-store surfaces.

## Deferred Findings

| Issue | Title                                                                                                         | Score | Assessment                                                                                                                                                                                                                                                                                                          |
| ----- | ------------------------------------------------------------------------------------------------------------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #508  | test(oob-editor): fix ineffective #506-3 guard test; add \_nodePath forwarding test; guard JSON.parse scalars | 3     | Three related test gaps: the `#506-3` variantIds guard has no effective test (variant in succession fixture can never reach `buildLeadersMap`); `_nodePath` forwarding from `OobTreeNode` is untested; `JSON.parse` accepts valid scalars (number/bool/null) silently producing type-corrupted `specialRules` data. |
| #509  | chore(oob-editor): add resolves-to-real-node test for \_nodePath 'union.hq' literal                           | 2     | The string `'union.hq'` in `oobTreeTransform.js:232` is disconnected from `UnionOOB.hq` in the Zod schema. A key rename silently breaks AotP HQ counter edits with no type check or test catching it. A guard test asserting the path resolves in a real fixture would close this gap.                              |
| #510  | test(oob-editor): minor test/comment polish from PR #507 review                                               | 1     | Three trivial items: undocumented catch fallback (one comment line), `specialRules: null` display not asserted (one test assertion), HQ fallback/real-entry test branches not discriminated by fixture id (rename one id). No functional risk.                                                                      |
| #511  | chore(oob-editor): check off spec acceptance criteria for oob-editor-bugs_20260604 track                      | 1     | All five acceptance criteria in `spec.md` remain `- [ ]` unchecked despite the track being marked complete. Breaks the audit trail the SDLC Completion Contract relies on.                                                                                                                                          |
| #512  | a11y(oob-editor): OobDetailPanel textarea labels + specialRules rows attribute                                | 1     | Pre-existing panel-wide gap: all inputs/textareas lack programmatic label association (`for`/`id`). The specialRules textarea also lacks a `rows` attribute; it clips multi-line JSON at 3 lines, which is worse for keyboard users who cannot drag the resize handle.                                              |

**Debt added this PR:** 8
**Debt closed this PR:** 0
**Net delta:** +8 (regression)

---

_Generated by `/tech-debt-report`_
