# Technical Debt Report — PR #586

**PR:** #586
**Date:** 2026-06-15
**Reviewer(s):** Parallel team review — security, architecture, testing, maintainability, domain, accessibility (6 dimensions)

## Fixed Findings

5 findings fixed in place during this review cycle.

1. MenuLayout `<main>` → `<div>` to avoid nested/duplicate WCAG 1.3.1 landmark and duplicate `id="main-content"` (App.vue already provides the canonical `<main>`; accessibility HIGH)
2. Remove orphaned `scenarioWarning` ref and dead banner block from GameView.vue after scenario fetch was moved to the store (architecture MEDIUM)
3. CSP `connect-src` derived from `CLIENT_ORIGIN` constant instead of hardcoded `localhost:5173` — custom ports/remote dev hosts now covered (maintainability/security MEDIUM)
4. Five conditional `if` guards in `resolveMorale.test.js` replaced with hard deterministic assertions: shaken no-SP-loss, normal/DG→DG, DG/DG→routed, leaderCasualty ×2, integration Step 3 and leaderCasualty integration (testing HIGH ×3)
5. Cascade test retitled to accurately document §6.3 deferral; misleading routed test title corrected (testing LOW)

## Second-Pass Review

NOT REQUIRED — review fixes were limited to a non-landmark element revert (MenuLayout), a dead ref removal (GameView), a constant derivation in the CSP config (server.js), and test assertion strengthening (resolveMorale.test.js). No auth/session/schema/rules-engine/store-boundary production LOC was changed.

## Deferred Findings

| Issue | Title                                                                       | Score | Assessment                                                                                                                                                                                                                                                                    |
| ----- | --------------------------------------------------------------------------- | ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #587  | ROLL_INITIATIVE: filter leader candidates to playerSide (LOB §10.3)         | 3     | Leader eligibility is not side-filtered; cross-side candidate pairs are possible. No state corruption (handlers don't write to wrong units), but violates LOB §10.3 own-command principle. Degraded mode has no handler-layer backstop. Checkpointed surface; assigned M6.    |
| #588  | useGameStore: scenario fetch swallows errors silently, breaks await pattern | 2     | Fire-and-forget `.catch(() => {})` makes scenario load failures invisible and breaks the await pattern in `loadGame`. Non-critical (scenario is non-blocking), but removes all diagnostic visibility for a class of failures. Assigned M7.                                    |
| #589  | Rate limiter: add trust proxy config and split create/join limiters         | 3     | Without `app.set('trust proxy', N)`, express-rate-limit keys by proxy IP in production, rendering the limiter ineffective per-IP and risking an unexpected validation warning violating the warning-free policy. Shared limiter coupling is a secondary concern. Assigned M8. |
| #590  | resolveMorale tests: multi-unit defender hex and mods propagation coverage  | 1     | Missing tests for stacked defenders and modifier propagation through the handler chain. Production code is correct; gaps are coverage-only with low regression risk given the deterministic engine. Assigned M7.                                                              |
| #591  | useGameStore: add scenario-fetch failure test (non-blocking contract)       | 1     | Missing parity test for "game still loads when scenario fetch fails" — the map-config failure path is tested but scenario is not. Low risk. Assigned M7.                                                                                                                      |

**Debt added this PR:** 10
**Debt closed this PR:** 0
**Net delta:** +10 (net regression — new deferred items from first team-review of this PR)

---

_Generated by `/tech-debt-report`_
