# Technical Debt Report — PR #178

**PR:** #178
**Date:** 2026-03-23
**Reviewer(s):** Team review — security, performance, architecture agents

## Fixed Findings

9 finding(s) fixed in place during this review cycle.

1. [FIXED] `useCalibration` extras pass-through bypassed write-path validation (Medium / Security + Architecture)
2. [FIXED] `onCalibrationLoaded` skipped all validation that `onCalibrationChange` enforced (Medium / Security + Architecture)
3. [FIXED] `loadCalibration` asymmetry — extras written by `onCalibrationChange` silently dropped on reload (Low / Security)
4. [FIXED] `gridData` embedded LOS/selection flags, causing `useEdgeLineLayer` to invalidate on every hex click (Medium / Performance)
5. [FIXED] `cellByColRow`/`cellById` Maps rebuilt on every `gridData` invalidation (Medium / Performance)
6. [FIXED] Object spread of `geoCell` per `gridData` recomputation (Low / Performance — resolved as side-effect of #4)
7. [FIXED] `edgeLineGroups.value` accessed inside inner loop in `_buildCellEdgeData` (Low / Performance)
8. [FIXED] Shared `activePanelOverlayConfig` ref mutation undocumented in `useEdgePanelWiring` (Low / Architecture)
9. [FIXED] `gridGeometry` in `defineExpose` lacked `@internal` annotation (Low / Architecture)

This PR also resolves all 5 previously open debt items: #151, #161, #169, #175, #176.

## Deferred Findings

None — all findings fixed in place.

**Total debt added this PR:** 0

---

_Generated by `/tech-debt-report`_
