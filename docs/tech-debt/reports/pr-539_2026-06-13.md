# Technical Debt Report — PR #539

**PR:** #539
**Date:** 2026-06-13
**Reviewer(s):** Claude Sonnet 4.6 (team-review: security, architecture, testing, maintainability, accessibility)

## Fixed Findings

6 findings fixed in place during this review cycle:

- HIGH-A1: Duplicated `DEFAULT_SLUG` constant — exported from `useModuleStore`, imported in router (eliminated duplicate)
- HIGH-M1: Stale `data/scenarios/south-mountain/` paths in `library.json` and `library.md` — updated to `data/modules/south-mountain/` and `scenarios/full-battle/`
- MED-S1: Rate limiting on PUT routes — already implemented in `moduleData.js` via `express-rate-limit` (non-finding; closed without issue)
- MED-A3: `EditorNav` links break when `moduleSlug` param absent — already handled via `?? 'THG'` fallback in computed (non-finding; closed without issue)
- MED-M2: Terminology drift ("scenario" vs "module") — addressed via path corrections in library docs
- MED-M1: Stale scenario path in `library.md` SM table — corrected to `data/modules/south-mountain/scenarios/full-battle/scenario.json`

## Second-Pass Review

NOT REQUIRED — review fixes were limited to exporting a string constant and correcting documentation paths. No auth, persistence, rules-engine, or shared composable surfaces touched.

## Deferred Findings

| Issue | Title                                                               | Score | Assessment                                                                                                                                                                           |
| ----- | ------------------------------------------------------------------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| #540  | Gate moduleData PUT routes behind MAP_EDITOR_ENABLED                | 4     | Write routes mounted unconditionally; any origin passing the CSRF check can overwrite data files. Intended as dev-only but lacks the guard used by all other editor routes.          |
| #541  | Resolve OOB persistence dual-mode divergence in useOobPersistence   | 3     | Composable silently falls back to legacy URL when `moduleSlug` is omitted; misdirected writes are invisible to callers and create a correctness hazard with multiple active modules. |
| #542  | Make editor views reactive to moduleSlug route param changes        | 2     | Slug captured non-reactively at setup; current routing guarantees remount on slug change, so no active breakage, but fragile against future in-page navigation changes.              |
| #543  | Add direct unit tests for useMapPersistence and useOobPersistence   | 2     | Composables only tested indirectly through view mount tests; composable-level URL construction bugs not caught until full view renders.                                              |
| #544  | Add useModuleStore modulePath/defaultScenarioPath helper tests      | 1     | Helpers build all client API and router URLs but have no direct assertions; simple string operations, low breakage risk.                                                             |
| #545  | Register ModuleNotFoundError in Express error handler middleware    | 2     | Error caught inline in helpers; a future route that forgets the inline catch will surface an unhandled 500 instead of a clean 404.                                                   |
| #546  | Expand moduleData route tests — unknown slug 404 and scenario paths | 2     | Missing coverage for unknown-slug 404, nested scenario sub-routes, and invalid scenarioSlug rejection.                                                                               |
| #547  | Add router test coverage for legacy redirect destinations           | 1     | Legacy redirect targets are string constants; a typo would not be caught by current test suite.                                                                                      |

**Debt added this PR:** 17
**Debt closed this PR:** 0
**Net delta:** +17 (regression)

---

_Generated by `/tech-debt-report`_
