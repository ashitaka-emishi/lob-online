# Technical Debt Report — PR #632

**PR:** #632
**Date:** 2026-06-18
**Reviewer(s):** agent-teams parallel review — security, architecture, testing, maintainability, domain, accessibility (6 dimensions); targeted second-pass review — architecture + testing

## Fixed Findings

23 findings fixed in place during this review cycle:

**Primary review (High):** H1 (FIRE_ARTILLERY server-side range validation), H2/H6 (random event once-per-phase guard + ctx.rollFn injection), H4 (dead JSDoc for updateHexControl), H5 (drain-level VP/game-over tests), H7 (VP text WCAG AA contrast), H8 (game-over modal focus management, inert, aria-\*)

**Primary review (Medium):** M2 (turn not incremented past totalTurns), M3 (schema terminal contract refine), M1, M4–M12 (all fixed in place during implementation)

**Primary review (Low):** L1–L5 (all fixed in place)

**Second-pass (High):** Random event block was structurally unreachable — `handleEndPhase` nulled `ordersPhase` before `drainAutoSteps` reached the ATTACK_RECOVERY block, so the `ordersPhase !== null` guard was always false and random events never fired. Fixed by keeping `ordersPhase` alive through ATTACK_RECOVERY; nulled when drainAutoSteps advances to FLUKE_STOPPAGE.

**Second-pass (Low):** `endPhase.test.js` fixture encoded an engine-impossible state (ATTACK_RECOVERY with non-null ordersPhase). Fixed to assert the new correct contract.

## Second-Pass Review

COMPLETED — review fixes touched shared rules-engine logic (`drainAutoSteps`, action handlers, `ordersPhase` state machine) and data validation schemas (`gameState.schema.js`), both Checkpointed surfaces per `.claude/rules/agentic-quality-rails.md`. Second pass focused on architecture and testing dimensions. Found 1 High (unreachable random-event block) and 1 Low (impossible fixture) — both fixed in place; no new debt deferred.

## Deferred Findings

| Issue | Title                                                                             | Score | Assessment                                                                                                                                                                                                                                                                                                                                          |
| ----- | --------------------------------------------------------------------------------- | ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #633  | fix(artillery): depletion band mapping contradicts Combat Table (LOB §8.2a/§8.2c) | 4     | The handler maps whole left/right column bands to shell/canister depletion, but the actual LOB Charts Combat Table (lob-tables.pdf p.2) uses per-cell color bands. The mapping is inverted for the canister case and has no rule basis for shell. Requires domain-expert consultation and per-cell color-band modeling. Assigned to M8.             |
| #634  | feat(vp): wire updateHexControl into movement/retreat path (SM §5.1 terrain VP)   | 3     | `updateHexControl` and `isVpControlEligible` are fully implemented but have no production caller. `state.hexControl` stays `{}` and `computeTerrainVP` always returns 0 — the dominant VP component per SM §5.1 contributes nothing to the end-game tally. Wiring deferred to M8+ when the MOVE action is implemented. JSDoc updated with #634 ref. |

**Debt added this PR:** 7
**Debt closed this PR:** 0 (issues: none)
**Net delta:** +7 (more debt)

---

_Generated by `/tech-debt-report`_
