# Technical Debt Report — PR #709

**PR:** #709
**Date:** 2026-08-13
**Reviewer(s):** Claude Code — 4-dimension `/team-review` (security, architecture, testing, maintainability) plus a mandatory targeted second-pass review (triggered by the review-fix commit touching auth/session/persistence surfaces)

## Fixed Findings

17 findings fixed in place across two review passes:

**Primary implementation (Phases 1-4, closing #698/#699/#700 directly):**

1. `regenerateSession()` in `games.js` called `req.session.regenerate()` explicitly before `req.login()`, which regenerates the session internally itself (traced in `node_modules/passport/lib/sessionmanager.js`) — a redundant second SQLite session-store round trip on every create/join
2. `configurePassport()` was not idempotent — `passport.serializeUser`/`deserializeUser` push onto internal arrays with no public reset, so a second call left a stale, possibly-closed-DB-bound deserializer permanently in effect
3. Auth module duplicated `users`-table SQL instead of using a shared abstraction — extracted `createUserQueries(db)`
4. SQLite migration had no forward-version guard — a DB with `user_version` newer than the binary supports now throws instead of silently proceeding
5. `requireSide`'s "no game-side session" branch changed 401 → 403 (user-approved after a client-code audit confirmed no dependency on the distinction)
6. Added Zod validation (`DiscordProfileSchema`) on the Discord OAuth profile before it reaches the DB
7. Added a logout UI control wired to the already-implemented `authStore.logout()`
8. Added direct route-level tests for the Discord strategy dispatch
9. Fixed three SQLite migration test gaps (pre-v1 insert ordering, missing `side_b_user_id` assertions, no file-based-DB round-trip test)
10. Added a `useAuthStore.loading` test
11. Normalized `req.user?.id` → `req.user.id` at all inconsistent call sites (requireAuth guarantees non-null)
12. Added `requiresAuth` guard to the `GameView` client route

**Primary review (Phase 5) — 14 additional findings, 4 Medium + 10 Low:**

13. Mutation-confirmed vacuous test: `auth.test.js`'s OAuth-route tests asserted against `passport.authenticate`'s global mock call log rather than the actual per-route handler — swapping the two routes' options left every test green
14. Mutation-confirmed vacuous test: a new migration round-trip test asserted only that `user_version` was still 2 after reopening, which an idempotent-but-unconditionally-rerunning migration also satisfies — fixed via a `db.transaction` spy
15. Real ordering hazard: the new `createUserQueries(db)` factory prepared statements against the `users` table with no enforced guarantee migration had run — added a fail-fast guard
16. Conductor track metadata drift (stale task/phase counts) — third recurrence of this bug class this session
17. Plus: a stale 401-referencing comment (`games.js`) and doc table row (`action-contract.md`); 4 tautological `_callOrder` assertions removed; a shape guard added around the `passport._serializers`/`_deserializers` reset; now-fully-dead `upsertUser`/`getUser` barrel exports removed; `.logout-btn` composed from `.menu-btn` instead of duplicating its styles; `DiscordProfileSchema` tightened (snowflake-shaped id, bounded username, non-optional avatar)

**Second-pass review (mandatory — the fix commit touched auth/session/persistence):** confirmed all five substantive fixes correct via independent mutation-testing, confirmed the `DiscordProfileSchema` tightening broke no other call site repo-wide, confirmed the removed exports had zero remaining references. Found 3 more trivial Low findings (a stale design-doc reference, a metadata field not yet updated in the same commit that added it, a 1-property CSS delta), all fixed.

## Second-Pass Review

COMPLETED — triggered because the Phase 5 review-fix commit touched auth/session/persistence/schema-validation surfaces (explicit Checkpointed triggers per this project's quality rails). Confirmed the fix commit correct; found and fixed 3 more trivial Low findings.

## Deferred Findings

| Issue | Title                                                                                                              | Score | Assessment                                                                                                                                                                                                                                                         |
| ----- | ------------------------------------------------------------------------------------------------------------------ | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| #708  | `useAuthStore.logout()` clears client state on network failure; `games.js` depends on external `requireAuth` mount | 2     | Two genuinely pre-existing, out-of-scope findings — neither caused nor worsened by this PR. Finding 1 is a deliberate, already-tested tradeoff (product decision, not a bug); finding 2 is pre-existing architecture (2 of 5 affected call sites predate this PR). |

**Debt added this PR:** 2
**Debt closed this PR:** 7 (issues: #698, #699, #700)
**Net delta:** -5 (net improvement)

---

_Generated by `/tech-debt-report`_
