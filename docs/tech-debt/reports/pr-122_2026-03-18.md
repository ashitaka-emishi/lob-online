# Technical Debt Report — PR #122

**PR:** #122
**Date:** 2026-03-18
**Reviewer(s):** agent-teams (security, performance, architecture — 3rd pass)

## Fixed Findings

6 finding(s) fixed in place during this review cycle.

- M2: Extracted `applyPaint()` helper in `useHexInteraction` — eliminated duplicate paint-mutation blocks in `onHexClick` and `onHexMouseenter`
- L1: Destructure only known calibration keys from localStorage in `loadCalibration()` — guards all field types explicitly
- L5: Moved `OPPOSITE_DIR` to `hexGeometry.js` alongside `DIRS` and `DIR_TO_CORNERS`; added 2 tests
- L8: Added `isValidDraft()` check to server responses in `fetchMapData` and `_executePull`
- L9: Clarified JSDoc in `useHexInteraction` — `selectedHexIds` is writable by contract, not truly caller-owned
- L10: Added composable dependency-order comment to `MapEditorView.vue`

## Deferred Findings

| Issue | Title                                                                 | Score | Assessment                                                                                                                                                                                                          |
| ----- | --------------------------------------------------------------------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #123  | perf: coalesce reactive writes in paint mode onHexMouseenter          | 2     | `onMutated()` sets `unsaved.value = true` on every mouseenter in paint mode, even when already true. A guard eliminates redundant Vue proxy writes during fast drag strokes. Low visible impact on a dev-only tool. |
| #124  | refactor: introduce onHexUpdateBatch to unify dual mutation paths     | 3     | `applyTrace` and `useBulkOperations` bypass `onHexUpdate` for batch efficiency. If `onHexUpdate` gains side effects (undo history, validation), both batch paths silently skip them — maintenance coupling risk.    |
| #125  | refactor: reduce useMapPersistence API surface (23 return values)     | 3     | 23 return values with push/pull dialog state mixed into a persistence composable. Reduces reusability and increases caller coupling. Grouping into sub-objects would clarify ownership.                             |
| #126  | refactor: extract useCalibration and useMapExport from MapEditorView  | 3     | MapEditorView still carries ~378 lines of inline logic despite the extraction of 8 composables. Calibration and export are self-contained concerns that weren't in scope for this PR.                               |
| #127  | chore: deepen isValidDraft validation to check known top-level keys   | 1     | Current validation checks `hexes` array structure only. Arbitrary extra top-level keys can flow into application state. Low risk given server-side Zod validation, but inconsistent defense-in-depth.               |
| #128  | chore: add comment explaining double onHexUpdate calls in onEdgeClick | 1     | `onEdgeClick` calls `onHexUpdate` twice (once per hex), scheduling two debounce timers. Debounce handles correctness; a comment or batch path when #124 lands would clarify intent.                                 |
| #129  | chore: rename resolveHex → resolveHexOrStub or add optional fallback  | 1     | `resolveHex` silently returns a stub on miss. Name doesn't communicate fallback behavior. Low risk in editor context; becomes misleading when game-logic phases adopt the utility.                                  |
| #130  | refactor: encapsulate TOOL_PANEL_MODES inside useEditorAccordion      | 2     | `TOOL_PANEL_MODES` is imported by the view layer's keyboard handler, leaking accordion internals. An encapsulating method would keep the mode-mapping knowledge inside the composable.                              |

**Total debt added this PR:** 16

---

_Generated by `/tech-debt-report`_
