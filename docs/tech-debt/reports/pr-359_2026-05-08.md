# Technical Debt Report — PR #359

**PR:** #359
**Date:** 2026-05-08
**Reviewer(s):** Claude (security, architecture, testing, maintainability, domain)

## Fixed Findings

11 findings fixed in place during this review cycle:

- 3 missing `UnitOrderState` schema cross-field refinements (accepted→deliveryTurnDue===null, accepted→type!==null, non-delay→deliveryTurnDue===null)
- 4 missing `UnitOrderState` rejection tests (accepted+nonNull deliveryTurnDue, accepted+null type, delay+null type, none+nonNull deliveryTurnDue)
- 3 missing `UnitStateSchema.isDetached` tests (accepts true, rejects missing, rejects non-boolean)
- 1 `gameFile.test.js` round-trip fixture missing `isDetached: false`

## Second-Pass Review

NOT REQUIRED

Not required — review fixes were limited to additive schema refinements and test additions; no auth, persistence, shared state, or broad multi-file surfaces were touched.

## Deferred Findings

| Issue | Title                                                          | Score | Assessment                                                                                                                                                                                                                       |
| ----- | -------------------------------------------------------------- | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #360  | Reinforcement units silently drop `orderType`                  | 4     | `initGameState()` reads `orderType` from reinforcement group entries but ignores it, initializing all reinforcement units with `orders: null`. Will cause incorrect behavior when the M5 order-delivery pipeline is implemented. |
| #361  | Scenario-start detached brigades not flagged `isDetached:true` | 4     | Scenario data model has no field to declare initial detachment state; `processSetupSide()` has no path to set `isDetached: true`. Makes the SM scenario unplayable without a fix.                                                |
| #362  | No cross-field invariant between `isDetached` and `orders`     | 3     | Schema allows `isDetached: true` with `orders: null` and vice versa. Per SM detachment rules, only divisions and detached brigades hold independent orders; invalid states are representable.                                    |
| #363  | No migration path for breaking `orders` schema change          | 3     | `orders` changed from raw string to `UnitOrderState` object with no migration utility or versioned schema check. No production risk yet, but must be addressed before M5 ships with persistent saves.                            |
| #364  | Dual representation of no-order state                          | 2     | Two semantically distinct representations exist: `orders: null` (non-order-holding) and `{type:null, status:'none'}` (order-holding, no current order). Query code must double-check both unless consolidated.                   |
| #365  | `isDetached` missing SM section rule citation                  | 2     | Comment reads "SM detachment rules" without a specific section number, violating the project's rule-traceability coding standard.                                                                                                |
| #366  | `OrderType` comment ambiguity ("in pipeline")                  | 1     | Comment `null = no current order in pipeline` conflates `OrderType` null with `UnitOrderState.status === 'delay'`. Misleading for future developers.                                                                             |
| #367  | `isDetached` has no Zod schema default                         | 2     | `z.boolean()` with no `.default(false)` means any programmatic unit creation that omits `isDetached` will fail validation rather than defaulting safely.                                                                         |
| #368  | Parent-order inheritance undocumented                          | 2     | The mechanism for resolving effective order when `orders: null` (inheriting from parent division) is not documented in the schema or any engine module. Will slow future query-layer implementation.                             |
| #369  | `'stopped'` order status not reserved in schema                | 2     | No slot reserved for a future 'stopped'/'hold' status; adding it later will require a schema migration.                                                                                                                          |
| #370  | `complexDefense` sentinel lacks source citation                | 1     | `mapOrder()` comment cites `LOB_GAME_UPDATES SM section` without a specific section or page reference.                                                                                                                           |
| #371  | `defaultUnit()` positional params fragile                      | 2     | 6 positional args with no guard against transposition; an options-object pattern would be safer.                                                                                                                                 |
| #372  | `UnitOrderState` `.refine()` missing path metadata             | 1     | All five `.refine()` calls omit the `path` option, surfacing errors at the object root rather than the offending field.                                                                                                          |

**Debt added this PR:** 29
**Debt closed this PR:** 0
**Net delta:** +29 (regression)

---

_Generated by `/tech-debt-report`_
