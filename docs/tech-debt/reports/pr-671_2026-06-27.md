# Technical Debt Report — PR #671

**PR:** #671
**Date:** 2026-06-27
**Reviewer(s):** security, architecture, testing, maintainability, domain (parallel team-review)

## Fixed Findings

10 findings fixed in place during this review cycle.

1. [HIGH] `morale.js:5` — module-level file header still cited `§6.3 — cascade` after the PR's own central correction
2. [HIGH] `morale.js:279` — `resolvePendingMorale` JSDoc line still cited `LOB §6.3 — moraleCheck resolves brigade cascade check`
3. [MED] `oob.js:301` — `findBrigadeForUnit` JSDoc cited `§6.3` and re-asserted the wrong hex-vs-brigade scope claim the PR documents away
4. [MED] `morale.test.js:279` — one test description retained `(LOB §9.1a, §6.3)` for a cascade-adjacent assertion
5. [MED] `fireCombat.test.js:626` — CBF positive test asserted `cbfMarker` without first asserting `spLoss > 0` (precondition)
6. [LOW] `discord.test.js` — three skip-path tests emitted unasserted `console.warn` output (Warning-Free policy)
7. [LOW] `discord.test.js:170` — #651 test under-specified the validate-stored-then-route ordering; complementary test added
8. [LOW] `games.test.js:1325` — getGame-once test didn't exercise the webhook branch where the original redundant read lived

Two additional Low findings were acknowledged as accepted design decisions with no action required:

- Security [LOW]: override dispatch target unvalidated — intentional; override gated to `NODE_ENV !== 'production'` only
- Architecture [LOW]: triple `getGame` across join branches — future refactor if a third branch appears; no correctness risk today

## Second-Pass Review

NOT REQUIRED — all review fixes were comment corrections and test-only additions (spy setup, mock configuration, one new assertion). No production logic was modified; no high-risk surfaces touched by the fix diff.

## Deferred Findings

None — all findings fixed in place (including #672 and #673, resolved before PR merge in commit 1d61cb5).

**Debt added this PR:** 0
**Debt closed this PR:** 2 (issues: #664)
**Net delta:** -2

---

_Generated by `/tech-debt-report`_
