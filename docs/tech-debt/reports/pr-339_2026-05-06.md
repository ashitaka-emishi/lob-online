# Technical Debt Report — PR #339

**PR:** #339
**Date:** 2026-05-06
**Reviewer(s):** agent-teams (security, performance, architecture)

## Fixed Findings

14 findings fixed in place during this review cycle.

1. ARCH-H1: `startServer()` ESM main-module guard (`process.argv[1]` check)
2. ARCH-H3/M7: `saveGame` now returns the persisted state with incremented version
3. ARCH-H4: `createGame` called before `saveGame` in `POST /api/v1/games` to prevent orphan file risk
4. ARCH-H5: Convenience delegates (createGame, joinGame, etc.) guarded with `requireStore()` sentinel
5. ARCH-M1: `GameStateSchema` cross-field `.refine()` enforcing `phase === null ↔ status === 'setup'`
6. ARCH-M2: Typed `GameNotFoundError` / `GameNotOpenError` replacing generic `Error` throws
7. ARCH-M3: `process.once('SIGTERM', ...)` with `httpServer.close(() => getDb().close())` shutdown chain
8. ARCH-M4: `initDb()` closes prior `_db` singleton before re-initializing (idempotent)
9. ARCH-M5: `GAMES_DIR` env var for configurable game file directory (mirrors `DB_PATH` pattern)
10. PERF-H1: Removed `getGame` pre-check in `POST /:id/join`; typed error dispatch replaces SELECT+error
11. PERF-M1: `LIMIT 200` on `listGames` SQL query to cap unbounded memory growth
12. SEC-M1: `req.session.regenerate()` called before `setPlayerSession` on create and join (session fixation)
13. SEC-M2: `SESSION_SECRET` required in all `NODE_ENV !== 'test'` environments (not just production)
14. SEC-M3: `COOKIE_SECURE` env var replaces `NODE_ENV === 'production'` check for explicit control

## Deferred Findings

| Issue | Title                                                        | Score | Assessment                                                                                                                                                                                                                                                               |
| ----- | ------------------------------------------------------------ | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| #340  | Fix sideToken validation and add auth guard to join endpoint | 4     | Any caller knowing a game UUID can claim the confederate seat; `sideToken` is stored without format validation, allowing empty strings or arbitrary values. Real access-control gap that must be closed before M5 order submission wires token-based move authorization. |

**Debt added this PR:** 4
**Debt closed this PR:** 32 (issues: #329 #330 #331 #332 #333 #334 #335 #336 #338)
**Net delta:** −28 (net improvement)

---

_Generated by `/tech-debt-report`_
