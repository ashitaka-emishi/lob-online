# Technical Debt Report — PR #475

**PR:** #475
**Date:** 2026-05-31
**Reviewer(s):** Claude team-reviewer (security · architecture · testing · maintainability · accessibility)

## Fixed Findings

11 findings fixed in place during this review cycle:

- H1: Added `server/src/routes/leaders.test.js` (200/503/no-cache coverage)
- H4: Live region now includes page position so paging re-announces correctly
- M7: Updated JSDoc to include `weapon` in `oobUnitMap` value shape
- M8: Added comment explaining `displayUnits` intentionally uses narrower shape than `enrichUnit`
- M9/L9: Documented `hexUnits`-takes-precedence invariant on `UnitStatsPanel` `unit` prop
- L6: Weapon test changed from `wrapper.text().toContain('R')` to `.find('.stat-row--weapon').text()`
- L8: Added comment documenting `expectedVersion` non-numeric opt-out behavior
- L10: Extracted `MAX_LABEL_CHARS` constant and added abbreviation collision warning
- L11: Added loadLeaders rationale comment matching `loadOob`'s no-cache explanation
- L14: Added `:focus` fallback alongside `:focus-visible` for SVG `<g>` elements
- H3 (architecture): Socket action loop client consumer correctly deferred to #472/#473/#474

## Second-Pass Review

NOT REQUIRED — in-place fixes were limited to a new test file, JSDoc updates, a live-region string change, a CSS rule addition, and code comments. No production auth, persistence, composable, or rules-engine surfaces were modified.

## Deferred Findings

| Issue | Title                                                                               | Score | Assessment                                                                                                                                                                                                                                                                |
| ----- | ----------------------------------------------------------------------------------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #476  | useOobData: test leaders fetch URL, failure branch, and map merge                   | 3     | The parallel leaders fetch has no URL-assertion test, no failure-branch test, and the leaders-merge path in `oobUnitMap` is entirely unverified. A regression dropping the leaders call or its error handling would pass the full suite silently.                         |
| #477  | requireSide: verify sideToken against DB record on action requests                  | 3     | `requireSide` authorizes only on `session.gameId === req.params.id`; stored side tokens are never compared. A stale session retains full action authority even after a game is deleted or tokens rotate. Pre-existing gap now on the hot action path.                     |
| #478  | games.js actions route: sanitize INVALID_STATE message + add INVALID_PAYLOAD status | 3     | Server-fault `ActionError` messages embed raw Zod output and internal phase/step identifiers that reach the client verbatim. `INVALID_PAYLOAD` (client input error) falls through `?? 500` and is misreported as a server fault. The engine's own TODO flags the leakage. |
| #479  | useOobData: degrade gracefully when /api/v1/leaders unavailable                     | 3     | A leaders-only outage blanks all OOB enrichment (unit names, counters, faction colors) even when `/api/v1/oob` responded correctly. Disproportionate blast radius for a secondary enrichment source.                                                                      |
| #480  | UnitCounterLayer: add aria-pressed and focus management for keyboard/AT users       | 3     | Interactive `<g role="button">` counters expose no selected state to assistive technology. Selection is conveyed by color only (WCAG 1.4.1/4.1.2). Post-activation focus is not managed (WCAG 2.4.3).                                                                     |
| #481  | games.js + socket + UnitStatsPanel: test quality improvements                       | 2     | Success path cannot distinguish dispatch result from saveGame result (same mock object). 409 omitted-version bypass untested. game:leave auth policy not pinned. Paging direction/wrap unverifiable with length-2 arrays. `select-unit` emission contract unasserted.     |
| #482  | games.js POST /actions: guard req.app.locals.io emit post-save                      | 2     | `req.app.locals.io.to(id).emit(...)` runs after `saveGame` with no guard. A missing `io` throws after a committed state change, producing a misleading 500 for an otherwise successful action.                                                                            |

**Debt added this PR:** 19
**Debt closed this PR:** 0
**Net delta:** +19

---

_Generated by `/tech-debt-report`_
