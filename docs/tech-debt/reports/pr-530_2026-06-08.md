# Technical Debt Report — PR #530

**PR:** #530
**Date:** 2026-06-08
**Reviewer(s):** Claude Sonnet 4.6 (team-review: security, architecture, testing, maintainability, accessibility)

## Fixed Findings

3 finding(s) fixed in place during this review cycle.

1. [FIXED] Critical: `init.js timeToTurn()` — reinforcement arrival turns all computed as NaN after
   `minutesPerTurn` was removed from the schema; fixed to use LOB §1.1 constant (15 min/turn).
2. [FIXED] High: Missing test for `updateLightingRow` auto-visibility when changing an existing
   row's condition — added test asserting day→night triggers visibility update to 2.
3. [FIXED] High (accessibility): Lighting table missing `scope="col"` on `<th>`, no `sr-only` text
   on action column header, no `aria-label` on row inputs/selects/delete buttons — all added.

## Second-Pass Review

COMPLETED — `init.js` changes touch the domain-critical reinforcement timing path; targeted
domain+testing second-pass ran. Two Low findings were resolved in-place: comment tightened to
document the night-turn boundary assumption and failure mode; boundary regression test added
locking 17:00 → turn 33 (SM's latest real reinforcement). Zero additional findings deferred.

## Deferred Findings

| Issue | Title                                                              | Score | Assessment                                                                                                                                                                          |
| ----- | ------------------------------------------------------------------ | ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #531  | Extract MINUTES_PER_CONDITION into shared engine module            | 3     | Constant lives only in `ScenarioEditorView.vue`; `init.js` hardcodes `15` with comment. If condition-aware turn math is needed in the engine, the constant needs a shared location. |
| #532  | Enforce startTurn uniqueness in lighting schedule schema           | 2     | No Zod `.superRefine()` check on the array; duplicate `startTurn` values pass validation but produce undefined behavior in the turn-walk logic.                                     |
| #533  | Remove totalTurns from scenario.json (now derived)                 | 2     | `totalTurns` is now computed by the client but still stored in `scenario.json` and required by the schema; mixed source-of-truth creates confusion and can drift.                   |
| #534  | Add lightingStartTimes night-turn clock test                       | 1     | No test verifying that `lightingStartTimes` correctly accumulates 30-min night turns when computing clock times for entries after a night condition transition.                     |
| #535  | Add totalTurns edge case tests                                     | 1     | Guard conditions for empty schedule, `lastTurn` before `firstTurn`, and missing turn fields are untested.                                                                           |
| #536  | Add upper bound to visibilityHexes validation                      | 1     | `visibilityHexes` validated as `z.number().int().positive()` with no upper bound; the `999` day sentinel is undocumented in the schema.                                             |
| #537  | Introduce VISIBILITY_UNLIMITED constant for day sentinel value 999 | 1     | Magic number `999` appears in `VISIBILITY_DEFAULTS`, template display logic, and test fixtures; naming it `VISIBILITY_UNLIMITED` clarifies intent.                                  |
| #538  | Improve contrast ratio for derived-value spans in scenario editor  | 1     | `.derived-value` CSS color may not meet WCAG AA contrast ratio (4.5:1) against the panel background for `totalTurns` and lighting-time clock displays.                              |

**Debt added this PR:** 12
**Debt closed this PR:** 0 (issues: none)
**Net delta:** +12 (more debt)

---

_Generated by `/tech-debt-report`_
