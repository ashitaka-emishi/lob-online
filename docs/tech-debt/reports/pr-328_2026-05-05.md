# Technical Debt Report — PR #328

**PR:** #328
**Date:** 2026-05-05
**Reviewer(s):** claude agent team (security, performance, architecture)

## Fixed Findings

4 finding(s) fixed in place during this review cycle.

1. Path traversal in `gameFile.js` — UUID `router.param` validation + path boundary assert
2. `listGames` leaks `side_a_token`/`side_b_token` — fixed to `SELECT id, status, created_at`
3. Weak `SESSION_SECRET` fallback — added production fail-fast guard; added `sameSite: 'lax'`
4. `loadGame` returned unvalidated JSON — added `GameStateSchema.parse()` in `loadGame`

## Deferred Findings

| Issue | Title                                                                  | Score | Assessment                                                                                                                                                                                                         |
| ----- | ---------------------------------------------------------------------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| #332  | GameStateSchema version field for optimistic concurrency control       | 5     | No version field or write serialization on game state. M5 simultaneous order submission will silently overwrite one player's orders. Critical correctness hazard that blocks M5 from being safe.                   |
| #329  | Configure persistent session store (replace MemoryStore)               | 4     | Default MemoryStore loses all sessions on restart, making in-progress games unjoinable. No `maxAge`; unbounded memory growth. Production reliability risk that impacts every M5 endpoint.                          |
| #330  | Add authorization middleware on GET /api/v1/games/:id                  | 4     | Full game state returned to any caller with the ID. M5 adds hidden orders — without this gate, M5 ships with an information-disclosure flaw letting players read each other's orders.                              |
| #333  | Remove `'setup'` from phase enum — overlaps with status field          | 4     | `phase` and `status` both accept `'setup'` for two different concepts. M5 phase-transition code will need error-prone compound predicates. Decide the model before M5 wires transitions.                           |
| #334  | Create store/index.js interface to enable M8 storage swap              | 3     | Routes import `gameFile.js` and `gameSqlite.js` directly. No abstraction layer — M8 Spaces swap requires editing every route and every M5 turn-loop file. Coupling grows with each M5 endpoint added.              |
| #331  | Refactor gameSqlite to factory + DI; cache prepared statements         | 3     | Module-level singleton forces `vi.resetModules()` hacks in tests. No DI seam means M5 turn-loop integration tests will be difficult. Prepared statements re-created on every call (2–5× parse overhead per query). |
| #335  | Add auth/session.js abstraction for player session access              | 3     | Session shape (`sideToken`, `gameId`, `side`) is three independent assignments per route. Multiplies with M5 routes. M8 OAuth swap means editing every route that touches the session.                             |
| #336  | Fix joinGame TOCTOU race — conditional UPDATE instead of SELECT+UPDATE | 3     | Two simultaneous join requests can both pass the status check. Also executes 3 sequential blocking SQLite queries (getGame + joinGame SELECT + joinGame UPDATE) where 1 suffices.                                  |
| #338  | Move initDb() into startup function; register SIGTERM handler          | 3     | `initDb()` runs as a module-load side effect with no error handling and no `db.close()` on SIGTERM. PM2 restarts may corrupt the SQLite file; DB path is not env-configurable.                                     |
| #337  | Fix getScenario cache invalidation when scenario editor saves          | 2     | Module-level `_scenario` cache is never invalidated. Scenario editor can modify `scenario.json`; new games will use stale data until server restart. Breaks the dev workflow.                                      |

**Debt added this PR:** 34
**Debt closed this PR:** 0 (issues: none)
**Net delta:** +34 (more debt)

---

_Generated by `/tech-debt-report`_
