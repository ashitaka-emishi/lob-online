# Technical Debt Report — PR #158

**PR:** #158
**Date:** 2026-03-22
**Reviewer(s):** agent-teams (security, performance, architecture)

## Fixed Findings

3 finding(s) fixed in place during this review cycle.

- **H1** — Consolidated COMPASS_12 triple duplication: `formulas/compass.js` is now canonical; `utils/compass.js` is a re-export shim; `hexGeometry.js` delegates `getEdgeLabels` to `compassLabel` from formulas.
- **M5** — Added documentation comments to `defineExpose` in `RoadToolPanel`, `StreamWallToolPanel`, and `ContourToolPanel` clarifying the exposed methods exist for test instrumentation, not MapEditorView imperative calls.
- **L3** — Hoisted `ALL_EDGE_GROUPS` to module scope in `MapEditorView.vue`, eliminating the inline spread recomputed on every `overlayConfig` reactive update.

## Deferred Findings

| Issue | Title                                                                             | Score | Assessment                                                                                                                                                          |
| ----- | --------------------------------------------------------------------------------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #159  | perf: deduplicate findNearestEdge call in HexMapOverlay mousemove handler         | 2     | Two calls per frame on a hot path — once for hoverInfo, once in tooltip lookup. Redundant since hoverInfo already contains nearDir. Low urgency with rAF gate.      |
| #160  | perf: add rAF throttle to HexMapOverlay edge-hover path                           | 2     | Edge-hover state writes trigger Vue re-renders on every mousemove event. Should match the hex-hover rAF pattern from PR #88 before full-density map testing.        |
| #161  | refactor: extract per-tool-panel wiring in MapEditorView into a shared composable | 2     | Three nearly-identical panel-wiring blocks in MapEditorView will grow with each new tool panel. Requires design decision on composable API shape.                   |
| #162  | refactor: deduplicate RoadToolPanel / StreamWallToolPanel structural boilerplate  | 2     | Near-identical structure across both panels; changes to shared chrome must be applied in two places. Low urgency until a third structurally identical panel exists. |
| #163  | perf: move inline v-for filter expression to computed in HexMapOverlay            | 1     | Inline `.filter()` in v-for re-runs on every render and suppresses Vue list-diffing. Negligible cost with current list sizes.                                       |
| #164  | perf: memoize wedgePolygonPoints to avoid per-render recomputation                | 1     | Polygon strings recomputed every render despite corners being stable between calibration changes. Low cost per hex; revisit with full-density maps.                 |
| #165  | perf: remove console.assert calls from utils/compass.js hot path                  | 1     | Establishes convention for dev-only diagnostic assertions on hot-path formula functions. The shim no longer has guards; this tracks the future pattern.             |
| #166  | style: consolidate duplicated scoped CSS across edge tool panels                  | 1     | Same panel chrome CSS copied across three `<style scoped>` blocks. Minor maintenance burden; extract when a fourth panel is added.                                  |
| #167  | refactor: reduce HexMapOverlay prop list — consider a single overlayConfig object | 1     | Flat prop list of 12+ props will grow with each new overlay layer. Aligning the full API around the declarative config pattern is a future clean-up pass.           |

**Total debt added this PR:** 13

---

_Generated by `/tech-debt-report`_
