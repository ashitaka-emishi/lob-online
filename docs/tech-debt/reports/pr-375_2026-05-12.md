# Technical Debt Report — PR #375

**PR:** #375
**Date:** 2026-05-12
**Reviewer(s):** Claude (security, architecture, testing, maintainability, domain)

## Fixed Findings

10 findings fixed in place during this review cycle:

- **H1** — Design doc incorrectly described Command Phase as bilateral; corrected to reflect LOB §2.0 (single active player only)
- **H3** — INVALID_STATE dispatch path had no test; added `dispatch.invalid-state.test.js` with `vi.mock` ESM isolation
- **H4** — Added `.strict()` to all 9 Zod object schemas in `gameState.schema.js` to reject unknown fields
- **H5** — Documented `activePlayer` double-flip invariant in `endPhase.js` with inline comment
- **H7** — Added `e.code` assertions to all ActionError `throw` tests across 4 test files
- **H8** — Added `ROLL_INITIATIVE → ISSUE_ORDER` and `ACTIVATE_STACK → END_ACTIVATION → END_PHASE` integration round-trip tests; added `drainAutoSteps` null-phase and idempotence tests
- **M3** — Replaced unbounded `while(true)` in `drainAutoSteps` with bounded `for` loop (max 8 iterations) + `DRAIN_LOOP` throw
- **M7** — Replaced `structuredClone()` with `JSON.parse(JSON.stringify())` in all 4 immutability tests (ESLint Node compat)
- **M8** — Added `completedSteps` noise tests asserting unrelated tokens do not trigger `bothDone` prematurely
- **M10** — Added `TODO(M6)` comments with LOB rule citations to all three stub auto-step branches

## Second-Pass Review

**REQUIRED** — The `.strict()` additions to `gameState.schema.js` touch a schema-compatibility surface (Checkpointed per agentic-quality-rails.md). A targeted second pass is needed on those changes before merge.

**Scope:** `server/src/schemas/gameState.schema.js` only — verify `.strict()` does not break any existing schema tests and that all 9 schemas correctly reject an extra key.

**Status:** `npm run test` passes green (2110 tests, 116 files). `gameState.schema.test.js` (66 tests) all pass. Second-pass review can be waived if reviewer agrees the Zod `.strict()` addition is additive-safe given the green schema test suite.

## Deferred Findings

| Issue | Title                                                                       | Score | Assessment                                                                                                                                                                                           |
| ----- | --------------------------------------------------------------------------- | ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #377  | Validate playerSide against activePlayer in dispatch                        | 2     | No auth bypass at M5 depth; API route boundary will own the session-to-side check in M6. Score 2: must not reach production without a fix but safe to defer until the HTTP action endpoint is built. |
| #378  | drainAutoSteps should guard against non-null activityPhase outside activity | 2     | The Zod `.strict()` schema catches corruption before the engine sees it. Risk is hypothetical future callers bypassing `dispatch()`. Deferred to a focused cross-field schema refinement track.      |
| #379  | getValidActions should enumerate all legal actions for current state        | 2     | Returns stubs by design at M5 depth; full enumeration requires unit/leader position data from the game map UI. Deferred to M6 game map track.                                                        |
| #380  | Add cross-field Zod refinements for phase/step/activePlayer consistency     | 2     | Individual field validation is complete; cross-field invariants are the next hardening layer. No known exploit path. Deferred to a schema-refinement track.                                          |
| #381  | Implement Attack Recovery step handler (LOB §10.6b)                         | 2     | Correctly stubbed at M5 depth; requires combat result data (stopped attack orders) from M6 combat track. Score 2: no game-correctness impact until attack orders can be stopped.                     |
| #382  | Implement Fluke Stoppage step handler (LOB §10.7)                           | 2     | Same as Attack Recovery — requires accepted attack order data from M6. Score 2: no impact at M5 depth.                                                                                               |
| #383  | Implement Rally Phase handler with per-unit rally rolls (LOB §6.3)          | 2     | Requires morale state tracking (DG/Routed units) from M6. No units qualify at M5. Score 2: safe stub.                                                                                                |
| #384  | UNKNOWN_ACTION error message should include action.type value               | 1     | Minor DX issue; no correctness impact. Score 1: quick fix but out of scope for M5 closeout.                                                                                                          |
| #385  | Add property-based / fuzz tests for dispatch round-trips                    | 1     | Nice-to-have for a rules engine; not blocking. Score 1: no correctness impact; no milestone assigned.                                                                                                |

**Debt added this PR:** 16 (9 issues, scores: 2+2+2+2+2+2+2+1+1)
**Debt closed this PR:** 0
**Net delta:** +16

---

_Generated by `/tech-debt-report`_
