# Technical Debt Report — PR #649

**PR:** #649
**Date:** 2026-06-23
**Reviewer(s):** Claude agent-teams (security, architecture, testing) — primary + second-pass

## Fixed Findings

15 finding(s) fixed in place during this review cycle.

Primary review (9): import ordering, misleading test name ("does not call fetch when url is falsy"), undefined-turn branch untested, VP round-trip assertion weak (`>=` → `toBe`), SSRF re-validation gap (`isAllowedDiscordWebhook` inside `notifyWebhook`), `DISCORD_WEBHOOK_TEST_URL` production bypass, missing `allowed_mentions`, redundant DB read (`getGame` → `req.game` via `requireSide`), missing route integration tests (webhook called/skipped/fire-and-forget).

Second-pass (6, 2 fixed): allowlist duplication between `discord.js` and create route (fixed: exported `isAllowedDiscordWebhook`, used in both places); `req.game` exposing token columns (fixed: projected to `{ id, status, discord_webhook }`); 4 low-severity documentation/test items deferred.

## Second-Pass Review

COMPLETED — fix diff touched `server/src/auth/requireSide.js` (auth middleware) and `server/src/routes/games.js` (action route), triggering a targeted security + architecture second pass; 2 medium/low findings fixed in place, 4 low findings deferred as issues.

## Deferred Findings

| Issue | Title                                                          | Score | Assessment                                                                                                                                                                                                                                                                                 |
| ----- | -------------------------------------------------------------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| #654  | Document DISCORD_WEBHOOK_TEST_URL staging risk in .env.example | 2     | `NODE_ENV !== 'production'` honors the override in staging environments, bypassing the SSRF allowlist. Production is correctly pinned in `ecosystem.config.cjs` but the behavior is undocumented for deployed non-production tiers — a footgun if a staging env inherits a leaked env var. |
| #655  | Document req.game contract and shape in requireSide JSDoc      | 1     | The JSDoc documents the response-code matrix but says nothing about setting `req.game` on success, its shape, or that handlers must not serialize it wholesale. No functional risk (projection already omits token columns) but creates an undocumented implicit contract.                 |
| #656  | Make requireSide dependency explicit in action route handler   | 1     | Handler reads `req.game?.discord_webhook` with silent degradation (`?.`) if middleware is absent. Lost notifications would be invisible. Low risk given all `/:id` routes already mount `requireSide`, but the dependency is convention-only.                                              |
| #657  | Add unit tests for isAllowedDiscordWebhook in discord.test.js  | 1     | The exported SSRF allowlist function has no direct unit tests; only indirectly exercised through route tests via a pass-through mock. Pins the security contract independently of the route layer.                                                                                         |

**Debt added this PR:** 5
**Debt closed this PR:** 0 (none)
**Net delta:** +5 (regression)

---

_Generated by `/tech-debt-report`_
