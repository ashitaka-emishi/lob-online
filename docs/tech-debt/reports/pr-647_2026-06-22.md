# Technical Debt Report — PR #647

**PR:** #647
**Date:** 2026-06-22
**Reviewer(s):** agent-teams (security, architecture, testing, maintainability)

## Fixed Findings

18 findings fixed in place during this review cycle (16 from the primary team review, 2 from the
targeted second-pass review).

**Primary review — fixed in place:**

1. [CRITICAL] Unauthenticated DELETE route — deferred, see below
2. [HIGH] Non-atomic version check in `saveGame` — version-conflict throw moved outside `try/catch`
3. [HIGH] SSRF in `discordWebhook` — restricted to Discord hostnames + `https:` only
4. [HIGH] `GameNotFoundError` `instanceof` identity hazard — extracted to shared `store/errors.js`
5. [MEDIUM] Dual-write create rollback missing — added `try/catch` around `saveGame` with `deleteGame` compensation
6. [MEDIUM] Delete ordering (SQLite before Spaces) — reversed to Spaces-first for idempotency
7. [MEDIUM] 409 status before token check in `requireSide` — reordered to check token first (fail more closed)
8. [MEDIUM] `player.token` vs `sideToken` field name drift — standardized to `sideToken` throughout `session.js` + all callers
9. [MEDIUM] Redundant `getGame` call in `GET /:id` after `requireSide` already verified the row
10. [MEDIUM] SQLite migration not transacted — wrapped in `db.transaction()` for atomic DDL + version bump
11. [LOW] `isNotFound` inline predicate duplicated across `saveGame`/`loadGame` — extracted to helper
12. [LOW] Inaccurate comment on `ADD COLUMN` SQLite no-op behavior
13. [LOW] Missing TOCTOU documentation on optimistic concurrency check
14. [LOW] Test: `getPlayerSession` expected `token` field; now returns `sideToken`
15. [LOW] Test: `requireSide` 403-before-409 ordering not covered
16. [LOW] Test: re-join same-side token reuse not asserted

**Second-pass review — fixed in place:**

17. [MEDIUM] `loadGame` handlers (`GET /:id`, `GET /:id/actions`, `POST /:id/actions`) returned opaque 500 for orphaned games (Spaces object missing, SQLite row present) — added `instanceof GameNotFoundError → 404` branch in all three handlers
18. [MEDIUM] Create-rollback `deleteGame` could mask the original `saveGame` error if the rollback itself threw — wrapped in inner `try/catch` to always propagate the causal error

## Second-Pass Review

COMPLETED — auth/session/persistence/store-contract surfaces triggered the second-pass policy.

Security second-pass: all six targeted fixes verified correct and complete. Token rename clean (zero remaining `player.token` references), `requireSide` ordering correct for all five cases, delete ordering idempotent and crash-safe, `GameNotFoundError` identity verified at the class-object level across both stores. No exploitable gaps found. Findings: 0 Critical, 0 High, 1 Medium (fixed in place), 2 Low (deferred-acceptable).

Architecture second-pass: migration transaction correct (`user_version` PRAGMA participates safely in transactions). `GameNotFoundError` barrel export clean (single class identity from `errors.js`, no double-export conflict). Findings: 0 Critical, 0 High, 1 Medium (fixed in place), 2 Low (deferred-acceptable, filed below as #649 and #650).

## Deferred Findings

| Issue | Title                                                       | Score | Assessment                                                                                                                                                                                                                       |
| ----- | ----------------------------------------------------------- | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #648  | sec: add requireSide auth guard to DELETE /api/v1/games/:id | 5     | Any caller with a game ID can destroy the game without a session token — no authentication required on the delete path. Intentional deferral during single-player scaffolding phase; must be closed before M8 multiplayer ships. |

**Debt added this PR:** 5
**Debt closed this PR:** 0
**Net delta:** +5

---

_Generated by `/tech-debt-report`_
