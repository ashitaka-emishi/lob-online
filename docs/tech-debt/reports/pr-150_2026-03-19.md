# Technical Debt Report — PR #150

**PR:** #150
**Date:** 2026-03-19
**Reviewer(s):** `/team-review` (security, performance, architecture dimensions)

## Fixed Findings

7 finding(s) fixed in place during this review cycle.

- **M1** — `toggleableLayers` computed mutated `toggleStates` reactive state (moved to `watch`)
- **M2** — `HexMapOverlay` coupled to tool panel names via hardcoded Sets (replaced with boolean props)
- **L1** — `edge-hover` event emitted with no registered consumer (removed)
- **L2** — `hexFillOpacity` called `hexFillColor` internally creating double computation (inlined)
- **L3** — Nested `.filter()` with `Array.includes()` in EdgeLineLayer hot loop (replaced with `Set.has()`)
- **L4** — `findNearestEdge` ran unconditionally on every mousemove via `requestAnimationFrame` (removed with handler)
- **L5** — `overlayConfig` computed created new closure objects on every reactive change (lifted to module-level stable refs)

## Deferred Findings

| Issue | Title                                                                                                     | Score | Assessment                                                                                                                                                                                                                       |
| ----- | --------------------------------------------------------------------------------------------------------- | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #151  | perf: gridData rebuilds full cell array on any calibration change                                         | 3     | ~2,240 cell objects rebuild on any calibration field change including strokeWidth/northOffset nudges. Causes visible lag on slower machines. Fix requires splitting gridData into geometry + cell layers — non-trivial refactor. |
| #153  | refactor: overlayConfig ownership bridge in MapEditorView should move to tool panels                      | 3     | MapEditorView knowing each tool's rendering preferences violates the tool-owns-its-overlays design principle. Bridge will grow with each tool panel added in #137–#142. Deferred until tool panels exist to own it.              |
| #152  | refactor: HexMapOverlay mixes declarative overlayConfig with legacy explicit props                        | 2     | Two parallel rendering systems increase cognitive overhead. No functional risk now; becomes a maintenance burden as more tools are implemented. Should be resolved after the tool panel migration is complete.                   |
| #155  | docs: document composable overlap/composition contract (useHexPaintTool/useEdgePaintTool/useClickHexside) | 2     | Three composables share overlapping signatures with no documented composition contract. Risk materializes in #137–#142 — a future implementer may wire events incorrectly or duplicate stroke batching.                          |
| #154  | fix: ToolChooser item.color is not validated — accepts any string                                         | 1     | Silent visual failure (wrong color) when an invalid CSS color string is passed. Low risk since item arrays come from the controlled feature-types.js registry; a typo there would be caught in code review.                      |

**Total debt added this PR:** 11

---

_Generated by `/tech-debt-report`_
