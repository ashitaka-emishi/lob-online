# Technical Debt Report — PR #639

**PR:** #639
**Date:** 2026-06-22
**Reviewer(s):** Parallel agent team — security, architecture, testing, maintainability

## Fixed Findings

5 findings fixed in place during this review cycle.

1. MinIO ports rebound to `127.0.0.1` (was `0.0.0.0`) — security
2. Discord sink rebound to `127.0.0.1` (was `0.0.0.0`) — security
3. `express` declared as explicit root `devDependency` — architecture
4. CLAUDE.md "Local Infrastructure (M8)" section added (High finding) — maintainability
5. JSON body size limit `{ limit: '64kb' }` added to discord sink body parser — security

## Second-Pass Review

NOT REQUIRED

Review fixes were limited to port binding config, a devDependency declaration, and CLAUDE.md
documentation. No auth, persistence, game-engine, or shared-store surfaces were touched.

## Deferred Findings

| Issue | Title                                                             | Score | Assessment                                                                                                                                                                    |
| ----- | ----------------------------------------------------------------- | ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #640  | Auto-create lob-online-dev bucket on docker compose up            | 3     | First-run devs hit `NoSuchBucket` immediately with no documented fix; blocks local M8 persistence development until an `minio/mc` init container or manual step is documented |
| #641  | Add SPACES_REGION to .env.example                                 | 2     | AWS SDK v3 requires a region even for MinIO; omission causes a silent runtime error when the S3 client is first instantiated in the persistence track                         |
| #642  | Add healthcheck and restart policy to docker-compose.yml          | 1     | Low impact until the bucket-init container (#640) is added, at which point `service_healthy` gating becomes load-bearing                                                      |
| #643  | Rename DISCORD_WEBHOOK_TEST_URL to DISCORD_WEBHOOK_URL            | 2     | `_TEST_` suffix is misleading for a production-load-bearing value; will cause operator confusion when M8 notification code reads it                                           |
| #644  | Separate app export from app.listen in discord-test-server.js     | 1     | Prevents route-handler tests in the existing scripts Vitest project; minimal impact since the sink has no business logic                                                      |
| #645  | Make discord-test-server.js port env-configurable                 | 1     | Silent drift risk between hardcoded port and .env.example; low impact since both default to 4040 and the script is dev-only                                                   |
| #646  | Note DO*SPACES*\_ → SPACES\_\_ rename in production deploy config | 2     | Production deploy config will silently fail once M8 persistence ships if not updated; risk window is short but consequence is a broken deploy                                 |

**Debt added this PR:** 12
**Debt closed this PR:** 0
**Net delta:** +12

---

_Generated by `/tech-debt-report`_
