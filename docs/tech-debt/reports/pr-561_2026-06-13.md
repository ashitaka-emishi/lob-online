# Technical Debt Report — PR #561

**PR:** #561
**Date:** 2026-06-13
**Reviewer(s):** Claude team-review (security, architecture, testing, accessibility)

## Fixed Findings

5 findings fixed in place during this review cycle.

1. Stale JSDoc in `requireSide` — 401 row still named gameId-mismatch (now 403); updated response-code matrix with separate rows for wrong-game and invalid-token 403 paths.
2. 403 route tests missing DB-skip assertions — added `getGame`/`loadGame` not-called assertions to `GET /:id` and `POST /:id/actions` different-game tests.
3. Redundant Session guard describe block lacked not-called assertions — assertions added to match unit-test-level rigor.
4. Active-game click behavior not functionally tested — added click tests for USA/CSA on active games asserting `joinGame` fires.
5. No route-level test for token-mismatch 403 — added tests for same-gameId + invalid token on both guarded routes.

## Second-Pass Review

NOT REQUIRED — review fixes were limited to a JSDoc comment update and test additions; no production logic changed.

## Deferred Findings

| Issue | Title                                                                                | Score | Assessment                                                                                                                                                                                                                                                                                                                                        |
| ----- | ------------------------------------------------------------------------------------ | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #562  | Security: bind side tokens to factions in DB; derive player.side from token match    | 4     | DB stores two opaque tokens with no faction binding; `requireSide` validates token matches either slot but never checks which side it belongs to. A player can claim either faction. Intentional single-user testing scaffolding; hard prerequisite for M8 multiplayer — requires DB migration, session rewrite, and engine authorization update. |
| #563  | Security: enforce side binding on re-join — reject side-switch to opponent's faction | 3     | Re-join path accepts any `side` from request body while reusing existing token, allowing side impersonation between turns. Acknowledged scaffolding (#349); depends on #562 (token/faction binding) to derive correct side. Deferred to M8 alongside full auth hardening.                                                                         |
| #564  | Lobby: restore join-button disabled state and accessible labels for production UX    | 2     | USA/CSA buttons always enabled post-PR; screen readers get no programmatic signal that joining an active game will fail. Status badge in sibling cell is not associated with the buttons. Low functional risk; `role="alert"` error banner exists. Fix at M8 lobby UX pass.                                                                       |

**Debt added this PR:** 9
**Debt closed this PR:** 0
**Net delta:** +9 (regression — 3 new deferred items, all intentional testing scaffolding deferred to M8)

---

_Generated by `/tech-debt-report`_
