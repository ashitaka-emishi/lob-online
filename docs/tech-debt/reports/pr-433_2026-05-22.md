# Technical Debt Report — PR #433

**PR:** #433
**Date:** 2026-05-22
**Reviewer(s):** Claude team-review (security, architecture, testing, maintainability, accessibility — 5 parallel reviewers)

## Fixed Findings

7 findings fixed in place during this review cycle.

1. `fetchOob()` missing `res.ok` check — server 503 silently treated as success (H1)
2. `collectOobUnits` intent and scope documented in comments (H2)
3. `hexSideLength` prop default corrected from 28 to 35 to match calibration (M3)
4. `GameView.test.js`: added real OOB error state test for error-banner branch (M4)
5. OOBSchema top-level key coupling documented in `useOobData.js` (M5)
6. `loadOob()` doc comment clarified; `DEFAULT_OOB_PATH` single-scenario note added (M1/M2)
7. `SIDES` cross-reference comments; `sizeRatio >1.0` overlap intent noted; `#SEC-M1` tag removed from `regenerateSession` (L4/L5/L6)

## Second-Pass Review

NOT REQUIRED — review fixes were limited to a 3-line `res.ok` guard (directly specified by 4/5 reviewers with exact code), comment additions, a prop default correction, and a new test assertion. No new behavioral surface introduced; all changes are additive cleanup of the exact findings raised.

## Deferred Findings

| Issue | Title                                                                            | Score | Assessment                                                                                                                                                                                                                                       |
| ----- | -------------------------------------------------------------------------------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| #434  | a11y: SVG `<image>` button AT reliability + missing focus indicator              | 3     | Pre-existing pattern in `UnitCounterLayer.vue` touched by this PR's aria-label fix. WCAG SC 4.1.2/2.1.1/2.4.7 at risk. Fix requires wrapping counters in SVG `<a>` or HTML `<button>` overlay — appropriately a dedicated accessibility track.   |
| #435  | a11y: `overflow:hidden` on html/body clips non-game-route scroll and zoom reflow | 2     | `App.vue` sets document-level `overflow:hidden` for game map containment, which removes scroll from all routes including `LobbyView`. Low practical impact today; grows as more routes or game rows accumulate. Scope to game-route layout only. |
| #436  | test: useOobData deep-recursion gap; `oobError` raw server message to DOM        | 1     | `collectOobUnits` tested only one level deep; 3-level traversal regression undetected. Raw fetch error message assigned to reactive ref rendered in error banner — benign source but poor pattern.                                               |

**Debt added this PR:** 6
**Debt closed this PR:** 11 (issues: #401, #404, #411, #412, #346, #413)
**Net delta:** −5 (net improvement)

---

_Generated by `/tech-debt-report`_
