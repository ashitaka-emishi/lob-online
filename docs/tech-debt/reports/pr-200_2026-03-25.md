# Technical Debt Report — PR #200

**PR:** #200
**Date:** 2026-03-25
**Reviewer(s):** Claude Code (team-review — security, performance, architecture)

## Fixed Findings

4 finding(s) fixed in place during this review cycle.

- **M1** — Extracted OOB tree transformation logic into `client/src/utils/oobTreeTransform.js`
- **M3** — Fixed `isSelected` in `OobTreeNode` to use ID-based comparison instead of reference identity
- **M4** — Added prototype pollution guard to `updateField`; documented Vue reactivity limitation for new nested properties
- **M5** — Added `isSyncing` and `syncError` refs to store; push/pull now surfaces errors; view shows sync feedback and disables buttons during sync

## Deferred Findings

| Issue | Title                                                                               | Score | Assessment                                                                                                                                                                                                                                                                                                                                  |
| ----- | ----------------------------------------------------------------------------------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #201  | oobTreeTransform: pre-index arty entries for O(1) lookups                           | 1     | Nested linear scan in `distributeCorpsArtillery` is O(M×(D+D×B)). No observable impact at South Mountain scale; flagged for awareness if OOB grows significantly.                                                                                                                                                                           |
| #202  | Add schema validation for localStorage and API responses in useOobStore             | 2     | Parsed data bypasses Zod schema validation used by the server. Risk is low (dev-only tool, no production exposure, no XSS vector via auto-escaped templates), but malformed localStorage values could cause runtime errors.                                                                                                                 |
| #203  | OOB editor push/pull — add confirmation dialogs                                     | 2     | Clicking Push immediately overwrites the server's oob.json/leaders.json with no warning. Data is git-tracked so recovery is possible, but the UX gap is inconsistent with the map editor's confirmation flow.                                                                                                                               |
| #204  | OobTreeNode expand/collapse: 200–300 per-instance watchers on shared signals        | 1     | Each of ~200 tree nodes installs two watch() calls on injected counter refs. Correct and fast at current scale; flagged for awareness if tree grows to 1000+ nodes.                                                                                                                                                                         |
| #205  | useOobStore: consider lazy-loading bundled JSON fallbacks                           | 1     | Static imports of oob.json and leaders.json are included in the store chunk even when the server fetch succeeds. Impact is minimal due to lazy-loaded route; noted for future bundle analysis.                                                                                                                                              |
| #206  | Align map/scenario editor routes to use lazy loading like oob-editor                | 1     | Map and scenario editor routes use eager imports while the OOB editor uses dynamic import. Consistency improvement only; no functional impact.                                                                                                                                                                                              |
| #207  | Extract shared persistence composable for OOB editor (align with useMapPersistence) | 3     | The OOB store embeds persistence logic that diverges from the established `useMapPersistence` composable pattern. Missing: confirmation dialogs (#203), offline detection, data validation (#202). This architectural gap will require resolution before Phase 2 (editing) to avoid duplication and missing safety checks in the edit flow. |

**Total debt added this PR:** 11

---

_Generated by `/tech-debt-report`_
