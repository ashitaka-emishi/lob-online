# Technical Debt Report — PR #120

**PR:** #120
**Date:** 2026-03-18
**Reviewer(s):** agent-teams parallel review (security, performance, architecture)

## Fixed Findings

6 finding(s) fixed in place during this review cycle.

1. 3x inline `gameCol/gameRow/id` formula duplication in `HexMapOverlay.vue` — extracted to `hexToGameId` (debt #118)
2. No test coverage on `onSvgClick` / `onSvgContextMenu` hex ID emit paths — added click/context-menu emit tests (debt #119)
3. `adjacentHexId` (hexGeometry.js:226) still had inline `padStart` formatting — now delegates to `formatGameId`
4. `los.js:43` private `formatHexId` not consolidated — now imports `formatGameId` from hexGeometry.js
5. `hexToGameId` coordinate-system ambiguity in name/JSDoc — addressed structurally by `formatGameId` extraction making the two-layer contract explicit
6. `makeSvgPoint` test helper mirrors `gridData` internals — accepted as-is (inherent integration test tradeoff); existing comment on line 469 documents the coupling

## Deferred Findings

None — all findings fixed in place.

**Total debt added this PR:** 0

---

_Generated by `/tech-debt-report`_
