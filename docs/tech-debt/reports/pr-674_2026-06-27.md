# Technical Debt Report — PR #674

**PR:** #674
**Date:** 2026-06-27
**Reviewer(s):** Claude Code (team-review — 6 parallel dimensions: security, architecture, testing, maintainability, domain, performance)

## Fixed Findings

13 findings fixed in place during this review cycle.

1. H1 — Unit-ownership guard added to `resolveMove` (mirror combat handler pattern)
2. H2 — Null-formation guard added before `movementPath` call (unlimbered artillery)
3. H3 — `INVALID_MOVE` and `INSUFFICIENT_MPS` added to `ACTION_ERROR_STATUS` in `games.js`
4. H5 — `handleActivateStack` remainingMPs initialization fully tested in `activateStack.test.js` (7 cases)
5. H6 — `resolveMovementFormation` branch tests added (cavalry, leader, limbered, unlimbered)
6. H7 — `ctx.hexIndex` forwarded to `movementPath` (avoids rebuilding 2240-hex index per MOVE)
7. M2 — `MOVE` added to `CTX_RECOMMENDED_ACTIONS` for dispatch-level ctx warning
8. M7 — `INSUFFICIENT_MPs` renamed to `INSUFFICIENT_MPS` (SCREAMING_SNAKE_CASE convention)
9. M8 — VP-control eligibility negative test added (cavalry → `hexControl` unchanged)
10. M9 — Boundary MP test added (`totalCost === remainingMPs` succeeds; `remainingMPs → 0`)
11. M10 — Partial-payload contract documented on MOVE candidates in `index.js`
12. L3 — Misleading "Mutate" comment fixed in `move.js`
13. L5 — `throw`-recatch pattern replaced with `expect.assertions(2)` in all 9 error tests

## Second-Pass Review

NOT REQUIRED

Fix diff added defensive guards only (ownership check at move.js:75, null-formation guard at move.js:89) with no changes to movement cost logic or the state-mutation path; 3232 tests pass including new formation-branch and ownership cases.

## Deferred Findings

| Issue | Title                                                                        | Score | Assessment                                                                                                                                                                                                           |
| ----- | ---------------------------------------------------------------------------- | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #675  | feat(engine): charge submitted-path cost, not Dijkstra optimal               | 3     | Handler charges optimal-path cost regardless of submitted path[]. Under LOB §3 a unit pays cost for hexes entered; player detours are undercharged and mid-route impassable hexes are silently bypassed.             |
| #676  | perf(engine): cache loadOob() — eliminate N+2 disk reads per dispatch        | 3     | loadOob() re-reads and validates oob.json on every call. getValidActions invokes it N+2 times per mid-activation dispatch — synchronous disk I/O blocks the event loop on the hottest action path.                   |
| #680  | fix(engine): allow multiple MOVE per activation (LOB §3)                     | 3     | The `unit.hex === activation.hex` guard fires after the first MOVE, blocking any follow-up MOVE with remaining MPs. LOB §3 permits splitting MPs across multiple MOVE actions; a unit with 6 MPs can only move once. |
| #677  | refactor(engine): extract resolveFormationKey() — eliminate duplicate ladder | 2     | `resolveMovementFormation` and `resolveUnitMPs` encode the same classification logic. A future unit type added to one but not the other causes MP init and movement-cost formation to silently diverge.              |
| #678  | feat(engine): honor SM §5.1 VP control for hexes moved through               | 2     | `updateHexControl` is called only for the destination hex. SM §5.1 awards control for "moved through" hexes; units sweeping across VP road hexes en route to a non-VP destination don't claim those hexes.           |
| #679  | feat(engine): model column formation toggle — infantry road movement         | 2     | Infantry always defaults to 'line'. Column formation (road movement 0.5/hex) is never applied, understating movement range on SM's road network. Requires a CHANGE_FORMATION action.                                 |
| #681  | chore(engine): extract safeFindOobUnit() helper                              | 1     | IIFE-try/catch OOB lookup pattern duplicated in move.js, activateStack.js, and index.js. Minor readability cost, no correctness risk.                                                                                |

**Debt added this PR:** 16
**Debt closed this PR:** 3 (issue: #634 — updateHexControl wired into resolveMove; hexControl now maintained in production)
**Net delta:** +13 (regression — MOVE handler introduces real rules gaps that must be resolved in M9)

---

_Generated by `/tech-debt-report`_
