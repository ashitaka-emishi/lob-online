# Technical Debt Report — PR #409

**PR:** #409
**Date:** 2026-05-21
**Reviewer(s):** Claude team-review (security, architecture, testing, maintainability, accessibility)

## Fixed Findings

7 findings fixed in place during this review cycle:

- `lobby.js fetchGames()` — `/me` failure now resets identity to null instead of silently retaining stale values (#4)
- `games.test.js` — Session mock shape inconsistency (`token` vs `sideToken`) corrected (#11)
- `lobby.store.test.js` — Added test for partial `/me` failure (games succeeds, /me fails) (#12)
- `games.js` — Renamed `(res, rej)` Promise executor params to `(resolve, reject)` to eliminate Express `res` shadowing (#17)
- `gameFile.js` — Added comment documenting intentional `force:true` divergence from SQLite behavior (#18)
- `LobbyView.vue` — Added `role="alert"` to error div for screen-reader announcement (#23)
- `LobbyView.vue` — Added `aria-label="Available games"` to table (#22)

## Second-Pass Review

NOT REQUIRED — review fixes were limited to test additions, a comment, an identifier rename (no behavior change), and a minor store behavior fix (identity reset on `/me` failure). No auth, session, persistence, or rules-engine surfaces touched.

## Deferred Findings

| Issue | Title                                                                     | Score | Assessment                                                                                                                                                                                                                                                                                                                                                                                                               |
| ----- | ------------------------------------------------------------------------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| #410  | Implement lobby auth, ownership, and CSRF correctly (post-scaffolding)    | 5     | Bundled scaffolded-lobby security and correctness debt: unauthenticated DELETE IDOR, unauthorized side-switching, no CSRF, inverted DELETE persistence ordering, missing `deleteGameFile` tests, missing deleteGame-then-fetchGames-failure test, no confirmation on destructive delete, and missing accessible names on action buttons. All intentionally deferred; security findings depend on M8 OAuth identity work. |
| #411  | Extract `session.regenerate()` into a helper in `games.js`                | 2     | Promise wrapper copy-pasted in three places; a future fix to error handling in one site will not propagate to the others. Low coupling risk today but grows with each new route.                                                                                                                                                                                                                                         |
| #412  | Define SIDES constant — eliminate `'union'`/`'confederate'` magic strings | 2     | Side values duplicated across server, client, and tests. Rename risk is low but grows with each new endpoint or side-aware test.                                                                                                                                                                                                                                                                                         |
| #413  | Fix `.error` text color contrast to meet WCAG AA in LobbyView             | 1     | `color: red` (#FF0000) on white yields ~4.0:1 contrast, below the 4.5:1 AA threshold. Trivial CSS fix.                                                                                                                                                                                                                                                                                                                   |
| #414  | Remove unnecessary `/me` refetch inside `deleteGame` in lobby store       | 1     | `deleteGame` calls `fetchGames()` which fires two round-trips (`/games` + `/me`). `/me` cannot change from deleting a game. Minor inefficiency with no correctness impact.                                                                                                                                                                                                                                               |

**Debt added this PR:** 11
**Debt closed this PR:** 0
**Net delta:** +11 (net regression — retroactive review surfaced pre-existing deferred scaffolding debt)

---

_Generated by `/tech-debt-report`_
