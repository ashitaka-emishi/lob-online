# Technical Debt Report — PR #386

**PR:** #386
**Date:** 2026-05-13
**Reviewer(s):** Claude Code team-review (security, architecture, testing, maintainability, domain)

## Fixed Findings

10 finding(s) fixed in place during this review cycle:

- [M1] Rally phase had no phase-envelope biconditional in GameStateSchema — fixed by adding
  two split `.refine()` calls with correct field paths
- [M2] Rally-phase states missing from biconditional test suite — fixed with positive +
  two rejection tests
- [M3] Setup-state dispatch error path not asserted in test — fixed by asserting message
  matches `/not valid in the current state/`
- [M4] `dispatch.unknown-action.test.js` had no isolation warning — fixed with comment block
- [L1] playerSide guard / getValidActions duplication undocumented — fixed with layering
  comment explaining public error vs. defense-in-depth intent
- [L4] Iteration cap `8` was a magic number — fixed with `MAX_AUTO_STEPS` constant
- [L6] No test for correct-side dispatch during `pendingResolution` — fixed with new test
- [SP-L1] Rally refinement error path asymmetric (ordersPhase violation reported under
  activityPhase path) — fixed by splitting into two separate refinements
- [SP-L2] Rally refinement logically redundant without explanatory comment — fixed with
  belt-and-suspenders comment documenting the intentional duplication
- [SP-Info] `endPhase.js` rally transition relied on inherited `ordersPhase: null` —
  fixed by making the null explicit

## Second-Pass Review

COMPLETED — rally-phase schema changes touched `GameStateSchema` (persistence/wire-format
surface), triggering a targeted second-pass on the fix diff. Two Low findings (asymmetric
error path, undocumented redundancy) and one Info finding (implicit null inheritance) were
identified and all fixed in place; no new deferred items from the second pass.

## Deferred Findings

| Issue | Title                                                                 | Score | Assessment                                                                                                                                                                                 |
| ----- | --------------------------------------------------------------------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| #387  | playerSide in dispatch must be sourced from session, not request body | 1     | No action endpoint exists yet (#356); the session-to-side mapping is a route-layer responsibility. Enforce at the route boundary when #356 is implemented.                                 |
| #388  | Extract PHASES and STEPS constants to eliminate magic string literals | 2     | Manageable with 3 phases; becomes fragile in M6 when morale/combat phases are added. Extract to shared `phases.js` before adding new phases.                                               |
| #389  | drainAutoSteps transient states bypass schema validation mid-loop     | 2     | All transitions are correctly atomic today. Risk is future contributors adding partial multi-step updates across drain iterations. Add per-iteration invariant check before M6 extensions. |

**Debt added this PR:** 5
**Debt closed this PR:** 7 (issues: #377 #378 #380 #384)
**Net delta:** −2 (net improvement)

---

_Generated by `/tech-debt-report`_
