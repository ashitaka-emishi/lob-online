# Technical Debt Report — PR #305

**PR:** #305
**Date:** 2026-04-12
**Reviewer(s):** /team-review (security, performance, architecture)

## Fixed Findings

2 finding(s) fixed in place during this review cycle.

- [FIXED] M1 — Extract `wrapTableRoute` HOF + `requireNumber`/`requireEnum` helpers in `tableTest.js` (11-endpoint validation boilerplate)
- [FIXED] M2 — Extract `useTableFetch` composable and update all 11 panel SFCs

## Deferred Findings

| Issue | Title                                                                            | Score | Assessment                                                                                                                                                                        |
| ----- | -------------------------------------------------------------------------------- | ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #306  | table-test: filter modifier objects against property allowlist before forwarding | 1     | Dev-only tool with 5MB body limit and engine destructuring already neutralizing unknowns. Pure defense-in-depth; no functional risk today.                                        |
| #307  | table-test: add allowlist validation for incomingResult on /morale-transition    | 1     | Engine returns null for unknown values which the route converts to a 400. Inconsistency with the other validated parameters but no correctness hazard.                            |
| #308  | table-test: standardize modifier param name — modifiers vs mods inconsistency    | 1     | Naming inconsistency between `/morale` (`modifiers`) and `/closing-roll` (`mods`). Both work correctly; only affects API learnability.                                            |
| #309  | table-test: extract defaultModifiers() factory in MoralePanel                    | 1     | Default modifiers object duplicated between `ref()` init and `reset()`. No coupling risk; only matters when a new modifier flag is added to the Morale Table.                     |
| #310  | table-test: convert activePanel() function to computed in TableTestView          | 1     | `PANELS.find()` runs on every render rather than being cached. Scanning 11 static items is imperceptible; this is a Vue idiom correctness issue, not a performance problem today. |
| #311  | table-test: lazy-load panel SFCs with defineAsyncComponent in TableTestView      | 1     | All 11 panels imported eagerly. Dev-only tool with small SFCs; no perceptible load impact today. Good practice for when panel complexity grows.                                   |

**Total debt added this PR:** 6

---

_Generated by `/tech-debt-report`_
