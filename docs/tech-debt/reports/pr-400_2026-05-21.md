# Technical Debt Report — PR #400

**PR:** #400
**Date:** 2026-05-21
**Reviewer(s):** Claude Sonnet 4.6 (5-dimension parallel team review: security, architecture, testing,
maintainability, accessibility)

## Fixed Findings

13 findings fixed in place during this review cycle:

- H1 [Testing] — unit-click → selectUnit wiring test added to `GameView.test.js`
- H3 [Maintainability] — loading banner and OOB error banner added to `GameView.vue` template
- H4 [Accessibility] — `aria-label`, `role="button"`, and `tabindex="0"` added to counter `<image>` elements
- H5 [Accessibility] — keyboard handler (`keydown` Enter/Space) added to `UnitCounterLayer`
- M6 [Security] — `counterFile` sanitized via `SAFE_FILENAME_RE` before use in SVG `href`
- M7 [Architecture] — counter image base path extracted to `counterBasePath` prop (default `/counters/`)
- M9 [Testing] — `displayUnits` filtering and OOB-mapping logic tested
- M10 [Testing] — OOB fetch error path tested (component renders safely with empty units)
- M11 [Maintainability] — side fallback unified to `null` in both `displayUnits` and `selectedDisplayUnit`; `UnitStatsPanel` shows "Unknown" for null side
- M12 [Accessibility] — `aria-live="polite"` region added to `UnitStatsPanel` for selection announcements
- M13 [Accessibility] — `dt` color improved from `#8a7860` to `#a09278`, empty-state from `#6a5a40` to `#8a7860`
- L16 [Testing] — shaken, routed, and null-side morale state tests added to `UnitStatsPanel.test.js`
- L17 [Accessibility] — `role="application"` and `aria-label="Hex map overlay"` added to `HexMapOverlay` SVG

## Second-Pass Review

NOT REQUIRED — review fixes were limited to accessibility attributes, a CSS color change, a validation
regex, a new Vue prop, test additions, and a two-line template guard. No changes to auth, sessions,
persistence, rules-engine logic, or shared store/API boundaries. Total production LOC changed is well
under the 300-line trigger threshold.

## Deferred Findings

| Issue | Title                                                    | Score | Assessment                                                                                                                                                                                    |
| ----- | -------------------------------------------------------- | ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #401  | Extract OOB enrichment from GameView into a composable   | 3     | OOB fetch + walk + computed live in the view layer. Any future game view (M6, M7) that needs OOB metadata will duplicate this logic. Extraction to `useOobData.js` resolves it cleanly.       |
| #402  | Unify GameView.vue fetch strategies (store vs raw fetch) | 2     | Three different loading patterns in one `onMounted`: store action, raw fetch ×2. Low coupling risk today; natural resolution at M8 when dev-tool endpoints are replaced by production routes. |
| #403  | Add Content-Security-Policy headers to Express server    | 2     | No CSP on the Express server. Low risk in dev-only deployment; becomes a meaningful surface when M8 ships public upload routes. Address with `helmet()` at M8 auth hardening.                 |
| #404  | Extract `_collectOobUnits` for unit-testability          | 1     | Module-private function in `GameView.vue`; only exercised via integration tests. Naturally resolved when #401 (useOobData composable) is implemented.                                         |

**Debt added this PR:** 8
**Debt closed this PR:** 0
**Net delta:** +8 (regression — 4 new deferred items)

---

_Generated by `/tech-debt-report`_
