# Technical Debt Report — PR #227

**PR:** #227
**Date:** 2026-04-02
**Reviewer(s):** agent-teams (security-reviewer, performance-reviewer, architecture-reviewer)

## Fixed Findings

13 finding(s) fixed in place during this review cycle.

1. `vi.mock('fs')` placed inside `beforeEach` — hoisted to module load regardless; misleading placement
2. JSON extraction regex `/\{[^}]*\}/` breaks on nested objects — replaced with brace-matching `extractFirstJson()`
3. Dynamic MIME type interpolation `image/${ext}` — replaced with `MIME_MAP` constant + null guard
4. No path traversal guard in `main()` after `join()`
5. `collectOOBRecords` missing `supplyTrain` node — counterRefs on supply train invisible to matcher
6. `collectOOBRecords` missing corps `hq` node — counterRefs on corps HQ invisible to matcher
7. Header comment claims threshold ≥ 0.80 but actual threshold is 0.0
8. `vitest.config.js` mixed scripts tests into server project — no isolation
9. No tests for `extractFirstJson`
10. No tests for `identifyCounter`
11. No tests for `loadJSON` / `saveJSON`
12. No tests for `collectLeaderRecords`
13. `writeCounterRef` tests did not assert null initialization of sibling fields

## Deferred Findings

None — all findings fixed in place.

**Total debt added this PR:** 0

---

_Generated by `/tech-debt-report`_
