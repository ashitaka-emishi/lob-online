# Technical Debt Report — PR #391

**PR:** #391
**Date:** 2026-05-18
**Reviewer(s):** team-review (security, architecture, testing, maintainability, domain)

## Fixed Findings

18 findings fixed in place during this review cycle:

- [High/Domain] Inaccurate Union detachment comment (1st Corps detaches one Division, not Brigade)
- [Medium/Maintainability] `/** @type {const} */` replaced with `Object.freeze()` to match project convention
- [Medium/Architecture] Moved `phases.js` to `server/src/constants/` (neutral dep layer reachable by both schema and engine); schema now imports PHASES and uses `Object.values(PHASES)` for the phase enum — single source of truth
- [Medium/Architecture] Client/wire access resolved by same constants move (client can import from `constants/phases.js`)
- [Medium/Testing] Added compound-failure test: `isDetached:true` + structurally invalid `UnitOrderState`
- [Medium/Domain] Added OrderType deferral note for Reserve (§10.4c) and Association (§10.8e)
- [Low/Security] Added route-layer TODO on INVALID_STATE message sanitization
- [Low/Architecture] Added intentional-collision comment on STEPS.RALLY / PHASES.RALLY
- [Low/Architecture] Extracted `assertEnvelope()` helper — eliminates duplicate guard blocks and prepares for M6 envelope additions
- [Low/Architecture] Envelope guard comment updated to cite schema biconditional location and define "envelope"
- [Low/Architecture/Security] Schema evolution note added as inline comment near `.default(false)` usage
- [Low/Testing] Fixed double-invocation pattern: added `expect.assertions(3)` and single try/catch with message specificity
- [Low/Testing] Added rally-phase corruption test (third envelope-guard case)
- [Low/Testing] Message assertions now pin diagnostic text (e.g. `/ordersPhase is non-null outside command phase/`)
- [Low/Maintainability] drainAutoSteps guard comment cites schema file location; defines "envelope"
- [Low/Maintainability] Duplicate guard blocks eliminated via `assertEnvelope()` helper
- [Low/Maintainability] Conductor `index.md` updated to reflect completed track state
- [Low/Domain] `orders` field comment updated: labeled as SM modeling convention (§10.3f relay), not universal LOB rule

## Second-Pass Review

NOT REQUIRED

The fix diff touched the schema (Zod refinements) and rules-engine (`drainAutoSteps`) — normally trigger surfaces. However, all schema changes are pure substitution of string literals with equivalent constant references (`z.enum(['command', 'activity', 'rally'])` → `z.enum(Object.values(PHASES))`); all refinement comparisons replaced string literals with constants that resolve to identical values. The `assertEnvelope()` extraction is a mechanical restructuring — identical error code, message template, and guard condition. No new logic, no new conditionals, no new branch paths. All 2126 tests pass.

## Deferred Findings

None — all findings fixed in place.

**Debt added this PR:** 0
**Debt closed this PR:** 17 (issues: #362, #365, #366, #367, #368, #369, #372, #388, #389)
**Net delta:** −17 (net improvement)

---

_Generated by `/tech-debt-report`_
