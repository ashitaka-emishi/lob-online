# Technical Debt Report — PR #297

**PR:** #297
**Date:** 2026-04-12
**Reviewer(s):** agent-teams parallel reviewer (security, performance, architecture)

## Fixed Findings

9 finding(s) fixed in place during this review cycle.

1. HIGH-1: `commandRange()` redundant `buildHexIndex()` — added optional `hexIndex` param; route passes cached index
2. HIGH-2a: Missing enum allowlist validation for `formation` and `commanderLevel` in all route handlers
3. HIGH-2b: No `try/catch` around engine calls in route handlers — added with HTTP 500 responses
4. MED-1: `MovementPathPanel` fillFn accessed reactive `result.value` inside closure — snapshotted as Set at emit time
5. MED-2: `LosPanel` fillFn accessed reactive refs inside closure — snapshotted all result data before emit
6. MED-3: User-supplied hex value reflected verbatim in `/hex-info` 404 error message — sanitized
7. LOW-1: `MovementRangePanel` fillFn accessed reactive `originHex.value` inside closure — snapshotted as local const
8. LOW-6: No comment explaining origin hex is intentionally classified as `withinRadius` — added
9. Global Express error handler missing from `server.js` — added

## Deferred Findings

| Issue | Title                                                        | Score | Assessment                                                                                                                                                                                                                             |
| ----- | ------------------------------------------------------------ | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #298  | refactor: extract shared panel CSS across five SFCs          | 2     | Identical scoped CSS for `.panel`, `.panel-header`, `.hint`, `.clear-btn`, `.loading`, `.error` duplicated across all five panel SFCs. No functional risk, but a visual consistency fix requires touching five files simultaneously.   |
| #299  | refactor: extract `mockFetch` test utility from panel tests  | 2     | `vi.stubGlobal('fetch', ...)` helper duplicated across all five panel test files. Test-only duplication; a mock contract change must be applied in five places independently.                                                          |
| #300  | test: add MapTestView orchestration tests                    | 3     | `MapTestView.vue` orchestrator has no test coverage. `togglePanel`, overlay routing, and click dispatch are untested. A future accordion/panel refactor could silently break tab-switch state reset with no failing tests to catch it. |
| #301  | refactor: replace v-if dispatch table with dynamic component | 2     | Adding a sixth panel requires touching both the `PANELS` array and the v-if template block. Low coupling risk today but becomes friction as M4 adds more tool panels.                                                                  |
| #302  | fix: add hex-ID format validation before engine calls        | 3     | All five map-test route handlers accept hex IDs directly from query strings with no format guard. A malformed hex ID (non-numeric, path-traversal string) reaches engine code and can propagate NaN through Dijkstra cost comparisons. |
| #303  | refactor: decouple map-test from map-editor data endpoint    | 3     | `MapTestView.vue` fetches from `/api/tools/map-editor/data`. If the map-editor route is renamed or split, map-test silently breaks with no compile-time or test-time warning. Cross-tool implicit coupling.                            |
| #304  | refactor: lazy data load in `mapTest.js`                     | 2     | `loadMap()`, `loadScenario()`, and `buildHexIndex()` called at module import time. A data-load failure throws during startup with no HTTP error path, making diagnosis harder than a request-time 500.                                 |

**Total debt added this PR:** 17

---

_Generated by `/tech-debt-report`_
