# Technical Debt Report — PR #548

**PR:** #548
**Date:** 2026-06-13
**Reviewer(s):** Claude Sonnet 4.6 (team-review: security, architecture, testing, maintainability)

## Fixed Findings

9 finding(s) fixed in place during this review cycle:

- **M1** — Rate limiter ordered after guard in `moduleData.js`; swapped so throttling applies even when editor is disabled
- **M2** — `visibilityHexes` Zod error message used hardcoded numbers; rebuilt from `VISIBILITY_MAX`/`VISIBILITY_UNLIMITED` constants
- **M3** — No sync test for sister-module constants; added `server/src/engine/turnTime.test.js` that imports both and deep-compares
- **M4** — `useMapPersistence` had no test for PUT/save URL; added assertion that `save()` targets `/api/v1/modules/SM/map`
- **M5** — `useMapPersistence.test.js` emitted `console.error` in error-path tests; added `vi.spyOn` suppression in `beforeEach`
- **L1** — `fog`/`rain` entries in both `turnTime.js` files lacked LOB citations; added inline comments noting no separate LOB clause
- **L2** — `VISIBILITY_UNLIMITED` lived in `turnTime.js` (wrong concern); extracted to `client/src/config/visibility.js` with `VISIBILITY_MAX`
- **L3** — Schema test used positional `issues[0].message` which is order-dependent; replaced with `.some()` predicate
- **L4** — URL construction tests didn't assert `console.warn` behavior; added `toHaveBeenCalled()` on legacy path and `not.toHaveBeenCalled()` on module-scoped path

## Second-Pass Review

NOT REQUIRED

Not required — review fixes were limited to middleware reorder, constant extraction, test spy suppression, and inline comment additions. No auth, game state, schema behavior, or rules-engine logic was altered.

## Deferred Findings

None — all findings fixed in place.

**Debt added this PR:** 0
**Debt closed this PR:** 18 (issues: #540, #541, #531, #532, #534, #535, #536, #537, #543)
**Net delta:** −18 (net improvement)

---

_Generated by `/tech-debt-report`_
