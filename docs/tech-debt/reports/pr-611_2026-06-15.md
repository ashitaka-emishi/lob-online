# Technical Debt Report — PR #611

**PR:** #611
**Date:** 2026-06-15
**Reviewer(s):** AI parallel reviewers — security, architecture, testing, maintainability, domain

## Fixed Findings

0 findings fixed in place during this review cycle. All findings were deferred as GitHub issues.

## Second-Pass Review

NOT REQUIRED — review fixes were limited to filing debt issues. No production code was modified during the review cycle; all findings are deferred to M6/M7 cleanup tracks.

## Deferred Findings

| Issue | Title                                                                                                                       | Score | Assessment                                                                                                                                                                                                                                                                |
| ----- | --------------------------------------------------------------------------------------------------------------------------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- | --------------------------------------------------------------------------------- |
| #612  | Security: add Zod payload schema for CLOSE_COMBAT — route forwards raw payload without field-level validation               | 4     | The route forwards `req.body.payload` raw; in-handler checks don't validate types or integer bounds. `closingDie: 3.7` or `closingDie: "abc"` passes the `< 1                                                                                                             |     | > 6` gate and corrupts deterministic game state on a shared authoritative server. |
| #616  | Domain: confirm LOB §7.0 close combat SP gate is ≥4 (inclusive) and uses printed vs current SPs                             | 3     | Gate uses `attackerSPs >= 4` per plan wording but ≥4 vs >4 cannot be confirmed from code. Also uses printed OOB SPs — LOB §7.0 "engaged SPs" may intend current strength. Edge case at exactly 4 SPs; tests don't cover the exclusive boundary.                           |
| #617  | Domain: confirm LOB §9.1a leader loss scope — §7.0c automatic loss only, or any SP loss?                                    | 3     | `leaderLossCheckRequired: defenderSpLoss > 0` couples leader loss only to the §7.0c automatic gate. A <4-SP charge that inflicts loss via Opening Volley or cascade would skip the leader-loss check entirely.                                                            |
| #613  | Domain: verify LOB §6.3 rally roll thresholds (A=10…F=5) and 2d6-roll-under mechanic before M7 wiring                       | 3     | `rallyRollResult()` is dead code at M6 — `drainAutoSteps` auto-advances and discards the result. A wrong threshold value would go undetected until M7 interactive dice are wired. Thresholds are plausible but unverified against the LOB §6.3 table.                     |
| #614  | Refactor: extract effectiveSPs() helper — DG-halving SP accumulation duplicated in closeCombat.js, fireCombat.js (LOB §5.3) | 2     | DG-halving effective-SP accumulation (`Math.floor(printed / 2)`) appears in three locations, all citing LOB §5.3. Future §5.3 errata would require finding and editing every copy with drift risk.                                                                        |
| #615  | Refactor: collapse applySection64AutoRecovery two-pass algorithm into single pass                                           | 2     | Pass 2 re-scans `updated` while checking original `units[id]?.moraleState`, relying on an implicit invariant (shaken+cbfMarker units are untouched in pass 1). A future edit to the pass-1 chain could silently break the invariant.                                      |
| #618  | Domain: confirm LOB §6.4 shaken two-step (shaken→normal) vs DG one-step (DG→shaken) asymmetry                               | 2     | Shaken-no-CBF auto-recovers two steps to normal; DG improves one step to shaken. Likely per §6.4 but unusual asymmetry. DG→shaken flip is also unconditional (not gated on cbfMarker) — needs confirmation.                                                               |
| #619  | Test: fix ordering-invariant test in rally.test.js — DG unit cannot distinguish §6.4-before-§8.1 ordering                   | 2     | The "§6.4 runs BEFORE §8.1" test uses a disorganized unit; DG→shaken is unconditional and ignores cbfMarker, so both orderings yield the same result. The test cannot fail and provides zero protection for the invariant it claims to guard.                             |
| #620  | Test: add bloodlust, 2d6 boundary, and normal-state rally roll coverage in rally.test.js                                    | 2     | No test asserts bloodlust units are unchanged by `applySection64AutoRecovery`. No test covers `diceRoll = 2` (always-succeed boundary) or `diceRoll = 12` (always-fail). `rallyRollResult` fallback for already-normal state is untested.                                 |
| #621  | Domain: reconcile Fluke Stoppage/Attack Recovery rule input (unit MA vs leader Command Value) and section citations         | 2     | Plan prose describes rolling against "unit MA"; existing table functions use leader Command Value. Stub comment citations (§10.8c/§10.7b) differ from plan citations (§10.6b/§10.7). Deferral to M7 is correct but ambiguity should be resolved before M7 implementation. |
| #622  | Test/Docs: hoist oob3Sp fixture; fix vacuous conditional assertion; add JSDoc param to rallyRollResult                      | 1     | Three trivial cleanup items: (1) `oob3Sp` nested override copy-pasted in 3 tests; (2) cbfMarker assertion wrapped in `if (openingVolleySpLoss > 0)` — can pass vacuously; (3) `rallyRollResult` JSDoc missing `currentMoraleState` param.                                 |
| #623  | Cleanup: index.js \_pendingRallyRoll dead assignment; moraleCheckRequired unconditional; OV abort comment block             | 1     | `_pendingRallyRoll` is a dead assignment (TODO comment covers intent). `moraleCheckRequired: true` always true — asymmetric with gated `leaderLossCheckRequired`, needs a comment if intentional. OV abort described in 5-line comment block with no corresponding code.  |

**Debt added this PR:** 27
**Debt closed this PR:** 14 (issues: #578, #579, #581, #383, #382, #381)
**Net delta:** +13 (net regression — deferred domain verification work outweighs resolved implementation stubs)

---

_Generated by `/tech-debt-report`_
