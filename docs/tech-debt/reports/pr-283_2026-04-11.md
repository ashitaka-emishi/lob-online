# Technical Debt Report — PR #283

**PR:** #283
**Date:** 2026-04-11
**Reviewer(s):** agent-teams parallel review (security · performance · architecture)

## Fixed Findings

5 findings fixed in place during this review cycle.

1. **[FIXED H1]** `hex.js` — O(V²) Dijkstra linear priority queue replaced with binary min-heap (O((V+E) log V))
2. **[FIXED H2/M2]** `movement.js`, `los.js` — redundant hexIndex rebuild on every call eliminated; `movementPath`, `movementRange`, and `computeLOS` accept optional pre-built `hexIndex` parameter
3. **[FIXED H3]** `movement.js` — inconsistent `Object.values(mapData.hexes)` replaced with `for (const hex of mapData.hexes)` to match Zod schema definition (`z.array(HexEntry)`)
4. **[FIXED H4]** extracted `server/src/engine/map.js` — `loadMap` and `buildHexIndex` now live in a shared module; `los.test.js` imports `loadMap` from `map.js` directly
5. **[FIXED perf-M3]** `hex.js` `reconstructPath` — replaced O(n²) `unshift` loop with `push` + `reverse`

## Deferred Findings

| Issue | Title                                                                                            | Score | Assessment                                                                                                                                                                                                                      |
| ----- | ------------------------------------------------------------------------------------------------ | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #284  | engine: path traversal risk in loadMap/loadScenario — add directory containment guard            | 3     | Both loader functions accept arbitrary string paths with no containment check. Public signatures allow future callers to pass user-supplied input enabling path traversal. Error messages also leak internal file paths.        |
| #285  | engine: parseHexId has no input validation — NaN propagates silently                             | 2     | No guard against null/undefined/malformed input. NaN propagates through cube coordinate math and into Dijkstra cost comparisons, producing incorrect neighbors or silent infinite loops.                                        |
| #286  | engine: hexEntryCost has no dirIndex range validation                                            | 1     | Out-of-range dirIndex silently returns undefined from array lookups. Low risk since only the Dijkstra loop calls this today with validated indices.                                                                             |
| #287  | engine: combatResult silently accepts out-of-range diceRoll                                      | 2     | Roll of 1 gives array index -1 → undefined → silent "no effect". Roll > 12 also falls off the table silently. Should validate and throw or return a clearly-marked invalid sentinel.                                            |
| #288  | engine: movementPath cost breakdown always sets hexsideCost: 0                                   | 3     | Return type promises `{terrainCost, hexsideCost, total}` per step but hexsideCost is always 0. The planned Map Test Tool will display misleading per-hex breakdowns until this is fixed.                                        |
| #289  | engine: LOS terrain heights hardcoded; should be data-driven like movement costs                 | 3     | `TERRAIN_LOS_HEIGHT_FLAG` hardcodes terrain → LOS height bonus mappings in code while movement costs are fully data-driven. New terrain types for future scenarios silently get 0 LOS bonus without a code change.              |
| #290  | engine: inconsistent parameter ordering between movementPath and computeLOS                      | 2     | `movementPath` takes `(scenario, mapData)`; `computeLOS` takes `(mapData, scenario)`. Both are hex-pair functions taking the same data objects in opposite order — easy to swap silently since both are plain objects.          |
| #291  | engine: combat.js ammoTypeShift threshold check is tautological                                  | 2     | `if (firerSPs >= threshold)` where threshold is derived from the tier firerSPs falls into — the check can never fail. Either rules intent is misunderstood or the code is dead. Domain-expert consult required before changing. |
| #292  | engine: extract formations.js — FORMATION_EFFECTS and ACTIVITY_EFFECTS belong in separate module | 2     | `weapons.js` mixes weapon data with formation/activity charts from different LOB Charts pages. Developers looking for formation rules won't find them in `weapons.js`, reducing maintainability.                                |
| #293  | engine: loadMap/loadScenario use synchronous file I/O with no architectural guard                | 1     | Both use `readFileSync`. "Call once at startup" is documented but not enforced. For M4+ game loop, loaders should be async or enforced via a startup bootstrap to prevent event-loop blocking.                                  |
| #294  | engine: hex.js hot-path allocations — memoize formatHexId/parseHexId                             | 2     | `parseHexId` and `formatHexId` called ~13K+ times per Dijkstra run, each creating temporary allocations. Only worth addressing if profiling confirms bottleneck after H1/H2 fixes.                                              |
| #295  | engine: Dijkstra lacks early termination for point-to-point queries                              | 2     | `movementPath` passes `Infinity` maxCost, exploring the entire reachable graph even for single-target queries. Adding a `targetHex` early-exit would give 2–5× speedup for nearby pairs.                                        |
| #296  | engine: noEffectTerrain Set allocated inside hexEntryCost hot loop                               | 2     | `new Set(noEffectTerrain ?? [])` created inside `hexEntryCost` on every Dijkstra edge expansion (~13K calls per query, ~780K allocations per turn for 60-unit movement ranges).                                                 |

**Total debt added this PR:** 27

---

_Generated by `/tech-debt-report`_
