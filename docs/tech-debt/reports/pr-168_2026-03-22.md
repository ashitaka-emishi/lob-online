# Technical Debt Report — PR #168

**PR:** #168
**Date:** 2026-03-22
**Reviewer(s):** Claude Code (security, performance, architecture dimensions via /team-review)

## Fixed Findings

2 finding(s) fixed in place during this review cycle.

1. **[FIXED]** Extract `EDGE_SNAP_THRESHOLD_PX = 6` named constant — links the three snap threshold
   call sites in `onSvgContextMenu`, `onSvgMouseDown`, and `onSvgMouseMove` to a single source of
   truth instead of a bare magic number repeated across two files.

2. **[FIXED]** Update `findNearestEdge` JSDoc `@returns` annotation to document the new `dist` field
   (`{hexId:string, dir:string, dist:number}|null`).

## Resolved Prior Debt

4 previously deferred items were resolved by the implementation in PR #168.

| Issue | Title                                                          | Score |
| ----- | -------------------------------------------------------------- | ----- |
| #159  | perf: deduplicate findNearestEdge call in HexMapOverlay        | 2     |
| #160  | perf: add rAF throttle to HexMapOverlay edge-hover path        | 2     |
| #163  | perf: move inline v-for filter expression to computed          | 1     |
| #164  | perf: memoize wedgePolygonPoints to avoid per-render recompute | 1     |

**Total debt resolved this PR:** 6

## Deferred Findings

| Issue | Title                                                             | Score | Assessment                                                                                                                                                                                                                                                                                                                                                                   |
| ----- | ----------------------------------------------------------------- | ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #169  | refactor: extract cellsForEdges into useEdgeLineLayer composable  | 2     | `cellsForEdges` invalidates on any `gridData` change (including LOS/selection state) because it depends on `cells`, which is downstream of the monolithic `gridData` computed. The structure also encodes the template's nested v-for shape in script setup. Natural fit for extraction into `useEdgeLineLayer(cells, overlayConfig)` during overlay-arch-refactor_20260322. |
| #170  | test: add integration test for onSvgMouseMove rAF callback output | 1     | The rAF gate tests verify scheduling but not the callback's output path (coordinate transform → findNearestEdge → snap → hoverInfo). Individual pieces are unit-tested; this gap is narrow but would catch regressions in the full pipeline.                                                                                                                                 |

**Total debt added this PR:** 3

---

_Generated by `/tech-debt-report`_
