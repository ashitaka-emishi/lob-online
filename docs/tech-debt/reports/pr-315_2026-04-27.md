# Technical Debt Report — PR #315

**PR:** #315
**Date:** 2026-04-27
**Reviewer(s):** Claude (team-review: security, architecture, performance, testing)

## Fixed Findings

13 finding(s) fixed in place during this review cycle.

**Pre-review fixes (committed during implementation):**

- Dead `threshold` local variable removed from `combat.js`
- `_startupError` changed from Error object retention to boolean flag in `mapTest.js`
- `loadMap`/`buildHexIndex` import moved from `movement.js` re-export to direct `map.js` import
- `Number()` double-coercion eliminated in `pickMods` numeric path (`tableTest.js`)
- `movement.js` re-export of `loadMap`/`buildHexIndex` removed (inverted dependency)
- `formations.js` JSDoc clarified (pre-positioned status, no production callers yet)

**Post-review fixes (in response to team-review findings):**

- `oob.schema.js` + `leaders.schema.js` — `EntityId`/`EntityName` bounded primitives applied to all entity `id`/`name`/`commandsId`/`successionIds` fields (#316)
- `TableTestView.vue` — `<KeepAlive>` added around `<component :is>` (#317)
- `table-test/test-utils/mockFetch.js` created; 11 inline helpers replaced with import (#318)
- `MapTestView.vue` — comment added documenting intentional eager loading (#319)
- `morale.js` + `charge.js` — `MORALE_BOOL_MOD_KEYS`, `MORALE_NUM_MOD_KEYS`, `CLOSING_BOOL_MOD_KEYS` exported; `tableTest.js` imports them (#320)
- `tableTest.test.js` — `__proto__` regression test added for `pickMods` (#321)
- `panel-shared.css` renamed to `panel-base.css`; 5 SFC imports updated (#323)

## Deferred Findings

| Issue | Title                                                            | Score | Assessment                                                                                                                                                                                                    |
| ----- | ---------------------------------------------------------------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #322  | feat: add numeric bounds validation in pickMods                  | 2     | `pickMods()` coerces numeric modifiers but applies no min/max bounds. Valid ranges require domain knowledge of each modifier's game rule; deferring until M4 adds callers that can inform the correct bounds. |
| #324  | perf: hoist noEffectTerrain Set construction out of hexEntryCost | 2     | `hexEntryCostBreakdown` allocates `new Set()` per call when invoked without a pre-built Set. No high-frequency public callers today; deferred until M4 profiling confirms it is a bottleneck.                 |

**Debt added this PR:** 4
**Debt closed this PR:** 21 (issues: #304, #301, #299, #298, #292, #291, #311, #310, #309, #306, #293, #286, #259, #256, #255)
**Net delta:** -17 (net improvement)

---

_Generated by `/tech-debt-report`_
