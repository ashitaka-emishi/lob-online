# Technical Debt Report — PR #697

**PR:** #697
**Date:** 2026-08-13
**Reviewer(s):** security-reviewer, architecture-reviewer, testing-reviewer, maintainability-reviewer (initial pass); second-pass-reviewer (targeted, validate-data.js trigger)

## Fixed Findings

20 findings fixed in place during this review cycle (0 deferred). Initial pass: 4 Medium, 10
Low/grouped-Low across security, architecture, testing, and maintainability dimensions.
Second-pass: 3 Low + 2 nits, all addressed.

Highlights:

- `scripts/validate-data.js`'s direct-run guard (added earlier in this same PR to make the
  script importable for testing) failed open — flagged independently by all four initial
  reviewers and confirmed by empirical reproduction (space-in-path, symlink, and combined).
  Fixed via `pathToFileURL(realpathSync(...))`; mutation-verified before and after.
- `checkGridCoverage` counted raw hex records instead of distinct hex IDs (duplicate-ID blind
  spot) and silently no-op'd when `gridSpec.cols`/`rows` were absent, inconsistent with this
  schema's own fail-closed precedent. Both fixed.
- The `checkGridCoverage` boundary-marker test was vacuous — mutation-verified that removing
  the row-bounds filter left the suite green. Rewritten to be genuinely discriminating.
- Track `spec.md` was left at `Status: Draft` with all 7 acceptance criteria unchecked despite
  `plan.md`/`metadata.json`/`index.md` all reflecting completion — the same recurring
  spec/plan drift bug class this project has hit before.
- `docs/tech-debt/report.md`'s Debt Over Time table was missing its main `PR #697` row (Net
  Delta −8 unrecorded), breaking the table's own audit-trail convention.
- The `playable` schema field's scope comment mislabeled `engine/edge-strip.js` as
  "client-side rendering" when it's a server-side save-path stripping transform.
- `client/src/utils/calibration.js`'s `DEFAULT_CALIBRATION.cols` had no regression guard
  against the exact drift class (#693) this PR closed; added a cross-layer contract test.
- Second pass caught the guard fix itself had zero regression coverage (a spawn-based test was
  added — mutation-verified) plus a shared client fixture (`client/src/test/fixtures.js`) with
  the same stale `cols: 64` under a now-false "matches the real SM scenario dimensions" comment.

## Second-Pass Review

COMPLETED

Triggered because the initial-pass fix diff touched `scripts/validate-data.js`'s core
validation logic (a data-validation gate), an explicit trigger surface per this project's
second-pass policy. The targeted pass (security + architecture concerns) verified all initial
fixes were correct via independent reproduction and found 3 additional Low findings plus 2
nits — all fixed in place. No further review cycle required.

## Deferred Findings

None — all findings fixed in place, per this project's rule that debt-cleanup PRs must not
generate new deferred debt.

**Debt added this PR:** 0
**Debt closed this PR:** 8 (issues: #693, #694, #695, #696 — resolved in an earlier commit on
this PR, prior to this review cycle)
**Net delta:** -8 (net improvement)

---

_Generated by `/tech-debt-report`_
