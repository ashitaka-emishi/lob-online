# Technical Debt Report — PR #236

**PR:** #236
**Date:** 2026-04-03
**Reviewer(s):** agent-teams (security, architecture, testing)

## Fixed Findings

1 finding fixed in place during this review cycle.

- **[FIXED] H2 — `_executePush` silently ignored succession push failure** (`useOobPersistence.js:181`): `Promise.all` result was destructured as `[oobRes, leadersRes]`, dropping the succession response. A failed succession PUT would clear `dirty` and purge localStorage — silent data loss. Fixed by switching to `results.find(r => !r.ok)` covering all responses uniformly.

## Deferred Findings

| Issue | Title                                                                             | Score | Assessment                                                                                                                                                                                                                                                                                                                       |
| ----- | --------------------------------------------------------------------------------- | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #237  | fix(oobTreeTransform): processUSACavDiv should delegate to withLeader             | 3     | `processUSACavDiv` inlines variant-attachment logic instead of using `withLeader`. Farnsworth's succession variants will never render. Any future change to `withLeader` silently diverges. Architectural consistency gap.                                                                                                       |
| #238  | test(succession.schema): add dedicated schema test file                           | 2     | No `succession.schema.test.js`. Schema regressions (wrong field types, missing required fields) won't be caught. Runtime Zod validation still protects on server load, but field-level edge cases are dark.                                                                                                                      |
| #239  | test(useOobStore): add succession state and mockFetch coverage                    | 2     | `store.succession` init, load, `usedCounterFiles`, and push cleanup are untested. `mockFetch` wraps at 2 responses so the succession fetch silently receives wrong data — tests may be passing for the wrong reason.                                                                                                             |
| #240  | test(SuccessionList): add isVariant rendering and allLeaders merge tests          | 2     | `allLeaders` merge with succession variants and `isVariant` flag driving the `(variant)` tag are entirely untested. A regression in display would not be caught.                                                                                                                                                                 |
| #241  | fix(successionEditor): wrap writeFile in try/catch                                | 2     | Main `writeFile` has no try/catch — a disk-full or permissions error produces an unhandled rejection that may expose stack traces. Pre-existing pattern in all 5 editor routes; should be fixed cross-cuttingly.                                                                                                                 |
| #242  | refactor(schemas): extract shared CommandLevel enum and CounterRef shape          | 2     | `CommandLevel` enum and `CounterRef` shape are duplicated verbatim in `succession.schema.js` and `leaders.schema.js`. Adding a new command level requires updating both files independently.                                                                                                                                     |
| #243  | refactor(oobTreeTransform): buildDisplayTree 4-arg positional API                 | 2     | Two early-return tests pass `'union'` as the `succession` arg (old 3-arg call). Tests pass vacuously via null-guard; the wrong-arity calls do not exercise the correct contract. API design invites future callers to get arg order wrong.                                                                                       |
| #244  | docs(useOobPersistence): rename \_isValidSidedShape and document shape families   | 1     | `_isValidSidedShape` is now misleading — succession uses arrays not objects. A comment and rename prevent future contributor confusion when adding a fourth data file.                                                                                                                                                           |
| #245  | refactor(server/routes): extract editorRouteFactory                               | 3     | `successionEditor.js` is the third near-verbatim copy of `leadersEditor.js`. Backup rotation logic is duplicated across 3 files. A bug fix to that logic must be applied independently in 3 places.                                                                                                                              |
| #246  | test(oobTreeTransform): add union-side variant and cavalry division variant tests | 2     | All succession variant tests cover only the CSA brigade path. Union corps variants, cavalry division Pleasonton variants (bespoke path, see #237), and multiple variants per leader are untested.                                                                                                                                |
| #247  | test(useOobPersistence): add dedicated test coverage for succession paths         | 3     | No `useOobPersistence.test.js`. Core composable for all data I/O extended significantly in this PR. Untested: `_isValidSuccessionShape` rejection, localStorage partial load, push skipping null succession, pull with succession endpoint down. Edge cases around partial data availability could corrupt store state silently. |
| #248  | test(OobTreeNode): add component test for leader-variant rendering                | 2     | No `OobTreeNode.test.js`. The `leader-variant` nodeType, `'Var'` badge, and `_variants` sibling rendering added in this PR have zero component-level coverage.                                                                                                                                                                   |
| #249  | test(successionEditor): add GET error path and field-level rejection tests        | 1     | GET with missing file (ENOENT) untested. Field-level Zod rejection better addressed by #238. Minor hygiene gap matching what other editor test files already cover.                                                                                                                                                              |
| #250  | docs(oobTreeTransform): comment on withLeader explaining \_variants nesting       | 1     | `_variants` is nested on `_leader` (breaking the flat `_supply`/`_hq` convention) without explanation. A comment prevents future contributors from "fixing" semantically correct nesting.                                                                                                                                        |
| #251  | security: consider atomic writes for all editor routes                            | 2     | Non-atomic `writeFile` across all 5 editor routes — a crash mid-write could corrupt the data file. Backup enables manual recovery but not automatic. Should be addressed cross-cuttingly.                                                                                                                                        |

**Total debt added this PR:** 30

---

_Generated by `/tech-debt-report`_
