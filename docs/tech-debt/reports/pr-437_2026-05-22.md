# Technical Debt Report — PR #437

**PR:** #437
**Date:** 2026-05-22
**Reviewer(s):** Claude Sonnet 4.6 (security, architecture, testing, maintainability, accessibility — 5 parallel agents)

## Fixed Findings

13 findings fixed in place during this review cycle.

- **H1–H5:** `encodeURIComponent` on `scenarioId` URL, `.json()` try/catch routing parse errors to `mapConfigError`, `loadMap` cache test isolation (`_clearMapCacheForTests` + `beforeEach`), `SUPPORTED_SCENARIOS` allowlist + 404 guard in scenarios router, replaced ESM-incompatible `vi.spyOn(fsModule)` with a `_clearMapCacheForTests` round-trip test
- **M1–M2:** ARIA live region attributes on all three banners (`role`, `aria-live`, `aria-atomic`, `.sr-only`), `v-show`→`isVisible()` fix in `GameView.test.js`
- **M5–M9, L5:** Cache describe block `beforeEach` isolation, `calibration.test.js` sparse-input and empty-object tests, `useGameStore.test.js` `toEqual({ ...DEFAULT_CALIBRATION, ...STUB_GRID_SPEC })`, malformed JSON test, scenarios 404 + improved 503 tests (body assertion + `errSpy`), `fixtures.js` jsdoc warning

## Second-Pass Review

NOT REQUIRED

Not required — review fixes were limited to defensive URL hardening (`encodeURIComponent`), error routing (JSON parse error → `mapConfigError`), ARIA attributes, and test improvements. No auth/session/token, persistence, rules-engine, or broad multi-file refactor surfaces were touched.

## Deferred Findings

| Issue | Title                                                          | Score | Assessment                                                                                                                                                                                                                                         |
| ----- | -------------------------------------------------------------- | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #438  | arch: sanitizeCalibration called twice                         | 2     | `GameView` spreads `DEFAULT_CALIBRATION` over `gridSpec` and then calls `sanitizeCalibration`; the store already calls `sanitizeCalibration` at its boundary, creating redundant double-merge. No correctness bug; minor architectural redundancy. |
| #439  | cleanup: GridSpecSchema exported but unused                    | 1     | `GridSpecSchema` exported from `map.schema.js` has no production consumer. Trivial dead export; safe to remove or wire in when multi-scenario validation is added.                                                                                 |
| #440  | perf: sequential fetch latency in loadGame                     | 2     | `loadGame` serializes game-state and map-config fetches because `scenarioId` is only known after the first response. Could be parallelized with `scenarioId` in the URL or pre-passed. No user-visible issue at current scale.                     |
| #441  | bug: loadGame double-call race                                 | 2     | A second `loadGame` call while the first is in-flight may briefly expose stale `gridSpec`/`hexes` from the first call's map-config write. Low probability in current single-game UI; worth tracking before multi-game lobby.                       |
| #442  | test: 503 test ordering fragility                              | 2     | `vi.resetModules()` in the 503 test is not wrapped in `beforeEach`/`afterEach`; test isolation depends on run order. Safe at 5-test file size; will become a flakiness source as the file grows.                                                   |
| #443  | cleanup: useCalibration.js re-exports create dual import paths | 1     | Both `calibration.js` and `useCalibration.js` are valid import paths for `sanitizeCalibration`. Navigation confusion only; no functional risk.                                                                                                     |
| #444  | cleanup: STUB_GRID_SPEC name collision                         | 1     | Client `STUB_GRID_SPEC` intentionally omits six calibration fields that `sanitizeCalibration` fills from defaults. Name does not signal this shape difference to contributors. Trivial; fix with a rename to `STUB_GRID_SPEC_WIRE`.                |
| #445  | a11y: banner stack missing landmark semantics                  | 2     | Loading/error/warning banners are generic `<div>` elements; screen-reader users cannot navigate to the status area by landmark. Not WCAG-blocking today; worth addressing before public accessibility review.                                      |
| #446  | test: makeMultiFetch substring matching latent brittleness     | 2     | URL substring matching fires the first matching pattern; if a future endpoint URL is a substring of another, the wrong handler fires. Latent brittleness; no test broken today.                                                                    |
| #447  | test: no health-check for scenarios router startup             | 2     | No integration test exercises the real `loadMap` path at the server level. The 503 test covers failure; the success path (real map data, 200 + non-empty gridSpec) has no coverage.                                                                |

**Debt added this PR:** 17
**Debt closed this PR:** 17 (issues: #421, #422, #423, #424, #425, #426, #427, #428, #429, #430)
**Net delta:** 0 (neutral)

---

_Generated by `/tech-debt-report`_
