# Technical Debt Report — PR #489

**PR:** #489
**Date:** 2026-06-02
**Reviewer(s):** Claude team-reviewer agents (security, architecture, testing, maintainability, accessibility)

## Fixed Findings

15 findings fixed in place during this review cycle (4 Medium, 11 Low).

| Severity | Finding                                                                                                                               |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| Medium   | Space key auto-repeat toggled selection rapidly — fixed with `if (event.repeat) return`                                               |
| Medium   | Leaders `.json()` parse error was fatal — fixed with inner try/catch that degrades gracefully                                         |
| Medium   | `console.warn` in degradation tests unasserted (Warning-Free Policy) — added spy + assertion                                          |
| Medium   | `requireSide` JSDoc missing — added full response-code matrix documentation                                                           |
| Low      | Store barrel import in requireSide — reverted direct `gameSqlite` import (test mock conflict); coupling concern documented in comment |
| Low      | 409-before-403 ordering — documented as intentional in JSDoc                                                                          |
| Low      | `getGame` called on early-401 paths — added `not.toHaveBeenCalled()` assertions to all 3 tests                                        |
| Low      | INVALID_PAYLOAD verbatim client messages — added comment that throw sites must be client-safe                                         |
| Low      | Stable aria-label model — dropped Select/Deselect verbosity; `aria-pressed` carries state                                             |
| Low      | Stacked counters indistinguishable to SR — hex ID appended to aria-label when stackIndex > 0                                          |
| Low      | `displayName` duplicated in aria-label branches — extracted into renderUnits entries                                                  |
| Low      | `stackIndex` needed for stacking disambiguation — added to renderUnits entries                                                        |
| Low      | Overlapping degradation tests — oobUnitMap assertion added to deduplication test                                                      |
| Low      | Tab order follows data array — comment added; roving tabindex deferred as future enhancement                                          |
| Low      | games.test.js default active-game mock — comment improved explaining override pattern                                                 |

## Second-Pass Review

COMPLETED — review fixes touched `requireSide.js` (auth surface) and `useOobData.js` (shared composable), triggering the auth and API-boundary criteria. The `requireSide.js` changes were documentation-only (JSDoc + comment, no logic change); the `useOobData.js` change added an isolated try/catch for the leaders parse path with no auth or state-routing impact. No additional findings.

## Deferred Findings

None — all findings fixed in place.

**Debt added this PR:** 0
**Debt closed this PR:** 15 (issues: #476, #477, #478, #479, #480 — all score 3)
**Net delta:** −15 (significant net improvement)

---

_Generated by `/tech-debt-report`_
