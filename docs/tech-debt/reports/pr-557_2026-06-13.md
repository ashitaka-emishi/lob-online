# Technical Debt Report — PR #557

**PR:** #557
**Date:** 2026-06-13
**Reviewer(s):** Claude team-review (security, architecture, testing, maintainability)

## Fixed Findings

11 findings fixed in place during this review cycle (9 from primary review, 2 from second-pass).

Primary review:

1. [FIXED] Extract `errorHandler` to `middleware/errorHandler.js`; import in `server.js` and test (H1, M2)
2. [FIXED] Add `watch(moduleSlug, ...)` re-fetch to `ScenarioEditorView` and `MapEditorView` (M1)
3. [FIXED] Rename `_moduleSlug`/`_scenarioSlug` to drop underscore prefix in all 3 editors (L2)
4. [FIXED] Add `encodeURIComponent` to slug in `useMapPersistence` and `ScenarioEditorView` API_URL (L1)
5. [FIXED] Remove `totalTurns: 36` from `VALID_SCENARIO` fixture in `moduleData.test.js` (L3)
6. [FIXED] Add negative `totalTurns: -1` test to `scenario.schema.test.js` (L4)
7. [FIXED] Strengthen `firstTurn`/`lastTurn` to `toMatch(/^\d{2}:\d{2}$/)` in `scenario.test.js` (L5)
8. [FIXED] Add removal-path comment to `totalTurns` in `scenario.schema.js` (L6)
9. [FIXED] `errorHandler.test.js` now imports real handler (combined with H1 extraction) (M2)

Second-pass fixes:

10. [FIXED] Suppress `console.error` in 500-branch test; assert it was called (SP-L1)
11. [FIXED] Watch `[moduleSlug, scenarioSlug]` in `ScenarioEditorView` to cover scenario slug changes (SP-L3)

## Second-Pass Review

COMPLETED — fix diff touched shared Express middleware extraction (`errorHandler.js`) and editor orchestration (`watch()` additions in `ScenarioEditorView` and `MapEditorView`). Architecture verified correct: Vue computed deps update synchronously before watchers fire; `encodeURIComponent` harmless for alpha slugs; import path and circular-dependency checks passed. Two Low findings identified (console.error suppression, scenarioSlug watch gap); both fixed in place. One Low finding deferred (#558).

## Deferred Findings

| Issue | Title                                                             | Score | Assessment                                                                                                                                                                                                                                           |
| ----- | ----------------------------------------------------------------- | ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #558  | Add component test for #542 moduleSlug reactivity in editor views | 2     | No automated test asserts that changing `route.params.moduleSlug` triggers a re-fetch in the editor views. The `watch()` behavior is simple and visually confirmed, but a future refactor could silently drop it without CI catching the regression. |

**Debt added this PR:** 2
**Debt closed this PR:** 8 (issues: #542, #545, #546, #533)
**Net delta:** −6 (net improvement)

---

_Generated by `/tech-debt-report`_
