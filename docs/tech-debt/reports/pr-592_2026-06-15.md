# Technical Debt Report — PR #592

**PR:** #592
**Date:** 2026-06-15
**Reviewer(s):** Team Review (security, architecture, testing, domain, maintainability)

## Fixed Findings

6 finding(s) fixed in place during this review cycle:

- **C1** (Critical): Removed premature `closingRoll` and `moraleCheck` branches from `getValidActions()` — these surfaced `RESOLVE_MORALE` for pending types the handler cannot process, converting a safe empty-action state into a guaranteed throw-loop. Fixed in a follow-up commit on the same branch.
- **M3** (Medium): Added citation comment on the `ctx` parameter in `dispatch()` signature.
- **L1–L4** (Low): Naming, comment, and test fixture nits resolved inline during the C1 fix commit.

## Second-Pass Review

COMPLETED — The C1 fix touched `getValidActions()` in the shared rules-engine (`engine/actions/index.js`), which is an explicit trigger for a targeted second-pass review per `.claude/rules/sdlc.md`. The fix was small and local (two if-blocks removed, two test expectations corrected, 6 insertions / 16 deletions). No additional findings were surfaced. The behavior change is strictly safer — removes an invalid code path that would have thrown on dispatch.

## Deferred Findings

| Issue | Title                                                                                     | Score | Assessment                                                                                                                                                                                                     |
| ----- | ----------------------------------------------------------------------------------------- | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #593  | Perf: memoize loadOob/loadMap/getScenario in games.js — sync I/O on every action dispatch | 3     | `loadOob()`, `loadMap()`, and `getScenario()` are synchronous file reads called on every action, including non-combat actions. Blocks the event loop. Fix: cache at module-init or use a memoized singleton.   |
| #594  | Reliability: dispatch() ctx degradation silent — combat handlers fall back to stale data  | 3     | `dispatch(state, action, ctx = {})` defaults ctx to `{}`. Omitting ctx causes silent degradation in handlers rather than a fail-fast error. Future call sites can silently get incorrect LOS/range validation. |
| #595  | Test: add route-layer integration test for FIRE_COMBAT → RESOLVE_MORALE two-step          | 2     | No integration test covers the full combat dispatch sequence through the HTTP route layer. `games.test.js` mocks `dispatch()` entirely, leaving the ctx wiring fix without a regression test.                  |
| #596  | Perf: findOobUnit is O(n) tree walk — add unit-id index to oob.js for O(1) lookup         | 2     | `findOobUnit` performs a linear OOB tree walk on every call. Multiple calls per action in cascade-morale scenarios. Fix: build a flat `Map<unitId, unit>` index.                                               |
| #597  | Test: index.test.js soft-lock tests for closingRoll/moraleCheck were vacuous              | 1     | The original soft-lock tests asserted `getValidActions()` output without verifying `dispatch()` would complete. Tests passed vacuously while the handler would have thrown on the surfaced action.             |

**Debt added this PR:** 11
**Debt closed this PR:** 10 (issues: #571, #572, #573)
**Net delta:** +1 (net regression)

---

_Generated by `/tech-debt-report`_
