# Technical Debt Report — PR #174

**PR:** #174
**Date:** 2026-03-23
**Reviewer(s):** agent-teams (security, performance, architecture)

## Fixed Findings

4 finding(s) fixed in place during this review cycle.

1. **[FIXED]** `safeString` has no allowlist for orientation values — replaced with `safeOrientation` validating against `['flat', 'pointy']` (`useCalibration.js:26`)
2. **[FIXED]** `STORAGE_KEY` duplicated in `useCalibration.js` and `MapEditorView.vue` — exported from composable, imported everywhere (`useCalibration.js:3`)
3. **[FIXED]** `getEngineExport` over-exposed in composable return — removed from return; exported `stripPrivateFields` as named export for direct test access (`useMapExport.js:56`)
4. **[FIXED]** `serverSavedAt` flagged as unused by consumer — reviewed and retained: tests use it to set up conflict scenarios and it represents valid observable state. Reviewer finding was based on MapEditorView not consuming it in the template, but tests are valid consumers.

## Deferred Findings

| Issue | Title                                                                               | Score | Assessment                                                                                                                                                                                                                 |
| ----- | ----------------------------------------------------------------------------------- | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #175  | perf: add depth limit to stripPrivateFields to prevent deep-walk on malformed input | 1     | `stripPrivateFields` has no recursion depth guard. No exploit path exists — all callers pass Zod-validated mapData with bounded nesting. Theoretical risk only; defer until a practical need emerges.                      |
| #176  | refactor: add write-path validation to onCalibrationChange in useCalibration        | 1     | `onCalibrationChange` writes to localStorage without running the same `safeNumeric`/`safeBoolean`/`safeOrientation` guards used on the read path. Internal API with a single trusted caller. Acceptable asymmetry for now. |

**Total debt added this PR:** 2

---

_Generated by `/tech-debt-report`_
