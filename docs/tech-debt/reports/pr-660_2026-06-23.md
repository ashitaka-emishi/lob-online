# Technical Debt Report — PR #660

**PR:** #660
**Date:** 2026-06-23
**Reviewer(s):** Claude Code security, architecture, maintainability agents (parallel `/team-review`)

## Fixed Findings

13 findings fixed in place during this review cycle:

1. [FIXED] State bucket = app bucket — `terraform destroy` deletes own state (Critical)
2. [FIXED] Wrong env var names in cloud-init (`SESSION_SECRET`/`DB_PATH` instead of `JWT_SECRET`/`SQLITE_PATH`) (Critical)
3. [FIXED] App started by cloud-init before secrets populated — PM2 crash-loop on `CHANGE_ME` (Critical)
4. [FIXED] Port 3000 exposed to internet, bypassing nginx (High)
5. [FIXED] App ran as root — PM2 and Node process (High)
6. [FIXED] Provider version pins unbounded `>= 2.0` / `>= 6.0` (High)
7. [FIXED] PM2 log dir `/var/log/lob-online` never created (Medium)
8. [FIXED] Region variable does not control backend endpoint — silent split-brain if changed (Medium)
9. [FIXED] `DO_SSH_USER` hardcoded `"root"` literal in github.tf (Medium)
10. [FIXED] SSH fingerprint docs assumed `id_rsa` only — excluded ed25519/ecdsa users (Medium)
11. [FIXED] `cloud-init status --wait` missing from provisioning guide (Medium)
12. [FIXED] NodeSource install via unverified `curl | bash` with no GPG (Medium)
13. [FIXED] PROVISIONING.md bucket names, env var table, destroy warning, Step 5 resource count inconsistent (Medium)

## Second-Pass Review

NOT REQUIRED — review fixes were limited to Terraform config files, cloud-init YAML, and documentation. No app code, auth surfaces, persistence schema, rules-engine logic, or shared Vue stores were touched.

## Deferred Findings

None — all findings fixed in place.

**Debt added this PR:** 0
**Debt closed this PR:** 0 (no open register items resolved)
**Net delta:** 0 (neutral)

---

_Generated by `/tech-debt-report`_
