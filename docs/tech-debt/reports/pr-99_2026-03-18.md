# Technical Debt Report — PR #99

**PR:** #99
**Date:** 2026-03-18
**Reviewer(s):** Claude (agent-teams: security, architecture, testing)

## Fixed Findings

2 finding(s) fixed in place during this review cycle.

- **M2** — null guard added to `updateElevationSystem` in `CalibrationControls.vue` to prevent
  spreading `null` into an incomplete `elevationSystem` object.
- **L1** — `onElevationSystemChange` in `MapEditorView.vue` restored whitelist + bounds validation
  that was removed when `onElevationLevelsChange` was replaced.

## Deferred Findings

| Issue | Title                                                       | Score | Assessment                                                                                                                                                                                                                                                           |
| ----- | ----------------------------------------------------------- | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #100  | Extract `ElevationSystemControls` component                 | 3     | `CalibrationControls` now owns two unrelated data domains (`gridSpec` and `elevationSystem`) with different persistence semantics and lock behavior. As additional `elevationSystem` fields gain UI, the component will accumulate further misplaced responsibility. |
| #101  | Replace index-based input selectors with `data-testid`      | 2     | Tests select inputs by positional index (`inputs[9]`, `inputs[10]`); any template change silently breaks selectors and can produce false positives. Pattern compounds with each new input added to the component.                                                    |
| #102  | Replace mutable shared test fixtures with factory functions | 2     | `VALID_MAP_WITH_ELEVATION` is a module-level mutable object; two tests already require `JSON.parse(JSON.stringify(...))` workarounds. If a future test forgets the guard, the suite becomes order-dependent and non-deterministic.                                   |
| #103  | Extend `superRefine` to validate `wedgeElevations`          | 2     | Static `±21` bounds on wedge offsets remain independent of the dynamic `elevationLevels` value. A future scenario with fewer levels would allow structurally invalid wedge offsets to pass schema validation silently.                                               |
| #104  | Behavioral tests for disabled elevation inputs              | 1     | Missing behavioral coverage (no-emit when disabled). Low risk since the `disabled` attribute prevents user interaction in practice; mirrors an existing gap on the north picker.                                                                                     |
| #105  | Cover NaN/empty input in `updateElevationSystem`            | 1     | `Number('')` → `0` and `Number('abc')` → `NaN` are passed through without tests documenting the contract. Handler-level validation in `MapEditorView` mitigates downstream risk.                                                                                     |
| #106  | Assert error path in `superRefine` rejection tests          | 1     | Rejection tests only check `result.success === false`; a different schema rule firing would still pass them. Low regression risk given the schema's simplicity.                                                                                                      |

**Total debt added this PR:** 12

---

_Generated by `/tech-debt-report`_
