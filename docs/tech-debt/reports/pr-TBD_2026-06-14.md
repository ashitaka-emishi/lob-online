# Technical Debt Report — PR #TBD (m6-ui_20260614)

**PR:** #TBD (branch: feat/m6-ui_20260614 — to be assigned at merge)
**Date:** 2026-06-14
**Reviewer(s):** Claude Sonnet 4.6 (5-agent parallel team-review: security, architecture, testing, maintainability, accessibility)

## Fixed Findings

17 findings fixed in place during this review cycle:

- H1: Replaced hardcoded `999` sentinel with `VISIBILITY_UNLIMITED` import in `turnTime.js` and `TurnControl.vue`
- H2: Added `role="region"` + `aria-label="Turn status"` to TurnControl; added `aria-label` to each value span
- H3: Added GameView tests asserting TurnControl renders and receives correct props from game store + fetch
- H4: Added module-prefixed route to `makeRouter`; added scenario fetch URL test; added `routePath` param to `mountGameView`
- M3: Added `scenarioWarning` ref; scenario fetch failure now surfaced in status-banners (matching `mapConfigError` treatment)
- M4: Added `// LOB §6.1` citation at `VISIBILITY_UNLIMITED` day-default return in `turnTime.js`
- M5: Added sr-only "(opens in new tab)" to all 4 external links in `AboutView.vue`
- M6: Gave `lob-series-link` and `mmp-lob-link` distinct URLs (both previously pointed to `/line-of-battle`)
- M7: Replaced `+ ' hex'` with pluralizing `visibilityLabel` computed (handles "1 hex" vs "N hexes")
- L2: Wrapped `←` arrow in `aria-hidden="true"` span so screen readers announce "Home" only
- L3: Added `href`, `target`, `rel` assertions to AboutView external link tests; asserts distinct URLs
- L4: Added edge-case tests for empty `lightingSchedule: []` array and `turnNumber=0`
- L5: Added `setProps` reactive test (turn 1→45 re-renders time/condition/visibility)
- L6: Added WHY comment for `'full-battle'` scenarioSlug fallback
- Info: Added `if (!moduleSlug)` null guard before scenario fetch
- Info: Expanded JSDoc `@param scenario` to document required `turnStructure.firstTurn` and `.date` sub-fields
- Info: Added lint fix (`global.fetch` → `fetch` in GameView test)

## Second-Pass Review

NOT REQUIRED — review fixes were limited to component ARIA enhancements, test additions, a null guard, and a CSS visibility label fix. No auth, store, schema, rules-engine, or API contract surfaces were touched.

## Deferred Findings

| Issue | Title                                                                        | Score | Assessment                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| ----- | ---------------------------------------------------------------------------- | ----- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #583  | refactor: move scenario fetch into useGameStore (scenarioId source of truth) | 2     | GameView fetches scenario data inline via route params rather than via `useGameStore`, diverging from the established pattern where all game snapshot data lives in the store. Two bundled issues: (1) fetch placement — should live in `loadGame` alongside map-config; (2) dual source of truth — route slug vs `gameState.scenarioId` can diverge for redirected/bookmarked URLs, causing TurnControl to display lighting from the wrong scenario. Low urgency in current single-user deployment where full-battle is always the default scenario. |
| #584  | a11y: add `<main>` landmark to MenuLayout for screen-reader navigation       | 2     | `MenuLayout.vue` wraps slot content in a generic `<div>`, so `HomeView` and `AboutView` expose no `<main>` landmark for screen-reader navigation (WCAG 1.3.1). Pre-existing issue not introduced by this PR; simple one-line fix. Low impact for current dev-only deployment.                                                                                                                                                                                                                                                                         |

**Debt added this PR:** 4 (2 + 2)
**Debt closed this PR:** 0
**Net delta:** +4 (regression — 2 new minor debt items)

---

_Generated by `/tech-debt-report`_
