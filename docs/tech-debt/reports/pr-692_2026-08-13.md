# Technical Debt Report — PR #692

**PR:** #692
**Date:** 2026-08-13
**Reviewer(s):** Claude Code (`agent-teams:team-reviewer` subagents — security, testing, maintainability dimensions completed; architecture and domain dimensions hit an API session rate limit mid-run and were not retried)

## Fixed Findings

8 finding(s) fixed in place during this review cycle:

1. [FIXED] `plan.md` checkbox contradictions — `Status: Complete` while "issue closed" and the entire Final Verification section were unchecked (the same self-inflicted drift bug class flagged in two sibling PRs this week)
2. [FIXED] Stale, now-inaccurate present-tense comment in `scripts/validate-data.js` still describing #691 as unresolved
3. [FIXED] Stale `hex.js` cache-bound comment (`64×35 = 2240` → `63×35 = 2205`)
4. [FIXED] Stale `move.js` hex-index comment (`2240-hex index` → `~2.3k-hex index`)
5. [FIXED] Stale, dead-code `cols: 64` fallback in `los.js`'s `evaluateLos()` — dropped the unused field entirely rather than just correcting the number
6. [FIXED] HLD risk-register row said `Resolved` while its own mitigation text admits #685 is still open — softened to `Reduced — Low`
7. [FIXED] Misleading "SM map grid spec" comment on a synthetic test fixture in `hex.test.js` (independently flagged by two reviewers)
8. [FIXED] Missing pointer in `map-data-recovery_20260811/index.md` — the column-64 gap it left open is now resolved

## Second-Pass Review

NOT REQUIRED

The review-fix diff touches 20 files, but only one line of actual code changed — a field removed from a default-parameter fallback in `client/src/utils/los.js`'s `evaluateLos()`. Independently verified (not taken from a reviewer's claim): the removed field was never read in the function, and the function has zero non-test callers anywhere in the codebase — unreachable dead code, not a behavioral change to any live rules-engine path, despite matching the "LOS" trigger category by filename.

## Deferred Findings

| Issue | Title                                                                                                   | Score | Assessment                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ----- | ------------------------------------------------------------------------------------------------------- | ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #693  | test: no regression guard for `gridSpec.cols=63`; map editor is a live write path that could revert it  | 2     | Mutation-proven unguarded (reverting `cols` to 64 keeps all tests green). The map editor writes `gridSpec` back to disk and seeds calibration from `localStorage` before any server load, so a stale cached calibration is a plausible silent-revert vector. Bounded, no data-integrity risk today, but this exact failure mode caused the original #691 confusion.                                                                     |
| #694  | test: `validate-data.js` grid-coverage check is a non-blocking warning with zero test coverage          | 2     | The check exits 0 even in a mutated broken state, and the script itself has no automated test coverage at all — nothing proves the checker logic still fires correctly after a future refactor. No current exposure (SM is genuinely at 100% coverage), but both the detection and its severity can regress silently.                                                                                                                   |
| #695  | arch: `playable:false` has no movement semantics — undocumented, unenforced, no regression test         | 2     | Confirmed only `edge-strip.js`/`edge-model.js` read the flag; nothing in `movement.js`/`hex.js`/`los.js` does. This is the exact gap that caused this track's own initial wrong fix attempt (recording column 64 as `playable:false` would not have excluded it from movement). No known live exploitation (the 56 existing `playable:false` hexes are also out-of-grid), but it's now caused one real near-miss during implementation. |
| #696  | docs: South Mountain map-status facts duplicated across 7 live locations with no single source of truth | 2     | Consistent today, but consistency is maintained by hand across `map.json`, `library.md`, `library.json`, `domain-expert/design.md`, and three HLD locations. This is the third PR in a row (#687, #690, #692) to pay the cost of re-syncing all seven when the underlying numbers change.                                                                                                                                               |

**Debt added this PR:** 8
**Debt closed this PR:** 0 (none of the 5 existing open items — #676, #677, #678, #679, #681 — are related to this PR's scope)
**Net delta:** +8 (regression — this PR's own narrow fix was clean, but its review surfaced four genuine pre-existing cross-cutting gaps that were correctly out of scope for it to close itself)

---

_Generated by `/tech-debt-report`_
