# Technical Debt Report — PR #683

**PR:** #683
**Date:** 2026-06-27
**Reviewer(s):** Claude Code (6 parallel dimensions: security, architecture, testing, maintainability, performance, domain)

## Fixed Findings

13 findings fixed in place during this review cycle.

| ID  | Severity | Description                                                                                                         |
| --- | -------- | ------------------------------------------------------------------------------------------------------------------- |
| H1  | High     | Schema migration gap: `activatedUnitIds` required in `.strict()` schema with no default — fixed with `.default([])` |
| H2  | High     | §3.0d multi-unit interleaving not enforced — fixed with `lastMovedUnitId` + `movedUnitIds` tracking                 |
| M1  | Medium   | Unbounded `path[]` enables event-loop DoS — fixed with `MAX_PATH_HEXES = 50` cap                                    |
| M2  | Medium   | `activatedUnitIds?.includes()` contradicts required schema — dropped `?.`                                           |
| M3  | Medium   | No test for two different units each making MOVE in same activation — added multi-unit MOVE test                    |
| M4  | Medium   | §3.0c one-hex move guarantee not enforced — implemented; three tests added                                          |
| M5  | Medium   | `pathCost([])` untested, JSDoc precondition unenforced — test added pinning 0-return contract                       |
| M6  | Medium   | `pathCost` constant-formation assumption not cited — §3.1 note added to JSDoc/comment                               |
| L1  | Low      | Citation specificity: bare `§3` replaced with `§3.0`, `§3.0c`, `§3.0d` at each rule point                           |
| L2  | Low      | LOB §3 citation inside JSDoc block — moved to standalone `//` comment                                               |
| L3  | Low      | `activatedUnitIds` snapshot semantics undocumented — comment added                                                  |
| L4  | Low      | activateStack.js comment should state failure mode — strengthened                                                   |
| L5  | Low      | `makeState()` roster default implicit coupling — load-bearing comment added                                         |

## Second-Pass Review

COMPLETED — targeted inline review.

The review-fix diff touched `gameState.schema.js` (schema validation) and `move.js` / `activateStack.js` (rules-engine), meeting the second-pass trigger criteria on both surfaces. All fixes implement the exact approaches recommended by the original 6 reviewers (domain reviewer proposed §3.0c/§3.0d semantics; architecture reviewer proposed `.default([])`); no novel design choices were introduced. Schema change is additive (`.default([])` is backward-compatible). 3249 tests pass. No additional findings.

## Deferred Findings

None — all findings fixed in place.

**Debt added this PR:** 0
**Debt closed this PR:** 6 (issues: #675 score 3, #680 score 3)
**Net delta:** −6 (net improvement)

---

_Generated by `/tech-debt-report`_
