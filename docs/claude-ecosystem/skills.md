# Skill Reference

> Auto-generated by /ecosystem-docs-generate — do not edit by hand.
> Source of truth: docs/agents/\*/design.md, .claude/agents/registry.json,
> .claude/commands/\*.md

Skills are reusable Markdown prompt files in `.claude/commands/`. They run in the current
context (not a subprocess), encode numbered step-by-step procedures, and compose freely —
one skill can invoke another by name. Registry entries (`type: "skill"`) record the
canonical list; unregistered skills that exist in `.claude/commands/` are noted separately.

---

## `devops`-owned skills

### `/dev-build`

**Purpose:** Format, lint, and Vite-build the project in order, stopping on first failure.
**Owning agent:** `devops`
**Related skills:** called by `/pr-create`
**Claude Code:** `/dev-build`
**Manual equivalent:** `npm run format && npm run lint && npm run build`

---

### `/dev-start`

**Purpose:** Launch the Express server (port 3000) and Vite dev client (port 5173), logging
stdout/stderr to `logs/`.
**Owning agent:** `devops`
**Related skills:** none
**Claude Code:** `/dev-start`
**Manual equivalent:** `npm run dev:server & npm run dev` (with env vars)

---

### `/dev-stop`

**Purpose:** Gracefully stop server and Vite client; SIGKILL fallback after 10 s.
**Owning agent:** `devops`
**Related skills:** none
**Claude Code:** `/dev-stop`
**Manual equivalent:** kill PIDs stored in `.pids`

---

### `/dev-test`

**Purpose:** Run the Vitest suite; snapshot results to `test-results/`; detect flaky tests;
correlate failures with server logs.
**Owning agent:** `devops`
**Related skills:** none
**Claude Code:** `/dev-test`
**Manual equivalent:** `npm test`

---

## PR lifecycle skills (unowned)

### `/pr-create`

**Purpose:** Write a devlog entry for the PR, run CI checks, and open the pull request on
GitHub.
**Owning agent:** unowned (lob-specific extension)
**Related skills:** `/dev-build`
**Claude Code:** `/pr-create`
**Manual equivalent:** `gh pr create --title "..." --body "..."`

---

### `/pr-merge`

**Purpose:** Run a final CI check, present a human control point, then squash-merge and
delete the branch.
**Owning agent:** unowned
**Related skills:** none
**Claude Code:** `/pr-merge`
**Manual equivalent:** `gh pr merge --squash --delete-branch`

---

## Issue lifecycle skills (unowned)

### `/issue-close`

**Purpose:** Close a GitHub issue with a merge summary comment after the PR is merged.
**Owning agent:** unowned
**Related skills:** none
**Claude Code:** `/issue-close <number>`
**Manual equivalent:** `gh issue close <number> --comment "..."`

---

## Documentation skills (unowned)

### `/doc-sync`

**Purpose:** Diff-driven sync of `CLAUDE.md`, `docs/high-level-design.md`, and
`docs/agents/*/design.md` against the current branch's changes.
**Owning agent:** unowned
**Related skills:** none
**Claude Code:** `/doc-sync`
**Manual equivalent:** `git diff --name-only origin/master...HEAD` then manually review docs

---

### `/ecosystem-docs-generate`

**Purpose:** Rebuild all files in `docs/claude-ecosystem/` from source-of-truth inputs
(agent design docs, registry, skill command files).
**Owning agent:** unowned
**Related skills:** none
**Claude Code:** `/ecosystem-docs-generate`
**Manual equivalent:** read all source inputs, rewrite each file by hand

---

### `/design`

**Purpose:** Collaboratively author a design document for a new or changed component
(skill or agent) before writing issues.
**Owning agent:** unowned
**Related skills:** none
**Claude Code:** `/design`
**Manual equivalent:** create `docs/designs/{slug}.md` using `docs/designs/TEMPLATE.md`,
iterate in chat, commit on `design/{slug}` branch, open PR

---

## Unregistered skills

These files exist in `.claude/commands/` but are not in `registry.json`. They are fully
functional but not formally catalogued.

| Skill        | Purpose                                                                   |
| ------------ | ------------------------------------------------------------------------- |
| `/plan-wrap` | Write a devlog entry and update documentation after a plan is implemented |
